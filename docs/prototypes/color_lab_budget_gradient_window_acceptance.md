# Budget category-gradient window — acceptance checklist

| ID | Source | Code area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| BGD-01 | User, 2026-08-21 | `color_lab.html` category palette | All 21 canonical category gradients are selectable whole-gradient slots, centred above the centred Test Header. | Static contract: exact slot count, CSS-token binding, and layout ownership. | PARTIAL |
| BGD-02 | User correction, 2026-08-21 | New Budget gradient scale lab | The new, dedicated colour and opacity scales sit directly above the Test Header; the existing Traffic control is not repurposed or changed. | Static contract and served-browser inspection. | PARTIAL |
| BGD-03 | User correction, 2026-08-21 | New colour scale | The ten display-only bands form one constant automatic scale: exact white at the left, then progressively less white mixed with the selected B/category surface until the maximum selected surface at the right. Only one target slot at the right edge accepts the selected category. | Static contract and manual browser interaction. | PARTIAL |
| BGD-04 | User, 2026-08-21 | Dedicated manual window state | The numeric input controls only the movable window's horizontal width. The reusable drag window moves over the constant scale without changing that scale's colour distribution; tapping or dragging the scale moves its centre. | Static contract and DOM pointer/input state check passed; human touch/screenshot check still needed. | PARTIAL |
| BGD-05 | User correction, 2026-08-21 | Test Header gradient layer | The Header samples the rendered white-to-category scale at the movable window's left and right edges. Those samples become the Header's A/B ends, so the header is a dynamic window slice rather than a direct white-mixed overlay. The canonical category stops still determine the sampled scale colour. | Static contract and endpoint runtime check passed; human screenshot check still needed. | PARTIAL |
| BGD-06 | User, 2026-08-21 | Dedicated opacity scale + Test Header | The dedicated upper opacity scale controls the shared active Header palette. A later existing Header colour control still takes ownership as the last colour source. | Static contract and manual browser interaction. | PARTIAL |
| BGD-07 | User correction, 2026-08-21 | Dedicated manual window UI | The movable 28%-wide window is visibly outlined and has a high-contrast centre grip that can be dragged; the fixed right-edge category target stays tappable when the window reaches it. | Static contract passed; human touch/screenshot check still needed. | PARTIAL |
| BGD-08 | User screenshot correction, 2026-08-21 | Budget gradient Header binding | The selected Budget category scale follows the same window contract as Cool: sample the visible scale at the movable window's left and right edges, publish those distinct A/B endpoint colours through the existing Portal Header palette/animation owner, and retain that owner's moving A/B split. Do not cover it with a separate static surface. | Focused source/runtime checks passed; fresh human screenshot after drag remains. | PARTIAL |
