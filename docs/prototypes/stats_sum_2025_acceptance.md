# Stats Sum Prototype Acceptance Checklist

Reference HTML: `docs/prototypes/stats_sum_2025_sim.html`

Visual/code references:
- Screenshot: `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260709-221441.png`
- Fastfood layout reference: `d5de3b6f394744e1eddb2d8927e1eeb7c54bcc0a:docs/prototypes/stats_fastfood_2025_sim.html`
- FastInfo painter: `lib/features/stats/widgets/stats_fast_info_graph.dart`
- Closing math: `lib/features/stats/data/stats_closing_series.dart`
- Header metrics: `lib/features/transactions/widgets/header_card/transaction_header_metrics.dart`
- Magnet painter: `lib/features/transactions/widgets/header_card/magnet_strip.dart`

| ID | Source | Intended area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| SUM-HTML-01 | User: working HTML, not image | `stats_sum_2025_sim.html` DOM/SVG | No canvas or bitmap mockup; controls, charts, header, monthcards are real HTML/SVG elements | `rg "<canvas"` returns no matches; HTTP file contains SVG/DOM controls | DONE |
| SUM-FAST-01 | User: match `stats_fastfood_2025_sim.html` chart layout | FastInfo layout | FastInfo keeps 328 px height and uses the fastfood white panel geometry: panel `(14,42,384,268)`, primary chart `(47,104,337,126)`, helper chart `(47,264,337,26)` | JS syntax + code inspection | DONE |
| SUM-FAST-02 | User: "ez lesz a felső függvény, a hasáb" | Upper primary chart | Upper primary chart is a large bottom-baseline bar chart for threshold-filtered active scoped monthly amount | JS syntax + code inspection | DONE |
| SUM-FAST-03 | User: "újabb alsó grafikont ... maradékait mutatja (bevétel-kiadás)" | Lower helper chart | Lower helper chart is a smaller signed bar chart with zero line in the middle; bars show monthly income minus monthly expense, green above zero and red below zero | JS syntax + code inspection | DONE |
| SUM-MATH-01 | `StatsClosingSeries` mechanics + user net-close correction | JS sum math | JS computes deltas, pressure deltas, dynamic EMA windows, drift marker, close pulse bars, and monthly close bars over the common net close series | JS code inspection | DONE |
| SUM-HEAD-01 | `TransactionHeaderMetrics` + `MagnetStrip` | Header magnet | Header is 188 px high; magnet widget top 41 px, height 157.5 px; visible track is `height * 6 / 35 = 27 px`, centered inside magnet widget | CSS geometry inspection | DONE |
| SUM-HEAD-02 | `StatsPage._headerVisual` | Header magnet colors | Closing magnet gradient is `#EF4444 #FEE2E2 #FFFFFF #DCFCE7 #22C55E`; marker is line style at drift marker | CSS/JS inspection | DONE |
| SUM-HEAD-03 | User: remove descriptive text only, keep the view title in the magnet | Header card | Header/magnet keeps the `HÓZÁRÁS` view title, but contains no descriptive value text such as `romló hónap idén`; the closing state is represented by the magnet strip and marker | DOM/CSS/JS inspection | DONE |
| SUM-CLOSE-01 | User: monthly total income minus monthly total expense is common and independent of pills | Header/FastInfo/monthcards | Net close is computed as `monthlyIncome - monthlyExpense` and drives header drift, Close Pulse, monthcard primary value, and monthcard tint independent of the active pill; the lower H1 chart is separately active-side volume | JS code inspection | DONE |
| SUM-DYN-01 | User: graphs/magnet are currently only mockup | JS data model | Prototype uses generated daily categorized transaction records as the source for scoped active amounts and threshold-hit days instead of drawing charts from fixed visual mock data | JS code inspection | DONE |
| SUM-DYN-02 | User: bind graphs and magnet to scope | Header scope + FastInfo + magnet | Header scope control changes the scope chip and recomputes scoped close series, FastInfo charts, threshold points, monthcard secondary totals, and magnet marker in one render pass | Runtime VM interaction test | DONE |
| SUM-DYN-03 | User: bind graphs and magnet to slider | Slider + FastInfo + magnet | Slider/manual threshold changes recompute threshold-hit days, H1 points, Close Pulse pressure, and magnet marker from current scoped daily data | Runtime VM interaction test | DONE |
| SUM-DYN-04 | Prior approved rule: monthcards remain constant by primary close | Month cards | Monthcard primary value and tint still use full monthly income minus full monthly expense; active scope changes threshold dots, secondary active amount, and lower H1 active-volume bars | Runtime/code inspection | DONE |
| SUM-DYN-05 | User: graph looks constant/slider-independent | Mock daily distribution | Mock daily expense amounts vary enough that threshold changes in the normal slider range visibly change hit counts, Close Pulse, H1 threshold points, and magnet while leaving H1 volume bar heights tied to monthly active volume | Runtime VM threshold snapshots | DONE |
| SUM-H1-01 | User: lower function y is bar height from bottom axis | Lower sum chart | H1 draws bars from the bottom of the chart upward; there is no centered signed axis in H1 | SVG draw code inspection | DONE |
| SUM-H1-02 | User: one bar per month in scope | Lower sum chart data | H1 series includes only months with active scoped transactions for the selected income/expense side | Runtime VM test | DONE |
| SUM-H1-03 | User: more spending/income means taller bars | Lower sum chart data | H1 bar amount is monthly scoped active transaction total, so higher expense/income volume produces a taller bar | Runtime VM test | DONE |
| SUM-H1-04 | User: income/expense button separates transactions | Lower sum chart data | Switching Bevétel/Kiadás rebuilds H1 from the selected side only; expense and income transactions do not share H1 bars | Runtime VM test | DONE |
| SUM-MONTH-01 | User: monthcards below header | Month cards | Month cards remain visible below header, update when income/expense and threshold changes; threshold hit dots use active type color | JS code inspection | DONE |
| SUM-CTRL-01 | User: slider below monthcards | Slider/input | Slider and manual input update threshold, monthcard dots, monthly hit dots, and header/chart render in one render pass | JS event inspection | DONE |
| SUM-MONTH-02 | User: monthcards independent from income/expense pills | Month cards | Monthcard primary amount is always monthly income minus monthly expense and never changes when income/expense pill changes | JS code inspection + manual browser check | DONE |
| SUM-MONTH-03 | User: card color independent from pills | Month cards | Monthcard tint is green when monthly close is positive, red when negative, neutral when zero, independent from active pill | CSS/JS inspection | DONE |
| SUM-MONTH-04 | User: active pill controls only dots and active total | Month cards | Active pill switches only threshold dots and secondary monthly total label; closing amount and card tint remain unchanged | JS code inspection + manual browser check | DONE |
| SUM-MONTH-05 | User: avoid confusing active total with closing | Month card labels | Closing is explicitly labeled `zárás`; active income/expense amount is smaller, unsigned, and prefixed with a colored dot and type label | Visual/code inspection | DONE |
| SUM-H1-05 | User correction: "a monthcardok kijelölését szedd ki, ezt a treshold végzi" | H1 lower chart + month cards | Month cards are not selectable and do not control H1; H1 renders bars only for months where the threshold-filtered scoped active total is greater than zero | JS syntax + code inspection | DONE |
| SUM-H1-06 | User: "sok tranzakció van a tresholdban, akkor magasabb, ha kevesebb akkor alacsonyabb a hasáb" | H1 lower chart data | H1 bar height is proportional to the sum of active scoped transaction amounts that pass the slider threshold in that month | JS syntax + code inspection | DONE |
| SUM-H1-07 | User: "ezt az x tengelyen jelölni is kell hónapnevekkel" | H1 lower chart axis | H1 x-axis labels each rendered bar with the corresponding month name/abbreviation | JS syntax + code inspection done; browser visual check still needs manual check because Playwright is unsupported on Termux/Android | PARTIAL |
| SUM-H1-08 | User: "kösd be a sliderbe"; corrections: "ne legyen treshold pont", "ha nincs minta nincs print" | H1 lower chart thresholded volume | Slider/manual threshold changes recompute H1 bar presence and heights directly from threshold-filtered scoped active totals; H1 renders no threshold point circles and no zero-value month bars | JS syntax + code inspection done; browser interaction still needs manual check because Playwright is unsupported on Termux/Android | PARTIAL |
| SUM-H1-09 | User: "töröld a h1 feliratot, és a legendet az alsó függvényből, csak a cím maradjon" | H1 lower chart labels | Lower chart keeps only the descriptive title; it renders no `H1` prefix, no legend row, and no y-axis label | JS syntax + code inspection | DONE |
