# Fluvi Engineering Journal

Shared evidence journal for the Fluvi prompt-writer and coding agents. Read this file in full before every implementation attempt. Entries record physically tested application SHAs, observed behavior, evidence boundaries, failed loops, and the next allowed investigation scope. USER_MARK log entries are annotations and are never treated as runtime proof by themselves.

## 2026-09-10 — Physical feedback on `6e962187e90e2a82431b1f91b224d2b52a6e0ba7`

- Feedback time: 2026-09-10 10:07 Europe/Budapest.
- Exact physically tested application SHA proven by the refreshed runtime logs: `6e962187e90e2a82431b1f91b224d2b52a6e0ba7`.
- Application branch: `fix/avatar-fling-pacing-codex-20260910`.
- Parent: `33ee878b070345c336bb73df2b087d9a63c87c5a`.
- APK marker in the logs: `fluvi_HUMAN_DIAGNOSTIC_6e96218.apk`.
- Commit title at the tested SHA: `test(profile): correlate Avatar fling render pacing`.
- Current physical result: Avatar fling appears good and overall performance is good. Preserve this state as the new minimum performance floor while repairing the remaining defects.
- Remaining physical defects reported by the user:
  - switching between income and expense hitches/stutters;
  - after changing month, the limit-circle progress is not visibly populated until an Avatar is changed afterward.
- Avatar-log audit: the retained flight summary is healthy (`semanticTicks=61`, `duplicateTicks=0`, `coalescedTicks=48`, `acceptedPublications=27`, `paintedPublications=27`, final `physicalSettled=7`, `endingPendingTargetHandle=null`). Retained frame summary: median build 1.293 ms, p95 build 2.716 ms, p99 build 10.082 ms, max build 37.569 ms; median raster 1.030 ms, p95 raster 1.813 ms, p99 raster 5.740 ms, max raster 15.583 ms; median total 2.459 ms, p95 total 4.387 ms, p99 total 19.198 ms, max total 39.684 ms. Mismatch/reject events observed during rapid supersession are anomaly candidates, not proof of a surviving-target defect; do not touch the Avatar critical path unless a surviving/final target anomaly is proven.
- Avatar log frozen snapshot: Google Doc `Fluvi logs avatar fling`, ID `1FF1BnHJHOEygG3QhMeDfrehzjo0YzIztaGJfNTWZqNs`, plain-text export SHA-256 `dc6d3e987fa46e77a397b47cf5836b0b673e9efc8c5b7e28fef8776c30e084d6`, 1,411,657 bytes, 2,004 lines. Session `243426112422`; two retained windows cover union sequence 5873–6887 without observed gaps, with overlapping duplicate snapshots. The latest `USER_MARK` still names an older `avatar_filter_stuck` issue; the user's newer physical feedback says Avatar fling now appears good, and USER_MARK is not runtime proof.
- Time-log audit: captured retained flight is healthy (`semanticTicks=3`, `distinctTicks=3`, `duplicateTicks=0`, `maxTickGapMs=40`, `acceptedPublishesForFlight=3`, `matchingAcceptedPublishesForFlight=3`, `matchingPaintedPublishesForFlight=3`, `settleVisualDelta=0.0`, `correctlyCaptured=true`, no ending pending target/replay). A `USER_MARK issue=time_target_jump` and an isolated `SUMMARY_SETTLE_REJECTED_UNACCEPTED_TARGET` remain anomaly candidates and must be classified from surrounding runtime evidence before any Time production change.
- Time log frozen snapshot: Google Doc `Fluvi logs time fling`, ID `1XSzi1TO8CfVKDGxAMkUYhJihbqy7nEDUcmqs8mboxoE`, plain-text export SHA-256 `c76a6ae13e2d37abe77e5581d2038b30e097b68c47edc06f4b240101e08b9fb5`, 1,462,414 bytes, 2,004 lines. Session `243426112422`; two retained windows cover union sequence 8958–9972 with overlapping duplicate snapshots.
- Month/limit-progress evidence boundary: around the month interaction, the generic Budget progress pipeline does emit `LIVE_INTERACTION_ACCEPTED`, `LIMIT|STATE`, `BUDGET_PROGRESS_BOUND`, `BUDGET_PROGRESS_WIDGET_BUILT`, `BUDGET_PROGRESS_PAINTED`, and `SUMMARY_TARGET_PAINTED` events. Therefore the physical defect is NOT proven to be a generic “no progress publication” failure. The exact limit-circle projection/identity/state/listener/render invalidation path must be traced and the first failing boundary proven.
- Direction-switch evidence boundary: the physical hitch is proven by user testing, but its owner/root cause is not yet proven. Instrument and correlate the direction input/controller, target data/resource readiness, visible publication, Budget presentation, build/layout/paint/raster, and frame timing before changing behavior.
- Historical loop guards:
  - `aa61242c4b933ad9d650361ae1014d67fd184186`: physically smooth Avatar motion, but Avatar correctness/publication failed; do not return to this tradeoff.
  - `33ee878b070345c336bb73df2b087d9a63c87c5a`: Avatar correctness was restored, but physical fling became choppy; do not preserve correctness at unbounded performance cost.
  - `e8b73e3e939104164e55b09caf592f84ee59fb14`: previous protected motion floor recorded in `MILESTONE_COMMITS.md`.
  - `6e962187e90e2a82431b1f91b224d2b52a6e0ba7`: current physically good overall-performance floor and required base for the next repair. Do not accidentally base work on `separated-core-modes`, which is not the tested SHA.
- Milestone intent: the final application repair built on `6e962187...`, after it fixes the income/expense switch hitch and month-limit-circle defect while preserving the current Avatar/Time performance and correctness, is intended to become the next Fluvi milestone commit. The journal-only commit containing this entry is NOT the milestone application SHA.
- Required next scope: audit fresh Avatar and Time anomalies; repair only proven surviving anomalies; prove and repair the first causal boundary for direction-switch hitch; reproduce and repair month-change limit-circle invalidation without requiring an Avatar change; preserve the `6e962187...` performance floor and all existing gesture/physics/controller identities unless current evidence proves them causal.
- Prompt-writer source change: documentation journal only. No production source, tests, workflow, build configuration, graph, milestone file, or application behavior changed by this entry.
- Physical validation of any future repair: `PENDING — USER ONLY` until the exact produced APK is tested by the user.

## 2026-09-10 — Physical feedback and forensic audit on `0b108b73e8e13b7a3bbd397dd9c98e8bb0db79aa`

- Feedback time: 2026-09-10 15:15 Europe/Budapest.
- Exact physically tested diagnostic application SHA: `0b108b73e8e13b7a3bbd397dd9c98e8bb0db79aa` on `fix/direction-month-limit-circle-codex-20260910`; parent `9516875e62db00005c151faf8cbb527eb2f8475a`.
- Physical APK: `fluvi_HUMAN_DIAGNOSTIC_0b108b7.apk`, reported size `82,236,721` bytes, SHA-256 `ffdaca3aeba8deee546baf55f7066cd9334e5a03117667e0e8849b0331103a28`.
- Physical result changed from the previous round: income/expense switching is no longer perceived as lagging. Preserve this improvement. The remaining reproducible defect is that immediately after a direction switch the Avatar selection circle/progress is absent; moving to another Avatar makes it appear. The user did not observe the analogous circle-progress failure during Time fling in this physical run. Avatar fling remains physically acceptable.
- Milestone documentation commit `9516875e62db00005c151faf8cbb527eb2f8475a` records `6e962187e90e2a82431b1f91b224d2b52a6e0ba7` as the protected physical performance floor; it changed `MILESTONE_COMMITS.md` only. Diagnostic application commit `0b108b73...` added bounded direction and limit-period/render tracing plus focused tests and did not intentionally change Avatar/Time physics or user-visible behavior.
- GitHub Actions run `34465990867`, reported as still running in the agent handoff, was independently rechecked after completion: the overall run, `test-flutter`, `test-core`, `build-human-diagnostic-apk`, and `run-dashboard-profile` all concluded `success` for exact head `0b108b73...`. The A–K profile artifact exists. This automated green does not override the physical direction-circle rejection.
- A–K false-green boundary: scenario `G_direction_while_rail_open` exercises direction switching through the Time/horizontal semantic path and verifies visible semantic publication, but it does not require a different remembered Avatar target per direction and does not assert `physical Avatar centre == presentation.selectedHandle == selectedLimitVisual.targetHandle == actual selected-circle paint`. Therefore it can pass while the physical Avatar circle is missing after a direction switch.
- Fresh Drive snapshots, all from session `fluvi-1789045402258792`, build marker `profile/0b108b73e8e1`:
  - `Fluvi logs avatar fling`, ID `1FF1BnHJHOEygG3QhMeDfrehzjo0YzIztaGJfNTWZqNs`, modified `2026-09-10T13:04:02.162Z`, plain-text SHA-256 `de24763fd6570f7353bf148b9fe8868e83f3459c598bc663723384df2c00cc77`, 592,797 bytes, 2,002 lines. Two overlapping LIVE_TAIL snapshots: `9118–10117` and `9121–10120`; union `9118–10120`, 1,003 unique sequences, 997 identical duplicate copies, zero differing duplicates, no internal sequence gaps.
  - `Fluvi logs time fling`, ID `1XSzi1TO8CfVKDGxAMkUYhJihbqy7nEDUcmqs8mboxoE`, modified `2026-09-10T13:05:14.457Z`, plain-text export SHA-256 `3e4f07bf8a8ef9c6fb0f419f30a9fc4cc9d5f2ce94e071b876afac80674876a2`, 315,518 bytes, 1,001 lines. LIVE_TAIL `11663–12662`, 1,000 unique sequences, no duplicates, no internal gaps.
  - `Fluvi budget bug`, ID `1KCxfP-l4dKEDHwfdwMu7OttTUtUaqIeyA32ViPNUKxc`, modified `2026-09-10T13:06:55.131Z`, plain-text export SHA-256 `f73c15b94a0f0ac90001cff97e9287fbfc2b79e55b04a75319fc40aea35e538b`, 321,642 bytes, 1,001 lines. LIVE_TAIL `11690–12689`, 1,000 unique sequences, no duplicates, no internal gaps. Its overlap with the Time snapshot (`11690–12662`) is byte-for-event identical.
- Cross-snapshot retention boundary: same-session evidence retains `9118–10120`, then has a hard missing range `10121–11662` (1,542 sequences), then retains `11663–12689`. Do not infer a continuous event chain across this retention gap.
- PROVEN direction-circle failure chain from retained evidence: direction flow 7 requests `income -> expense` at sequence 9139; the expense financial presentation publishes target 4 with available limit (`LIMIT|STATE` seq 9148; `BUDGET_PROGRESS_BOUND` seq 9150); controller/live/fanout/navigation complete quickly and `DIRECTION_SWITCH_VISIBLE_PUBLISHED` occurs at seq 9173; then `BUDGET_PROGRESS_IDENTITY_MISMATCH` seq 9182 reports `avatarTargetHandle=0` while `visualTargetHandle=4`. This matches the user's missing-circle symptom.
- CURRENT-source cause is proven at the identity/rebase class. `DashboardBudgetPresentationController` keeps selected-target identity per direction and restores the new direction's own `selectedHandle`. In `BudgetTargetAvatarRail._replaceItems(...)`, however, non-initial domain replacement preferentially preserves the prior physical carousel centre by `previousCenterId` if that stable ID exists in the new catalogue, instead of rebasing physical/semantic centre to the new direction's authoritative `presentation.selectedHandle`. The selected Avatar chrome listens directly to the presentation; `BudgetCategoryAvatarArtwork` intentionally returns an empty chrome when `selectedLimitVisual.targetHandle != selectedTargetHandle`. The circle therefore disappears because the new direction presentation and the physical Avatar centre legitimately diverge; this is not a missing-listener or generic no-progress-publication failure.
- Proven root-cause class: `DIRECTION-DOMAIN SELECTED-TARGET REBASE ATOMICITY FAILURE` — cross-direction physical/semantic Avatar centre is not atomically rebased with the per-direction presentation selected target. Required repair must preserve existing controller, ScrollPosition and physics identity while making direction transition converge to one target authority without an Avatar nudge.
- Direction performance is no longer an active physical failure. Retained instrumented flows show the direction controller/live/budget-fanout path completing around the low-millisecond range and visible publication in the retained sample within roughly `2.5–12.3 ms`. Do not add new direction-performance work unless a regression is independently reproduced.
- Time evidence currently supports final-target liveness for the retained sampled flights: pending Phase-A candidates later reach exact terminal paint, `liveRootMisses=0` in the inspected summaries, and the user did not observe the circle defect during Time fling. However, retained Time examples predominantly exercise aggregate target 0, so they do not prove non-aggregate category month-circle correctness. Require an anti-false-green month-only test that keeps a positive-limit non-aggregate category selected across the month change without any Avatar movement before changing Time production code.
- PROVEN diagnostic inconsistency in the Time trace: the same Time `flowId` can emit a later second `TIME_FLIGHT_SUMMARY` with different canonical-settle accounting immediately before a rejected settle event (observed for flows 5 and 6). This proves at least a telemetry/bookkeeping one-shot/final-snapshot inconsistency; production semantic harm is not proven. Repair diagnostic finalization semantics if source audit confirms duplicate summary emission, but do not change Time behavior merely to silence diagnostics.
- Additional retained anomaly inventory requiring classification, not speculative broad repair:
  - Avatar/background `EXACT_LOCAL_WINDOW_REJECTED` / `LIVE_ROOT_RESOURCE_DEFERRED` events with `reason=pendingHotsetPlans`, `targetHandle=-`, `interactionEpoch=0`; no surviving-target harm proven.
  - `VERTICAL_PAGE_COMMIT_REJECTED reason=staleRequest` and ready-ahead deferrals during active motion; currently consistent with stale/superseded guarding unless source/log correlation proves otherwise.
  - retained `SCENE_WINDOW_SLICE_OVER_BUDGET` events, including some multi-millisecond UI-isolate slices; physical overall performance is accepted, so correlation to a surviving visible defect is required before production optimization.
  - `QUERY_CANDIDATE_SCENE_RETENTION_REJECTED` events when bounded retained rows exceed the configured row cap; bounded guard behavior is plausible but must be source-verified rather than assumed harmful.
  - Budget log `USER_MARK issue=mind_slider_no_live_list` at seq 12680 is an observation marker only. Inventory it, but do not broaden this atomic direction/circle repair into Mind production code unless a shared proven source cause is established; otherwise carry it as a separate open physical defect.
- Matching SCIP is unavailable for the tested diagnostic SHA. Tooling branch `tooling/scip-codegraph-v1` currently points to tooling commit `d985950733ea760c454218d497172893a13c4e95`; its `docs/codegraph/manifest.json` has `source_head=33ee878b070345c336bb73df2b087d9a63c87c5a`. Therefore: `GRAPH STALE FOR TESTED HEAD` for `0b108b73...`. Stale graph may be used only as historical/navigation aid; regenerate a matching graph before relying on graph impact for shared-symbol edits, then regenerate again for final application SHA.
- Next repair contract: first add a production-parent RED regression where income and expense remember DIFFERENT selected Avatar handles (for example one direction at aggregate 0 and the other at category 4), switch direction without touching Avatar, and hard-require `physical centre == semantic target == presentation.selectedHandle == selectedLimitVisual.targetHandle == selected circle build/paint target`. Then make the smallest source repair in the proven direction-domain rebase boundary. Preserve direction latency, Avatar/Time physics, ScrollPosition/controller identity and the `6e962187...` performance floor.
- Prompt-writer source change for this feedback: update this journal only, with `[skip ci]`; no application source, tests, workflow, graph, milestone file or runtime behavior may be changed by the prompt-writer.
- Physical validation of the next application repair remains: `PENDING — USER ONLY`.

## 2026-09-11 — Physical Avatar frame-pacing feedback on `92222edddeee5bb13792f62f939eedf81c867a52`

- Feedback time: 2026-09-11 06:31 Europe/Budapest.
- Exact physically tested application SHA: `92222edddeee5bb13792f62f939eedf81c867a52` on `fix/direction-month-limit-circle-codex-20260910`; parent `a658c08cee7207b1b1b22d0494f21e56b4e20a64`.
- Physical APK: `fluvi_HUMAN_DIAGNOSTIC_92222ed.apk`, size `82,236,721` bytes, SHA-256 `604f5e81de1265a8776af5b986288429b58c317fa3da63a8d97d607accbc1637`. GitHub prerelease tag targets the exact full application SHA.
- User physical result: Avatar fling is usable but intermittently slightly choppy; some flings feel smoother than others. The user explicitly states this may have existed before and was only less noticeable. Therefore do NOT claim a new regression introduced by `92222edd`. The next repair scope is EXCLUSIVELY Avatar-fling smoothness/frame pacing.
- Only `Fluvi logs avatar fling` was refreshed in this cycle. Time and Budget logs are historical-only for this round and must not be treated as current evidence.
- Fresh Avatar log frozen snapshot: Google Doc `Fluvi logs avatar fling`, ID `1FF1BnHJHOEygG3QhMeDfrehzjo0YzIztaGJfNTWZqNs`, modified `2026-09-11T04:29:32.195Z`, plain-text export SHA-256 `84ce186d3d7a92a843ce1fb9147e0c8a11d30cfd0447f85e9796e9d8d7787164`, 723,485 bytes, 2,002 lines. Session `fluvi-1789100927146293`; overlapping LIVE_TAIL snapshots are `8463–9462` and `8467–9466`; union `8463–9466`, 1,004 unique sequences, 996 identical overlapping copies, zero differing duplicates, zero internal sequence gaps. Visible retained timestamps span approximately `05:58:57.51–06:29:18.40`.
- Physical build identity is present in the log as `profile/92222edddeee`; USER_MARK entries are observations only. `issue=avatar_fling` supports the user's observation but does not identify an exact causal flight/frame. A later `issue=avatar_filter_stuck` marker is outside the user-locked next scope and is not runtime mechanism proof; do not broaden this repair into filter correctness.
- Three retained Avatar motion summaries prove material per-flight frame-pacing variability while correctness remains sampled healthy:
  - generation 21, interrupted by a new pointer: 19 FrameTiming samples, 11 missed frames; build p95 6.755 ms, raster p95 16.022 ms, total-span p95 29.480 ms; raw→semantic p95 19.334 ms, semantic→store p95 19.513 ms, store→paint p95 20.452 ms.
  - generation 22, interrupted by a new pointer: 19 samples, 8 missed; build p95 6.997 ms, raster p95 18.345 ms, total-span p95 34.089 ms; raw→semantic p95 32.645 ms, semantic→store p95 21.234 ms, store→paint p95 21.100 ms.
  - generation 23, settled: 14 samples, 3 missed; build p95 7.643 ms, raster p95 13.105 ms, total-span p95 45.910 ms; raw→semantic p95 36.208 ms, semantic→store p95 33.597 ms, store→paint p95 25.475 ms. Final semantic/painted/rich-painted/settled target is consistently handle 3, with no pending target and no settle visual delta.
- All three retained summaries report `avatarRailBuilds=0` and zero repository requests, index builds, scene prepares, and canonical persistence commits at semantic ticks. This argues against a rail rebuild storm or per-tick DB/index/canonical-query path as the current first proven performance boundary.
- PROVEN performance-contract anomaly inside the active Avatar exact-resource path: during generation 23's first target, `SCENE_WINDOW_SLICE_OVER_BUDGET` seq 9006 reports `rowDiscovery=10561 µs` against a `1000 µs` budget; seq 9007 reports `dayHeader=1088 µs`; seq 9008 reports `completion=2128 µs`; seq 9009 then reports `SCENE_WINDOW_PREPARE_COMPLETED` for `avatar-live-root...target:1`, owner `liveInteractionResource`, `durationMs=23`, `uiIsolateMicros=20771`, `largestContiguousUiSliceMicros=10561`, `yields=24`, with 24 projected rows, 19 new row layouts and one new scene. CURRENT source declares a hard maximum contiguous UI slice of 1 ms in `DashboardLogBoxPreparedSceneCache`, so the 10.561 ms slice is a real contract violation, not merely a large-looking diagnostic.
- The corresponding generation-23 first-target pipeline summary at seq 9033 records pointer→recognizer about 34.6 ms, pointer→first raw scroll about 39.3 ms, pointer→first semantic about 51.9 ms, pointer→preview about 51.9 ms, and pointer→exact Phase-A store about 85.4 ms; the candidate was later superseded/coalesced before LogBox paint. This is a useful causal candidate but not yet proof that the same exact frame was the one the user perceived as choppy.
- Target-local live-root preparation cost varies substantially between retained flights/targets. Generation 23 includes about 45.9 ms aggregate active Avatar live-root UI-isolate work across retained target preparations, with one target at 20.771 ms UI-isolate / 10.561 ms contiguous; generation 22's retained target preparations total about 39.3 ms and peak around 7.324 ms UI-isolate / 3.073 ms contiguous. This variability is consistent with intermittent rather than uniformly poor smoothness, but correlation to the user's exact subjective rough flight remains UNPROVEN.
- CURRENT source audit: `beginBudgetAvatarMotion` cancels scene-window maintenance on input; `_primeBudgetAvatarLiveRowResources` uses the existing `budgetAvatarPreview` resource lane and schedules exact missing target preparation through the existing live-interaction resource owner; `primeBudgetAvatarFocusHotset` is intended to prepare semantic focus/hotsets ahead of direct interaction. Therefore the seq 9009 `owner=liveInteractionResource` `avatar-live-root` work is part of the foreground exact-target painter/readability path, not generic background maintenance.
- `DashboardLogBoxPreparedSceneCache.prepareWindow` is the first proven contract-anomaly owner. Its implementation already contains yielding/checkpoint logic and a 1 ms hard contiguous-UI budget. The retained log proves `rowDiscovery` can nevertheless execute a >10 ms contiguous UI tranche. The exact inner operation responsible inside `rowDiscovery` is still UNPROVEN and must be isolated with bounded subphase/work-unit correlation before modifying production behavior.
- Application commit audit: final `92222edd` itself only adjusts the direction trace frame-match condition and profile harness behavior; it does not alter Avatar fling physics or the core Avatar motion mechanism. Earlier production commit `e7c28bbc0450f8e7c64386f5ba7874250df1c89d` repaired direction-domain Avatar rebasing and explicitly preserved motion physics. Therefore a `92222edd` physics regression is UNPROVEN, and the user explicitly allows that slight choppiness may predate this build.
- Matching SCIP is available and was actually queried: tooling commit `3f1a2bda6b529302ba59a04e226dc935f8fa446d`, manifest `source_head=92222edddeee5bb13792f62f939eedf81c867a52`, `scip_dart=1.6.2`, raw index SHA-256 `22898ae4bd1aa4bb9d5af51c3f68034adddbd0e5112c01fd8986d44e77e46c12`. Graph refs confirm `DashboardLogBoxPreparedSceneCache.prepareWindow` is used by the Core live-resource path and dedicated cache tests; `beginBudgetAvatarMotion`, `primeBudgetAvatarFocusHotset`, and `BudgetTargetAvatarRail` have production CoreDashboard/controller consumers and direct test consumers. Source was opened after graph navigation to verify implementation facts.
- CI/profile audit: GitHub Actions run `34534995472` is fully green for exact `92222edd`, including `test-flutter`, `test-core`, human APK build, and A–K Android profile. This does NOT clear the physical smoothness issue. The automated K profile reports `scene_preparation_largest_contiguous_ui_slice_micros=148`, while the exact physical build's fresh human trace reports 10,561 µs. Therefore the current automated workload does not reproduce the physical resource-preparation tail and is false-green for this performance symptom.
- Protected no-regression anchor remains `6e962187e90e2a82431b1f91b224d2b52a6e0ba7`, which was physically accepted for Avatar motion/performance. Do not promote `92222edd` to a performance milestone: its direction/circle correctness automation is green, but the user has now physically reported intermittent slight Avatar choppiness.
- PROVEN: intermittent frame-pacing variability exists in the retained `92222edd` human trace; active `avatar-live-root` exact-resource preparation can exceed the source-defined 1 ms contiguous UI-slice contract by more than 10x; the current sampled Avatar correctness path remains healthy; no per-tick repository/index/canonical work and no rail build storm are present in the retained summaries.
- UNPROVEN: the exact >10 ms `rowDiscovery` slice is the complete root cause of the user's perceived choppiness; which inner `rowDiscovery` work unit causes it; whether a prior build already had the same tail; whether direction-rebase work changed pacing indirectly; and whether any physics parameter is causal.
- MISSING EVIDENCE: deterministic frame-level correlation tying exact `avatar-live-root` subphase/work-unit execution to FrameTiming, raw scroll, semantic crossing, visible publication, paint/raster and a repeatable cold/warm fling workload on current production-parent composition. This must be established before a production optimization is accepted.
- Next repair scope is STRICTLY Avatar fling smoothness/frame pacing. Do not work on direction/circle, Time, Mind/Slider, filter behavior, layout, visual design, or unrelated diagnostics. Preserve semantic target coverage, correctness, controller/ScrollPosition/physics identity, latest-wins/stale rejection and visible publication behavior. Do not fake smoothness by dropping targets/publications, slowing physics, debouncing, adding cooldowns, settle-only correctness, placeholders, stale data, or larger timeouts.
- Prompt-writer source change for this feedback: this journal entry only, committed with `[skip ci]`; no application source, tests, workflow, graph, milestone file or runtime behavior may be changed by the prompt-writer.
- Physical validation of the next application repair: `PENDING — USER ONLY`.
