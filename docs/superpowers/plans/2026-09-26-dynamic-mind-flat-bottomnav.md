# Plan — Dynamic Mind scale and flat BottomNav stretch

1. Establish baseline and add RED resolver/settings/layout tests.
2. Extend the two existing presentation owners and their tuner controls.
3. Implement the immutable dynamic Mind scale resolver, then bind its animated presentation to cells, legend, and Mind range styling.
4. Add the pure flat-BottomNav geometry bridge, then pass its derived delta through the one dashboard geometry resolver and existing mode-content envelopes.
5. Run focused widget/geometry regressions, full fast suite, analyzer, formatter, and diff checks.
6. Re-read the acceptance checklist, commit only complete application scope, push, monitor online human APK, download/check it, regenerate exact SCIP, then record factual journal evidence separately.
