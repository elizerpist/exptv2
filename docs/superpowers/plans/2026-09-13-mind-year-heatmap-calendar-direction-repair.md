# Mind Year Heatmap physical repair execution plan

No approval pause applies: this is the user-directed repair plan.

1. Preserve the canonical-worktree migration evidence, inspect the screenshot,
   feature diffs/tests/profile artifacts and current source, then backfill the
   missing factual journal evidence in a `[skip ci]` documentation commit.
2. Use the matching pre-repair SCIP graph only for navigation and verify each
   requested symbol in current source.  Create RED tests before production
   code: pure calendar geometry; widget paint/layout ownership; production
   parent disjoint direction sets; and frame-by-frame direction atomicity.
3. Add the required bounded `MIND_HEATMAP|` diagnostics to the existing
   controller/live projection/viewport boundaries and the existing Debug
   Console filter.  Test All, Mind Heatmap and unrelated category behavior.
4. Repair calendar layout with the pure static geometry authority and a
   row-driven 3×4 annual layout.  Keep cell paint as one dynamic field per
   month and prove no slider-time calendar recomputation or static-shell
   rebuild.
5. Run the directional RED tests and instrumentation.  Identify the first
   divergent boundary from request through visible paint, then repair only that
   owner.  Verify exact direction/range intersection, no blank/wrong frame,
   stale rejection and rapid latest-wins behavior.
6. Profile representative and large annual data, collecting bounded source
   work, direction-to-paint/projection timings, FrameTiming and rebuild/paint
   counts.  Do not introduce a two-direction cache unless a measured
   bottleneck requires it.
7. Commit each independently meaningful application change with the mandated
   WHY/EVIDENCE/CHANGED/VALIDATION/KNOWN LIMITATIONS/PHYSICAL VALIDATION body;
   follow each with a factual `[skip ci]` journal commit.  Push and obtain the
   exact human APK for each pushed production commit.
8. Re-run relevant regressions and analysis, regenerate the final-source SCIP,
   prove final refs/worktree/untracked preservation, download/hash the final
   human APK, update every checklist status honestly, and report physical
   validation as user-only pending.
