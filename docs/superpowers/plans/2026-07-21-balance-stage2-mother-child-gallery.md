# Balance Stage2 Mother-Child Gallery Plan

1. Keep the original Color Lab `zoomViewport`, `zoomHost`, and `zoomSurface` mounted; replace only `zoomSurface` contents and invoke its native zoom function at 50%.
2. Clone the actual B3M source column 16 times inside the original Balance `common-header-mode` / `common-header-row` classes.
3. Convert every FastInfo entry to a block declaration: optional contextual progress/split, variable-length fact list, and a visual type selected from the resolver’s existing visual vocabulary.
4. Rebuild only the contents of the existing B3M child panel: remove blocks that do not apply to the insight and replace the B3M SVG payload with an appropriate threshold, bars, ring, zone, activity, forecast, calendar, category mix, delta, schedule, split, or comparison drawing.
5. Preserve the original B3M dimensions, glass header, reserve/ratio block, Stage2 panel, lower transaction content, Color Lab variables/classes, and native zoom mechanics without standalone replacement CSS.
6. Verify native zoom retention, the 16-card visual-type matrix, source identity, script syntax, and separate HTTP delivery. Keep Color Lab and unrelated dirty worktree files unchanged.
