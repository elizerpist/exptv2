# Short execution plan

1. Reproduce the two Budget publication/ownership races with deterministic
   tests, then make the presentation controller publish a mode-visible epoch
   and preserve a compatible active edit session.
2. Identify Daily Rhythm's intermediate collapse owner in an isolated render
   test, repair its shell/clip ownership, and change only the internal
   allocation by +10%.
3. Extract the existing minimum-amount Query semantics and bounds into one
   reusable threshold authority/control; bind the Query sheet and Mind card to
   it without adding a second Query state or direct-input I/O.
4. Run focused regressions and static analysis, record the evidence, commit,
   push, follow the exact GitHub Actions human build, and download its APK.
