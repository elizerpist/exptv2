# shared live-publication authority repair plan

Base: physical application source
`7cfc75de2e9d387b661678459ae237aff44b4c11`; repair branch
`fix/repair-shared-live-publication-authority-codex-20260904`.

1. DONE — Audit the physical session, matching SCIP navigation index, 7cfc lineage,
   visible-frame store, candidate preparation, prepared-resource cache, stable
   renderer and existing production-parent tests. Record the proven and
   unproven edges in the engineering journal and checklist.
2. DONE — Add deterministic red coverage for producer-local generation collision,
   optional candidate-scene retention failure, readable Phase-A rendering, and
   preview-to-committed acknowledgement. Add bounded scope identity context
   before attempting the currently unproven scope construction repair.
3. DONE — Centralize preview ordering at the visible-frame boundary with explicit
   producer and global user-intent order, preserving producer-local stale
   rejection and canonical reconciliation ownership.
4. DONE — Make semantic query candidate preparation/application independent of
   optional rich scene retention; retain strict semantic scope/index identity
   validation. Repair the proven scope construction only after its regression
   identifies the exact tuple mismatch.
5. DONE — Expose/reuse bounded immutable prepared row layouts to render readable
   Phase-A rows on the stable surface without creating text layouts during
   paint. Keep rich scenes Phase B.
6. DONE — Repair Avatar atomic binding and only the source-proven Time
   preview-to-committed acknowledgement transition. Preserve motion physics
   and resident tick performance.
7. IN PROGRESS — Re-read the checklist, run formatting/analyzer/focused and broad affected
   tests in Ubuntu proot, review changes, make evidence-backed app commits,
   push, monitor the exact GitHub human APK build and download/hash it. Then
   regenerate the matching SCIP graph in its separate tooling worktree or
   explicitly report graph provenance as stale.
