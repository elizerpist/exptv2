# Implementation plan — Budget scope refactor and physical geometry repairs

1. **Inventory and red tests** — record baseline/log/screenshot evidence; add failing contracts for the old Summary visual-offset model, FAB-centre mismatch, and legacy independent financial-limit periods. Verify the current prepared temporal YEAR/MONTH/DAY and motion-only Trio routes before changing them.
2. **Segmented Summary geometry** — replace quarter semantic tracks plus visual offsets with one layout resolver whose actual Rects own paint, clip, semantics and vertical carousel hit areas. Permanently use the existing Large Icon badge, remove its selector setting/tuner/tests, preserve the amount zone, and derive separator centres from neighbour Rects.
3. **BottomNav physical geometry** — introduce the one centred, mirrored contour source; route fill and optional foreground stroke through it and centre the unchanged-size FAB on the same x. Add pure mirror/clearance and final-composite regression coverage.
4. **Canonical monthly persistence** — migrate the Dart/native model and transport to base-month plus month override values, add deterministic idempotent legacy conversion, and provide atomic vector mutation. Isolate legacy classes to migration input only.
5. **Prepared resolution and analyses** — make a bounded resolved-month bank the denominator owner and create typed Month/Day/Year/TypicalMonth analysis values. Wire Core's injected as-of date and preserve day display-versus-edit canonical-actual separation.
6. **Shared ring and presentation/editing** — extract the Fluvi ring geometry/material and implement the four fill strategies. Route Header/avatar/partition and quick edits through one selected analysis; implement month override, day shared override, year vector and Sum base edits.
7. **Verification/delivery** — focused domain/geometry tests, the 273-test protected suite and `flutter analyze` are green in Ubuntu proot. Native Room unit execution is blocked locally by the documented PRoot AAPT2-daemon startup failure; use the post-push GitHub workflow as the authoritative native/build execution. Re-run diff check, inspect every checklist item, commit/push, monitor the exact GitHub human APK workflow and download the artifact before handoff.

No work is delegated: the persistence model, prepared snapshot, controller, painter, and native codec form one coupled ownership chain. Splitting them across concurrent edits would create conflicting API migrations.
