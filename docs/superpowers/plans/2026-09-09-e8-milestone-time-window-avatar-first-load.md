# Execution plan — preserve e8 and repair Time window / Avatar first load

## Approved scope and execution mode

The user supplied an implementation-authorized, evidence-driven specification.
Execution remains inline: the proposed Time rebase spans one tightly coupled
Core/runtime/presentation/cache transaction, so splitting it would risk
incompatible authority edits. e8 stays the rollback/performance anchor.

## Ordered work

1. Verify exact e8/tooling provenance, freeze the two Drive exports, and
   separately commit the permanent e8 milestone record.
2. Record the acceptance checklist, architecture ownership and audit
   evidence; query the matching SCIP graph and inspect current source/tests.
3. Establish e8 normal-path controls, then add production-parent red tests
   for lower-bound Time crossings, month crossings after rebase, temporal
   identity equality, producer ordering, post-Time Avatar category/aggregate
   admissions, and no-false-smoothness.
4. Instrument the exact Avatar admission boundary before any root-cause claim;
   retain bounded error type/message-digest/stack-fingerprint evidence while
   preserving the existing UI fail-safe.
5. Extend the existing `DashboardDataRuntime.prepareQuery` /
   `commitPreparedQuery` flow with one latest-wins Time prepared-window
   candidate. It must preserve the Summary interaction order, build a bounded
   symmetric window, obtain exact `timePreview` Phase A, then commit aligned
   index/navigation/Budget/LogBox state.
6. Run focused Time red/green controls after each logical change, then repair
   Avatar focus/base coherence only against the proven post-rebase boundary.
7. Add the retained first-Avatar flight evidence and reproduce the healthy
   cold path. Optimize only a measured readiness/scheduler/render boundary;
   do not alter physics or hide latency through dropped publications.
8. Coalesce diagnostic flood with bounded retention, profile scheduler slices
   and frame overlap, and make only evidence-backed queue changes.
9. Run Ubuntu/proot focused and broad validation, compare the known 19
   inherited presentation failures exactly, audit source/diff ownership, make
   atomic commits, push, monitor CI, download/hash the human APK, and
   regenerate deterministic SCIP artifacts for the final application SHA.

## Required checkpoints

- Before production code: E8M-03 graph/source/test audit and a red test that
  fails on unmodified e8 for the observed boundary.
- After each production edit: inspect diff, rerun its targeted regression,
  normal e8 controls, accepted-target count checks and protected-source audit.
- Before delivery: verify no Mind/Slider, physics or geometry production diff;
  verify final app SHA equals human APK embedded SHA equals SCIP manifest
  source head.

## Stop conditions

Only the user's stated provenance/evidence/architecture blockers stop work.
A failed hypothesis, a red test, a required shared-Core change or pending
user-only physical validation is not a stop condition.
