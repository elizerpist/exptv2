# b166 interaction-authority repair plan

Starting point: `b1662045c15c2940e263ff191ca6314c9fc2d72f` on the local
`separated-core-modes` repair lineage.  The remote historical branch is not
used as a reset target.

1. Complete source and log forensics, map the common rich-paint admission
   edge, and inventory the exact current owners.
2. Add deterministic production-parent red tests: immediate Avatar selection,
   time release/recenter and Mind pointer-down list updates under an unavailable
   or delayed rich scene; add a debug-console marker-menu legibility test.
3. Split the shared acceptance result into Phase-A exact semantic/list
   acceptance and optional Phase-B rich-scene readiness, preserving complete
   identity checks and latest-wins cancellation.
4. Adapt Avatar, segmented time and Mind to the centralized Phase-A contract;
   retain the current Avatar carousel physics and never make rich paint a
   semantic admission/settlement prerequisite.
5. Update bounded interaction summaries and the engineering journal, run
   focused and affected regressions in Ubuntu proot, review every diff, commit
   coherent checkpoints, push the exact final SHA, monitor GitHub Actions, and
   download/hash the human diagnostic APK.

No physical success is inferred at any step.  The user alone validates the
APK on device.
