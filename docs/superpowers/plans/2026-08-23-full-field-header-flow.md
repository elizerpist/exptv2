# Full-field Header Flow Implementation Plan

> **For agentic workers:** Execute inline in this session. The user explicitly prohibited subagents.

**Goal:** Preserve the current Cool renderer while providing historical classic effects for comparison and five new full-domain source-UV flow effects.

**Architecture:** The catalog owns explicit animation-family metadata and the controller owns the active family/effect with the existing single phase clock. The shader keeps the current canonical palette pipeline, isolates exact 69d109 classic equations, and adds a single analytical inverse-advection engine used by IDs 9–13. Tuner UI renders the one active family only.

**Tech stack:** Flutter/Dart, runtime fragment shader ABI v3, `package:flutter_test`, GitHub Actions profile APK delivery.

## Execution order

- [ ] Inspect 69d109 and current shader functions; freeze independent RED fixtures for classic parity and flow contracts.
- [ ] Make RED suites fail in the Ubuntu Flutter environment before production changes.
- [ ] Add catalog family metadata and isolated classic reference shader lane; prove the classic fixtures pass while ABI/palette tests remain green.
- [ ] Add shared seeded analytic velocity, soft-domain inverse midpoint backtrace, and IDs 9–13 with testable source-UV probes.
- [ ] Add controller family selection, tuner family UI, controls, diagnostics, Portal/touch composition and widget tests.
- [ ] Refactor dead seamless-only routing, run focused/protected suites, analyze, shader/profile checks, and record required commits.
- [ ] Push the behavioral feature, await human normal profile APK CI, download/hash it, then append the docs-only milestone without changing older text.

## Non-negotiable invariants

- Never restore endpoint RGB palette authority or alter Cool P/W/static 112° semantics.
- Existing shader IDs 0–8 remain stable; new IDs are explicitly 9–13.
- New effects have no palette owner, semantic centre, divider, global wrap, global translation or rotation primitive.
- New source coordinates flow before palette lookup; optics are post-palette only.
- The shared `DashboardHeaderVisualController` remains the only continuous Header clock.
- No modifications to dashboard/rail/query/LogBox/physics ownership.
