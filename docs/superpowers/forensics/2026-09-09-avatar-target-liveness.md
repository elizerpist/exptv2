# Avatar-only aa61242 physical rejection and repair investigation

AA61242 AVATAR CORRECTNESS — PHYSICALLY REJECTED BASELINE

AA61242 / E8 AVATAR MOTION — PRESERVE EXACTLY

TIME PRODUCTION SOURCE — NO TOUCH

NEW AVATAR CANDIDATE — PHYSICAL VALIDATION PENDING, USER ONLY

## Exact preflight

Dedicated branch fix/avatar-target-liveness-codex-20260909, created clean from aa61242c4b933ad9d650361ae1014d67fd184186 (parent024fdbc1fd5d9677c93090a1d18f5446fd6f2845). Existing repair branch origin is b2865023463dc9f9496a2f338f4fe40b6ad1a9c9, documentation-only descendant. No newer app source substituted. Origin separated-core-modes remains df1d4a3ca9ca83565260a6f7618ab3786ca5650e. Main worktree's unrelated dirty stats/prototype changes and prior worktrees' untracked index.scip/failure images preserved.

Tooling c8a4996842ec9738c2082a3b953e5ccd7c52cb85 manifest exactly names aa61242, parent024fdbc1, SCIP1.6.2, raw index hash215dc25247e686a6468f665ae706d9b530402ae0301dc89194d0223234370837 (also verified against retained raw file),429 documents,9859 repository symbols,70299 refs,20918 edges,76 changed-impact records.

Graph query DashboardBudgetLogboxDrilldownCoordinator:22 references (6 production,16 tests). Production consumers CoreDashboard, budget_dashboard_core_surface, budget_distribution_pager, budget_partner_distribution_card, dashboard_core_mode_host. Source confirms the Avatar target maps via existing coordinator/Core focus methods and presentation changes only in onVisibleSemanticCommit. Private _budgetAvatarExactLocalResourceWindowFor query has no unique exported graph match; inspected its current definition/callers directly. Graph is static impact evidence only.

## Immutable inputs

The seven files under /storage/emulated/0/Download/fluvi/diag contain five unique payloads. Download root prompt/audit/evidence copies were also hashed and matched. Nothing was deleted or moved.

| Unique input | Original SHA-256 | Treatment |
|---|---|---|
| Avatar-only coding prompt | ded3df5dde5168814776eebf7cdb4aebbb1c6424c7b8fe5e07932e9ea4ccb27c | Current implementation-authorized spec, read fully |
| Avatar forensic audit | 06beb568613dcae2d662f89b1811673f98151c6e931841b25ca248c8db52e9e0 | Current report, read fully |
| Deduplicated evidence | d81ca32e726179f51b769c3cc6e64ba413a31071679da6cc15c8f72069aedf07 | Parsed full JSON; duplicate flights/summary compared with audit/K |
| K baseline JSON | a21620c6029aec16acd0ba8669f9f2677e6adc036726790fa86352a6d5e408a3 | Parsed full JSON incl counters, timings, resource banks, actual paints and motion events |
| Historical temporal diagnosis/prompt | d5ef511695e36beaa55ec0edf21ebf126cfd79b9dcb10a0c1f51c74f13f2bfb8 | Read all1549 lines; UNRESOLVED/9e8 provenance and Time repair instructions superseded; NOT current implementation scope |

Drive metadata re-read: Fluvi logs avatar fling, id1FF1BnHJHOEygG3QhMeDfrehzjo0YzIztaGJfNTWZqNs, native Google Doc, modified2026-09-09T07:26:30.407Z. Complete text/plain export returned689397bytes. Readable fetch returned all matching text with original BOM/CRLF. Frozen losslessly as avatar-log.export.json: decode its content field to UTF-8 to recover exact original bytes. Verified689397bytes, SHA256dcab9c791ba80d09ad79f20ab01cb33ce8c07d366757c241428f76f3bc4183d3. No session mixing or silent normalization.

All 2000 event copies parsed by seq; 1025 unique events 2329–3353; 975 byte-identical duplicates; 0 conflicts. FLOW matching must allow metadata between stage and seq: a naive anchored stage/seq parser omitted 3 unique entries; the corrected sequence parser covers all 1025. Raw evidence confirms final target 7 derivation in 324 microseconds, an exact non-empty binder miss, generic `exactLocalHotsetUnavailable`, a pending candidate, and physical settle without a matching semantic frame. The original export does not contain the inner resource-window rejection reason. A newly instrumented reproducer can prove a reachable cause, but cannot retrospectively add that missing field to the physical session.

The retained human APK was independently rehashed during this audit:
`/storage/emulated/0/Download/fluvi/fluvi_HUMAN_DIAGNOSTIC_aa61242.apk`,
82,072,881 bytes, SHA-256
`38fae5e1f463eac1ae987ceed4a0d8a7b2d434e66fb41d1d02f44922ace4ffeb`.
Historical CI run `34317086666` is the supplied delivery record; this audit did
not query that online run again. The later `b2865023` delivery-document commit
has aa61242 as its direct parent and does not supply a different app baseline.

## Ten-commit source lineage

Local `git log`, commit bodies, changed-path lists, and Avatar-relevant source
diffs were inspected for the exact range
`5ba6c01eb391339c27861762ba888e29aae38593..aa61242c4b933ad9d650361ae1014d67fd184186`:
10 commits after 5ba, 11 commits inclusive. The verified totals are Core
+2229/-318, Avatar rail +546/-7, and Core focus test +3357/-1612. Those totals
are context, not evidence that each addition repaired the same failure.

| Commit, in order | Source change and resulting boundary | Evidence limit / treatment now |
| --- | --- | --- |
| `fc35c1bbe280fdcecb262960eefd1e282595f7b6` | Replaces the old global resource-ready prerequisite with exact Avatar binding; introduces one `_PendingBudgetAvatarPhaseACandidate`, preserves its original interaction order, and retries promotion from real resource completion. Adds aggregate-already-visible handling and quick-edit identity fencing. | Corrects the then-observed reject-and-forget path. The completion retry can still fail binding and retain a pending candidate without independently scheduling its exact resources. This is not proof of all later cold/hotset states. |
| `f39091d237e23ad9e2e1eb1182b2c9e9d7e13386` | Adds typed foreground Time/Avatar ownership and the existing per-lane cancellation capability. A new producer revokes old work; repeated admission of the same producer returns early. Cache reuse checks actual width/DPR. | Establishes cross-producer foreground handoff, not same-lane latest-target resource priority. Time admission/publication changes are protected. Semantic prepared-index comparison introduced for shared Time handling does not remove Avatar hotset object identity. |
| `5b43a6474964d5af75fd102a3e818ce3dda0d8cd` | Adds bounded Avatar first-target/build/paint/raster observations across rail, Budget presentation, artwork, Header contrast, and the Budget surface. | Actual acknowledgements improve observability; their presence alone does not establish final-target liveness. Motion geometry and physics are not redesigned. |
| `2159bbe3e7ec6607f96e11c49afa1150bc358bd6` | Records foreground handoff evidence, architecture, checklist, and plan. | Documentation only; separate Avatar and Time sessions must remain separate. |
| `c2a9046f28599304a85ff6caf9e5e5cc3a50e296` | Adds K through the real app, Header gesture, and Avatar rail, plus report checks and finite profile time allowances. | Its positive accepted/painted counts and final visible-frame equality do not require a final non-empty category, canonical coherence, zero pending candidates, or repeated flights. |
| `b7f168dfee250a2435852e43d2499eee24ca1fb8` | Threads startup readiness through shell/CoreDashboard/viewport/render surface and defers actual initial unreadable painting while still reporting attach/layout. | Repairs the separate initial `visiblePayloadWithoutDrawable` failure. It is not a general permission to publish later unreadable Avatar targets. |
| `e8b73e3e939104164e55b09caf592f84ee59fb14` | Classifies a missing rich Time bank as noncritical only when the real cache has a complete exact Phase-A fallback; renderer/viewport checks use that actual fallback. | Correct fallback accounting, not a resource-preparation liveness repair. This commit is the qualified physical motion milestone. |
| `135dcaa2165923b5b1ce58460c088ad4990e7a3c` | Records the e8 direct-manipulation performance floor. | The milestone explicitly retains cold Avatar and Time correctness work; do not promote it to correctness acceptance. |
| `024fdbc1fd5d9677c93090a1d18f5446fd6f2845` | Records e8 Time-window and first-Avatar evidence before implementation. | Documentation only; direct parent of aa61242. |
| `aa61242c4b933ad9d650361ae1014d67fd184186` | Rebases protected Time windows and adds an exact-local Avatar hotset resource window. Avatar requires no pending plans, non-empty required keys, every semantic entry, and an aggregate frame; its key includes the required hotset keys. Pointer admission re-primes semantic plans. Adds bounded Avatar rejection/error observations. | The exact-local null return logs failure and exits before scheduling replacement resource work. This new all-hotset prerequisite can prevent an otherwise derived current target from ever reaching a real completion callback. Time repair remains protected; this exact human Avatar build is physically rejected. |

The source review covered the full fc35 Avatar change, foreground/Avatar/cache
hunks in f390, all changed Avatar consumer paths, and the aa61242 Avatar
resource-window/candidate/pointer changes. Large unrelated Time implementation
hunks were read only as needed to establish shared contracts and the scope
boundary; this is not a new validation or proposed revision of Time behavior.

All 17 production paths changed by the range were classified. Paths in the
following table are relative to `lib/`; grouping avoids suggesting that their
shared contracts belong to a new Avatar owner.

| Changed consumer / contract | Audited responsibility |
| --- | --- |
| `features/dashboard/application/dashboard_core_controller.dart` | Candidate/order ownership, focus derivation/admission, exact binding, foreground lease, and deferred canonical install. |
| `features/dashboard/application/dashboard_budget_presentation_controller.dart` | Matching semantic-target selection and bounded paint observations; not a resource-preparation owner. |
| `features/dashboard/presentation/core_modes/budget_category_avatar_rail.dart` | Physical intents, coordinator completion, and actual target/paint accounting; preserve motion internals. |
| `core/categories/presentation/budget_category_avatar_artwork.dart`; `features/dashboard/presentation/core_modes/dashboard_header_contrast_text.dart`; `features/dashboard/presentation/core_modes/budget_dashboard_core_surface.dart` | Actual Budget-progress/Header acknowledgement plumbing. |
| `features/dashboard/presentation/core_modes/budget_limit_quick_edit_gesture.dart` | Existing displayed-target identity guard. |
| `features/dashboard/presentation/core_dashboard.dart`; `app/shell/fluvi_app_shell.dart` | Real coordinator/cache/renderer attachment and startup readiness threading. |
| `features/dashboard/logbox/application/dashboard_logbox_scene_window.dart`; `features/dashboard/presentation/widgets/dashboard_logbox_prepared_scene_cache.dart` | Typed shared resource contract, exact cache binding, metrics, cancellation, and fallback availability. |
| `features/dashboard/presentation/widgets/dashboard_logbox_render_surface.dart`; `features/dashboard/presentation/widgets/dashboard_logbox_viewport.dart` | Actual readable rendering and acknowledgement guards, including startup deferral and fallback classification. |
| `core/diagnostics/fluvi_diagnostic_logger.dart` | Bounded retained diagnostic observations; not target authority. |
| `features/dashboard/presentation/widgets/summary_pill_experiments.dart`; `features/dashboard/runtime/application/dashboard_presentation_controller.dart`; `features/dashboard/time_navigation/application/dashboard_segmented_target_acceptance.dart` | Adjacent protected foreground/Time admission contracts. Their historical changes do not authorize changes in this Avatar-only task. |

The coordinator, focus deriver, visible-frame store, and shared carousel
mechanism are not changed files in this historical range. Their existing
interfaces still determine the repair's single write path: physical intent →
coordinator → Core candidate and exact resource bind → visible semantic commit
→ Budget/Header/progress selection and actual LogBox paint → fenced canonical
installation. Independent visual patches would split that authority.

## Historical evidence and superseded conclusions

1. **5ba cold rejection.** The [2026-09-07 forensic report](2026-09-07-avatar-phase-a-publication-budget-coherence-repair.md)
   and its [session manifest](../evidence/2026-09-07-avatar-phase-a-publication-budget-coherence-session-fluvi-1788752412870045/manifest.json)
   identify session `fluvi-1788752412870045`: Avatar has 2,000 copies / 1,085
   unique events 1726–2810, with 915 duplicates; the separate retained Budget
   range is 8760–9759. The gap 2811–8759 is unknown. Forty-three Avatar requests
   were rejected with no accepted/painted target. fc35 repaired its specific
   reject-and-forget boundary. That earlier diagnosis does not prove which
   inner branch failed in the later aa61242 human session.
2. **fc35 foreground handoff.** The [2026-09-08 report](2026-09-08-foreground-phase-a-handoff-avatar-render-cadence.md)
   and [manifest](../evidence/2026-09-08-foreground-phase-a-handoff-avatar-render-cadence/manifest.md)
   keep Avatar `fluvi-1788868338296445` separate from Time
   `fluvi-1788868486605950`. Retained Avatar events are 2023–3023; Time events
   are 1450–2449. The primary Avatar fling is outside the retained Avatar
   document. The controlled Time foreground-priority red and actual-metrics
   cache-reuse red support those repairs, not a physical Avatar cadence or
   final-target correctness claim. The manifest explicitly distinguishes
   native DOCX bytes, decoded text bytes, and prompt-authoring hashes; they
   must not be reported as byte-identical representations.
3. **e8 performance floor and separate Time failure.** [MILESTONE_COMMITS.md](../../../MILESTONE_COMMITS.md)
   and the [2026-09-09 e8 report](2026-09-09-e8-milestone-time-window-avatar-first-load.md)
   preserve the user's qualified motion/performance acceptance. The
   [e8 evidence manifest](../evidence/2026-09-09-e8-milestone-time-window-avatar-first-load/manifest.md)
   identifies `fluvi-1788906284239925`: Avatar 6205–7205, Time 17335–18335,
   with the intervening gap unknown. The initial healthy Avatar flight aged
   out; substantial retained structural logging cannot reconstruct it. The
   proven Time exception is a 2010 target outside the prepared 2014–2038
   window, thrown by `railInteractionSceneWindowFor` before the later missing
   frame guard. aa61242 already repairs that protected path. It is not proof
   of the fresh Avatar resource-window subreason.
4. **aa61242 automated K is insufficient.** The [supplied K JSON](../evidence/2026-09-09-avatar-target-liveness/baseline-K-avatar-first-target.json)
   reports first outcome `exactEmpty`, eight accepted and eight painted
   targets. At a motion summary, physical target 3 differs from the earlier
   painted target 7 while target 3 awaits exact paint. A later paint does
   reach target 3, but it too is empty. Therefore the interim mismatch must
   not be described as proof that target 3 never paints; the actual false
   green is the missing non-empty final/canonical/zero-pending requirement.
   This is one pointer/flight, with no exact-painted aggregate 0 or endpoint
   8 evidence, not the required persistent multi-flight contract.
5. **Old temporal prompt is historical.** Its unresolved source/tooling claims
   and `9e8a7b3a0a1f` build prefix predate this exact aa61242/c8 provenance.
   Local git resolves that older source to
   `9e8a7b3a0a1f17bbd9433927e1d8a0afcd8413e9`. Its Time implementation scope
   and unresolved-milestone instructions are superseded by the current
   Avatar-only prompt. The original document remains unchanged.

No separate historical completion report was found among the examined
artifacts; the auditable record is the commit bodies, forensics, manifests,
architecture, plans, and checklists. The supplied older audit mentions
`facts.json` and `docs_content.json`, but those standalone files are not in
the current supplied bundle. Neither absence licenses reconstructing them or
inventing a completion verdict.

## Exact aa61242 physical failure

The authoritative session is `fluvi-1788938720595789`, profile
`aa61242c4b93`. It has 34 Avatar requests, 17 accepted, 18 rejected, 18 pending
candidate observations, 17 live-root misses, 18 resource-deferred events,
20 progress mismatches, and 17 LogBox paints. Counts are events, not a claim
that repeated or refreshed summaries represent distinct requests.

| Flight generation | Requested handles in order | Accepted handles | Retained endpoint evidence |
| --- | --- | --- | --- |
| 22 | 3,2,1,0,8,7,6 | none | Settles on 6 without a matching semantic target. |
| 23 | 5,4,3,2,1,0,8 | 5,4,3,2,1 | Interrupted; no retained settle. |
| 24 | 7,6,5,4,3,2,1 | 6,5,4,3,2,1 | No retained settle. |
| 25 | 0,8,7,6,5 | 6,5 | Settles on 5. |
| 26 | 4,3,2,1,0,8 | 4,3,2,1 | Settles on 8 with latest paint still 1. |
| 27 | 7 | none | Settles on 7 with no matching semantic/painted target. |

There are six distinct flight generations and seven motion-summary events;
the extra summary is not a seventh independent flight. The apparent Time
recovery accepts empty day-scoped targets 5,4,3,2,1,6. Aggregate 0 has three
non-empty rows, and endpoints 8 and 7 are also non-empty; these remain the
failing path. Empty-only acceptance cannot establish revived correctness.

The final retained trace is particularly discriminating:

- seq 3336 requests target 7; seq 3338 derives focus generation 161 from the
  prepared index at base revision 2458.
- seq 3339 records current-root derivation in 324 microseconds, prepared hit,
  no worker/full scan/copied rows. The focused all-time index has 18 entries;
  the exact day `2026-07-09` visible payload has one row. These are different
  scopes, not contradictory counts.
- seq 3340 records `LIVE_ROOT_MISS`, `resourcesReady=true`,
  `phaseAReady=false`, and that exact non-empty row. Global readiness is
  therefore insufficient for the exact target.
- seq 3342 records only `exactLocalHotsetUnavailable`; seq 3343 retains the
  candidate at interaction epoch 167/local generation 161. Seq 3344 shows
  physical Avatar 7 while the visual target is still 1.
- seq 3347 reports zero accepted/painted targets, 114 raw scroll events, zero
  rail builds, 12 timing samples, and no missed timing sample; seq 3348 settles
  on 7. Smooth movement did not publish exact data.
- seq 3353 contains the user's stuck-filter mark, with canonical category
  still corresponding to handle 5. A paint of target 1 and canonical target
  5 can both be stale relative to the final physical target 7.

## Source-supported causal path and observed regressions

On aa61242, `_bindBudgetAvatarLivePhaseA` asks the real cache for exact readable
resources. A non-empty miss defers the already-derived candidate.
`_budgetAvatarExactLocalResourceWindowFor` then requires every broad semantic
hotset prerequisite before the resource request can run. Its null branch
returns `false` without scheduling work. Promotion is driven by an actual
resource completion, and a later failed bind can again leave the candidate
pending. The current target's derivation can thus be cheap and correct while
the owner waits for a completion it did not schedule.

The original physical log does not distinguish pending plans, empty required
keys, a missing entry, aggregate construction, scope/base mismatch, or identity
drift. In particular, object-identity drift remains unproven and is not a
source-grounded justification for a repair. Typed diagnostics and the real
composition reproducer establish the reachable pending-plan case below.

### Cold real-composition red

[cold-production-composition-red.log](../evidence/2026-09-09-avatar-target-liveness/cold-production-composition-red.log)
is the behavioral red with typed instrumentation and the aa61242 liveness
behavior still present. The persistent real `CoreDashboard` owns its rail,
coordinator, cache, binder, Header, progress, and LogBox. The external data
fixture has eight disjoint non-empty category rows and their eight-row
aggregate; it does not fake resource availability or publication.

Only category 1 is initially primed. Mounting exposes cached=1/pending=6;
the real pointer re-prime reaches pending=7. For target 8 the diagnostic
records `pendingHotsetPlans`, required=8, cached=1, pending=7, missing=7,
`replacementScheduled=false`. The real rail settles on 8 after eight
crossings, while selected target remains 0: `Expected: <8>, Actual: <0>`.
This proves the pending-hotset branch can strand the current non-empty
target. The fixture's broad readiness is not identical to the original
physical `resourcesReady=true` state, so the test does not prove that this
was the original session's exact inner reason.

Earlier fixture compile/geometry mistakes and a busy automated-scheduler run
were setup problems, not product reds. The warm control completed 22
forward/reverse complete-ring flings in 55 seconds, but every endpoint was
aggregate 0. Its green result did not prove a non-empty category endpoint or
the cold semantic-hotset case. These weaker conclusions are superseded by
the endpoint-8 behavioral red and the later production-composition evidence.

### Latest exact target blocked behind superseded work

[latest-target-priority-red.log](../evidence/2026-09-09-avatar-target-liveness/latest-target-priority-red.log)
is a second independent behavioral red, observed after the initial exact
target preparation change. Real target-1 cache preparation is held at its
existing `yieldToBackground` checkpoint after resource/token acquisition.
Target 8 supersedes it, then target 8's own checkpoint is released. Target 8
still does not complete while old target 1 remains held: `Expected: true`,
`Actual: <false>` at the foreground-priority assertion.

The existing shared cache does not preempt equal-priority work by merely
queuing another Avatar request. Candidate supersession therefore also needs
to revoke the old Avatar lane through the already-existing cancellation
contract. The current change performs that cancellation after clearing the
old candidate slot. The shared cache scheduler is unchanged. The same test
then passes with target 8 readable before old target 1 is released, and the
late old completion cannot replace it.

### Lifecycle evidence and return-to-current follow-up

The [lifecycle report](../evidence/2026-09-09-avatar-target-liveness/lifecycle-test-report.md)
records the initial seven-test run: six pass, one genuine priority red; after
the lane-cancellation change all seven pass in eight seconds. Focused analysis
then passes after one braces-only correction. These tests use the real Core
and real cache and cover exact preparation with empty broad required keys,
original-order replay, pointer cancellation, disposal, explicit failed or
cancelled preparation, and older deferred canonical installation. Their six
initial passes establish preserved behavior in the implementation at that
time, not six invented aa61242 baseline failures. They do not synthesize
paint acknowledgements or claim physical rail verification.

A subsequent review found another ownership boundary: accept target 1, hold
target 8 preparation, then request current target 1 again in the same pointer.
The already-active fast return occurs before pending-candidate supersession
and generation fencing. The added eighth lifecycle regression has an
observed red in
`/data/data/com.termux/files/home/.codex/tmp/avatar-return-current-red.log`:
target 8 has not terminated when the latest target-1 request completes.
The regression now permits either immediate exact reuse or a new real-cache
preparation for latest target 1, while still requiring target 8's coalesced
terminal before releasing its held work and final canonical target 1.
That revised test's green result is not claimed by this audit; integration
verification must record it separately. The earlier seven-test report is
historical evidence for its stated revision, not a claim that the expanded
eight-test file was already verified.

## Remaining evidence obligations

- The original inner physical rejection reason is absent from the export.
  The pending-plan reproducer is a proven reachable cause, not a recovered
  missing physical log field.
- Historical fc35/e8 records retain presentation totals such as +586/-19
  and +595/-19. A bounded search of retained evidence and local temporary
  logs did not locate the original complete 19 test names and normalized
  failure outputs. Counts alone do not satisfy prompt §15. Compare the exact
  aa61242 baseline in the same environment if the original raw output cannot
  be recovered; do not alter unrelated goldens to hide this gap.
- Actual expanded production-composition, null-branch, terminal, canonical,
  sparse, repeated-flight, ownership-bound, protected-source, and profile
  results belong to their verification reports and the parent checklist.
  This historical audit does not mark those tasks complete.
- No new physical success, exact-SHA CI/profile acceptance, or APK delivery
  follows from this document. The old aa61242 correctness verdict remains
  physically rejected; e8/aa61242 motion remains protected; the new candidate
  remains **PHYSICAL VALIDATION PENDING — USER ONLY**.

See the [acceptance checklist](../checklists/2026-09-09-avatar-target-liveness.md)
for current task status. This audit updates only the forensic record and does
not change checklist or plan completion states.

## Final local integration follow-up

The earlier audit's uncompleted obligations are now resolved locally in [local-validation-final.md](../evidence/2026-09-09-avatar-target-liveness/local-validation-final.md):10 lifecycle tests pass;8 final persistent production-composition scenarios pass; full application311 and fast369 pass; full presentation605/19 exactly matches all19 baseline failure names and normalized outputs; full boundary25/1 matches its separately reproduced baseline regex failure. Final analysis/format are clean. The raw baseline and current logs, complete normalized assertions and comparison JSON are retained. No historical evidence or unrelated golden was rewritten.

The exact controlled baseline earlier1-painted/final8-miss race is now observed: physical8 with selected/canonical1 after a real binder miss. It does not identify the original physical export's missing inner reason. The natural cold baseline and instrumented pending-plan red remain separate evidence. All final retention assertions pass; source review supplies explicit finite index/listener ownership bounds rather than claiming runtime heap counts.

Exact-SHA CI/K/human APK/SCIP delivery is still pending at this application commit. Physical acceptance remains USER ONLY.

## Actual first CI: exact renderer collector false negative

Application174ad841 completed online Flutter analyzer/369 curated tests, native tests and the normal human diagnostic build. Its normal APK was downloaded and hash-verified. Actual Android K then stopped at the first final target: all ten target identities were3, category/query/value fields matched, pending0,8/8 nonempty accepted terminals, but final_target_exact_painted=false. The untouched JSON and strict-validator rejection are retained in this evidence directory; four completed flights and final K acceptance were not achieved.

The collector checks only hasReadablePhaseAPaint in three places, whereas the immutable production paint acknowledgement distinguishes Phase A and rich Phase B rows. Core accepts either genuine painted renderer after exact identity/resource checks. The missing raw Phase A/B counters in that first K export prevent claiming directly observed rich row values; the diagnosis is source-supported and requires a controlled rich-only regression plus explicit raw counters in the next actual profile. Task4/AVL16a corrects measurement only; no production code or physical motion change is authorized by this follow-up. The earlier source/automated records remain historical evidence.

The controlled rich-only predicate test fails before repair, and the expanded contract initially yields49 passed/13 failed. After correction,62 focused tests pass, including the existing false-green negatives and raw empty/zero/negative/missing/stale-revision rejection. The collector exports positive actual Phase A or rich Phase B counts through one pure report predicate; legacy Phase A fields remain literal. This follow-up changes only profile/test sources. The next actual CI raw fields must still confirm runtime renderer evidence.

## Actual a047 CI: K succeeds, incomplete suite is incorrectly reported green

CI34351134148 at a047e216803d3ac01d97b4d5ffd03842a1579db3 provides actual four-flight K proof. Root and profile agent independently validate final handles3,0,3,0; exact raw Phase A rows0 for every final and rich Phase B rows2,3,2,3; exact-empty false; paint/visible revisions2=2; all target/category/query/Header/progress/value identities equal. Each flight has8 requests,8 accepted nonempty terminals and zero pending candidates or Time interactions. The disjoint eight-category fixture contains94 rows total; all nine handles appear among32 exact renderer paints. This directly observes valid rich-only final painting in the new run; it does not invent missing old174 counters.

The whole A–K suite nevertheless times out at25minutes during J. The log records `Some tests failed.` and then the host driver's `All tests passed.`; the workflow conclusion is success. J and final whole-suite comparison evidence are absent. This green job is not accepted as complete A–K validation. The unchanged artifact ZIP has SHA256 `7c44a62dedbb82fc416b99dc32a1fe5958037db99fed35c81f6f65fc8e8d561f`; K raw JSON hash is `a2425d3fbf94ec53340c453eee2ec4e45130b046c5acddd1a762fc0d3d5497c8`. Raw K, post-renderer/completion data, the entire job log, timeline, provenance and independent validation are preserved as `ci-a047e216-*` in the evidence directory.

The observed timeline is progressive: K completes12:42:55, B/C/D/E each takes approximately4m18 from stable to ready, H completes13:02:17, J starts then, and the test times out13:04:53. The timeout stack names the test runner, not an application failure. The captured host has11988MiB available RAM and zero memory-pressure averages; an OOM cause is not supported. Task4/AVL16b must make the host fail closed on partial suite evidence and give the complete measured suite an appropriate finite overall budget while preserving all per-flight/performance assertions and production source.
