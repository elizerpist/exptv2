# Final exact-SHA profile verification — 33ee878b

**Required CI profile acceptance: PASSED. Physical-device performance: PENDING USER ONLY.**

Source `33ee878b070345c336bb73df2b087d9a63c87c5a`, first attempt of [CI34358444008](https://github.com/elizerpist/exptv2/actions/runs/34358444008), profile job102491184807. No edits, reruns, cancellations, retries, or artifact changes were made in this verification lane.

## Complete suite and failure propagation

The actual raw response contains all eleven A–K scenario reports and the version1 completion marker with `all_assertions_passed: true` and the full exact scenario-key set. The committed `DashboardProfileReport.validateCompleteSuite` passes. Its imported owner was byte-compared with `git show 33ee878b:integration_test/support/dashboard_profile_report.dart`.

All19 separately exported JSON maps match their corresponding values in the complete raw response. The final ten-fling timeline has10 entries, and all four density timelines have10 entries each. J reached READY at14:20:30UTC; the actual app reports `28:21 +2: All tests passed!` at14:20:32, followed by the host driver success at14:20:34. The entire727,389-byte job log has zero hidden failure matches, including test timeout, failed tests, unhandled exception, `[E]`, lost connection, and nonzero exit patterns. The full job/run metadata confirms success.

J took362.239 seconds including warmup; K took137.771 seconds. The complete28:21 suite fits the unchanged approved35-minute test budget with finite SDK/shell/workflow margins. No performance threshold or coverage reduction was needed.

## Actual K and post-renderer evidence

All four real K final targets are3,0,3,0. Physical, desired, semantic, actual painted, selected, focus, Header, progress, LogBox and canonical identities agree for each; category/query and displayed-value checks pass. Phase A rows are0,0,0,0; actual Rich B rows are2,3,2,3. Every exact-empty flag isfalse and every paint/visible Core revision pair is2=2.

Each flight has8 requests and8 accepted-exact-nonempty-painted terminals. Pending candidates, generic coordinator rejections and Time interactions are0. The separately exported post-renderer snapshot remains target0, Phase A0/Rich B3, revision2=2, coherent identities,8/8 terminals and pending0.

Eight disjoint categories are nonempty with counts1:5,2:11,3:7,4:2,5:4,6:10,7:18,8:37; aggregate94. Actual paint acknowledgements include every target0–8:32 exact-renderer paints, with the literal Phase A subset27. No rich-only paint is relabeled as Phase A.

## Independent comparisons and limits

The existing motion-isolation validator passes for all eleven reports. Independent inspection of the year populated/empty, month populated/empty, and first/tenth comparisons confirms identical targets, settle indices and semantic sequences, zero velocity/physical/logical endpoint differences, matching exported p95 deltas, and all unchanged duration gates. First/tenth motion durations are25.957/24.031 seconds. All required scene counters are0.

The optional raw physical-frame-target report truthfully remains `environment_requirement: physical-device-profile`, `passed: false`, with59 emulator frame-limit entries. The normal CI run does not enable the physical-device-only enforcement flag. Required CI correctness/motion/full-suite acceptance passed; this evidence does not establish physical-device frame performance.

## Artifact provenance and reproducibility

Artifact10108606541 is `dashboard-profile-results-33ee878b070345c336bb73df2b087d9a63c87c5a`,2,125,859bytes. Original ZIP SHA256 `adb92df9fd0bd2eb593dcff77037ba237a88ea767a6336221b80c6129d327218` matches the GitHub API digest exactly. All28 extracted files retain their original hashes. The complete raw response is26,068,357bytes; its SHA256 is `016400d624f7b1ab8d9da6be4378bb5d972e46dafb5ddd6f85881a1751c651a8`.

Files under `~/.codex/tmp/avatar-profile-33ee878b/`:

- Original `dashboard-profile-results.zip`, complete `artifact/`, API provenance files and `artifact-file-sha256.json`.
- `validate_actual_profile.dart` and `validation-result.json`: committed complete-suite/metric/motion/K/final validation, plus exact four-flight and timeline requirements; Ubuntu Dart exit0.
- `validation-compact-summary.json`: selected raw K/final fields and critical hashes without event dumps.
- `inspect_actual_comparisons.py` and `comparison-validation-result.json`: independent comparison/scene review, exit0.
- `raw-response-consistency.json`, `hidden-failure-scan.json`, `scenario-timings.json`, full `profile-current.log` and marker timeline.
- `test-flutter.log`: actual CI analyzer no issues23.6s and **417 tests passed**.

Parent-owned normal human APK, graph publication and final delivery remain separate delivery responsibilities. Earlier failed attempts remain preserved unchanged.
