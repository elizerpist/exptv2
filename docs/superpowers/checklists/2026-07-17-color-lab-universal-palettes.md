# Color Lab Universal Palettes Acceptance Checklist

| ID | Source instruction | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| CL-UP-001 | User: add Meadow green slider to test header with text-window input | `docs/prototypes/color_lab.html` standalone portal signature panel | `Meadow green` row appears below current sliders with range + 10–100 window input and exact colors `D9ED92 B5E48C 99D98C 76C893 52B69A 34A0A4 168AAD 1A759F 1E6091 184E77` | Static test + code inspection | DONE |
| CL-UP-002 | User: add Soft rainbow slider | `docs/prototypes/color_lab.html` standalone portal signature panel | `Soft rainbow` row appears with range + 10–100 window input and exact colors `FBF8CC FDE4CF FFCFD2 F1C0E8 CFBAF0 A3C4F3 90DBF4 8EECF5 98F5E1 B9FBC0` | Static test + code inspection | DONE |
| CL-UP-003 | User: add Ocean blue serenity slider | `docs/prototypes/color_lab.html` standalone portal signature panel | `Ocean blue serenity` row appears with range + 10–100 window input and exact colors `CAF0F8 ADE8F4 90E0EF 48CAE4 00B4D8 0096C7 0077B6` | Static test + code inspection | DONE |
| CL-UP-004 | User: dragged slider overrides previous testheader coloring | `applyMindPortalSignature`, `applyMindPortalMoneyFlow`, active palette attributes | Any signature slider writes the active portal source, active gradient, active palette colors, field split, label, and range readout | Static test + targeted code inspection | DONE |
| CL-UP-005 | User: all energy-field alternatives usable everywhere | `setMindPortalEnergyMode`, `drawMindPortalEnergyFrame`, `capturePortalBalanceFrame`, `color_lab_portal_energy.js` | Energy mode no longer forces Money flow or resets to static when changing color source; Balance samplers and non-Balance samplers both sample the active color palette | Static test + code inspection | DONE |
| CL-UP-006 | User: no dedicated animation per color/mode | `drawMindPortalEnergyFrame`, portal transition capture | Color source decides colors; mode decides field geometry; Balance modes can use Traffic/Limit/Cool/new/custom palettes and non-Balance modes can use Money flow | Static test + code inspection | DONE |
| CL-UP-007 | User: delete lower logo color shades and slider below | `spendeeLogoEditor` markup/CSS/JS | `Logo színirányok` preview panel and `Custom A/B/C logo skála` controls are removed; large logo editor remains | Static test + code inspection | DONE |
| CL-UP-008 | User: add 5 custom gradient slots to lower palette | `customGradientPaletteRow` markup/CSS/JS | Five custom slots appear in the lower palette; each has a swatch, left/right endpoint controls, and a compact boundary slider | Static test + code inspection | DONE |
| CL-UP-009 | User: custom slot can color anything in app | selection/apply color JS and logo editor gradient path | Tapping a custom slot selects its gradient through the existing palette selection path; gradient applies to CSS-background targets and logo paths | Static test + code inspection | DONE |
| CL-UP-010 | Project rule: tests before production code | `docs/prototypes/color_lab_static_test.js` | Static tests fail before implementation and pass after implementation | Recorded command output | DONE |
| CL-UP-011 | User feedback: sliders are only static and animations do not receive the selected colors | `applyMindPortalTestHeaderColors`, `drawMindPortalEnergyFrame`, `updatePortalMessageFieldPalette`, transition capture | Any testheader color slider invalidates/restarts animated palette consumers; testheader energy canvas, interior overlay, message field, and transition target frames sample the active palette instead of stale/dedicated colors | Static test + code inspection | DONE |
| CL-UP-012 | User feedback: still static after palette sync | test-header default state, energy RAF lifecycle, interior motion default | The standalone test header starts with a visible animated energy mode and enabled interior motion; slider changes wake/restart the shared energy RAF loop so active animation controls are not left as static CSS | Static test + code inspection | DONE |
| CL-UP-013 | User feedback: lower custom gradient sliders cannot move below 50% | `buildCustomGradientCss`, `initCustomGradientPalette` | Custom gradient boundary parsing preserves all 0–100 values, including 0–49, and no `|| 50` fallback can reset low slider values | Static regression test + code inspection | DONE |
| CL-UP-014 | User: add six D1-based font screens from `/storage/emulated/0/spendee/betuk.png` | Mind D1 runtime clone row, D1 brand/font CSS | A new row under Mind D1 duplicates D1 into six screens using Inter, Satoshi, Plus Jakarta Sans, SF Pro, Manrope, and Poppins; each screen applies its font to brand, header, logboxes, and content, and uses the reference logo/wordmark/tagline proportions | Static regression test + direct reference inspection | DONE |
| CL-UP-015 | User feedback: D1 font screenshot logo/slogan ratio and positioning do not match; logo must be floating | D1 font variant brand CSS and runtime variables | Font-variant screens use a floating logo/wordmark lockup: logo and text block are independently positioned/sized via CSS variables, not flex gap, so the reference logo baseline, 12–16dp spacing, and 4–6dp tagline spacing can be tuned freely | Static regression test + screenshot inspection | DONE |
| CL-UP-016 | User feedback on latest screenshot `Screenshot_20260717-162745.png`: result still ignores the guide lines from `/storage/emulated/0/spendee/betuk.png` | D1 font variant brand CSS, `commonMindD1FontVariants`, static contract | The D1 variant lockup is guide-calibrated from `betuk.png`: larger wordmark, smaller/independent logo, 12–16px logo-to-wordmark gap, optically shared top line, tagline under the wordmark, and runtime writes every sizing/position variable used by CSS | Static regression test + direct screenshot/reference inspection | DONE |
| CL-UP-017 | User feedback on latest screenshot `Screenshot_20260717-164705.png`: logo/slogan proportions are good, but they hang into the header; keep them in the area above the header screen | D1 font variant brand CSS, `commonMindD1FontVariants`, static contract | The D1 variant lockup preserves the accepted relative logo/wordmark/tagline sizes but moves the full lockup above the 104px header start; default and runtime `--d1-lockup-top` are calibrated so the current ~79px lockup height ends before the header with clearance | Static regression test + direct screenshot inspection | DONE |
| CL-UP-018 | User: icon and labels should be 20% smaller | D1 font variant brand CSS, `commonMindD1FontVariants`, static contract | The accepted D1 brand lockup keeps its layout and header-safe top, but icon and all brand text sizes are scaled to 80%: main logo `70px→56px`, SF logo `66px→52.8px`, wordmark `44px→35.2px`, and tagline follows the same wordmark-based ratio; intra-lockup offsets are tightened to fit the smaller lockup | Static regression test + code inspection | DONE |
| CL-UP-019 | User feedback on latest screenshot `Screenshot_20260717-170110.png`: brand lockup is still slightly high; padding between motto and header should equal padding between header bottom and Bevétel/Kiadás buttons | D1 font variant brand CSS, `commonMindD1FontVariants`, static contract | The 80% D1 brand lockup keeps its size, but moves down to `--d1-lockup-top: 24px`; calculated motto-bottom-to-header-top padding becomes ~16px, matching header-bottom-to-type-button-top padding (`208px → 224px`) | Static regression test + screenshot/CSS inspection | DONE |
| CL-UP-020 | User: logo and labels should be another 10% smaller, but the padding to the header top must not change | D1 font variant brand CSS, `commonMindD1FontVariants`, static contract | The current 80% lockup scales another 90% while preserving the motto-bottom-to-header-top padding: main logo `56px→50.4px`, SF logo `52.8px→47.52px`, wordmark `35.2px→31.68px`, scaled inner Y metrics, and `--d1-lockup-top: 30.4px` keeps the motto bottom at the previous visual baseline | Static regression test + code inspection | DONE |
| CL-UP-021 | User: make them another 5% smaller | D1 font variant brand CSS, `commonMindD1FontVariants`, static contract | The current lockup scales another 95% while preserving the existing motto-bottom-to-header-top padding: main logo `50.4px→47.88px`, SF logo `47.52px→45.144px`, wordmark `31.68px→30.096px`, scaled inner Y/gap metrics, scaled horizontal offsets, and `--d1-lockup-top: 33.3px` keeps the motto bottom at the same baseline | Static regression test + code inspection | DONE |
| CL-UP-022 | User: D3/D4/D5 contents are printed over balance cards; remove those backgrounds; D3 month cards must not be in a shared glass container; cards should sit naturally on the header like the `Minták volumene` function chart and use the same card background color. Later amended by CL-UP-023 to keep the mind stage1 charts and CL-UP-024 to restore glass panels for D3/D5. | Mind D3/D4/D5 heatmap builders, stage2 clone sync, heatmap card CSS | D3/D4/D5 heatmap screens do not clone or retain balance stage1/stage2 content, render month/year cards with the `fastinfo-chart-card` style background `rgba(255,255,255,.72)`, keep D4 direct, and defer the permitted mind chart clone behavior to CL-UP-023 plus the restored D3/D5 glass panel behavior to CL-UP-024 | Static regression test + code inspection | DONE |
| CL-UP-023 | User correction + latest screenshot `Screenshot_20260717-174909.png`: D3/D4/D5 lost the two stage1 mind charts; put them back, but D4/D5 still show balance content behind month/year cards and that must be removed | Mind D3/D4/D5 stage2 clone cleanup, mind stage1 chart clone preservation | D3/D4/D5 keep the `mind-boxed-graphs-d2` layer with both `expense-pattern-volume` and `period-expense-bars` chart cards above the heatmap, while all balance residue (`balance-reserve-summary`, `balance-income-expense`, `balance-diagnostics`) is explicitly removed from the heatmap screens | Static regression test + screenshot/code inspection | DONE |
| CL-UP-024 | User: instead put D3 month cards and D5 year cards back into a common glassy container; draw 12 years in D5 with 4 columns instead of 3 and recalculate content sizing | D3/D5 heatmap panel builders, D5 summary year data, D5 summary card CSS | D3 wraps the yearly month grid in `data-heatmap-panel="score-glass"`, D5 wraps the year-card grid in `data-heatmap-panel="sum-glass"`, D5 renders 12 year cards, and the D5 summary grid uses 4 columns with smaller card padding/gaps/month-cell sizes so content fits the denser layout | Static regression test + code inspection | DONE |
| CL-UP-025 | User: make D4 day cells taller so they exactly fill the available space; add another 12 years to D5 | D4 monthly heatmap CSS, D5 summary year data/static contract | D4 single-month heatmap day grid uses six equal-height rows that stretch to the remaining card area, with day cells at `height: 100%`; D5 renders 24 year cards total while keeping the 4-column summary grid | Static regression test + code inspection | DONE |
| CL-UP-026 | User: reduce opacity of D3 and D5 inner cards while keeping the glass container; D4 month-card background should be glass instead of whitish | D3/D5 scoped heatmap card CSS, D4 heatmap card variant resolver | D3 `score-glass` and D5 `sum-glass` containers remain unchanged, D3 inner month cards and D5 inner year cards use a lower-alpha card background, and D4 `month-single` resolves to the glass/frost card variant rather than the plain white card | Static regression test + code inspection | DONE |
| CL-UP-027 | User: apply the same glass container to D4 `Minták volumene` and `Havi kiadás` charts too; later corrected by CL-UP-028 to mean direct chart-card glass, not an outer absolute-layer wrapper | D4 mind stage1 chart clone presentation helper and CSS | Superseded by CL-UP-028: the intended D4 treatment is not a glass wrapper around the full absolute chart layer, but glass chart cards inside the existing transparent layout | Static regression test + code inspection | DONE |
| CL-UP-028 | User correction from latest screenshot `Screenshot_20260717-185736.png`: D4 size/layout is wrong; wants a glass container with the chart directly inside, no inner card background; the glass container itself is the background like the monthcard | D4 mind stage1 chart clone presentation helper and scoped chart-card CSS | D4 `mind-month-heatmap` must not turn the whole absolute chart layer into a heatmap glass panel; instead each `Minták volumene` and `Havi kiadás` chart card is the direct glass container with transparent layer layout preserved and no nested white card background | Static regression test + screenshot/code inspection | DONE |
| CL-UP-029 | User correction: D4/D3 `Minták volumene` and `Havi kiadás` still look like bright fixed-background cards, not glass; chart design itself must visibly be glass | D4 mind chart-card glass CSS and static visual contract | D4 `mind-month-heatmap` chart cards remain direct glass containers, but their background uses lower alpha glass gradients, stronger translucent border/blur, and explicitly does not use the bright `.66/.32` white gradient that reads as fixed card background | Static regression test + screenshot/code inspection | DONE |
| CL-UP-030 | User: add a D3-adjacent version of D3 where the charts are separated glass cards and the monthcards are also separated glass cards | Mind D3 alternate screen cloning, stage1 chart clone presentation, separated heatmap CSS | A new D3-adjacent Mind screen appears after the current D3 and before D4; it keeps the same two stage1 charts but renders each chart as its own glass card and renders the 12 monthcards directly as individual glass cards without the current shared `score-glass` collector panel | Static regression test + code inspection | DONE |
| CL-UP-031 | User: add the same separated glass alternative beside D5; charts and year cards should be separate glass with no shared background | Mind D5 alternate screen cloning, stage1 chart clone presentation, separated sum heatmap CSS | A new D5-adjacent Mind screen appears after the current D5; it keeps the same two stage1 charts but renders each chart as its own glass card and renders the 24 year cards directly as individual glass cards without the current shared `sum-glass` collector panel | Static regression test + code inspection | DONE |
| CL-UP-032 | User: in the D row there must be no placeholder cells anywhere, neither in the focused month card nor the annual multi-monthcards; if the month has fewer days, no extra cell should exist | Mind heatmap month-card renderer and blank-cell CSS | `buildMindHeatmapMonthCard` renders only real days `1..month.days`, does not create leading or trailing `.mind-heatmap-day.blank` cells, and the unused blank-cell CSS is removed so D3/D3G/D4 monthcards contain no placeholder day cells | Static regression test + code inspection | DONE |
| CL-UP-033 | User: add an alternate screen beside B2 where the fastinfo cards are glass | Balance B2 alternate screen cloning and scoped fastinfo-card material CSS | A new B2-adjacent Balance stage1 screen appears after B2 and before B3; it duplicates B2 content, keeps the same Balance/reserve/ratio layout, and scopes glass material to all three B2 fastinfo cards without changing the original B2 or leaking into Budget/Mind cloned rows | Static regression test + code inspection | DONE |
| CL-UP-034 | User: move the Q row to the very top, make it the first row, and focus Q1 after HTML refresh | Alternative design row ordering and refresh focus runtime | The Query row is visually/runtime-first under `.alternative-design > .screens`, `moveQueryMenuRowToTop()` moves it before the previous first row on refresh, and `focusQueryMenuQ1OnLoad()` scrolls/focuses Q1 instead of Mind D1 | Static regression test + code inspection | DONE |
| CL-UP-035 | User: query header should be as large as a stage0 header, keep its color, replace the magnifier with the Fluvi brand icon at the same D1A position/size, and remove transaction count / vs previous month so only amount plus scope elements remain | Q1 fullscreen query menu header and stage0 preview summary | Q1 uses a `--spendee-header-h` stage0-sized query-colored header; the header magnifier is replaced with a live Fluvi SVG at `left:30px`, `top:39.3px`, `47.88px` square; the Q1 summary removes transaction count and previous-month comparison, and shows only `-486 320 Ft` plus scope chips such as `2026`, `Élelmiszer`, `McDonald’s` | Static regression test + code inspection | DONE |
| CL-UP-036 | User correction from latest screenshot `Screenshot_20260717-204521.png`: the query screen became a double header; this concept is wrong, use one floating header at the top and remove the icon and slogan for now | Q1 fullscreen query menu header and amount/scope summary | Q1 has exactly one top floating stage0-sized query-colored header that contains the amount and scope chips directly; there is no separate `query-preview-card` under it, no Fluvi/icon SVG in the header, and no query slogan/subtitle copy | Static regression test + screenshot/code inspection | DONE |
| CL-UP-037 | User: the sum amount should be the same size as in the Balance header, and handle the case where not all chips fit; currently `McDonald’s` overflows | Q1 fullscreen query menu floating header typography and scope chip overflow behavior | Q1 amount typography matches the Balance header value (`23px`, `1.08`, `850`, `-.04em`, same ink shadow/stroke), the floating header clips its own rounded surface, and the scope chip row is a single-line horizontally scrollable/faded strip with non-shrinking chips so long scopes do not paint outside the header | Static regression test + screenshot/code inspection | DONE |
| CL-UP-038 | User: start with time; wants a working date picker where they can choose SUM, one month, a full year, or arbitrary months in a year such as September and December 2026 together; static 3-view selector is not optimal | Q1 fullscreen query menu `Nézet és idő` section, floating header time-scope chips, and query picker runtime | The old `data-query-view-selector` is replaced by a working `data-query-period-picker` with year navigation, SUM/full-year/months modes, twelve month buttons, multi-select arbitrary months, default example `2026 + Szeptember + December`, and JS that updates the section label plus floating-header time chips | Static regression test + code inspection | DONE |
| CL-UP-039 | User: show the other agent's `querytime.png` idea too; current picker still cannot select `2024 March` and `2026 April` together | Q1 fullscreen query menu `Nézet és idő` section, selected period data model, floating header time-scope chips, and query picker runtime | Q1 uses a cross-year `selectedPeriods` list (`YYYY-MM` values), not a single active year plus month numbers; default visible example includes `2024-03` and `2026-04`, quick presets remain (`Összeg`, `Éves`, `Havi`, `Egyedi`), the year blocks expose month chips for multiple years, the selected-period list shows removable `2024 Március` and `2026 Április` rows, and JS updates header chips from the selected period list | Static regression test + direct `querytime.png` inspection | DONE |
| CL-UP-040 | User: the current cross-year picker is wrong because 20 years would list all years on the main screen; wants a dropdown menu ordered by years with months indented below, random combinations selectable, year toggle selects/deselects all months; category and vendor lists must use the same model | Q1 fullscreen query menu `Nézet és idő`, `Kategóriák`, and `Vendorok` sections; shared tree/dropdown CSS and runtime | Q1 replaces expanded year cards/chip rows/vendor rows with shared `query-tree-dropdown` surfaces: time has sorted year parent rows plus indented month child rows, category/vendor sections have parent group rows plus indented selectable children, parent toggles activate/deactivate all children, child toggles still support arbitrary random combinations, and summaries/counts stay synchronized | Static regression test + code inspection | DONE |
| CL-UP-041 | User: remove the top period pills; only the current custom view should remain; SUM should be integrated inline like a parent row; category list must not be income vs expense grouped because the header-under pill prefilter determines that; category rows should be a simple list with name and category color dot based on the add-transaction/category menu design | Q1 fullscreen query menu `Nézet és idő` and `Kategóriák` sections; period tree runtime and category list styling | Q1 removes `query-period-preset-selector` and all `data-query-period-preset` controls, adds an inline `data-query-period-sum-toggle` row that selects/deselects every available month, keeps year/month arbitrary selection, removes category parent groups `Kiadások`/`Bevételek`, and renders categories as flat selectable rows with `data-query-category-dot` color dots plus names | Static regression test + code inspection | DONE |
| CL-UP-042 | User correction: month selection checkbox layout must match the category checkbox design exactly; right-side empty checkbox gets a tick when selected; SUM checkbox is part of the list but floats/stays visible at the top; remove custom `Nézet`/`Idő` badges and show only how many items are selected (`x aktív`) | Q1 fullscreen query menu `Nézet és idő` dropdown markup/CSS/runtime and floating header time-scope copy | Month rows use the same right-side `query-check` checkbox layout as category rows, selected months show a tick in the checkbox, `data-query-period-sum-toggle` is a sticky first row inside the scrollable time list, trigger/section/summary labels use `x aktív` count copy instead of `Egyedi`, `Nézet`, or `Idő` badges, and runtime updates the month checkboxes plus active count after toggles/removals | Static regression test + code inspection | DONE |
| CL-UP-043 | User correction: scrollable time-window rows must match the category dropdown rows; year checkboxes must be on the right; highlighted time rows must use the same neutral selected color as the category list; do not list selected months below the time list; add clear-all deselect controls for time and categories (original pill placement superseded by CL-UP-046) | Q1 fullscreen query menu time/category dropdown markup, CSS, and runtime clear actions | Time SUM/year parent rows render text/counter first and the checkbox on the right; selected time rows use the category selected background (`rgba(236,254,255,.78)`) instead of blue; `query-period-selected-list` and `data-query-period-remove` rows are removed; time/category clear-all deselect remains available via the CL-UP-046 header-level controls and clears selected periods/categories while updating counters/checks | Static regression test + code inspection | DONE |
| CL-UP-044 | User correction: category dropdown top must also have a `mind kijelöl` control like the time period SUM row, and it must float/stick at the top | Q1 fullscreen query menu category dropdown markup, CSS, and runtime selection summary | The `Kategóriák` dropdown starts with a sticky `data-query-filter-all="category"` row labeled `Mind / Összes kategória`, using the same right-side `query-check` and `selected/total` counter pattern as the time SUM row; clicking it selects every category, clicking it again when all categories are active deselects every category, and counters/check states stay synchronized | Static regression test + code inspection | DONE |
| CL-UP-045 | User correction: the circular markers beside the `Időszak`, `Kategóriák`, and `Vendorok` titles show an offset x-like glyph; remove those glyphs and put real icons there; remove the bottom period summary pill that lists all selected items | Q1 fullscreen query menu section header icons and period picker footer | The `Időszak`, `Kategóriák`, and `Vendorok` section icons are rendered with real existing lucide mask icons inside the circle, not text glyphs; the section title/subtitle styling does not offset the icon; the period picker no longer renders `data-query-period-summary`, `.query-period-summary`, or runtime code that maintains that bottom selected-item summary pill | Static regression test + code inspection | DONE |
| CL-UP-046 | User correction: opening a section and then opening an inner dropdown is a pointless double tap; tapping `Időszak`, `Kategóriák`, or `Vendorok` should expand the section and show the list directly; the X clear/dehighlight control must move from the removed inner pill into the mother section header for time, category, and vendor | Q1 fullscreen query menu section headers, time/category/vendor list containers, and query runtime | Q1 removes all inner `data-query-tree-trigger` / `query-tree-trigger` controls from time/category/vendor sections; the section body contains the list directly when expanded and is hidden only by section collapse; time/category/vendor section headers each have a dedicated `query-section-clear` control (`data-query-period-clear`, `data-query-filter-clear="category"`, `data-query-filter-clear="vendor"`) that clears highlights without toggling collapse; runtime no longer binds `toggleQueryDropdown` and keeps counts/disabled clear states synchronized from the section-level controls | Static regression test + code inspection | DONE |
| CL-UP-047 | User correction: the section-level X clear/dehighlight control is good, but it must sit on the left side of the active-count text, not on the far right side of the row | Q1 fullscreen query menu section header layout and query section toggle runtime | Time/category/vendor section headers render the clear button before the active-count status (`X` then `2 aktív` / `3 aktív` / `1 aktív`); the active-count/caret status remains tappable for expand/collapse; multiple section header toggle controls keep `aria-expanded` synchronized | Static regression test + code inspection | DONE |
| CL-UP-048 | User: draw every proposed non-hierarchical query UI alternative as a new screen next to Q1 | Q row prototype screens in `docs/prototypes/color_lab.html` plus static regression coverage | The Q row keeps Q1 first and adds four adjacent screens before Q2: `Q1A · Query kombinációs kosár`, `Q1B · Query feltételcsoportok`, `Q1C · Query mátrix`, and `Q1D · Query mondat builder`; all four screens visualize the exact pairwise OR example `2025 Jan · Élelmiszer` OR `2026 Feb · Lakás`; the existing Q2-Q15 screens remain after the alternatives | Static regression test + code inspection | DONE |
| CL-UP-049 | User correction: the alternative screens do not show what the user builds the basket from; draw them properly and make them interactive like Q1 | Q1A-Q1D alternative query screens, source composer markup, picker CSS, and prototype JS runtime | Q1A has a visible basket plus `Új kombináció összeállítása` composer with tappable Időszak/Kategória/Vendor source rows and picker panels; Q1B condition groups expose editable group fields and source pickers; Q1C matrix exposes row/column axis source selectors and tappable matrix cells; Q1D sentence builder exposes tappable sentence tokens and source picker panels; all alternatives bind shared JS (`initQueryAlternativePrototype`) that opens/closes picker panels, updates selected labels, toggles matrix cells, and can add a composed combination/group/sentence in prototype state | Static regression test + code inspection | DONE |
| CL-UP-050 | User correction: remove Q1A/Q1B/Q1C/Q1D; filter groups must live in Q1 under Vendorok; add a new filter-group button under vendors; category and vendor lists need a search pill above the list; in refinements the amount number must be tappable/editable and a slider below it must adjust the number | Q1 fullscreen query menu markup, CSS, and prototype runtime | Q row no longer renders the four Q1A-Q1D alternative screens or their `query-alt` runtime; category and vendor list bodies render `data-query-list-search="category"` / `"vendor"` pills before list rows; the amount threshold refinement renders `data-query-threshold-editor`, a tappable numeric control, and `data-query-threshold-slider` that syncs its displayed value in prototype JS; the original filter-group placement from this row was later superseded by CL-UP-052 | Static regression test + code inspection | DONE |
| CL-UP-051 | User correction: threshold amount was duplicated; remove the extra text input pill; after this, duplicate the same Q1 screen to the right with a different filter-group logic where groups are top tabs, each tab has an internal X delete control, and the final tab position is a plus button that adds a group | Q1 fullscreen query menu markup, duplicated tabbed filter-group Q screen, CSS, and prototype runtime | The original Q1 threshold editor has a single tappable/editable numeric value directly above the slider, with no separate `.query-threshold-value-input` visual pill; Q row renders a second Q1-adjacent screen `data-screen="alt-query-menu-tabbed-groups"` immediately after `alt-query-menu-fullscreen` and before Q2; the duplicate screen uses `data-query-filter-group-tabs`, renders group tabs with `data-query-filter-tab-remove`, and uses `data-query-filter-tab-add` as the final plus control; prototype JS can select, remove, and add tabbed filter groups | Static regression test + code inspection | DONE |
| CL-UP-052 | User correction after abort: swap saved filter groups and threshold/refinement position; threshold/refinements can be part of a saved group; saved filter groups are previous snapshots persisted by the system; when the user loads the query screen they can tick a saved group active; the menu content must always load the last tapped saved group for testing | Q1 fullscreen query menu order, saved snapshot cards, active checkbox controls, threshold/refinement snapshot data, and prototype JS runtime | In the original Q1 screen, `data-query-section="refinements"` renders after Vendorok and before `data-query-section="filter-groups"`; saved groups render as `data-query-saved-group` snapshot cards with `data-query-saved-group-load`, `data-query-saved-group-active`, snapshot period/category/vendor/threshold/refinement data attributes, and visible threshold/refinement chips; tapping a saved group updates `data-query-last-loaded-group`, the detail panel, header scope, period/category/vendor selections, and threshold slider/value; ticking its checkbox toggles active state and also makes that snapshot the last-loaded menu content | Static regression test + code inspection | DONE |
| CL-UP-053 | User correction: duplicate the Q1 tabbed screen beside it; in this new duplicate snapshots must not be their own section, but a shared top container directly under the header card with tabs, checkbox on each tab to activate the filter, and a plus button beside the final tab; tapping a tab loads that snapshot's specific settings | New Q1 adjacent top-snapshot-tabs duplicate screen, top tab container CSS/markup, and prototype runtime | Q row renders `data-screen="alt-query-menu-top-snapshot-tabs"` immediately after `alt-query-menu-tabbed-groups` and before Q2; that screen has `data-query-top-snapshot-container` after `.query-menu-head` and before `.query-menu-scroll`, with `data-query-top-snapshot-tabs`, tab load controls, per-tab `data-query-top-snapshot-active` checkboxes, and `data-query-top-snapshot-add`; it does not render a `data-query-section="filter-groups"` detailed section; tapping a top tab and toggling its checkbox both load that snapshot into the common menu sections. This was later promoted to the main Q1 screen by CL-UP-054 | Static regression test + code inspection | DONE |
| CL-UP-054 | User: make the current top-snapshot screen be Q1, delete the two screens to its left, then duplicate Q1 to the right as Q1A; Q1A keeps the discussed top-tab design but merges vendors and categories into a named hierarchy where vendors live inside categories, category checkbox is on the right like years, and the category color is on the left | Q row screen order, promoted Q1 top-snapshot screen, new Q1A category-vendor hierarchy markup/CSS/JS, and static regression coverage | Q row renders exactly two query menu screens before Q2: `data-screen="alt-query-menu-fullscreen"` as Q1 with `data-query-menu-mode="top-snapshot-tabs"` and `data-query-top-snapshot-container`, followed immediately by `data-screen="alt-query-menu-category-vendor-hierarchy"` as `Q1A · Kategória-vendor hierarchia`; the old `alt-query-menu-tabbed-groups` and separate `alt-query-menu-top-snapshot-tabs` screens are absent; Q1A has no separate `categories` or `vendors` section and instead renders one `data-query-section="category-vendors"` with `data-query-category-vendor-tree`; category parent rows expose category color on the left, title/count in the middle, and `query-check` checkbox on the right; tapping a category parent toggles its vendor children and the runtime resyncs parent mixed/selected state after direct vendor, clear, all, and snapshot changes | Static regression test + code inspection | DONE |
| CL-UP-055 | User: vendor highlight color should match the month highlight color because the current vendor highlight is too strong | Query Menu vendor/tree selected CSS and static regression coverage | `.query-filter-tree-item.selected` uses the same soft highlight palette as `.query-period-item.selected`: `background: rgba(236,254,255,.78)`, muted border, `color: #0f766e`, and no shadow; selected vendor child text remains readable in the same muted color and must not force white-on-cyan styling | Static regression test + code inspection | DONE |
| CL-UP-056 | User: next to categories, vendors, years, and months show how many transactions are in each row everywhere | Query Menu period/category/vendor row layout, transaction-count chips, and prototype runtime | Every period year, period month, category row, vendor parent row, vendor child row, and Q1A category-vendor hierarchy row receives a visible `data-query-row-transaction-count` chip in the row before the checkbox/selection controls; parent selection ratios such as `1/12` and `1/3` remain visible; the count chip uses compact muted styling and the runtime can inject/update counts for both Q1 and Q1A routes | Static regression test + code inspection | DONE |
| CL-UP-057 | User: there should be no X button in the header card; instead the footer should be like A3 | Query Menu Q1/Q1A header/footer markup, query footer CSS, and static regression coverage | Q1 and Q1A query menu headers do not render `.query-menu-close` or the `×` close button; `.query-menu-head` uses a single summary column; the old `.query-apply-bar` / `.query-apply-pill` footer is replaced by an A3-style `category-selector-footer vendor-selector-footer query-selector-footer` with summary text plus `Cancel` and `OK` buttons using `category-selector-footer-actions`; the footer stays sticky at the bottom and remains recolorable through the same bottom cancel/action color targets | Static regression test + code inspection | DONE |
