# Avatar carousel snap and vendor empty state checklist

| ID | Source instruction | Code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| CAR-001 | User report: avatar carousel sometimes jumps without animation during random rapid swipes | `lib/features/transactions/widgets/experimental/spendee_test_dashboard.dart` carousel drag/release state | A new carousel drag that starts while a release animation is still settling continues from the live controller residual offset instead of resetting visual offset to zero. | `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --plain-name "budget carousel continues an interrupted release drag"` and full dashboard interaction suite. | DONE |
| CAR-002 | User report: sometimes snaps without tick | `spendee_test_dashboard.dart` carousel motion frame application | Stale/cancelled carousel motion frames cannot mutate the current gesture, and the final selected budget item is synchronized from the settling controller index. | `flutter test test/spendeetest/spendee_center_carousel_inertia_test.dart` and full dashboard interaction suite. | DONE |
| CAR-003 | Provided performance logs with rapid repeated releases and delayed filter publish | `spendee_test_dashboard.dart` carousel idle publish timer | Rapid back-to-back swipes still publish only the latest settled item after the idle delay. | Full dashboard interaction suite, including `budget carousel publishes store filter only after idle`. | DONE |
| VND-001 | User request: category without vendors should show a blank placeholder diagram and `Nincs adat` on the vendor page | `_BudgetPiePanel`, `_BudgetPieContent`, `_BudgetPiePainter` | Empty vendor data keeps the vendor page visible with the donut placeholder and focus title `Nincs adat`, with no vendor rows. | `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --plain-name "stage 2 keeps blank vendor chart when selected data is empty"` and full dashboard interaction suite. | DONE |
