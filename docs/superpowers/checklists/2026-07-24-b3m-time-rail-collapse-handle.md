# B3M time-rail collapse handle acceptance checklist

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Status |
|---|---|---|---|---|---|
| TRH-001 | User, 2026-07-24: inspect the latest screenshot | `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260724-004911.png` | The handle occupies the existing free control strip immediately above the year pills. | Original-resolution screenshot inspection. | DONE |
| TRH-002 | User: collapse/expand currently triggers from every point of every screen | `.stage2-redesign-scroll-viewport`; `attachTodayRedesignScrollInteraction()` | The native full-screen vertical scroll is identified as the shared gesture source. | Direct CSS and event-chain inspection. | DONE |
| TRH-003 | User: “kizárólag az év finomító rail areában” | Exit-mode viewport CSS and the shared interaction controller | A vertical drag begun outside the dedicated year-rail handle cannot change collapse progress. | Focused source/runtime-mock contract confirmed no viewport pointer listeners and a hidden native Y overflow. | DONE |
| TRH-004 | User: add a handler, a small bar and a short clear label | `createTodayTimeScopeDrawer()` | The existing year-rail control row contains one button with a compact bar and the label `Húzd a nézetet`. | Focused DOM source contract passed. | DONE |
| TRH-005 | User: it is part of the rail container, not an extra component | Time-scope control-row CSS | The handle is absolutely positioned inside the existing 21 px control row; drawer, year rail and screen heights are unchanged. | Focused geometry contract and direct CSS inspection passed. | DONE |
| TRH-006 | Existing year-selection interaction | `attachTodayTimeScopeDrawer()` and year-rail CSS | Horizontal year swiping/clicking remains owned by the year rail and is not used for vertical collapse. | Permanent time-rail and full Balance regressions passed. | DONE |
| TRH-007 | Existing screen interaction | `attachTodayRedesignScrollInteraction()` | Dragging the handle upward collapses, dragging it downward expands, and release settles to the nearest endpoint. Clicking/keyboard-activating it toggles the state. | Runtime mock exercised both drag directions, release settling and native button clicks. | DONE |
| TRH-008 | User: “az összes screenben” | Shared time-scope factory used by B3M-A3, Budget, Budget 3D, Mind and annual Mind variants | Every current exit-mode clone receives its own handle and its locally rebound controller; no cross-screen reference is introduced. | Factory/call-site inspection plus all 12 Balance regressions passed. | DONE |
| TRH-009 | Existing compact-state behavior | Compact budget pill and shared controller | Programmatic scroll and compact-pill expansion continue to work after native viewport scrolling is disabled. | Existing interaction path retained; focused and geometry contracts passed. | DONE |
| TRH-010 | Visual acceptance | Approved screenshot plus post-change source geometry | The bar/label remain legible, do not overlap the year title or toggle, and no existing content moves. | Original screenshot re-read; the 92×21 px handle is zero-flow inside the unchanged 21 px row, and the unchanged 51/37 px rail geometry passed source contracts. | DONE |
| TRH-011 | User: commit and push | Git branch `spendeetest` | Only in-scope implementation, contract and documentation files are committed and the branch is pushed successfully. | Staged diff audit, commit output and remote push output. | NOT DONE |
