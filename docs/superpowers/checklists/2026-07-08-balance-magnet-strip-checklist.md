# 2026-07-08 Balance Magnet Strip Checklist

| ID | Source instruction | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| BMS-01 | "a mágnescsík a főmenuben mindig a balance-ból számol, azaz sumbevétel, sumkiadás" | `transaction_home_page.dart`, `transaction_store.dart`, header tests | The main home header passes all-time income and expense totals into `MagnetStrip`, independent of the active period. | RED: budget header test showed active category 80%; GREEN: `flutter test test/transactions/transaction_home_limits_test.dart --plain-name "magnet"` expects all-time 90%. | DONE |
| BMS-02 | "kell egy piros/zöld csík, melyben a zöld bal, piros jobb oldalt van, a határ nem éles, hanem átmenetes... ha a bevétel 90-át teszi ki az összesnek, akkor 90%-nál van a határ" | `magnet_strip.dart` painter | `fade` uses income ratio without 5/95 clamps and paints a soft transition centered at that ratio. | RED: missing `softBalanceStops`; GREEN: `flutter test test/transactions/magnet_strip_test.dart`. | DONE |
| BMS-03 | "kell egy fekete(szürke strip is, átmenettel) emulated 0 androidapps expt0926-ból nézd ki a színt. a mágnescsík ott kezd el halványodni, ahol a center point van" | `magnet_strip.dart` painter | `magnetcard` uses reference gray `#64748b` and fades from the balance center point toward transparent. | RED: missing `referenceGray`/`grayFadeStops`; GREEN: `flutter test test/transactions/magnet_strip_test.dart`. | DONE |
| BMS-04 | "kell egy progress bar is, itt a fekete csík nem halványul, hanem csökken, minél nagyobb a kiadás aránya annál kisebb" | `magnet_strip.dart` budget mode | `budget` renders a balance progress bar where width equals income ratio and does not use active category limit progress. | RED: old widget returned no/limit-based fill; GREEN: `flutter test test/transactions/magnet_strip_test.dart test/transactions/transaction_home_limits_test.dart --plain-name "magnet"`. | DONE |
| BMS-05 | "a kategória partition progress bar marad, az jó" | `magnet_strip.dart`, existing partition tests | `partitionedBudget` allocation rendering remains unchanged. | Existing partitioned magnet tests stay green in `magnet_strip_test.dart` and targeted home limits run. | DONE |
