# Balance Stage2 Mother-Child Gallery - Acceptance Checklist

Reference shell: `docs/prototypes/color_lab.html` B3 / B3M Balance header.
Data sources: `lib/features/settings/models/fast_info_card_catalog.dart`,
`lib/features/transactions/state/fast_info_metrics_resolver.dart`, and
`docs/superpowers/specs/2026-06-06-fastinfo-final-cards-design.md`.

Common FastInfo/Pulse status is maintained in
`docs/superpowers/checklists/2026-07-21-common-fastinfo-insights-pulse-acceptance.md`.
This file remains limited to the B3M gallery delivery and visual contract.

| ID | Source | Intended area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| GAL-001 | User request | New standalone prototype | A new HTML gallery exists; B3/B3M in Color Lab remains untouched by this package. | File review and static test | DONE |
| GAL-002 | User request + B3 reference | Shared phone shell | Every standard gallery screen has the same B3M Balance header, brand, static reserve/ratio strip, Stage1 belt, and Stage2 child structure. The sole `mai_koltes` test screen is a screenshot-driven exception inside the same `phone-screen`; its Balance value remains static and is not query-scoped. | Static source contract passed; browser comparison pending | PARTIAL |
| GAL-003 | User direction + current FastInfo catalog | Card inventory | Exactly the 11 selected insight IDs are represented, with no old `top_kategoria_ma` duplicate and no `kiadas_bevetel_arany`, `bevetel_ebben_a_honapban`, `megtakaritas`, `leggyorsabban_fogyo_kategorialimit`, `kovetkezo_ismetlo_kiadas`, or `havi_fix_koltseg_osszesen` gallery card. The live FastInfo catalog remains unchanged for the four Budget mode-owned IDs. | Static test cross-checks the gallery against the app catalog while excluding the header-owned ratio and the four Budget mode-owned IDs | DONE |
| GAL-004 | User request | Mother-child semantics | Each Stage1 mother has one compact fact; its Stage2 child only adds distinct context, evidence, or projection rather than restating that fact. | Data review and no-redundancy matrix | DONE |
| GAL-005 | FastInfo resolver | Financial semantics | Fixed, scheduled, variable, and all-transaction rules follow the resolver for each metric. | Resolver/source review | DONE |
| GAL-006 | User request | Interaction | Each independent B3M screen has a visible selected mother and its child. Interaction must not replace the B3M source component markup. | DOM source contract | DONE |
| GAL-007 | Portal pulse code | Attention behavior | `pulseMindPortalIdleField` is documented as visual attention feedback only; it is not rendered as an invented financial insight. | Source annotation and static test | DONE |
| GAL-008 | User request | Delivery | Gallery is served from a new local HTTP server on a port different from Color Lab. | HTTP 200 check | DONE |
| GAL-009 | User correction, 2026-07-21 + supplied redesign screenshot | Exact source identity | All non-Today screens use the actual B3M `phone-screen`, `common-header-stage2`, and `common-balance-mother-child-*` markup and Color Lab variables. `mai_koltes` remains on the actual B3M `phone-screen`, but its full inner composition is replaced by the user-supplied screenshot design. | Static source contract passed; browser comparison pending | PARTIAL |
| GAL-010 | User correction, 2026-07-21 + supplied redesign screenshot | Dimensions and color | Standard screens take dimensions, header glass, radii, shadows, type scale, Balance colors, reserve strip, bottom content, and Stage2 panel directly from `color_lab.html`. The isolated `mai_koltes` screen follows `/storage/emulated/0/spendee/stage2redesign.png` for those visual properties instead. | Static style contract passed; browser comparison pending | PARTIAL |
| GAL-011 | User correction, 2026-07-21 + supplied redesign screenshot | Content-only variation | Standard cards keep the B3M source shell and only alter B3M content. `mai_koltes` is the explicit screenshot-driven exception: it keeps the same phone container but uses the supplied full-screen Balance composition, without introducing a second global phone shell. | DOM contract passed; browser comparison pending | PARTIAL |
| GAL-012 | User correction, 2026-07-21 | Native zoom | The original Color Lab `viewport`, `zoom-host`, and `wide-canvas` remain mounted. The gallery opens at 50% and retains Color Lab pinch and Ctrl/Cmd-wheel zoom. | Source/runtime contract and Android browser load | DONE |
| GAL-013 | User correction, 2026-07-21 | Contextual Stage2 blocks | No universal progress + two facts + linechart template remains. Every insight declares only contextually relevant blocks: identity, progress/split, facts, and one appropriate visual or no visual. | Variant matrix and static test | DONE |
| GAL-014 | User correction, 2026-07-21 | Visual semantics | Stage2 visuals follow the selected B3M FastInfo resolver vocabulary: threshold, weekly bars, comparison, zone, activity, forecast, spike, no-spend, mini-avatar periods, and category delta. Budget mode-owned goal, category-limit, scheduled-load, and fixed-split visuals are not B3M gallery cards. | Resolver mapping review and static test | PARTIAL |
| GAL-015 | User request, 2026-07-21 | Active editing lane | The currently edited insight is removed from the horizontal gallery and rendered once in its own B3M row below. The first editing lane contains `koltesi_trend`; the Stage1 belt still selects it and retains its immediate catalog neighbors. | Static render contract and browser load | DONE |
| GAL-016 | User request, 2026-07-21 | Trend alternative lane | The original `koltesi_trend` B3M stays on the left; alternative B3M Stage2 screens are placed immediately beside it in the focused row. | Static render contract and browser review | PARTIAL |
| GAL-017 | User request, 2026-07-21 | Trend-specific Stage2 anatomy | The alternatives use only trend-specific evidence, rather than a large pink avatar, generic two-fact stack, or generic bottom line chart. | Static test and browser review | PARTIAL |
| GAL-018 | User correction, 2026-07-21 | Distinct trend archetypes | The lower lane contains four distinct Stage2 readings of the same trend: direct rolling-window comparison, Pulse decision map, additive spend equation, and rolling-window time tape. | Static test and browser review | PARTIAL |
| GAL-019 | Resolver + Pulse source | Shared data truth | Every trend alternative derives solely from `current = 191 200 Ft`, `previous = 162 000 Ft`, `deltaAmount = +29 200 Ft`, `deltaRatio = +18%`, the fixed-exclusion rule, and the accepted `+15%` / `+10 000 Ft` worsening threshold. B3M-E may use only the explicitly labelled, sum-consistent Color Lab daily-series fixture defined by GAL-021; it shows no merchant or category evidence. | Data review and static test | PARTIAL |
| GAL-020 | User correction, 2026-07-21 | Stage1-to-Stage2 graph continuity | The selected Stage1 trend card retains the actual Color Lab `.fastinfo-sparkline` filled surface. B3M-E expands that same source primitive with a data-derived `clip-path`; it must not render bars, grids, a new chart family, or a disconnected generic graph. | Static test passed; browser review pending | PARTIAL |
| GAL-021 | Prototype-data constraint | Daily-series fixture | The B3M-E Color Lab fixture contains exactly 30 prior and 30 current variable-expense values, sums to the visible totals, and is visibly identified as a Color Lab fixture. It is not represented as live app data; production must bind `rolling30VariableDailySeries` and an equivalent previous series from the resolver. | Static test and source review | PARTIAL |
| GAL-022 | User correction, 2026-07-21 | Exact reference duplication | The left FastInfo reference box is duplicated at code level: the source `.fastinfo-sparkline` class, its low-opacity vertical fill, radius, and fade remain in use. Only the `clip-path` is derived from the selected daily series. | Source contract and static test passed; browser review pending | PARTIAL |
| GAL-023 | User correction, 2026-07-21 | B3M-F two-period composition stripe | B3M-F uses the B3M-A red delta container as primary information and retains all three B3M secondary facts. Its final visual is one 8px stripe: `100% = previous + current`; the left previous-period segment is light gray, the right current-period segment is green when lower and red when higher. A left/right legend under the stripe identifies `Előző 30 nap` and `Aktuális 30 nap`. No centered delta scale, tick, marker, or axis remains. | Static behavior test passed; browser review pending | PARTIAL |
| GAL-024 | User request, 2026-07-21 | Top categories editing row | `top_kategoria_heten` is removed from the upper B3M carousel and rendered once in a separate third B3M row below the `koltesi_trend` editing row. This is an active editing layout, not an approved final composition. | Static render contract passed; browser review pending | PARTIAL |
| GAL-025 | User request, 2026-07-21 | Fixed-item exclusion wording | Where these cards exclude recurring/fixed transactions, their compact Stage1 label is `fixek nélkül`; their expanded Stage2 fact is `Fix tételek kizárva`. This is applied to `koltesi_trend` and `top_kategoria_heten`; the latter carries no amount in Stage1 and no longer uses `változó kiadás` or `Csak változó költés`. | Static text contract; browser review pending | PARTIAL |
| GAL-026 | User request, 2026-07-21 | Top categories lower icon strip | The lower Ma–Hét–Hó icon strip and its labels are absent from the `top_kategoria_heten` Stage2 child. The Stage1 category icon and the Stage2 textual facts remain. | Static render contract; browser review pending | PARTIAL |
| GAL-027 | User request, 2026-07-21 | Top categories Stage2 corner status | The inherited red status dot in the upper-right corner of the `top_kategoria_heten` Stage2 heading is absent. | Static render contract; browser review pending | PARTIAL |
| GAL-028 | User request, 2026-07-21 | Top categories annual amount | The `top_kategoria_heten` Stage2 facts include `Év: Lakás · 1 344 000 Ft` after the weekly and monthly rows. This is a sum-consistent prototype fixture; the production resolver currently calculates only Ma–Hét–Hó and will require an explicit yearly category aggregation. | Static text contract; browser review pending | PARTIAL |
| GAL-029 | User request, 2026-07-21 | Top categories per-period avatars | The Hét, Hó, and Év rows in `top_kategoria_heten` Stage2 each begin with a 16px circular category avatar and a semantic white icon: `utensils.svg` for Élelmiszer and `house.svg` for Lakás. The removed standalone lower icon strip remains absent. | Static render contract; browser review pending | PARTIAL |
| GAL-030 | User request, 2026-07-21 | Top categories Stage1 placeholder chart | The `top_kategoria_heten` Stage1 mini card has no inherited `fastinfo-sparkline` decoration. Its title, compact primary fact, and `fixek nélkül` label remain. | Static render contract; browser review pending | PARTIAL |
| GAL-031 | User request, 2026-07-21 | Remove header-owned ratio from gallery | `kiadas_bevetel_arany` is absent from the B3M gallery because the static Balance header already contains its information. This prototype change does not edit the current Flutter catalog or select a replacement gallery card. | Gallery static test and browser review | PARTIAL |
| GAL-032 | User request, 2026-07-21 | Remove generic monthly-income card | `bevetel_ebben_a_honapban` is absent from B3M and the live FastInfo catalog/config/surfaces. Its raw received-income, expected-income, and pending-income-ghost aggregates, plus the `megtakaritas` projection that consumes them, remain in Flutter. | Catalog/config/resolver tests; gallery static test; source-diff review; browser review | DONE |
| GAL-033 | User request, 2026-07-21 | Category-change editing row | `legnagyobb_novekedo_kategoria` is absent from the upper carousel and appears exactly once in a fourth B3M row below the Top categories row. It has no sibling alternatives. | Static render contract and browser review | PARTIAL |
| GAL-034 | Resolver | Category-change data truth | The base screen uses only `Közlekedés`, `5 800 Ft` previous, `20 000 Ft` current, `+14 200 Ft` absolute delta, `+245%` relative delta, `Új kategória: nem`, and fixed-item exclusion. | Static data contract and source review | PARTIAL |
| GAL-035 | User request | Category-box Stage2 anatomy | The default B3M child keeps its source shell, but its upper transaction row is a B3M-J-like category box: category icon, `Közlekedés`, category-change context, and `+14 200 Ft`. The generic difference axis is absent. | Static structural contract and browser review | PARTIAL |
| GAL-036 | User request, 2026-07-21 | No category-change alternatives | B3M-G through B3M-K are removed from the editing row and prototype code. The category-change card is deliberately one screen only. | Static render contract and source review | PARTIAL |
| GAL-037 | User request | No lower movement visual | The lower visual region is removed. The card does not show an axis, arrow, materiality threshold, daily data, ranking data, or a future projection. | Static data contract and browser review | PARTIAL |
| GAL-038 | User request, 2026-07-21 | Budget mode gallery exclusions | `megtakaritas`, `leggyorsabban_fogyo_kategorialimit`, `kovetkezo_ismetlo_kiadas`, and `havi_fix_koltseg_osszesen` are absent from the B3M HTML catalog. Their Flutter catalog entries, source aggregates, recurring ghosts, and Pulse inputs are not edited by this gallery change. | Static gallery/app-catalog cross-check and direct edit audit | DONE |
| GAL-039 | User request, 2026-07-21 | Latest-transaction editing row | `legutobbi_tranzakcio` remains in the 11-card B3M catalog, but is absent from the primary carousel and rendered exactly once in a fifth B3M row below the category-change row. Its Stage1 belt retains its immediate catalog neighbors and it has no sibling alternatives. | Static row/topology contract passed; browser review pending | PARTIAL |
| GAL-040 | User request, 2026-07-21 | No-spend editing row | `no_spend_napok_szama` remains in the 11-card B3M catalog, is absent from the primary carousel, and is rendered once in a sixth B3M row below the latest-transaction row. Its Stage1 belt retains its immediate catalog neighbors. | Static row/topology contract and browser review | PARTIAL |
| GAL-041 | User request + resolver + current Pulse inventory | No-spend alternative lane | The sixth row contains the base B3M no-spend screen plus three distinct Stage2 alternatives: monthly ratio ring, elapsed-month day map, and named seven-day cadence. They use only `weekValues`, monthly point semantics, `monthCount / elapsedMonthDays`, and the today/future markers. The Color Lab fixture is exactly 3/7 in the week and 9/21 = 43% in the elapsed month; it has 10 neutral future days. No streak, merchant/transaction evidence, forecast, or Pulse trigger is invented. The gallery renders no Pulse claim and does not decide the still-open common Pulse policy. | Static data/render contract and browser review | PARTIAL |
| GAL-042 | User request, 2026-07-21 | Average-daily-spend editing row | `atlagos_napi_koltes` remains in the 11-card B3M catalog, is absent from the primary carousel, and is rendered once in a seventh B3M row below the no-spend row. Its Stage1 belt retains its immediate catalog neighbors. | Static row/topology contract passed; browser review pending | PARTIAL |
| GAL-043 | User request + resolver + current Pulse inventory | Average-daily-spend alternative lane | The seventh row contains the base B3M average-daily screen plus three distinct Stage2 alternatives: balance-runway units, thresholded 30-day spike positions, and the `rolling30VariableExpense / 30` calculation. They use only `rolling30VariableDailySeries`, `rollingDailyAverage`, `balance / average`, and the resolver's `value > average * 1.5` spike rule. The Color Lab fixture is 191 100 Ft / 30 = 6 370 Ft, 58 buffer days, exactly three spikes at days 4/15/26, and a 19 800 Ft maximum. No calendar-month comparison, forecast, merchant/category evidence, or Pulse decision is invented; `HF-011_BALANCE_BUFFER_DAYS` remains open outside this gallery. | Static data/render contract passed; browser review pending | PARTIAL |
| GAL-044 | User screenshot `/storage/emulated/0/spendee/stage2redesign.png`, 853×1844 px | Today-spend redesign source of truth | The first screen of the upper B3M carousel (`mai_koltes`) follows the supplied full-screen screenshot instead of the prior one-surface interpretation. A neighboring explicit behavior alternative may reuse this exact visual composition, but must not alter the original screen. The screen is composed from code, with the screenshot retained as a local reference asset for the supplied avatar/action artwork. The other gallery screens remain on their current Color Lab B3M source shell. | Static reference/topology contract passed; browser comparison pending | PARTIAL |
| GAL-045 | User screenshot | Balance hero band | The top band reproduces the screenshot's left logo lockup, right three circular tools, large purple-pink Balance card, `Részletek` outlined action, white curved chart, `Tartalék 42%` progress, and `Balance arány` 32% / 68% split. Its large rounded surface, inset gloss, small white border, and soft downward shadow are distinct from the lower white cards. | Static structure/style contract passed; browser comparison pending | PARTIAL |
| GAL-046 | User screenshot + FastInfo data | Insight and daily-detail bands | Below the Balance hero are exactly three separate rounded insight cards: No-spend `3 / 7 nap`, Top kategóriák `Ma · Élelmiszer · fixek nélkül`, and Legnagyobb kategóriaváltozás `Közlekedés · +14 200 Ft · 30 nap`. The wide `Napi változó keret` card follows the screenshot anatomy: title/red dot, pink cart tile, `6 500 Ft`, three fact columns, separators, and the existing `8 900 Ft` / `10 100 Ft` threshold marker bar. | Static data/structure contract passed; browser comparison pending | PARTIAL |
| GAL-047 | User screenshot | Action and lower navigation bands | The daily-detail card is followed by two separate, illustrated gradient action cards (`Bevétel` and `Kiadás`), then the white `Aktuális hónap` summary, white search field with a separate filter control, and five-item bottom navigation with a central circular FAB. The screenshot's spacing, corner radii, directional shadow hierarchy, and white-card elevation are preserved; the two action cards are not collapsed into a shared panel. | Static structure/style contract passed; browser comparison pending | PARTIAL |
| GAL-048 | User animation reference `/storage/emulated/0/spendee/stage2anim.png`, 1024x1536 px | Today-spend interaction source of truth | The first carousel `mai_koltes` screen uses the supplied expanded/collapsed reference only for its scroll-state architecture. The earlier `stage2redesign.png` remains the visual source for the expanded card styling; `stage2anim.png` defines the state transition, collapsed composition, and transaction reveal. Both references are retained locally under `docs/prototypes/reference_assets/`. | Static reference contract passed; browser scroll comparison pending | PARTIAL |
| GAL-049 | User interaction direction + animation reference | Coordinator / hero ownership | The gradient Balance hero, insight strip, and detailed daily budget are coordinated by one Today-screen animation controller. The hero is an overlay outside the scrolling content layer; scrolling moves the lower content continuously under it. The gradient is not replaced or faded out during collapse. | Static DOM/style contract passed; browser scroll comparison pending | PARTIAL |
| GAL-050 | User interaction direction + animation reference | Expanded-to-collapsed scroll motion | During the first 180 px of scroll, the Balance hero reduces from the expanded state to a compact Balance-plus-mini-chart state; the reserve/ratio row recedes, the three insights scale from 1 to .9 while fading out, and the full daily-budget card shifts upward and fades. No section appears by a discrete layout jump. | Static motion-variable contract passed; browser scroll comparison pending | PARTIAL |
| GAL-051 | User interaction direction + animation reference; superseded by GAL-072 | Exiting daily-budget behavior | The detailed `Napi változó keret` card is not a transaction-list row. During the retained B3M-A collapse it fades away without producing a compact sticky `Napi keret` pill; the action and transaction surfaces move continuously into the released space. | Static interaction contract passed; browser scroll comparison pending | PARTIAL |
| GAL-052 | User animation reference | Collapsed transactions | The collapsed state makes room for a continuous `Mai tranzakciók` section with the reference fixture: Tesco / Élelmiszer / `-2 450 Ft` / 12:45, MOL / Közlekedés / `-8 900 Ft` / 11:02, and Starbucks / Egyéb / `-1 250 Ft` / 09:17, followed by the all-transactions action. These rows are prototype data and do not modify Flutter metrics. | Static data/structure contract passed; browser scroll comparison pending | PARTIAL |
| GAL-053 | User request | Native interactive behavior | The Today test screen is touch-scrollable inside the phone, uses `requestAnimationFrame`-coalesced scroll updates, honors reduced-motion preference, preserves the fixed lower navigation, and has no global effect on the remaining Color Lab B3M gallery screens. | Static interaction contract, script parse, and browser launch passed; browser scroll comparison pending | PARTIAL |
| GAL-054 | User correction, 2026-07-22 + Color Lab weekly-spend shell | Today top area and header alignment | The retained B3M-A area above the gradient header contains only the Fluvi lockup: it has no alert, statistics, or profile control. Its gradient hero is anchored to the same `--spendee-header-top` position as the normal weekly-spend B3M header, rather than the prior redesign-only offset. No compact budget pill remains in this variant. | Static DOM/style contract passed; browser comparison pending | PARTIAL |
| GAL-055 | User reference `http://127.0.0.1:8787/color_lab.html?v=20260721e#mind-layout`, B1 collapsed header | Today collapsed hero geometry | At the end of the B3M-A scroll animation, the gradient header is exactly the Color Lab B1 collapsed-header height: `104px`. With the shared `104px` top anchor, its lower edge is `208px`; the post-budget content anchors `11px` below that moving lower edge. The retained B3M-A screen has no compact budget pill. | Static B1 source/animation contract passed; browser end-state comparison pending | PARTIAL |
| GAL-056 | User screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-003953.png` + B3M-A correction | Today collapsed action spacing | At full B3M-A collapse, the faded insight strip and detailed budget leave no blank band above the `Bevétel / Kiadás` action row. The action row remains at the live `11px` header-to-surface gap defined by GAL-059; no compact `Napi keret` pill is rendered. | Static end-state geometry contract passed; browser screenshot comparison pending | PARTIAL |
| GAL-057 | User correction, 2026-07-22 + adjacent B3M Color Lab screen | Today collapsed action-button dimensions | The collapsed Today `Bevétel / Kiadás` controls retain the adjacent B3M controls' `42px` height, `10px` inter-button gap, and `21px` pill radius. Its former `28px` side inset / `173px` width rule is superseded by GAL-071 so the controls share the daily-budget surface width. The action row adds no extra vertical padding that would undo GAL-056's 4px gap. | Static source/style contract passed; browser screenshot comparison pending | PARTIAL |
| GAL-058 | User request, 2026-07-22; superseded by GAL-072 | Today disappearing-budget-pill behavior | The retained Today screen uses the same source composition and scroll range, but its daily-detail card fades away without producing a compact `Napi keret` pill. It is the sole Today prototype screen; no sticky-pill sibling remains. | Static alternate-screen/animation contract passed; browser scroll comparison pending | PARTIAL |
| GAL-059 | User screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-011354.png` + user correction | Disappearing-pill action anchor | Once the daily detail has faded on B3M-A, the `Bevétel / Kiadás` row remains at the same live `11px` header-to-surface gap used by the original collapsed header and its `Napi keret` pill. It must not visibly catch up only during the final scroll pixels. | Static scroll-geometry contract passed; browser screenshot comparison pending | PARTIAL |
| GAL-060 | User reference `http://localhost:49986/` Mind and Budget Scope Rail Comparison + user request, 2026-07-22 | B3M-A optional year-refinement drawer | The retained disappearing-pill `B3M-A · Mai költés` screen has an `ÉV FINOMÍTÁS` drawer immediately below its search pill and before the transaction section. It starts collapsed; its control expands and collapses an in-place, horizontally scrollable year rail. Selecting a year updates the compact selected-year state. No sticky-pill Today screen remains. The prior Kiadás-gradient visual direction is superseded by GAL-061. | Static DOM/style/interaction contract, script parse, HTTP launch passed; browser interaction review pending | PARTIAL |
| GAL-061 | User screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-075210.png`, 1084×2412 px + user correction, 2026-07-22 | B3M-A year-rail visual source | The B3M-A year choices retain the screenshot's pill composition: inactive raised white year pills with dark text; and one larger active `2024` pill using `#715efb` → `#b484f3` → `#e478c3`, white text, and an inset white selection dot. A centered gray/pink pagination-dot row remains under the year pills. The horizontal rail opens with the selected year centered, while the existing drawer open/close behavior remains available. The rail-surface card treatment is superseded by GAL-062. | Static DOM/style/interaction contract, screenshot source review, script parse, and HTTP launch passed; browser visual review pending | PARTIAL |
| GAL-062 | User correction, 2026-07-22 | B3M-A decontainerized year rail | The expanded time pills and pagination dots are not inside either a visual inner rail card or the outer control pill. A nonvisual structural root holds a compact outer time-control and its sibling rail. The outer control remains directly below search and is smaller by the removed rail height. When expanded, year pills and dots appear directly on the screen background below it with no background, border, shadow, radius, or padding container around them. Its final control appearance is defined by GAL-063. | Static DOM/style/interaction contract, script parse, and HTTP launch passed; browser review pending | PARTIAL |
| GAL-063 | User screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-081856.png`, 1084x2412 px + user correction, 2026-07-22 | B3M-A time-control source match | The compact control immediately above the decontainerized year rail matches the reference: a shallow `28px` `#f7f9fc` surface with the `ÉV FINOMÍTÁS` label and selected year on one inline row, and a separate `21px` pale-pink (`#fff5fa`) bordered icon button at right. The button has a real pink (`#df2374`) Lucide chevron asset, rotating between collapsed/down and expanded/up states. The rail and pagination dots remain sibling content directly on the screen background. | Screenshot inspection and static DOM/style/asset contract passed; browser review pending | PARTIAL |
| GAL-064 | User correction, 2026-07-22 | B3M-A fully decontainerized scope control | The outer scope-control surface introduced by GAL-063 is removed. The `ÉV FINOMÍTÁS` label and selected year sit directly on the B3M-A background, aligned with the screen content; the only remaining surface is the standalone pale-pink chevron icon button. The sibling year rail and pagination dots remain direct background content. This supersedes GAL-063's shallow `#f7f9fc` wrapper. | Static style/topology contract passed; browser review pending | PARTIAL |
| GAL-065 | User screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-083634.png`, 1084x2412 px + user correction, 2026-07-22 | Today expanded-header bottom alignment | At `scrollTop = 0`, the lower edge of the left Today screen's `Napi változó keret` card aligns with the lower edge of the adjacent legacy B3M expanded header. With the shared `104px` top anchor, the legacy header ends at `104 + 510 = 614px`; the Today hero is reduced from `201px` to `150px`, producing `104 + 150 + 11 + 128 + 11 + 210 = 614px`. The collapsed hero remains `104px`; no legacy B3M header geometry changes. | Static geometry contract passed; browser comparison pending | PARTIAL |
| GAL-066 | User request, 2026-07-22 | Today collapsed transaction affordance | The retained B3M-A Today screen does not render, expose, or retain a focusable `Összes tranzakció megtekintése` pill. The collapsed state ends with the date-grouped transaction surface rather than a redundant route control. | Static source/DOM contract, script parse, HTTP launch passed; browser review pending | PARTIAL |
| GAL-067 | User request, 2026-07-22 + `lib/features/transactions/widgets/transaction_log_list.dart` | Today transaction log grouping | The prototype follows `TransactionLogList._entries()` and `_DateHeader`: rows are grouped by their raw `TransactionRecord.date` value, each day has a compact `AppColors.gray500` / `12px` / `700`-semantic date title, and one elevated day card contains only that day's rows with internal separators. The test fixture contains exactly three date groups and at least fifteen transactions, so the collapsed log has multiple independently readable cards on the screen background. | Flutter source review, static fixture/DOM/style test, script parse, HTTP launch passed; browser scroll review pending | PARTIAL |
| GAL-068 | User request, 2026-07-22 | Today gesture-region split | The upper `header` swipe region alone controls the B3M-A 0–180px expand/collapse controller. The visual Balance hero passes vertical swipes through to that region, while its `Részletek` button remains tappable. The nested `transactions` swipe region owns native vertical scrolling for the date-card log, uses contained overscroll so it cannot chain into the header controller, and remains scrollable in the retained screen. | Static DOM/style/controller contract, script parse, HTTP launch passed; browser touch review pending | PARTIAL |
| GAL-069 | User screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-090106.png`, 1084x2412 px + user correction, 2026-07-22 | Today transaction viewport lower boundary | In the collapsed B3M-A screen, the nested date-card log continues to the `phone-screen` lower edge. It must not clip at the prior `276px` viewport boundary and leave an approximately `80px` empty column above the bottom navigation. After GAL-070 increases the summary height by `18px`, the B3M-A collapsed transaction top is `485px`; with Color Lab's `--screen-h: 892px`, its nested viewport must be `892 - 485 = 407px`. The bottom navigation remains a higher-z overlay, so scrolling rows disappear at the actual screen bottom/behind that overlay. | Static geometry/z-index contract passed; browser scroll review pending | PARTIAL |
| GAL-070 | User correction, 2026-07-22 + adjacent Color Lab B3M `HETI KÖLTÉS` screen | Today summary-pill source match | The Today `Aktuális hónap` surface retains the adjacent weekly-spend `summary-pill`'s `59px` height and `20px` radius. Its former `28px` outer inset / local `12px` margin is superseded by GAL-071; the existing content, color, and chevron remain unchanged. | Color Lab source comparison and static geometry contract passed; browser screenshot review pending | PARTIAL |
| GAL-071 | User screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-092755.png` + user correction, 2026-07-22 | Today shared post-budget surface width | The collapsed Today `Napi változó keret`, `Bevétel / Kiadás` action row, and `Aktuális hónap` summary use exactly the scroll-content's shared `16px` side inset, with no secondary local horizontal margin. On the `412px` phone shell this gives each full surface `380px` width; the action buttons are `185px × 42px`, separated by `10px`, while the summary remains `59px` high and `20px` round. | Static CSS/geometry contract passed; screenshot comparison and browser review pending | PARTIAL |
| GAL-072 | User request, 2026-07-22 | Canonical B3M-A prototype artifact | `balance_latest_layout.html` is the sole canonical HTML artifact at the project root. It loads the Color Lab and screenshot references from their retained `docs/prototypes` locations. The old gallery HTML path is removed. In the carousel, `mai_koltes` renders exactly one screen: the existing `B3M-A` / `data-today-budget-pill-behavior="exit"` design with the year refinement drawer. The former sticky-pill first screen is absent; all non-Today carousel and editing rows remain unchanged. | Static file/topology, inline-script parse, HTTP-resource, and staged-diff contracts passed; browser review pending | PARTIAL |

### Verification record

- 2026-07-22, GAL-060: `node docs/prototypes/balance_stage2_mother_child_gallery_static_test.js` passed after a verified RED cycle for the new scope-drawer contract. The inline script parsed and `http://127.0.0.1:8790/balance_latest_layout.html` returned HTTP 200. Browser interaction and visual review remain pending.
- 2026-07-22, GAL-061: reviewed `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-075210.png` directly and sampled its active-year gradient (`#715efb` / `#b484f3` / `#e478c3`) and rail surface (`#f8f8fe`). The gallery static test, inline script parse, and HTTP 200 check passed. Browser visual review remains pending.
- 2026-07-22, GAL-062: static checks confirm the nonvisual root, compact outer control, sibling rail topology, and the absence of visual inner-rail surface declarations. The inline script parsed and the gallery returned HTTP 200. Browser review remains pending.
- 2026-07-22, GAL-063: reviewed `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-081856.png` directly. The reference samples used are outer surface `#f7f9fc`, label `#51647a`, toggle `#fff5fa` / `#fcc4de`, and chevron `#df2374`. The new static contract passed after a verified RED cycle, including the actual `assets/icons/lucide/chevron-down.svg` file. Browser review remains pending.
- 2026-07-22, GAL-063 recheck: `node docs/prototypes/balance_stage2_mother_child_gallery_static_test.js` passed; the inline gallery script parsed; both the gallery and `/assets/icons/lucide/chevron-down.svg` returned HTTP 200 on port `8790`; tracked and untracked whitespace checks were clean. Browser visual review remains pending.
- 2026-07-22, GAL-064: after a verified RED cycle, the static contract confirms the outer scope wrapper is only a `21px` structural row with zero padding, border, radius, background, and shadow. The label/year and standalone chevron button retain their original DOM and interaction. Browser visual review remains pending.
- 2026-07-22, GAL-065: reviewed `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-083634.png` directly. A verified RED/GREEN cycle changed only the Today initial/default and controller expanded hero heights from `201px` to `150px`. The static contract confirms the `510px` legacy expanded-header offset equals the Today detail-card offset; browser comparison remains pending.
- 2026-07-22, GAL-066 to GAL-068: verified RED/GREEN with `node docs/prototypes/balance_stage2_mother_child_gallery_static_test.js`. The test now cross-checks Flutter `TransactionLogList._entries()` / `_DateHeader`, rejects all `Összes tranzakció megtekintése` residue, requires the three raw date groups and fifteen rows, requires separate contained header/transaction swipe regions, and verifies that the visual hero passes vertical swipe through while retaining the `Részletek` tap target. The inline script parsed, whitespace checks passed, and port `8790` returned HTTP `200`. Android browser scroll and touch review remain pending.
- 2026-07-22, GAL-069 initial pass: reviewed `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260722-090106.png` directly. The visible blank band was caused by the nested log's old fixed `276px` height ending before the phone bottom. The initial RED/GREEN cycle removed its bottom padding and extended it to the then-current transaction top. That `467px` geometry is superseded by GAL-070's larger Summary pill and the current `485px` formula below.
- 2026-07-22, GAL-069 to GAL-070: a verified RED/GREEN cycle inspected the adjacent Color Lab weekly `summary-pill` (`margin: 0 28px`, `min-height: 59px`, `border-radius: 20px`). The Today summary now adds `0 12px` within its existing `16px` content inset, uses `59px` height and `20px` radius, and keeps its original content. Its added `18px` height moves the B3M-A transaction top to `485px`, so the nested log viewport is correspondingly `calc(var(--screen-h) - 485px)`. Browser screenshot and scroll review remain pending.

## Canonical mother-child content matrix

| ID | Stage1 mother: one compact fact | Stage2 child: additional, non-duplicate information |
| --- | --- | --- |
| `mai_koltes` | Today’s all-expense amount | Today’s transaction count, daily variable ceiling, remaining variable room, and average marker. |
| `heti_koltes` | Week-to-date total | Mon-Sun bars, weekly allowance remainder, and same-day pace deviation. |
| `havi_koltes` | Month-to-date total | Variable-limit use, previous-month same-day index, and daily comparison line. |
| `koltesi_trend` | 30-day change direction | Current/prior 30-day zones and the fixed-free comparison basis. |
| `legutobbi_tranzakcio` | Latest merchant and amount | Category, timestamp, and the merchant’s recent occurrence pattern. |
| `varhato_ho_vegi_koltes` | Projected month-end amount | Confidence band, variable pace, planned fixed load, and limit-risk marker. |
| `leggyakoribb_kereskedo` | Most frequent merchant | Active-day strip, category association, and 14-day visit chronology. |
| `atlagos_napi_koltes` | Rolling 30-day daily average | Balance runway, thresholded spike positions, and rolling-total / 30 decomposition; no Pulse decision is implied. |
| `no_spend_napok_szama` | No-spend days in last 7 days | Elapsed-month ratio, month day map, today/future markers, and named weekly cadence; no streak or Pulse trigger is implied. |
| `top_kategoria_heten` | Leading period/category | Day/week/month mini-category avatars and their period-specific amounts, without creating a duplicate card. |
| `legnagyobb_novekedo_kategoria` | Largest category movement | Category box, previous/current amounts, and new-category state, without a lower visual. |

## Budget mode exclusions

`megtakaritas`, `leggyorsabban_fogyo_kategorialimit`,
`kovetkezo_ismetlo_kiadas`, and `havi_fix_koltseg_osszesen` are deliberately
not part of this Balance B3M gallery. They remain live FastInfo/Pulse inputs in
Flutter; this prototype change removes only their HTML gallery representation.

## Active Editing Lane

The gallery is a working board. `koltesi_trend` is the first focused insight: it is omitted from the primary horizontal row and rendered once in a separate B3M row underneath. `top_kategoria_heten` is also omitted from the upper carousel and rendered once in the next B3M row below it. `legnagyobb_novekedo_kategoria` is omitted from the upper carousel and rendered once alone in a fourth row. `legutobbi_tranzakcio` is omitted from the upper carousel and rendered once alone in a fifth row below it. `no_spend_napok_szama` is omitted from the upper carousel and rendered in a sixth row with its base B3M screen and three alternative Stage2 readings. `atlagos_napi_koltes` is omitted from the upper carousel and rendered in a seventh row with its base B3M screen and three alternative Stage2 readings. This makes the edited cards inspectable without duplication in the gallery; it is an active edit, not visual approval. Each embedded Stage1 belt keeps its catalog neighbors.

## Pulse inventory decision

`docs/prototypes/color_lab.html` uses `pulseMindPortalIdleField()` after a Mind portal pointer interaction. `color_lab_portal_energy.js` uses configurable visual pulse amount/speed. Neither is a FastInfo resolver output or a Balance calculation. The gallery may use a subtle attention pulse after a mother is focused, but must not present it as a metric or make it part of the financial card inventory.

## Correction audit

The first standalone gallery used custom `.phone`, `.balance-header`, `stage1-belt`, and custom child-visual CSS. That was a structural approximation of B3M and fails GAL-009 through GAL-011. Those custom renderers must be removed from the delivered page. The replacement is a same-origin Color Lab B3M render surface whose cloned screens retain the original Color Lab classes and styles.

The first B3M-source replacement preserved the same child DOM blocks for every card and only changed their text. That fails GAL-004, GAL-013, and GAL-014. The replacement must use the B3M panel as the shared surface but remove irrelevant blocks, vary the fact count, and generate a contextual visual inside the existing B3M SVG region.

## Verification Record

- `node docs/prototypes/balance_stage2_mother_child_gallery_static_test.js` passed after the contextual visual and no-redundancy checks were added.
- The inline gallery script passed a separate JavaScript syntax parse.
- The source contract confirms that only `zoomSurface` content is replaced; the original Color Lab `zoomViewport`, `zoomHost`, native `setCanvasZoom(.5)`, pinch, and wheel handlers remain mounted.
- The page, Color Lab source, and referenced Lucide assets returned HTTP 200 from port 8790. An Android browser intent loaded both the gallery and its Color Lab iframe. Android blocks Termux screen capture on this device, so no screenshot file was produced.
- The alternative `koltesi_trend` B3M has a focused-row sibling with `data-stage2-alternative="trend-comparison"`. The static test confirms its two time windows, absolute/relative delta, Pulse threshold, and the absence of the generic avatar/fact/line-chart anatomy. Pixel-level approval remains pending because this device blocks automated Android screenshots.
- The focused row also contains `trend-pulse-map`, `trend-equation`, and `trend-window-tape`. Their static contracts require no generic avatar/fact/line-chart anatomy, and only the same rolling-window trend values and accepted Pulse thresholds. Browser intent was refreshed after these screens were added; pixel-level approval remains pending.
- The prior B3M-E implementation incorrectly replaced the source sparkline with two bar rows. It is rejected by GAL-020 and GAL-022. The replacement must keep the exact Color Lab filled sparkline primitive and make only its `clip-path` data-driven.
- The replacement now reuses the source `fastinfo-sparkline` element in both the selected Stage1 mother and B3M-E. The current 30-day fixture is compressed into six five-day averages solely to generate its polygon; its source vertical low-opacity fill, radius, and fade are not reimplemented. Static contract and inline-script syntax checks passed, and port `8790` returned HTTP `200`; visual approval remains pending.
- The prior B3M-F implementation incorrectly used a centered signed-delta scale and only one secondary line. It is rejected. The replacement must retain all B3M facts and render one two-period composition stripe from the two rolling-window totals.
- The replacement uses the three original B3M trend fact rows and one `45.87% / 54.13%` composition stripe. Its left previous-period segment is gray; the right current-period segment is red for the current higher spend. The static contract verifies the `353 200 Ft` combined total and the absence of the rejected centered scale; browser review remains pending.
- `top_kategoria_heten` now has its own third B3M editing row and is absent from the upper carousel. The static render contract verifies its single lower-row clone, its preserved Stage1 neighbors, and the expanded three-row canvas; this is still an active edit rather than visual approval.
- The category-change row is deliberately reduced to its base B3M screen. B3M-G through B3M-K are removed from the row and prototype code. The retained screen replaces only the upper transaction row with a B3M-J-like category box; its lower visual region is removed together with the rejected difference axis and upward arrow. Browser review remains pending, so GAL-033 through GAL-037 stay `PARTIAL`.
- The B3M catalog now excludes the four Budget mode-owned cards: `megtakaritas`, `leggyorsabban_fogyo_kategorialimit`, `kovetkezo_ismetlo_kiadas`, and `havi_fix_koltseg_osszesen`. The static contract confirms 11 gallery IDs, confirms that all four remain in Flutter's FastInfo catalog, and confirms that none remains as an HTML card. The inline script parses, `git diff --check` is clean, and port `8790` returns HTTP `200`. Only the gallery HTML, its static test, and this checklist were edited.
- `legutobbi_tranzakcio` now remains in the 11-card catalog but is excluded from the primary carousel and rendered once in `exact-b3m-latest-transaction-editor`, directly after the category-change row. Its B3M clone preserves catalog neighbors in Stage1 and has no sibling alternatives. The static gallery test, inline JavaScript parse, and HTTP `200` check passed; browser review remains pending.
- `no_spend_napok_szama` now remains in the 11-card catalog but is excluded from the primary carousel and rendered in `exact-b3m-no-spend-editor` after the latest-transaction row. The row contains the base B3M screen plus the monthly ratio ring, elapsed-month day map, and named seven-day cadence alternatives. The fixture is verified as 3/7 weekly, 9/21 elapsed-month days, 43%, and 10 neutral future days. It renders no Pulse or streak claim. The static gallery test, inline JavaScript parse, whitespace check, and HTTP `200` check passed; Android browser review remains pending because this device has no local headless browser or working Termux screenshot capture.
- `atlagos_napi_koltes` now remains in the 11-card catalog but is excluded from the primary carousel and rendered in `exact-b3m-average-daily-editor` after the no-spend row. The row contains the base B3M screen plus balance-runway units, the actual three thresholded daily positions, and the `191 100 Ft / 30 nap = 6 370 Ft / nap` decomposition. The fixture is verified as 30 daily values totaling `191 100 Ft`, an average of `6 370 Ft`, `58` balance-buffer days, spikes on days 4/15/26 above `9 555 Ft`, and a `19 800 Ft` high. It renders no decision claim. The static gallery test, inline JavaScript parse, whitespace check, and HTTP `200` check passed; Android browser review remains pending because this device has no local headless browser or working Termux screenshot capture.
- The prior first-carousel `mai_koltes` dashboard interpretation is rejected. It merged sections that are visibly distinct in the supplied `stage2redesign.png` and therefore does not meet the screenshot source of truth. GAL-044 through GAL-047 replace it; no visual completion claim is valid until the browser is compared against that image.
- The supplied `stage2redesign.png` was inspected directly at 853x1844 px and retained at `docs/prototypes/reference_assets/stage2redesign.png`. The first carousel screen now replaces only its B3M phone contents with the reference's eight-band composition: brand/tools, Balance hero, three insight cards, daily-detail card, two separate action cards, month summary, search/filter, and bottom navigation. The profile and action illustrations use crops from that supplied local image; all other structure is code-generated. The static gallery contract and inline JavaScript syntax parse passed, both the gallery and reference asset returned HTTP `200` from port `8790`, and the Android browser intent was opened. Termux screen capture still fails with status `-1`, so visual comparison remains pending user review.
- The supplied `stage2anim.png` was inspected directly at 1024x1536 px and retained at `docs/prototypes/reference_assets/stage2anim.png`. The `mai_koltes` screen now has a dedicated native scroll viewport plus an overlay gradient hero. Its first `180` px of scroll drives frame-coalesced CSS variables: the hero moves from `201px` to `163px`, the insight strip scales from `1` to `.9` and fades, the detailed budget card fades upward, and a sticky `Napi keret · 6 500 Ft` pill appears. The collapsed source fixture adds Tesco, MOL, and Starbucks rows without touching Flutter data. The static contract, inline parse, gallery HTTP `200`, reference-asset HTTP `200`, and Android browser launch passed. Android screen capture remains unavailable, so GAL-048 through GAL-053 require browser scroll review before approval.
- GAL-054 removes the alert, statistics, and profile controls from the Today topbar. The gradient hero and compact budget pill now use the Color Lab weekly B3M `--spendee-header-top` anchor. The static gallery contract, inline JavaScript parse, and gallery HTTP `200` check passed; browser comparison remains pending.
- GAL-055 uses Color Lab B1's `104px` collapsed-header height with the existing shared `104px` header top, so the Today hero's animated end edge is `208px`. The content and compact budget pill derive their position from the live hero height rather than a second offset. Static source/animation verification passed; browser end-state comparison remains pending.
- GAL-056 identifies the large blank band from the prior screenshot as the two fading but still flow-resident `128px` / `210px` surfaces. At full collapse, the post-budget block now derives a `-126px` shift from those dimensions, the `180px` scroll distance, the `50px` compact pill, and B3M's `4px` content gap. Static contract, inline parse, and whitespace verification passed; browser screenshot comparison remains pending.
- GAL-057 changes only the Today custom action-grid geometry. Its existing `16px` scroll inset plus a `12px` local inset produces the B3M `28px` screen inset; with the shared `10px` gap on a `412px` screen each control is `173px` wide. The height and radius now derive from the Color Lab `--spendee-type-pill-h: 42px` source. Static contract, inline parse, and whitespace verification passed; browser screenshot comparison remains pending.
- GAL-058 adds `B3M-A · Mai költés` directly after the original sticky-pill screen. The duplicated source composition uses `data-today-budget-pill-behavior="exit"`: no compact `Napi keret` pill can materialize or receive focus. The original screen retains its sticky-pill behavior. Static contract, inline parse, and whitespace verification passed; browser scroll comparison remains pending.
- GAL-059 replaces the exit variant's prior fixed `-187px` post-budget shift with a scroll-relative shift. Once the daily detail has faded, the action row remains at the original screen's `11px` header-to-`Napi keret` anchor throughout the rest of the collapse, rather than catching up only in the final pixels. Static contract passed; browser screenshot comparison remains pending.
- GAL-071 removes the Today action row's and summary's separate `12px` local margins. Both now inherit the same `16px` side inset as the daily-budget card, producing a shared `380px` surface width on the `412px` screen and `185px` action buttons around their preserved `10px` gap. The static contract was intentionally RED before the CSS change, then passed; the inline script parsed, no whitespace errors were reported for the untracked prototype files, and the gallery returned HTTP `200`. Screenshot comparison and browser review remain pending.
- GAL-072 records the canonical root artifact. A verified RED/GREEN cycle first required `balance_latest_layout.html` at the project root and removal of the former gallery path, then required one `todayB3mAColumn` with `data-today-budget-pill-behavior="exit"` and no sticky Today sibling. The root file points to the retained Color Lab and screenshot sources under `docs/prototypes`. The balance static test, Color Lab static test, and five targeted FastInfo Flutter test files (52 tests) passed. The root script parsed; the root page, Color Lab source, both screenshot assets, and the chevron returned HTTP `200`; the old gallery path returned `404`; and `git diff --cached --check` passed. Browser review remains pending.
