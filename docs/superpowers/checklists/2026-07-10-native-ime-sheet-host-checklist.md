# Native IME Sheet Host Checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| NATIVE-IME-001 | User: "nem foltozás", Perplexity: native-owned motion with Flutter-only content | `android/app/src/main/kotlin/com/exptv2/app/NativeImeSheetMotion.kt` | The native sheet motion model translates the moving container directly from current IME bottom inset, without Dart-side frame decisions. | Android unit test added for `imeBottomPx -> -imeBottomPx` and negative clamp; GitHub Actions run `29121658171` passed Android unit tests. Local Gradle remains blocked by Android AAPT2/proot resource compiler startup failure. | DONE |
| NATIVE-IME-002 | User-approved architecture switch | `android/app/src/main/kotlin/com/exptv2/app/NativeImeSheetHost.kt`; `MainActivity.kt` | A native overlay host can open/close a bottom sheet container and applies IME progress in `WindowInsetsAnimationCompat.Callback.onProgress`. | Direct code inspection confirms callback-owned `translationY` path; GitHub Actions run `29121658171` passed Android unit tests and debug APK build. | DONE |
| NATIVE-IME-003 | Perplexity: FlutterView inside native moving container | `main.dart`; `lib/features/diagnostics/native_ime_sheet_probe.dart`; `NativeImeSheetHost.kt` | The native host embeds a dedicated Flutter probe surface, while Flutter content only owns fields and semantic close intent. | `flutter analyze`, Flutter tests, Android unit tests, and debug APK build passed in GitHub Actions run `29121658171`. | DONE |
| NATIVE-IME-004 | Need measurable rollout before AddTransaction migration | `lib/core/debug/*`; `lib/services/native_ime_sheet_bridge.dart` | The POC is manually openable from Debug Console and does not replace the production AddTransaction FAB path yet. | Flutter widget test taps the Debug Console native IME probe button and verifies `openProbe` method-channel call. | DONE |
| NATIVE-IME-005 | User log shows Flutter visual frame arrives halfway through IME duration | Native logs; Debug Console logs | The POC emits native host logs for open/close and IME translation samples so device testing can compare native motion against Flutter frame lag. | Direct code inspection confirms native log points; manual device log validation still required after CI APK build. | PARTIAL |
