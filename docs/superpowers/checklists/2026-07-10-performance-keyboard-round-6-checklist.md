# 2026-07-10 Performance + Keyboard Round 6 Checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| BUG6-001 | User: keyboard trace lags and buttons move at different speed than Android keyboard everywhere | `lib/core/keyboard/keyboard_inset_follower.dart`, sheet users | Keyboard-dependent UI uses the raw Android inset in the first frame; debug logs expose raw/effective/lag so copied logs show whether follower is behind | RED/GREEN: `test/widget_test.dart --plain-name "transaction editor keeps date-time and save gap stable while keyboard appears"`; full affected widget suite | DONE |
| BUG6-002 | User: vendor keyboard not smooth, only bottom action should follow keyboard | `VendorFilterPanel` in `transaction_home_page.dart` | Vendor sheet body/search/list stay fixed while only footer bottom follows raw keyboard inset; vendor list is not recomputed on every keyboard frame | Existing vendor keyboard test plus code inspection of cached `_VendorVisibleModel`; full affected widget suite | DONE |
| BUG6-003 | User: category/vendor scroll, income/expense switch, fastfilter lag | `expense_surface.dart`, `transaction_log_list.dart`, vendor list rendering | High-volume unpressed logbox surfaces no longer spam `DebugConsole`; list diagnostics stay summary-level and do not iterate/build full large lists for debug | RED/GREEN: no-spam logbox test and vendor virtualization test; `flutter analyze`; full affected widget suite | DONE |
| BUG6-004 | User: fastfilter lag when tapping logbox avatar with category filter active | `transaction_log_box.dart`, `expense_surface.dart`, `transaction_home_page.dart` | Press/filter flow keeps useful pressed-surface traces but avoids per-row unpressed traces during rebuild | Existing logbox press tests plus new no-spam test in full affected widget suite | DONE |
| BUG6-005 | User: AddNewTransaction sheet still changes size/gap when keyboard appears | `add_transaction_sheet.dart`, `slide_up_menu_card.dart` | AddTransaction panel height and date/time-to-save gap stay unchanged on the first keyboard frame; logs include panel/content/keyboard/gap diagnostics | RED/GREEN immediate keyboard widget test; full affected widget suite | DONE |
| BUG6-006 | User: tests keep passing while real behavior is wrong | Flutter widget tests | Tests check first-frame behavior and large-list virtualization, not only settled animation or tiny fixture data | RED failures observed, then GREEN targeted tests and `150` affected widget tests passing | DONE |
