# Native IME Sheet Close State Machine Checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| CLOSE-ORDER | User logs 2026-07-11 01:05 and external Android architecture review | `NativeImeSheetHost`, native close lifecycle | `sheetClosed` is not emitted while IME bottom is still non-zero during normal close; overlay remains visible and translated until IME reaches 0 or bounded timeout fires. | Kotlin unit tests for close state machine; inspect host close path; targeted Android unit test command. | DONE: implemented, inspected, and verified by CI Android unit tests. |
| REENTER | User reports repeated lag/crash during multiple focus and close cycles | `NativeImeSheetHost`, close guard | Repeated close requests and stale timeout callbacks cannot double-notify Flutter or tear down the overlay out of order; reopen cancels pending close. | Kotlin unit tests covering duplicate close and stale timeout. | DONE: regression tests cover duplicate close, stale timeout, settled duplicate close, and timeout completion; CI Android unit tests passed. |
| IME-DYNAMIC | User logs show IME height changes 252.2/332.2 | `NativeImeSheetHost`, insets callback | Native motion uses current IME inset each frame and does not cache a single keyboard height. | Code inspection; existing/native motion tests. | DONE: host reads current `WindowInsetsCompat.Type.ime()` in apply-insets, onProgress, and onEnd. |
| DIAGNOSTICS | User reports “nem tudja, hol a keyboard teteje” | Native logs | Close request, close wait, close completion, timeout, and native IME progress/end logs expose the IME px and close state. | Code inspection; log strings in host. | DONE: close request/wait/timeout/complete logs include IME px, phase/reason, notify flag, and token. |
| REGRESSION | Existing native sheet tests | Flutter and Android/JVM tests | Existing native sheet behavior tests still pass. | Flutter targeted tests; Android JVM test command in Ubuntu proot; GitHub Actions. | DONE: local targeted Flutter tests passed, local `flutter analyze` passed, and GitHub Actions analyze/Flutter tests/Android unit tests/debug APK build passed. |
