# Category Sheet And Orbit Backheader Acceptance Checklist

| ID | Source | Intended Code Area | Acceptance Condition | Verification Method | Status |
| --- | --- | --- | --- | --- | --- |
| CS1 | User: "kategória menu ha sheet, fedje a bottom navot" | `TransactionHomePage`, `ExptShell`, sheet host/navigation layering | Slide-up category menu is rendered above or otherwise covers bottom nav and FAB; nav/FAB are not visible/tappable through it. | Widget test in shell/home context checks sheet bottom covers viewport and bottom nav/FAB are hidden or occluded. | DONE |
| CS2 | User: "nem látom a sheet alján a hozzáadás gombot" | `CategoryMenuPanel`, slide sheet safe-area layout | Bottom `Új kategória` pill is visible at the bottom of slide-up category sheet above safe area. | Widget test checks `category-menu-add-pill` rect is inside sheet and near sheet bottom. | DONE |
| CS3 | User: "slide interferál a scrollal. draggolni csak a handleren lehessen" | `SlideUpMenuCard`, `CategoryMenuPanel`, category sheet integration | Category grid scroll/content drag does not move the sheet; only the handler can start sheet drag. | Widget test drags grid and verifies transform remains stable; handler drag still moves. | DONE |
| CS4 | User: category sheet drag behavior from earlier plus handler-only clarification | `SlideUpMenuCard`, category sheet integration | Handler drag snaps back before threshold, dismisses after threshold, and fast downward handler swipe closes; horizontal/diagonal drags do not close. | Widget tests for short/long/fast/diagonal handler gestures. | DONE |
| CS5 | User: "kategória menuben, ha sheet formában van, akkor a hozzáadás gomb felett mindig legyen annyi padding, mint a legutolsó screenshoton... a body scroll zone ... a hozzáadás felett" + screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260705-045048.png` | `CategoryMenuPanel`, slide sheet body/add-pill layout | In slide-up sheet mode, the category body/grid scroll zone ends above the fixed bottom `Új kategória` pill, with persistent visual padding between the last row/body bottom and the pill matching the latest screenshot; the scrollable body must not extend behind or to the screen bottom. | Widget layout test verifies scrollable body bottom is above `category-menu-add-pill.top`, with a stable minimum gap, and screenshot/manual verification against the referenced screenshot. | DONE |
| OB1 | User: "tervezd át a backheadrt, az orbit budget modeot (csak azt)" | `BackheaderStyleSurface`, `CategoryBudgetStage` | Only `orbitBudget` layout changes; classic and hero-token keep existing behavior. | Widget tests render all backheader styles and compare expected keys/behavior. | DONE |
| OB2 | User: "bal oldalt az adott kategóriaikon kell. mellette név. jobb oldalt pedig az összegek" | `BackheaderStyleSurface`, category icon widgets | OrbitBudget collapsed layout shows left icon, middle name, right amount. | Widget test checks icon/name/amount keys and relative rect ordering. | DONE |
| OB3 | User: "körkörös progress bar tűnjön el a középső betű ikonnal együtt" | `BackheaderStyleSurface` | Old orbit segmented chart and center initial avatar are absent in orbitBudget. | Widget test asserts old orbit/initial-avatar keys are absent. | DONE |
| OB4 | User: "színes card alján legyen ugyanaz a partition progress bar..." | `BackheaderStyleSurface`, `CategoryLimitPartitionBar`, allocation helpers | OrbitBudget bottom uses `CategoryLimitPartitionBar` with same allocation logic as the limit editor. | Widget/unit tests inspect partition bar and segment count/active values. | DONE |
| OB5 | User: "körkörös progress bar a kategória ikon körül... fehér..." | `BackheaderStyleSurface`, custom painter/widget for ring | Limited active item shows white ring around icon with progress based on spent/limit; no ring without limit. | Widget test checks ring widget/painter values for limited and unlimited items. | DONE |
| OB6 | User: "ha ez a backheader mód aktív, akkor az addnewbudget/addnew limit sheet tűnjön el..." | `TransactionHomePage`, `CategoryBudgetStage`, `ExptShell` | In orbitBudget, tapping/expanding budget target does not open `BudgetTargetEditorSheet`; editor controls live inside backheader. | Widget test taps orbitBudget backheader and asserts sheet absent, inline editor present after handle drag. | DONE |
| OB7 | User: handle drag expand to income/expense button bottom, haptic trigger, snap-back before trigger | `CategoryBudgetStage`, `TransactionHomePage`, haptic integration | Dragging orbit handle down expands only to type-pill bottom; trigger tick fires once; release before trigger snaps closed; release after trigger stays expanded. | Widget tests for handle drag distances and debug/haptic channel where available. | DONE |
| OB8 | User: "ha expandálva van... kb 10px... shrink back triggerelve... cancelezni..." | `CategoryBudgetStage` | Expanded handle can arm shrink with extra downward drag, tick on arm, cancel by dragging back, release armed collapses. | Widget tests for arm/cancel/release state transitions. | DONE |
| OB9 | User: "ha a user a slidert állítja... élőben változik" | `CategoryBudgetStage`, limit editor state helpers | Slider changes update ring, amount text, and partition bar immediately before repository refresh completes. | Widget test moves slider and checks UI values update in same pump cycle. | DONE |
| OB10 | User: "nem kell dedikált mentés gomb, mentés mindig azonnali..." | `CategoryBudgetStage`, `TransactionStore` callbacks | Inline orbit editor has no save button; slider change immediately calls overview/category save callback; shrink does not save. | Widget test with fake repository verifies save on slider change and no save on shrink-only drag. | DONE |
| OB11 | User: "vigyázz, hogy ne interferáljon... oldalra swipepal, átlósan ne lehessen húzni" | `CategoryBudgetStage` gesture recognizers | Horizontal item swipe remains available; vertical expand only starts from handle; diagonal gestures do nothing. | Widget tests for horizontal swipe, vertical handle drag, and diagonal drag. | DONE |
| OB12 | User: "a slider állítása nem smooth, laggol, és crashel az app" | `CategoryBudgetStage`, inline save scheduler | Orbit inline slider updates UI every drag frame but persists through serialized/debounced saves with a final flush on drag end; async save errors are caught/logged instead of becoming unhandled crashes. | Widget test drives rapid slider updates with delayed callbacks and verifies limited save count, latest amount persisted, and no uncaught exception. | DONE |
| OB13 | User: "a text inputot ... nem raktad fel a sheetből" | `CategoryBudgetStage`, inline orbit editor controls | Orbit inline editor includes the amount text input from the sheet (`limit-amount-input`) and input edits update preview immediately and persist without a save button. | Widget test expands orbit editor, enters an amount, and verifies UI/save callback update without `limit-save-button`. | DONE |
| OB14 | User: "sem a kuka" | `CategoryBudgetStage`, inline orbit editor controls | Orbit inline editor includes the reset/delete icon (`limit-reset-inline-button`) and tapping it clears the limit, updates preview, and persists. | Widget test taps reset and verifies persisted amount is 0 and alert is false. | DONE |
| OB15 | User: "sem a jump to bevétel" | `CategoryBudgetStage`, inline orbit editor controls | Orbit inline editor includes an explicit jump-to-overview/income-budget button for category items, not only a hidden long-press/double-tap gesture. | Widget test taps the jump button from a category and verifies the active overview/income item is selected. | DONE |
| OB16 | User: "a gombokat ... semelyiket sem"; superseded by OB19 for previous/next chevrons | `CategoryBudgetStage`, inline orbit editor controls | Orbit inline editor carries over the needed sheet controls: max where applicable and no dedicated save button; previous/next chevrons are removed by OB19 because horizontal swipe handles navigation. | Widget tests verify `limit-slider-end-button` where applicable, absence of `limit-save-button`, and OB19 verifies chevrons are absent. | DONE |
| OB17 | User: "helyezd 10px lejjebb a backheader felső contentjét, mert a header card alja kitakarja" | `BackheaderStyleSurface` orbitBudget layout | OrbitBudget top row content is moved 10 px lower without changing non-orbit backheader styles. | Widget layout test compares orbit content top positions and checks classic/heroToken are unchanged. | DONE |
| OB18 | User: "még mindig túl fent van az ikon a körrel" + screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260705-055624.png` | `BackheaderStyleSurface` orbitBudget layout | OrbitBudget icon/ring row is moved further down so it is not visually tucked under the header card while staying in the same colored surface. | Widget layout test checks expanded orbit icon top is at least 64 px and hero/classic remain unchanged. | DONE |
| OB19 | User: "chevron gombok nem kellenek, mert az oldalra swipe ellátja ezt a funkciót" | `CategoryBudgetStage` inline orbit editor | Inline orbit editor does not render previous/next chevron buttons; horizontal swipe remains the item navigation. | Widget test asserts `limit-card-previous-button` and `limit-card-next-button` are absent while the existing orbit horizontal swipe test remains green. | DONE |
| OB20 | User: "a limitszám duplikálva van, a jobb felső sarokban már szerepel" | `CategoryBudgetStage` inline orbit editor | Inline orbit editor removes the duplicated center amount text and relies on the existing top-right amount. | Widget test asserts `backheader-orbit-inline-amount` is absent and `backheader-orbit-amount` remains. | DONE |
| OB21 | User: "csak a slider fér ki, a text pill, a kuka, és a jump to bevétel gomb nem" + "ennyi felületen minden férjen el" | `CategoryBudgetStage`, `BackheaderStyleSurface` compact editor layout | Within the same expanded backheader surface, slider, amount text pill, reset/delete button, and jump-to-income/overview button are all visible above the handle without increasing max expansion. | Widget layout test expands orbit editor and verifies slider/input/reset/jump rects are inside the colored surface and above the handle; home integration test verifies the handle remains draggable when the header card is layered above it. | DONE |
| OB22 | User: "szedd le a nyíl iknt a header cardról" | `TransactionHeaderCard`, header tests | The separate circular arrow expand button and its hit area are not rendered on the header card. | Widget tests assert `header-expand-button` and `header-expand-button-hit-area` are absent. | DONE |
| OB23 | User: "a header card chip \"camera\" icont cseréld le budget ikonra ez lesz a budget/backheader trigger gomb" | `TransactionHeaderCard`, `TransactionHomePage`, header tests | The former camera chip is a budget/backheader trigger chip with a budget icon; tapping it opens the backheader/expanded header state, while orbitBudget closing is handled by OB27 overexpand. | Widget tests assert no camera icon, find `header-budget-trigger-chip`, and tapping it opens the home backheader. | DONE |
| OB24 | User: "helyette kell handler a header cardra" | `TransactionHeaderCard`, header drag tests | A visible header-card drag handle replaces the removed arrow affordance and participates in the existing header pull/drag gesture. | Widget tests find `header-card-drag-handle` and start the header pull gesture from it; orbitBudget integration verifies the header handle passes hit-testing through to the backheader handle while expanded. | DONE |
| OB25 | User: "partition progress bar körül legyen egy fehér border... sarkoknál... legyen kicsivel vastagabb" | `CategoryLimitPartitionBar`, partition bar tests | Partition progress bars render with a thicker visible white outer border that remains visible around rounded corners while colored segments stay clipped inside it. | Widget tests verify the outer bar has a white border wider than 1 px and colored segment rects are inset from the outer edge. | DONE |
| OB26 | User: "backheader ikon... legyen 30% kisebb (a circle progressbar is) és fix legyen a pozíciója, ne változzon az expand során" + screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260705-064837.png` | `BackheaderStyleSurface`, orbitBudget layout tests | OrbitBudget icon/ring size is reduced to 70% of the previous 58 px size and the icon top position is identical collapsed vs expanded. | Widget layout tests verify icon/ring size around 41 px and same top before/after expanding. | DONE |
| OB27 | User: "miután a chevron gombot törlöd, ezért a backheadert overexpanddal lehessen bezárni, trigger point 1, jump to normal height, trigger point2 lejjebb, close" | `CategoryBudgetStage`, orbit handle gesture tests | From expanded orbitBudget, dragging below normal height has two downward trigger points: trigger 1 haptics/snaps back to normal expanded height on release; trigger 2 haptics/closes. | Widget tests drag expanded orbit handle to trigger 1 and trigger 2 and verify snap vs close. | DONE |
| OB28 | User: "dupla handler lett, inkább szedd le a sötétebb handlert a vackheaderből, és a headercardról is, csak a backheader fehér handleje maradjon, ami már ott volt" | `TransactionHeaderCard`, `BackheaderStyleSurface`, header/backheader tests | Remove the newer dark header-card handle and any extra dark backheader handle affordance; only the existing white `backheader-orbit-handle` remains visible when the orbit backheader is open. | Widget tests assert `header-card-drag-handle` is absent and exactly one visible `backheader-orbit-handle` exists in orbitBudget. | DONE |
| OB29 | User: "nem lehet bezárni a backheadert, hogy a header card visszaslideoljon" | `CategoryBudgetStage`, `TransactionHomePage`, header slide tests | OrbitBudget can fully close via handle drag and the header card slides back to normal collapsed position. | Home widget test opens orbitBudget, performs close drag on the white handle, and verifies `transaction-header-card` slide progress returns to 0/backheader is closed. | DONE |
| OB30 | User: "nem kell expanded backheader... csak lejjebb lehet kicsit húzni a backheadert" | `CategoryBudgetStage`, `BackheaderStyleSurface` | Remove the expanded inline-editor state and extra stage-height growth; the handle supports only a small overpull range for close arming and always snaps back or closes. | Widget tests assert `backheader-orbit-inline-editor` and `backheader-orbit-slider` are absent and no expanded inline editor or slider is rendered, and close overpull snaps back or closes. | DONE |
| OB31 | User: "slide-tick-close funkció... trigger point 1, jump to normal height, trigger point2 lejjebb, close" | `CategoryBudgetStage`, haptic/gesture tests | Dragging the white handle down from compact orbitBudget arms trigger 1 with tick and snaps back to compact normal on release; dragging further arms trigger 2 with tick and closes the backheader. | Widget gesture tests verify short pull snaps, trigger-1 pull remains open at normal height, trigger-2 pull closes; diagonal/horizontal gestures do not close. | DONE |
| OB32 | User: "header card chip maradjon sárga mint ami volt, ne kék" | `TransactionHeaderCard`, header tests | `header-budget-trigger-chip` uses the previous camera-chip yellow `Color(0xFFFBBF24)` while keeping the budget icon and open action. | Widget test inspects chip `BoxDecoration.color` and taps it to open backheader. | DONE |
| OB33 | User: "header card mágnescsík... átmenetes mágnescsík magassága optimális, a többi mágnescsík designt is ilyen magasra rakd" | `MagnetStrip`, magnet tests | All magnet strip visual variants use the same visible track/slab height as the fade/gradient track; non-fade designs are no taller than the fade visual track. | Widget/painter tests verify `nofade`, `budget`, `magnetcard`, and `adaptive` exposed track/slab metrics match the fade visual height. | DONE |
| OB34 | User: "backheader partition progress bar kapott egy belső paddingot is. ne kapjon, elég a fehér border" | `CategoryLimitPartitionBar`, partition tests | The partition bar keeps the white border but removes the inner padding/inset; colored segments fill directly under the foreground border without a layout gap. | Widget test verifies first segment starts at the outer bar left/top/bottom and the white border is painted as foreground decoration, not as padding. | DONE |
| OB35 | User: "partition progress bar ugyanabban a pozícióban legyen, mint a mágnescsík, és szélessége/magassága is egyezzen" | `BackheaderStyleSurface`, `CategoryBudgetStage`, geometry tests | OrbitBudget partition bar uses the same top/left/right/height visual geometry as the header card fade magnet strip track. | Layout tests compare orbit partition rect against header magnet visual track geometry. | DONE |
| OB36 | User: "partition progress bar felett lesz az ikon... mellette a kategória neve... felslideolt header card aljától 10px padding" | `BackheaderStyleSurface`, `TransactionHeaderMetrics`, layout tests | Icon/ring and title sit above the partition bar with 10 px clearance below the slid-up header-card/status-bar boundary; the row does not overlap the header. | Widget layout test computes slid-up header bottom from metrics and verifies orbit row top/bottom spacing. | DONE |
| OB37 | User: "limitszám x/y kerüljön a partition progress bar alá... ahol a balance összeg van, és ugyanazzal a betűmérettel" | `BackheaderStyleSurface`, text/input tests | Limit x/y text moves below the partition bar at the balance-value vertical position and uses the header balance font size/weight. | Widget layout/style test checks amount rect below bar and TextStyle font size equals header balance size. | DONE |
| OB38 | User: "a slidert kiveszed... partition progress bar... kapja meg a slidert is" | `CategoryLimitPartitionBar` or new orbit partition slider widget, `CategoryBudgetStage` | The visible partition progress bar is also the drag slider for category/budget limits; dragging it updates amount, ring, and segments live and persists immediately. | Widget test drags the partition bar and verifies preview amount/ring/bar update plus save callback without rendering `CategoryLimitSlider`. | DONE |
| OB39 | User: "ha van... limitösszeg, annak tappelhetőnek kell lennie... szerkeszthető... nem kell külön textpill... kategórialimit, és a budgetlimit is" | `BackheaderStyleSurface`, `CategoryBudgetStage` amount input | The x/y limit text itself becomes an inline editable numeric input for both category limits and overview/budget limits; no separate text pill is rendered. | Widget tests tap amount text for category and overview, enter a value, and verify preview/save update via existing callbacks. | DONE |
| OB40 | User: "a kuka, és fast budgetlimit (sumbevétel) nyíl gomb kerüljön a jobb alsó sarokba. jump to budget, jump to bevétel gomb nem kell" | `BackheaderStyleSurface`, `CategoryBudgetStage` actions | Compact orbit backheader shows reset/delete and max-to-period-income action at bottom right when applicable; old jump-to-budget/income controls are absent. | Widget tests assert `limit-reset-inline-button` and max button positions at bottom right, and `backheader-overview-jump-button` is absent. | DONE |
| OB41 | User: "ha nincs kategórialimit megadva, akkor egy üres pill legyen ott, ami szerkeszthető, és bele lehet írni az x/y számokat a bal alsó sarokban" | `BackheaderStyleSurface`, `CategoryBudgetStage` amount input | For categories without a limit, the lower-left amount area renders an empty editable pill/input affordance; entering a value creates/saves the category limit. | Widget test renders unlimited category, finds empty editable amount pill at lower-left, enters amount, and verifies save callback. | DONE |
| OB42 | User: "a header card legyen kicsit magasabb... lefele növeled... content rajta marad a jelenlegi pozíciójában... balance alatti padding fog növekedni" | `TransactionHeaderMetrics`, `TransactionHeaderCard`, home/header tests | Header card height increases downward from 176 to 188 px; title, chip, magnet, balance label/value, and category button top metrics remain unchanged. | Widget tests assert `TransactionHeaderMetrics.cardHeight == 188`, existing content top metrics are unchanged, and rendered header height equals the metric. | DONE |
| OB43 | User: "a header card alatt annyi padding legyen, mint a bevétel kiadás-és summarypill teteje közt" | `TransactionHeaderMetrics`, `TransactionMenuMetrics`, home layout tests | Header bottom-to-type-pill top gap equals `TransactionMenuMetrics.typePillBottomPadding` (the type pill bottom-to-summary top gap). | Widget/metric test verifies `contentTop + typePillTopPadding - cardHeight == typePillBottomPadding`. | DONE |
| OB44 | User: "a színes backheader mérete=headercard mérete, ezért ez is fog nőni" | `CategoryBudgetStage`, `BackheaderStyleSurface` | Orbit backheader colored surface/stage height tracks the new `TransactionHeaderMetrics.cardHeight`. | Widget test asserts `category-budget-stage` height equals the new header card height for orbitBudget. | DONE |
| OB45 | User: "partitionprogressbar körüli border túl vastag, akkora legyen a stroke width, mint a circle progress bar stroke width" | `CategoryLimitPartitionBar`, `BackheaderStyleSurface`, partition tests | Partition progress border width is 1.6 px, matching the orbit circle progress ring stroke width. | Widget test inspects partition foreground border width as 1.6; direct code inspection confirms `_OrbitProgressRingPainter` uses stroke width 1.6. | DONE |
| OB46 | User: "partition progress bar magassága legyen 30% kevesebb... az aljából veszed le... felső pozíciója fix" | `CategoryBudgetStage`, `BackheaderStyleSurface`, geometry tests | Orbit partition slider height is 70% of the previous fade visual track height; its top position remains equal to the previous partition top, so the bottom moves upward. | Widget layout test compares partition top to old formula and height to `fadeTrackHeight * 0.7`. | DONE |
| OB47 | User: "több padding lesz a progress bar és az x/y limitszám közt. a limitszám mérete legyen 30% kevesebb" | `BackheaderStyleSurface`, `CategoryBudgetStage`, amount layout tests | Limit amount row remains below the thinner partition bar, gains extra vertical gap, and uses 16.8 px font size (70% of 24). | Widget style/layout test verifies amount font size and partition-to-amount gap grows after height reduction. | DONE |
| OB48 | User: "ne legyen fehér textbox... opacityje 0 legyen... textbox etc felesleges" | `CategoryBudgetStage` amount editor | Orbit amount editing does not render an opaque white text field box; `backheader-orbit-amount-input` uses transparent fill/border and only shows text/cursor inside the pill. | Widget test inspects `TextField.decoration` for transparent/no filled box and verifies no standalone white input container surrounds the whole x/y row. | DONE |
| OB49 | User: "a kuka, és nyíl (sumbevétel) gombok kerüljenek a jobb felső sarokba" | `BackheaderStyleSurface`, `CategoryBudgetStage` actions | Reset and max-to-period-income action buttons render in the orbit backheader top-right area. | Widget layout test verifies action buttons are above the partition bar and near the right edge. | DONE |
| OB50 | User: "van egy adaptív szélességű pill... fehér cursor... pillek designja... háttere kicsit átlátszó... számok is fehérek" | `CategoryBudgetStage` amount editor | The editable x value is an adaptive-width translucent pill with white text and white cursor. | Widget test verifies the x pill key, translucent decoration, adaptive width after entering a longer value, white text style, and white cursor. | DONE |
| OB51 | User: "limitszám minden esetben x pill/ y szám... csak az x pill írható... y mindig automatikus... ha nincs limit, akkor az x pill n/a" | `CategoryBudgetStage` amount editor | Orbit amount row always renders `x pill / y text`; only x is editable; y is automatic spent/current amount; no-limit x shows `n/a`. | Widget tests cover limited category, overview budget, and no-limit category, verifying x input/pill text, slash separator, y text, and input edit behavior. | DONE |
| OB52 | User: "továbbá nincs a partition slider en handle, nincs vizuál feedback. legyen." | `CategoryBudgetStage`, `CategoryLimitPartitionBar` or orbit slider overlay | The partition bar-as-slider displays a visible white handle at the active x-limit ratio and moves live while dragging. | Widget test finds `backheader-orbit-partition-handle`, checks initial x position from limit/range ratio, drags the bar, and verifies the handle position changes with the saved amount. | DONE |
