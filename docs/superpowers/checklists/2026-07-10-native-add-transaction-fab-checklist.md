# Native AddTransaction FAB Checklist

| ID | Source | Intended code area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| NAF-1 | User: "oldd meg akkor, működjön" after native-host architecture discussion | `ExptShell` FAB path, `NativeImeSheetBridge`, Android `NativeImeSheetHost` | FAB add-transaction requests `openAddTransaction` on the native IME sheet host before using the old `SlideUpMenuCard` path | `flutter test test/widget_test.dart --name "FAB uses native add transaction host when available\|native hosted add transaction content omits SlideUpMenuCard"` | DONE |
| NAF-2 | User: "nem sheet gap... masszív keyboard slide lag" | Android `NativeImeSheetHost` | The visible AddTransaction host container is translated in Android `WindowInsetsAnimationCompat.Callback.onProgress`, not by Dart keyboard inset `setState` | Code inspection: `openAddTransaction` uses native host, `onProgress` applies `sheet.translationY`; native-hosted widget test asserts no `SlideUpMenuCard` | DONE |
| NAF-3 | Existing app behavior | Native hosted Flutter sheet content | The native hosted sheet still renders the AddTransaction form, supports category/date/time/name/amount, and saves via the existing native database bridge | Native-hosted widget test enters name/amount, taps save, and verifies `expenseAddTransaction` payload | DONE |
| NAF-4 | Existing app behavior after saving | Main shell transaction store | After native-hosted save, the main shell refreshes transaction data/notifications | `test/core/debug_floating_button_test.dart` verifies `transactionCommitted` callback; code inspection verifies shell calls `refreshAfterRecurringProcessing()` | DONE |
| NAF-5 | Test environment and unsupported platform fallback | `NativeImeSheetBridge`, `ExptShell` | If the native method is missing/unavailable, the existing Flutter `SlideUpMenuCard` AddTransaction path still opens | Full `flutter test test/widget_test.dart` passed with native sheet mock returning `false` by default | DONE |
| NAF-6 | Existing debug probe from prior work | Debug console native IME probe | The debug native IME probe remains usable | `flutter test test/core/debug_floating_button_test.dart` | DONE |
