# BudgetV2 acceptance checklist

**Reference:** `balance_latest_layout.html`, SHA-256
`ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`.
The authoritative Budget B3M-B cascade and component tree are at lines
`5447–6047` and `7607–7977`. No screenshot or prototype approximation is
evidence.

| ID | Requirement | Target code | Acceptance / verification | Status |
| --- | --- | --- | --- | --- |
| BV2-001 | Header dropdown exposes `BudgetV2` and retains Balance/Budget/Mind selection behavior. | dashboard-mode, shell mode forwarding, header menu | production widget semantics selects `BudgetV2` and mounts its dashboard. | DONE |
| BV2-002 | BudgetV2 reuses the Balance shell: action toggle, SummaryPill, SearchPill, time rail, collapse handle and LogBox keep their exact Balance geometry and callbacks. | `SpendeeBalanceDashboard` composition | production host compares shared keyed rects and actions in both modes. | DONE |
| BV2-003 | B3M-B header uses the final budget gradient; expanded/collapsed geometry equals Balance, collapsed copy is the selected scope `spent / limit`, expanded copy contains the B3M-B partition bar plus spent/remaining texts. | BudgetV2 header/spec | HTML parser and production geometry/material/semantics tests. | DONE |
| BV2-004 | Expanded B3M-B category avatars use the Balance ticking driver, B3M-B geometry and the central category colour/slot-icon resolver with the Fluvi 3D disc SVG. No opaque viewport or clipping ancestor sits behind the belt. | BudgetV2 avatar belt/SVG | production test checks tick prebuild, resolver propagation, exact SVG contract and page-background ancestry. | DONE |
| BV2-005 | One direct-on-page mother card contains selected category avatar/name, limit amount and exactly one edit control. | BudgetV2 mother card | production geometry/material/semantics test checks no extra opaque host, central resolver and edit flow. | DONE |
| BV2-006 | Mother-card left column renders the B3M-B Fluvi circle-limit SVG and weekly-rhythm SVG with exact source colours, dimensions and dynamic amount/percent labels. | BudgetV2 SVG widgets | HTML contract plus widget tree/SVG string test. | DONE |
| BV2-007 | Mother-card right column has the B3M-B paged distribution SVG/top-three legend and merchant-list page, with deterministic two-page swipe semantics and dots. | BudgetV2 summary pager/SVG | production swipe/semantics/SVG contract test. | DONE |
| BV2-008 | The new BudgetV2 production contract reads the frozen HTML before asserting every header/avatar/mother-card literal and preserves direct page background around the cards. | BudgetV2 contract tests | changed HTML declaration fixture fails; real 412x892 production host passes. | DONE |
| BV2-009 | The category-distribution Fluvi clay donut is generated from every received limited-category amount: each top and depth arc uses its real proportion, selected/highlighted category identity, and B3M-B geometry. It must render non-empty in the production mother card. | BudgetV2 pie data model/SVG/pager and Balance shell bar forwarding | deterministic SVG geometry contract plus real production host semantics. | DONE |
| BV2-010 | The limit-circle SVG uses the selected category's live spent/limit ratio for its dash offset, highlight dasharray, center percentage and amount label; it must not retain an old fixture or an aggregate value. | BudgetV2 selected-card data/SVG | deterministic dynamic-SVG contract and real production host semantics. | DONE |
| BV2-011 | Weekly rhythm receives the selected category's real trailing-seven-day amounts, normalized to that category's live limit, and emits the B3M-B SVG rule exactly (zero days have no bar; nonzero bars retain the source gradient/shadow/highlight geometry). | Balance dashboard rhythm resolver and BudgetV2 rhythm SVG | deterministic source-derived geometry contract and production host semantics. | DONE |
| BV2-012 | Budget avatar discs reproduce the B3M-B Fluvi source colour mixing, top highlight and lower depth/shadow from the central category resolver; no hard-coded white/black approximation remains. | BudgetV2 avatar SVG | deterministic exact-colour SVG contract and production host source test. | DONE |
| BV2-013 | The distribution card must paint a non-empty, proportional category SVG from the active filtered BudgetV2 period. Every slice receives its colour only from `CategoryColorResolver`; the rendered SVG must expose a parser failure instead of silently leaving the chart blank. | `spendee_budget_v2_components.dart` pie data/render boundary; active `categoryBudgetBars` source | The live card has painted slices for the same 36/11/9-style data its legend presents, and logs the slice count/total/renderer outcome. | Widget renderer regression + focused live-data test + device debug log. | PARTIAL — renderer proof is green; fresh device screenshot/log is pending. |
| BV2-014 | A 100% category limit uses a complete clockwise pink-to-purple SVG ring, not partial dash fragments. Other ratios retain their exact live-progress proportion. | `BudgetV2FluviSvg.circleProgress`; limit card renderer | Flutter receives physical circumference dash lengths rather than unsupported `pathLength` values; 100% has zero dash gap. | Deterministic SVG contract + widget renderer regression + device debug log. | PARTIAL — SVG proof is green; fresh device screenshot/log is pending. |
| BV2-015 | The expanded Budget header reports the real aggregate spent-to-limit percentage for the active SummaryPill period, including an over-budget ratio; it must not silently relabel every ratio above one as `100%`. | `BudgetV2BudgetSummary`, `SpendeeBudgetV2Header` | An input of 1,500 / 1,000 exposes `150%` in the header diagnostic/copy while the finite visual track remains bounded. | Focused summary/widget regression plus debug log. | PARTIAL — automated proof is green; fresh device evidence is pending. |
| BV2-016 | Exactly the currently selected category is enlarged/exploded in the proportional category-distribution SVG. The highest-spend category is not highlighted unless it is selected. | `_BudgetV2PiePage`, `BudgetV2FluviSvg.clayDonut` | A selected 7% category with a separate 41% leader creates precisely one highlighted SVG slice, with the selected key only. | Deterministic SVG regression plus production host widget test. | PARTIAL — automated proof is green; fresh device evidence is pending. |
| BV2-017 | BudgetV2’s ticking category-avatar rail retains every supplied active category, not a truncated five-item data list. Five remains the maximum simultaneously visible window; offscreen categories are reachable by the same ticker and retain central colour/icon resolution. | `SpendeeBalanceDashboard` BudgetV2 bar selection and avatar belt | A six-plus-category input has a six-plus item ticker/dot count and can select the final category’s mother card. | Production host ticker/selection regression plus scoped diagnostics. | PARTIAL — automated proof is green; fresh device evidence is pending. |
| BV2-018 | The mother-card limit circle must not parse runtime SVG. A Flutter-drawn component reproduces the frozen B3M-B circle exactly: the 122/96 source radii, pale full track, clockwise pink-to-purple live arc, white 24% gloss, center gradient, highlight, soft lower shadow and 58/18 source typography. | `spendee_budget_v2_components.dart` limit-circle renderer | The active selected category paints a bounded live fraction with no `SvgPicture` below `spendee-budget-v2-limit-circle`; 100% closes the entire arc and the painter keeps the exact source palette/geometry. | Red-green widget/painter regression plus focused analyze. | PARTIAL — automated proof is green; a fresh device launch is still needed to confirm the reported crash no longer occurs. |
| BV2-019 | The Android APK workflow must no longer run the unrelated Balance V3 release gate before build, artifact preparation, or release upload. | `.github/workflows/android-build.yml` | The workflow contains no `check_balance_v3_gate.sh` invocation; normal analyze, Flutter tests, Android tests and APK/release steps remain. | Workflow source regression and direct YAML inspection. | DONE |
| BV2-020 | The native limit ring keeps breathing room around its percentage, has no visual `limit állása` caption under that percentage, and derives its two-stop live arc from the selected avatar's central category colour: A is the resolver colour and B is its deterministic lilac companion. | `spendee_budget_v2_components.dart`; `spendee_budget_v2_contract_test.dart` | The live painter receives the selected bar's resolver colour, exposes a distinct deterministic companion colour, uses the fixed B3M-B A/45%-mix/B proportions, and paints only the compact centered percentage. | Focused widget/painter regression, source contract, analyzer, device screenshot. | PARTIAL — automated proof is green; a fresh APK screenshot is pending. |
| BV2-021 | The active category-distribution slice remains the sole highlighted slice but uses the deliberately reduced 178.2px outer radius (10% below the previous 198px active radius); ordinary slices remain at 164px. | `BudgetV2FluviSvg.clayDonut` | Generated SVG exposes 178.2px selected geometry and no 198px active geometry, while the selected identity is unchanged. | Deterministic SVG regression. | PARTIAL — automated proof is green; fresh APK screenshot is pending. |
| BV2-022 | Tapping the mother-card background — never either inner small card nor the edit control — toggles the readable category-distribution alternative: one direct-on-page large card with an almost-full-height chart on the left and the plotted category legend on the right. | `SpendeeBudgetV2MotherCard`; distribution model/view; BudgetV2 contract test | Root-background tap mounts the alternate key; inner-card tap does not. The alternate chart uses the same active filtered data and central colours as the compact chart, with all plotted category rows visible. | Production widget interaction/geometry/semantics regression and device screenshot. | PARTIAL — automated proof is green; fresh APK screenshot is pending. |

## Evidence ledger

Every `DONE` row receives the exact test command and result before commit.

### Evidence — BV2-001 … BV2-008

- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; the frozen-HTML literal contract, direct 412×892 widget composition, real `TransactionHomePage → SpendeeTestDashboard → SpendeeBalanceDashboard` host, and header-dropdown `BudgetV2` selection all passed.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_balance_dashboard_test.dart'`
- Result: exit `0`; 17 existing Balance geometry/collapse/rail/cache contracts passed after the shared-shell change.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_balance_production_contract_test.dart'`
- Result: exit `0`; 7 V3 production material, geometry, F1-surface and exception-free-host contracts passed.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter analyze lib/features/transactions/widgets/experimental/balance/spendee_budget_v2_components.dart lib/features/transactions/widgets/experimental/balance/spendee_balance_dashboard.dart lib/features/transactions/widgets/experimental/spendee_dashboard_mode.dart lib/features/transactions/widgets/experimental/spendee_test_dashboard.dart lib/features/shell/expt_shell.dart lib/features/shell/widgets/spendee_test_bottom_nav.dart test/spendeetest/spendee_budget_v2_contract_test.dart test/spendeetest/helpers/balance_production_host.dart'`
- Result: exit `0`; `No issues found`.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_balance_debug_trace_test.dart test/spendeetest/spendee_dashboard_interaction_test.dart test/widget_test.dart test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `163` tests passed, `1` explicitly skipped. This includes the production BudgetV2 contract and the Balance shell key/trace regressions discovered by the first online build.

### Evidence — BV2-009 … BV2-012

- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_budget_v2_contract_test.dart test/spendeetest/spendee_balance_dashboard_test.dart test/spendeetest/spendee_balance_production_contract_test.dart'`
- Result: exit `0`; `25` tests passed. The production host mounts the live BudgetV2 SVGs; the SVG contract proves proportional all-category donut arcs, selected live circle progress, real trailing-seven-day rhythm geometry and central-resolver avatar colour/depth data.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter analyze --no-pub lib/features/transactions/widgets/experimental/balance/spendee_budget_v2_components.dart lib/features/transactions/widgets/experimental/balance/spendee_balance_dashboard.dart test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `No issues found!`.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1'`
- Result: exit `0`; `1,296` tests passed and `1` pre-existing test was skipped. The full run includes the real production BudgetV2 host, the V3 production-host contract and the required 14,000-row rail traces.

### Evidence — BV2-013 … BV2-014

- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `7` tests passed. The suite now mounts the real `flutter_svg` parser on a proportional, centrally-coloured three-slice donut and fails if it produces an error widget. It also proves that the 100% ring has no `pathLength` and uses the physical `603.185789 0` full-circle dash length.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter analyze --no-pub lib/features/transactions/widgets/experimental/balance/spendee_budget_v2_components.dart test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `No issues found!`.
- Pending device evidence: a fresh BudgetV2 run must show the painted pie and full 100% ring, with the new `[BudgetV2Chart] distribution` and `[BudgetV2Chart] limit-circle` diagnostics. No build or screenshot automation was run for this focused code fix.

### Evidence — BV2-015 … BV2-017

- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `10` tests passed. The red-to-green regression proves that 1,500 / 1,000 remains 150% with a negative remaining amount, a non-leading selected pie category is the sole enlarged slice, and an unlimited sixth supplied category is retained in the avatar ticker.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_dashboard_test.dart'`
- Result: exit `0`; `13` shared Balance dashboard geometry, rail, cache and interaction regressions passed after the full-avatar-source change.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter analyze --no-pub lib/features/transactions/widgets/experimental/balance/spendee_budget_v2_components.dart lib/features/transactions/widgets/experimental/balance/spendee_balance_dashboard.dart test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `No issues found! (ran in 40.0s)`.
- Pending device evidence: a fresh BudgetV2 build must show the raw header ratio/over-budget copy when applicable, exactly one enlarged selected donut slice, and the complete avatar sequence. The new `[BudgetV2Chart] header` and `[BudgetV2Chart] avatar_belt` diagnostics provide the active period, raw/visual percent, remaining amount and supplied avatar count.

### Evidence — BV2-018 … BV2-019

- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `25` tests passed across the BudgetV2 and shared Balance-dashboard suites. The red-to-green limit-circle regression proves that the live mother-card circle has no descendant `SvgPicture`, uses `CustomPaint`, maps `63 240 / 125 000` to the source-correct rounded 51% arc, closes at 100%, draws the source-correct 1% minimum at zero, and keeps the frozen 308px viewport and 122/96px radii. The same suite proves the APK workflow has no `check_balance_v3_gate.sh` invocation while retaining normal analyze/test/build commands.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_dashboard_test.dart'`
- Result: exit `0`; `13` shared Balance dashboard regressions passed.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter analyze --no-pub lib/features/transactions/widgets/experimental/balance/spendee_budget_v2_components.dart lib/features/transactions/widgets/experimental/balance/spendee_balance_dashboard.dart test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `No issues found! (ran in 40.0s)`.
- Pending device evidence: no current `com.exptv2.app` crash trace was available in local logcat. The exact runtime SVG parser boundary in the affected limit ring is removed; a fresh launch of the new APK is still required to establish that the user-observed crash is gone.

### Evidence — BV2-020

- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_budget_v2_contract_test.dart test/spendeetest/spendee_balance_dashboard_test.dart'`
- Result: exit `0`; `25` tests passed. The red-to-green live painter regression proves the selected bar's `CategoryColorResolver` colour is gradient A, gradient B is the deterministic source-relationship companion, the stops remain `0/.45/1`, the percentage uses the compact 48px source type at baseline 172, and no center caption is painted.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter analyze --no-pub lib/features/transactions/widgets/experimental/balance/spendee_budget_v2_components.dart test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `No issues found!`.
- Pending device evidence: install the new APK and confirm that each selected avatar produces its own A-to-lilac-B arc and that the compact center percentage has visible ring clearance with no visual `limit állása` caption.

### Evidence — BV2-021 … BV2-022

- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r compact --concurrency=1 test/spendeetest/spendee_budget_v2_contract_test.dart test/spendeetest/spendee_balance_dashboard_test.dart'`
- Result: exit `0`; `27` tests passed. The red-to-green regressions prove the selected donut radius is `178.2` with no remaining `198` active geometry, and the direct mother-card gutter opens the 172px chart/legend alternative while the limit card and heading edit control cannot trigger it. The alternate chart reuses the compact filtered bars, selected slice and central resolver colours.
- Command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter analyze --no-pub lib/features/transactions/widgets/experimental/balance/spendee_budget_v2_components.dart test/spendeetest/spendee_budget_v2_contract_test.dart'`
- Result: exit `0`; `No issues found! (ran in 42.3s)`.
- Pending device evidence: install the resulting APK and confirm the large distribution alternative is readable, keeps the mother-card geometry, and has no visual clipping or opaque nested surface.
