# FAB Keyboard Sheet Refactor Checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| FKS-001 | User: refactor as suggested, not more native patching | `lib/features/shell/expt_shell.dart` | FAB AddTransaction opens the Flutter `SlideUpMenuCard` path directly. | `flutter test test/widget_test.dart --plain-name "FAB opens Flutter add transaction sheet directly"` passed and asserts no `openAddTransaction` call. | DONE |
| FKS-002 | User: use Flutter keyboard-sync package direction | `pubspec.yaml`, `lib/exptv2_app.dart`, `lib/features/transactions/widgets/slide_up_menu_card.dart` | Main app has `KeyboardProvider`; AddTransaction can use `flutter_keyboard_controller` height as its keyboard motion source. | Category picker/widget tests passed, keyboard-controller motion test passed, and `flutter analyze` passed. | DONE |
| FKS-003 | User: footer is separate and may be the problem | `lib/features/transactions/widgets/add_transaction_sheet.dart` | Save footer stays fixed inside the panel and does not apply keyboard-visible special padding. | `Flutter add transaction sheet uses scroll body and fixed footer`, `transaction editor keeps save button above the safe zone`, and closed-layout tests passed. | DONE |
| FKS-004 | User: content wait/blank native sheet is unacceptable | `android/app/src/main/kotlin/com/exptv2/app/NativeImeSheetHost.kt`, shell FAB path | Normal FAB flow does not create/prewarm native AddTransaction overlay or blank FlutterView container. | Widget test asserts no native method call; code inspection confirms native AddTransaction auto-prewarm was removed. | DONE |
| FKS-005 | User: sheet slide up/down lag | `SlideUpMenuCard`, `AddTransactionSheet` | AddTransaction uses one panel transform for entry, drag and keyboard lift; no second AddTransaction footer keyboard transform. | Keyboard event widget test passed with `source=keyboard-controller`; selected slide/native keyboard tests passed. | DONE |
| FKS-006 | Project rule: no local Termux Flutter binary | Verification commands | Flutter tests/analyze run through Ubuntu proot, not Termux Flutter directly. | Targeted tests, selected tests, full `test/widget_test.dart`, full `flutter test` (`+746: All tests passed!`), and `flutter analyze` were run through Ubuntu proot. | DONE |
