# Native sheet layout and startup jank checklist

| ID | Source instruction / evidence | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| NSL-1 | User: "last screenshot: layout hiba"; screenshot `Screenshot_20260711-001308.png` shows `BOTTOM OVERFLOWED BY 40 PIXELS` in native add transaction sheet while keyboard is open | `AddTransactionSheet` native host layout | Native hosted add transaction content does not overflow at compact phone size with keyboard insets | Failing-then-passing widget test `native hosted add transaction avoids keyboard overflow`; full `test/transactions/native_ime_sheet_app_test.dart` | DONE |
| NSL-2 | User: "együtt mozog, de laggol mindkettő"; log shows main `Store prewarm`/`HomeBuild frame ... jank=true` after native sheet open | `TransactionStore`, `ExptShell` native sheet lifecycle | Transaction store startup/prewarm notification can be deferred while native sheet is open and resumed after close | Failing-then-passing unit test `store can defer startup notify and prewarm during native sheet motion`; full `test/transactions/transaction_store_test.dart` | DONE |
| NSL-3 | Same log: native open is fast, but keyboard sessions lag later | `NativeImeSheetHost.kt` | Embedded sheet surface uses a render mode suitable for native translation of the whole view | Android code inspection; `:app:compileDebugKotlin` reached app Kotlin compiler with no `NativeImeSheetHost.kt` error, but full local Gradle is blocked by proot AAPT2 | DONE |
| NSL-4 | Existing native IME architecture requirement | `NativeImeSheetHost.kt` | Sheet motion remains Android-owned through `WindowInsetsAnimationCompat.onProgress` | Direct code inspection: `onProgress` still applies `sheet.translationY = NativeImeSheetMotion.translationYForIme(imeBottomPx)` | DONE |
| NSL-5 | Project requirement: verify before completion | Flutter tests/analyze/GitHub Actions | Targeted tests, analyzer, full widget smoke, and online Android build pass | `native_ime_sheet_app_test.dart`, `transaction_store_test.dart`, `widget_test.dart`, `flutter analyze` passed locally; local Gradle blocked by AAPT2/proot and needs GitHub Actions for Android build | PARTIAL |
