# Category / Month / Vendor Checkbox Parity Checklist

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| CMV-01 | User 2026-07-17: "a category menu checkbox designt add át a hónap ... választónak is" | `lib/features/transactions/widgets/summary_scope_picker_sheet.dart` | The month selector uses the same selected/unselected visual language as the category menu selection state instead of looking like a standalone switch-only row. | Widget test + code inspection | DONE |
| CMV-02 | User 2026-07-17: "a category menu checkbox designt add át ... vendorválasztónak is" | `lib/features/transactions/transaction_home_page.dart` `VendorFilterPanel` / vendor card widgets | Vendor selection uses the same active-card selection treatment as category menu cards, including the selected visual state and pressed/inset behavior. | Widget test + code inspection | DONE |
| CMV-03 | User 2026-07-17: "a vendorválasztó ugyanolyan legyen designban mint a kategory list" | `VendorFilterPanel`, `_VendorFilterRow`, possible shared category card helper | Vendor picker card layout, card height, radius, surface color/style, avatar size/position, typography, spacing, and selected-state styling match the category list design. | Widget test + direct code comparison with `category_card.dart` / `category_menu_panel.dart` | DONE |
| CMV-04 | Existing behavior preservation | `VendorFilterPanel` and caller slots in `transaction_home_page.dart` / `expt_shell.dart` | Vendor search, rename/reset, selected vendor toggling, totals/counts, empty state, keyboard-aware footer, and apply behavior continue to work. | Existing targeted widget tests + new regression tests | DONE |
| CMV-05 | Existing behavior preservation | `SummaryScopePickerSheet` | Year/month enabling rules remain unchanged: enabling month enables year; disabling year disables month; drag and +/- still change the displayed values; apply returns the same `SummaryScopeSelection` semantics. | Existing `summary scope picker toggles year and month with drag` test + new visual-state test | DONE |
| CMV-06 | Project verification rules | Targeted tests under `test/transactions/` | Tests first fail for missing parity, then pass after implementation; final status is reported only after targeted tests pass in Ubuntu/proot Flutter. | TDD red/green logs + `flutter test` via `proot-distro login ubuntu` | DONE |
