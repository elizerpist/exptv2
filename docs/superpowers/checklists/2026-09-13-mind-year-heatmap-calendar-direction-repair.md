# Mind Year direction-regression repair acceptance checklist

Source of truth: the 2026-09-14 physical-repair instruction, `Fluvi logs
other` Drive snapshot `1R2plbzazBUuuWoi_unktR_Lu7QNYgM9RPIH5J6ioNIM`,
`MILESTONE_COMMITS.md`, the full engineering journal, and canonical branch
`fix/mind-year-heatmap-calendar-direction-fluvi-20260913`. Earlier green
claims are historical only: this checklist is the active completion gate.

| ID | Requirement source | Intended owner/area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| DRR-01 | §§1–3, 14 | canonical Fluvi checkout / SCIP tooling | Canonical branch/head is audited; matching graph is generated before shared-owner decisions and again for final app SHA. | git preflight, provenance manifest, source verification | PARTIAL — canonical `101ac1a4…` graph generated; final graph pending. |
| DRR-02 | §15 | CI baseline | Exact `2fc02e…` failed job and test body are recorded without calling an unrelated job failure `test-flutter`. | `gh run view --log-failed`, local relevant suite | DONE — `run-dashboard-profile` missed post-quiescence engine marker; test-flutter and human APK succeeded. |
| DRR-03 | §§5–6, 10, 16A | Core prepared-base lifecycle | A cold inactive target no longer reaches `preparedBaseUnavailable`; target base is revision/scope compatible before target visible publish, without retries/delay/unbounded cache. | production-parent RED→GREEN cold income→expense and reverse tests; source lifecycle trace | PARTIAL — source/Drive boundary is repaired by early exact-index handoff plus a bounded two-direction base map. `DRR-03`, `DRR-03b`, and `DRR-03c` prove Phase-B cannot hold the index, a promoted hotset uses that same handoff, and two cold direction scopes serialize on the one native lane. A physically matched cold parent reproducer remains missing. |
| DRR-04 | §§5, 11, 17 | Mind projection | Exact Income and Expense colored-day sets are correct; no target chrome is paired with stale or blank heatmap because its compatible base is absent. | intermediate-frame tests, warm/cold profile, existing calendar tests | PARTIAL — existing mounted warm frame/day-set tests pass; physical cold-frame and profile evidence remain pending. |
| DRR-05 | §§7, 16B | LogBox render boundary | `LOGBOX|VISIBLE_ROWS_BOUND` records actual painted/visible safe identity: selected/canonical/payload direction, query digest, revision/generations, viewport/presentation identity, count, row-ID digest, direction summary, safe edge digests, preview/committed and source-match status. | focused render-surface test + log inspection | PARTIAL — production painter instrumentation and focused privacy-safe render test are green. The provenance is captured with the composition and the digest is emitted from its matching paint, not a later live controller read; a physical stale-row event is still required to locate its owner. |
| DRR-06 | §§7–8, 11, 16C | visible-frame/Core/LogBox contract | No visible mixed generation: target chrome, heatmap, canonical query, and actual rows converge under one explicit contract; leaving Year is never required to correct rows. | production-parent intermediate-frame actual-row tests, re-entry test | NOT DONE |
| DRR-07 | §§10–11 | direction regressions | Cold both directions, warm both directions, rapid income→expense→income→expense latest-wins, exact row IDs and day sets are covered without hidden target warmup or `pumpAndSettle()`. | RED evidence before production change; focused GREEN suite | PARTIAL — exact optional-scene-held candidate/hotset tests and the two-cold-scope one-lane test are green; warm mounted day/frame tests pass. The requested physical cold pair and actual-row cross-surface test are still missing. |
| DRR-08 | §12 | protected shared systems | Avatar/Time/Summary identities/physics, paging/scroll, Query filters/menu, slider hot path, geometry/footer, debug ring remain unchanged unless source evidence proves an owner change. | no-touch matrix plus focused regression matrix | NOT DONE |
| DRR-09 | §13 | `DebugConsoleDialog` | Existing All/Mind Heatmap log filter remains; MARK BUG NOW has `Mind Heatmap` → `mind_heatmap`, retaining Other, Avatar, Time, Budget and Mind-slider entries. | dedicated widget test selecting menu item and inspecting `USER_MARK` | DONE — dedicated MARK BUG NOW widget test is green; the existing separate log-filter test remains. |
| DRR-10 | §17 | performance diagnostics | Cold/warm request→all-surfaces-consistent p50/p95/max and FrameTiming are captured; no repository/index/source-row work in slider ticks; diagnostics bounded. | profile scenario/report + counters | NOT DONE |
| DRR-11 | §§19–20 | commits, journal, delivery | Each app commit has mandated evidence body followed by journal-only `[skip ci]`; pushed final SHA produces/downloads normal human APK to `/storage/emulated/0/Download/fluvi`, with size/hash/source marker. | git log, Actions, local file SHA | NOT DONE |
| DRR-12 | §21 | user Android device | Final physical direction/heatmap/LogBox validation is user-only. | user test | PENDING — USER ONLY |
