# Center Badge Continuous Carousel Checklist

Source session:
- User request on 2026-07-06: center badge backheader should lose redundant bottom delete/max buttons; limit sheet should be shorter with less gap between partition progress bar and limit pill; center badge should optionally show a second outer circular partition progress bar using the same partition math/coloring as the straight partition bar and updating immediately; center carousel should use 7 badges with decreasing size/opacity; carousel should become continuously draggable with tick/haptic transitions and no disappearing edge badges or center flash; orbit budget partition bar height should be reduced by another 10%.
- Visual reference inspected: `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260706-115943.png`.
- Resume audit on 2026-07-06: previous session left implementation changes uncommitted and the final logged full Flutter test did not reach a conclusive pass line. Treat all items as `PARTIAL` until fresh verification in this resumed session updates them.
- Fresh local verification in resumed session: targeted Flutter tests for settings/bridge/category stage/limit sheet passed 103/103; `flutter analyze` passed with `No issues found`; full `flutter test --reporter compact` passed 573/573. Local Android unit test is blocked by proot AAPT2 daemon startup failure, so native persistence was verified through GitHub Actions instead.
- GitHub Actions run `28788897737` on branch `feature/center-badge-backheader-partition` passed Flutter analyze, Flutter tests, Android unit tests, debug APK build, and release publish; CBC-04 native persistence is verified by the Android unit step.
- 2026-07-06 follow-up screenshot inspected: `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260706-162206.png`. User correction: during drag the adjacent green badge sits as an offset/lower stale preview instead of continuing the carousel rail until click snaps it; fast swipe should keep spinning briefly after release; center and direct neighbor badges need a few px more padding so progress rings do not touch; badge rail should move about 2 px up, handle about 2 px down, with more usable text/handle spacing; non-highlighted white badges on colored background need a less steep opacity falloff.
- Follow-up local verification: `flutter test test/transactions/category_budget_stage_test.dart --reporter compact` passed 55/55 after RED failures confirmed CBC-11 through CBC-15; targeted settings/category/home-limit regression run passed 104/104; `flutter analyze` passed with `No issues found`; full `flutter test --reporter compact` passed 574/574 with the pre-existing non-fatal `slide-up-menu-veil` hit-test warning.
- 2026-07-06 latest user correction: previous fix did not solve the carousel; add detailed copyable onscreen `DebugConsole` logs; side badge is still offset downward, scroll is laggy/choppy, release fling speed/step count is inconsistent, icons flicker with wrong icons, some gestures jump two items/double tick; move badge row 1 px higher; show the active period in the center-badge top-right corner (`sum`, `2026`, or `2026 július`).
- Latest local RED/GREEN verification: `flutter test test/transactions/category_budget_stage_test.dart --reporter compact` first failed on missing period label, missing item-qualified preview keys, drag-time active changes, and missing `[CenterCarousel]` logs; after implementation the same command passed 58/58.
- Latest full local verification: `git diff --check` passed; `flutter analyze` passed with `No issues found`; full `flutter test --reporter compact` passed 577/577 with the pre-existing non-fatal `slide-up-menu-veil` hit-test warning.
- Code review follow-up: reviewer found release momentum could still depend on the last pointer-move packet; fixed by deriving release velocity from total accepted drag over the gesture window and choosing direction from total drag once it passes the switch threshold. Targeted `flutter test test/transactions/category_budget_stage_test.dart --reporter compact` passed 60/60 with reverse-tail and equivalent-chunking regressions.
- Post-review full local verification: `git diff --check` passed; `flutter analyze` passed with `No issues found`; full `flutter test --reporter compact` passed 579/579.

| ID | Source Instruction / Reference | Intended Code Area | Acceptance Condition | Verification Method | Status |
| --- | --- | --- | --- | --- | --- |
| CBC-01 | "center badge esetében a backheader felületén ne legyen ott az alsó törlés és nyíl gomb" plus screenshot showing bottom right delete/max controls on the center backheader | `CategoryBudgetStage._buildExperimentalStage`, `BackheaderStyleSurface._CenterBadgeBudget` | Center Badge Budget backheader never renders `backheader-center-reset-action` or `backheader-center-max-action`; sheet/input actions remain available. | Widget test asserts the center action keys are absent while the sheet reset/max controls still render after opening the limit sheet. | DONE |
| CBC-02 | "a limitsheetben... nem kell ilyen magasnak lennie a cardnak" and screenshot sheet height | `BudgetTargetEditorSheet._panelHeightFor`, `SlideUpPanelMetrics`, sheet layout tests | Budget target editor sheet/card is shorter after removal of obsolete content, with save button still visible and no clipped controls on 390x844. | Widget layout test asserts `SlideUpPanelMetrics.budgetBaseHeight == 333`, card reaches screen bottom, and critical controls are visible. | DONE |
| CBC-03 | "túl sok a hely a partition progress bar, és a limitpill közt (check last screenshot)" | `_BudgetLimitCard` layout in `budget_target_editor_sheet.dart` | Vertical gap between `category-limit-partition-bar` bottom and `limit-amount-input` top is reduced substantially from the screenshot/current spacer layout while preserving title/avatar/bar order. | Widget rect measurement test asserts partition/input gap and save/input gap remain tight. | DONE |
| CBC-04 | "legyen még egy válaszrható lehetőség a center badge esetében... ki be kapcsolható legyen" | `AppThemeSettings`, `BackheaderStyleOptionsPanel`, native `ExpenseSettingsStore`, bridge tests | A persisted boolean setting controls whether Center Badge Budget shows the outer circular partition ring; default is off to preserve existing look. | Model round-trip, panel toggle, Flutter bridge payload tests are green; Android native store test passed in GitHub Actions run `28788897737`. | DONE |
| CBC-05 | "circle progress bar körül egy második külső progress bar... tökéletesen a partition progress bar lesz, csak nem egyenes, hanem kör alakú. de a matek, színezés ugyanaz legyen... ugyanazt olvassa" | `CategoryBudgetStage` allocation creation, `BackheaderStyleSurface`, new/extended center ring painter | When enabled, Center Badge Budget paints an outer ring from the same `LimitAllocationManager.build(...)` `LimitAllocationData.segments` used by the straight partition bar; segment fractions and colors match the allocation data. | Widget painter test checks the known allocation segment color sequence; code consumes `LimitAllocationData` directly. | DONE |
| CBC-06 | "ugyanúgy azonnal reagáljon" for the outer circular partition bar | `CategoryBudgetStage` pending amount flow and center ring props | Manual sheet input and partition bar drag update the outer circular partition ring in the same pump as the existing amount/progress text. | Widget test updates `pendingAmountsByKey` and verifies the outer ring painter changes on the next pump; home sheet amount regression remains green. | DONE |
| CBC-07 | "a center badge esetében a jelenlegi 5 db os carousal helyett 7 legyen inkább, fokozatosan kisebbedik, és csökken az opacity" | `_CenterBadgeWheel`, neighbor resolver in `CategoryBudgetStage` | Center carousel renders 7 logical slots: center, +/-1, +/-2, +/-3; farther slots are smaller and lower opacity; slot keys are stable. | Widget test asserts all seven slot keys exist and relative sizes/opacities decrease away from center. | DONE |
| CBC-08 | "a carousalt is fejleszd tovább... szélső badgek eltűnnek... középső badge villan... szakaszos tick helyett, folyamatos drag lesz" | `CategoryBudgetStage` horizontal gesture state, `_CenterBadgeWheel` rendering | During horizontal drag, all seven slots remain rendered; center item moves continuously with drag, scales/fades down as it leaves center, incoming item scales/fades up, and no separate two-badge animation replaces the wheel. | Widget gesture test at mid-drag asserts edge slot keys still exist, center transform is offset, and old `backheader-center-wheel-outgoing/incoming` keys are absent. | DONE |
| CBC-09 | "ticket megtartod, mert az lesz a haptic feedback, hogy új badge van középen... gyors swipe esetén pedig pörgeti" | Center carousel settle/tick logic in `CategoryBudgetStage` | Drag crossing each slot threshold updates the selected item once per tick and calls haptic feedback; fast swipe can advance multiple items while using continuous wheel motion before release. | Widget test asserts a fast swipe advances through six slots; code inspection shows `HapticFeedback.selectionClick()` on each slot crossing. | DONE |
| CBC-10 | "orbit budget: a partition progress bar mmagasságát csökkentsd 10%-al" | `_orbitPartitionHeight` in `CategoryBudgetStage` and orbit layout tests | Orbit Budget partition bar height is 10% lower than the current implementation while width/allocation coloring and handle behavior remain unchanged. | Existing orbit partition height test updated to the new expected height plus targeted orbit partition slider tests. | DONE |
| CBC-11 | Latest screenshot follow-up: adjacent green badge is lower/offset during drag and only snaps into place after click | `_CenterBadgeWheel` slot positioning and preview vertical alignment | Every visible slot, including previews, stays on the same carousel rail during drag; preview badges do not receive a separate downward offset that causes a jump when they become active. | Widget test compares active and preview center Y positions before and during drag, and verifies they differ only by expected size centering, not a fixed lower preview offset. | DONE |
| CBC-12 | Latest follow-up: fast swipe release should keep spinning briefly instead of stopping immediately | `CategoryBudgetStage._tickCenterCarouselBySteps`, release/fling handling | Fast swipe release advances multiple items through visible slot-by-slot animation after the finger is lifted, preserving per-step active changes and haptic ticks. | Widget test flings the center surface, pumps a partial duration, asserts intermediate movement/partial selection, then settles and asserts final multi-step selection sequence. | DONE |
| CBC-13 | Latest follow-up: add a few px padding between center badge and immediate neighbor badges so progress rings do not touch | `_CenterBadgeWheel` geometry constants and layout tests | The direct neighbor slot centers are farther from the active center than before while all seven slots still fit the 390 px layout. | Widget test measures active-to-neighbor center distance and asserts a larger minimum gap while farthest slots remain present. | DONE |
| CBC-14 | Latest follow-up: "még mindig túl kevés a tér a handle felett"; badges about 2 px up, handle about 2 px down | `_CenterBadgeBudget` rail/handle positioning and existing layout test | Badge rail is lifted about 2 px, handle is lowered about 2 px, and title remains between badge and handle with increased bottom clearance. | Widget rect test asserts badge top moved up, handle top moved down, and title bottom has a larger minimum gap from the handle. | DONE |
| CBC-15 | Latest follow-up: non-highlighted white badges on colored background are barely visible; opacity falloff should be less steep | `_CenterBadgeWheel._opacityForDistance`, colored center visual tests | Preview badges still fade by distance, but inner/outer/farthest opacity floors are higher so white-on-colored previews remain legible. | Widget test reads preview `Opacity` values and asserts monotonic fade with higher minimum opacity thresholds. | DONE |
| CBC-16 | Latest correction: "adj hozzá debug logokat, hogy az onscreen debug panelből bemásolhassam" | `CategoryBudgetStage` center-surface pointer/settle/tick handlers and `DebugConsole` | Center carousel logs pointer down/accept/move/up/cancel, release step planning, settle-back, tap-offset, and per-step animation start/end with index/key/title/dx/velocity data in `DebugConsole`. | Widget test clears `DebugConsole`, performs a drag/fling, and asserts copyable `[CenterCarousel]` entries include move/up/plan/step diagnostics. | DONE |
| CBC-17 | Latest correction: side badge remains offset, icons flicker, wrong icon flashes, double tick/jump during drag | `CategoryBudgetStage._applyCenterSurfaceDragDelta`, `_tickCenterCarouselBySteps`, `_CenterBadgeWheel` preview keys | Drag movement never mutates `_index` or emits active-change/haptic ticks before release; preview widget keys include both slot and item identity so slot reuse cannot flash a stale icon. | Widget test drags beyond one slot while held and asserts active title/callback stay unchanged; key test asserts item-qualified preview keys exist. | DONE |
| CBC-18 | Latest correction: fling should keep spinning smoothly and consistently, not slow-fast/choppy or variable step count | Center carousel release planning and animation durations | Release steps are computed once from total drag distance plus release velocity derived from the full gesture window; direction follows total drag after threshold so the last pointer packet cannot flip direction; equivalent chunking settles to the same sequence. | Widget tests verify fast short fling settles through `Travel -> Books -> Health`, reverse-tail keeps the total-drag direction, equivalent move chunking settles to the same sequence, and drag-held emits no pre-release double tick. | DONE |
| CBC-19 | Latest correction: "badge sort helyezd feljebb még 1 px" | `_CenterBadgeBudget` rail geometry and layout tests | Center badge rail is 1 px higher than the previous accepted layout while amount/title/handle spacing remains non-overlapping. | Existing layout rect test updated from `surface.top + 73` to `surface.top + 72`. | DONE |
| CBC-20 | Latest correction: top-right should show which period is visible (`sum`, `2026`, `2026 július`) | `CategoryBudgetStage` -> `BackheaderStyleSurface` -> `_CenterBadgeBudget` props | Center Badge Budget renders `periodLabel` in the top-right corner, single-line ellipsized, with colored-design contrast handling. | Widget test pumps `periodLabel: '2026 július'` and asserts `backheader-center-period-label` text/position. | DONE |
