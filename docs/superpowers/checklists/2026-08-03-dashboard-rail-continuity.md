# Dashboard rail continuity acceptance checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| DRC-01 | User device report, 2026-08-03 | `lib/shared/motion/centered_carousel/{centered_carousel.dart,centered_carousel_controller.dart}` | Shared carousel has the exact milestone `5b71141` implementation; no new semantic gate or altered recenter path remains. | `git diff --exit-code 5b71141 -- <two files>` plus focused carousel tests. | NOT DONE |
| DRC-02 | User: startup must not scroll itself | `presentation/core_dashboard.dart` | At startup with rail closed, `TimeRefinementRail` is absent from the tree and no carousel viewport can attach. Opening mounts it; close fade keeps it only while `railReveal > 0`. | Widget regression inspects tree before/after rail visibility and navigation revision remains zero at startup. | NOT DONE |
| DRC-03 | User: every fling tick updates immediately | summary/logbox application coordinators | Every `childPreviewIndex` and `childSettledIndex` with an active finite deck selects the exact same-key/revision snapshot synchronously; populated and explicit-empty children both render immediately. | Controller tests with multi-child deck and five logical crossings; assert five concrete metric/count/LogBox states and no loading state. | NOT DONE |
| DRC-04 | User: no flash, no empty, no `—` | summary/logbox application and presentation states | Between two concrete finite states no null amount/count and no `DashboardLogPreviewLoading`/`DashboardLogInitialLoading` becomes visible. A cold/failure guard retains the prior immutable snapshot without relabelling it. | Failing-then-green unit/widget regressions for target deck delay and query settle. | NOT DONE |
| DRC-05 | User: direction must be consistent | `DashboardCoreController`, bundle controller, query adapter | Direction switches stage target finite deck plus exact first page; activation and query direction commit happen coherently, with no stale-direction display or fallback tick prefetch. | Deferred recording-repository core regression; assert old-or-new complete identity per observed frame. | NOT DONE |
| DRC-06 | User: preserve fluent milestone rail | dashboard boundaries | Rail mount eligibility and finite-deck readiness remain in dashboard layers; widgets initiate no I/O and the shared motor is unchanged. | Architecture/source inspection and existing boundary regressions. | NOT DONE |
| DRC-07 | Delivery gate | focused dashboard tests, analyzer, CI | All focused tests and proot analyzer pass; all checklist rows are honestly updated before commit/push/online APK. | Ubuntu-proot commands, GitHub Actions, artifact inspection. | NOT DONE |
