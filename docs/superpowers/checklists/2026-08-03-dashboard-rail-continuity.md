# Dashboard rail continuity acceptance checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| DRC-01 | User device report, 2026-08-03 | `lib/shared/motion/centered_carousel/{centered_carousel.dart,centered_carousel_controller.dart}` | Shared carousel has the exact milestone `5b71141` implementation; no new semantic gate or altered recenter path remains. | `git diff --exit-code 5b71141 -- <two files>` plus focused carousel tests. | DONE — exact diff exited 0; full suite includes all centered-carousel controller/widget regressions. |
| DRC-02 | User: startup must not scroll itself | `presentation/core_dashboard.dart` | At startup with rail closed, `TimeRefinementRail` is absent from the tree and no carousel viewport can attach. Opening mounts it; close fade keeps it only while `railReveal > 0`. | Widget regression inspects tree before/after rail visibility and navigation revision remains zero at startup. | DONE — `core_dashboard_test` plus `time_refinement_rail_test` verify absent/inert closed state and normal opened motion. |
| DRC-03 | User: every fling tick updates immediately | summary/logbox application coordinators | Every `childPreviewIndex` and `childSettledIndex` with an active finite deck selects the exact same-key/revision snapshot synchronously; populated and explicit-empty children both render immediately. | Controller tests with multi-child deck and five logical crossings; assert five concrete metric/count/LogBox states and no loading state. | DONE — five-tick complete-deck regression asserts exact sequential LogBox keys, including explicit empty and settle. |
| DRC-04 | User: no flash, no empty, no `—` | summary/logbox application and presentation states | Between two concrete finite states no null amount/count and no `DashboardLogPreviewLoading`/`DashboardLogInitialLoading` becomes visible. A cold/failure guard retains the prior immutable snapshot without relabelling it. | Failing-then-green unit/widget regressions for target deck delay and query settle. | DONE — delayed deck and cache-miss regressions retain the prior immutable summary/LogBox state without a loading delegate. |
| DRC-05 | User: direction must be consistent | `DashboardCoreController`, bundle controller, query adapter | Direction switches stage target finite deck plus exact first page; activation and query direction commit happen coherently, with no stale-direction display or fallback tick prefetch. | Deferred recording-repository core regression; assert old-or-new complete identity per observed frame. | DONE — deferred target-direction regression verifies old-or-new complete identity, zero fallback tick read, and coherent commit. |
| DRC-06 | User: preserve fluent milestone rail | dashboard boundaries | Rail mount eligibility and finite-deck readiness remain in dashboard layers; widgets initiate no I/O and the shared motor is unchanged. | Architecture/source inspection and existing boundary regressions. | DONE — lifecycle/readiness gates are dashboard adapter/core queries only; no widget I/O or shared motor changes. |
| DRC-07 | Delivery gate | focused dashboard tests, analyzer, CI | All focused tests and proot analyzer pass; all checklist rows are honestly updated before commit/push/online APK. | Ubuntu-proot commands, GitHub Actions, artifact inspection. | DONE — local full suite: 296 passed; proot analyzer: no issues; GitHub Actions run 30819608703 passed (Flutter, Room, native bridge, APK); `fluvi_bf9874b.apk` was downloaded and SHA-256 verified. |
