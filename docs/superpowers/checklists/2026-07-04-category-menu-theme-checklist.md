# Category Menu Theme Acceptance Checklist

| ID | Source | Code Area | Acceptance Condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| R1 | User: "kategória menu a jelenlegi inline menu vs slide up sheet" | `AppThemeSettings`, `ThemeOptionsPanel`, native bridge, `TransactionHomePage` | Theme settings expose and persist `inline` and `slideUpSheet`; default remains current inline behavior. | `flutter test`; model/bridge/settings widget tests; native store code and Kotlin test added for CI. | DONE |
| R2 | User: "slide up sheet esetében ez is legyen előtérben mint az addnewcategory menu" | `CategoryMenuOverlay`, `TransactionHomePage`, `SlideUpMenuCard` | Slide-up category menu uses the shared focused sheet layer with veil and covers bottom navigation. | `flutter test`; widget test checks `category-menu-slide-card`, veil, bottom coverage. | DONE |
| R3 | User: "addnew category gomb ne a sheet tetején legyen, hanem alul egy pill" | `CategoryMenuPanel` | In slide-up mode, add category is a bottom pill button; inline mode keeps current top add control. | `flutter test`; widget test checks bottom pill placement and absence of top add button in sheet mode. | DONE |
| R4 | User: "addnewcategory felslideol, de a kategória sheet ott marad a háttérben" | `TransactionHomePage`, `CategoryEditorSheet` | Tapping add opens category editor above the category sheet while the category sheet remains mounted behind it. | `flutter test`; widget test opens add from slide-up mode and checks both sheets remain present. | DONE |
| R5 | User: "kategória sheet csak akkor slideol le..." | `TransactionHomePage`, `SlideUpMenuCard` | Category sheet closes on category selection, threshold drag dismiss, and fast downward swipe; short downward drag snaps back. | `flutter test`; widget tests for select close, short drag snap, long/fast dismiss. | DONE |
| R6 | User: "kategória kártyáknak is választható legyen, hogy legyen árnyéka vagy nem" | `AppThemeSettings`, `CategoryCard`, `CategoryMenuPanel` | Category card neutral shadow can be enabled/disabled from theme settings. | `flutter test`; model/settings/widget tests inspect `boxShadow`. | DONE |
| R7 | User: "ez legyen választható a logboxok esetében is" | `TransactionLogBox`, `RecurringGhostLogBox`, `TransactionLogList` | Transaction and ghost logbox neutral shadows can be enabled/disabled from theme settings. | `flutter test`; widget tests inspect logbox/ghost decoration. | DONE |
| R8 | User: "headercard alatti bevétel/kiadás gombok, és summary pill, searchpill esetében is" | `TransactionTypePills`, `SummaryPill`, `SearchPill`, `TransactionHomePage` | Type pill, summary pill, and search pill neutral shadows can be enabled/disabled from theme settings. | `flutter test`; widget tests inspect decorations. | DONE |
| R9 | User: "kategória ikonok... méretük ne változzon, de a strokewidth legyen vékonyabb" | `CategorySlotIcon`, `CategoryIconBadge`, category/logbox widgets | Category icons on cards and logboxes keep existing size but render with thinner stroke; slot grid icons keep current stroke. | `flutter test`; widget/unit tests verify configured stroke width and unchanged sizes. | DONE |

Verification snapshot:
- `flutter analyze`: no issues.
- `flutter test`: 534 tests passed.
- Local Android Gradle unit test execution is not used as a gate in this Termux/proot environment: `:app:testDebugUnitTest --tests ...themeSettingsPersistCategoryPresentationAndShadowControls` did not reach assertions because the local Gradle cache reported `CorruptedCacheException`, then `:app:processDebugResources` failed with AAPT2 daemon startup failures. The native persistence test is included for GitHub Actions.
