# Spendee Glass Surface Menu Expansion Acceptance Checklist

Date: 2026-07-18

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| GSM-001 | User: "header container is ilyen designt kaphat... header maga is egy ilyen üveg lesz" | `lib/features/transactions/widgets/experimental/spendee_test_dashboard.dart`, `spendee_header_glass.dart` | The design menu has a Header section with exactly three states: normal, C2 design, and liquid glass. Header normal preserves current behavior by default. | Widget test opens the menu and asserts the three header actions and checked state. | DONE |
| GSM-002 | User: "azonban ne fade out legyen, hanem az alja is legyen rendes üveg" | C2 glass wrapper shared by header/avatar/chart | C2 glass can be used without the bottom fade mask; header C2 and the new avatar C2 behavior use the no-fade version so the bottom remains full glass. | Widget tests assert no `ShaderMask` fade key for no-fade C2 surfaces and render sample remains cyan-biased. | DONE |
| GSM-003 | User: "gondolom ezt az objectet méretezed, és rárakod a színes headerre, és arra rakod rá a számokat, feliratot, contenteket" | Header surface composition | Header C2/liquid glass overlays are full-size header surfaces above the colored header paint and below all semantic content: label, amount, partition, menu, avatar/chart layers, and handle. | Widget tree/order tests plus rendered pixel sample under header content. | DONE |
| GSM-004 | User: "az avataroknak lesz 4 lehetőség: semmi, régi, új c2 design glass, és liquid glass" | Avatar stage1 surface menu and `_BudgetExtendedInfo` | Avatar menu exposes exactly four choices: no background, old glass, C2 glass, liquid glass. All choices keep carousel gestures and layout. | Widget tests select each option and assert stable keys/geometry/gesture content. | DONE |
| GSM-005 | User: "ugyanígy a diagramnak is ugyanezt a 4 lehetőséget berakod" | Stage2 chart menu and `_BudgetPiePanel` | Chart menu exposes exactly four choices: no background, old glass, C2 glass, liquid glass. Chart content and vendor page remain functional in all glass choices. | Widget tests select chart C2 and liquid choices at stage2 and assert category/vendor content remains visible. | DONE |
| GSM-006 | User-provided package: `https://pub.dev/packages/liquid_glass_renderer` | `pubspec.yaml`, glass wrapper implementation | Liquid glass option is based on `liquid_glass_renderer`. Because the package documents Web as unsupported, the web build uses a non-breaking fallback while native-supported platforms can use the package glass widget path. | Dependency resolution, widget tests for liquid keys/fallback, `flutter analyze`, Flutter web server GET returns HTTP 200. | DONE |
| GSM-007 | Existing C2 source of truth `docs/prototypes/color_lab.html:2183`-`2206` | C2 painter/wrapper | C2 values remain tied to the HTML source: radius 17, radial gloss at 14%/8%, linear white .36 -> .16, top inset .46, outer shadow 0/6/18 .08, blur 12. | Direct code inspection and widget tests that assert C2 surface keys and rendered color sample. | DONE |
| GSM-008 | Project workflow | Tests and server restart | Implementation is test-first, analyzer-clean, and the Flutter webserver is restarted after the change. | RED/GREEN targeted tests, interaction/regression tests, `flutter analyze`, restart background server and report port/PID. | DONE |
| GSM-009 | User: "blank screen, wont load"; package README says Web is unsupported | `spendee_liquid_glass_surface.dart`, web debug bootstrap | Flutter web must not import `liquid_glass_renderer` modules during initial app bootstrap; web uses a pure Flutter fallback while IO/native can import the package implementation. | RED/GREEN shell check against `main_module.bootstrap.js`, full widget tests, `flutter analyze`, restarted webserver returns HTTP 200. | DONE |
| GSM-010 | User: "fluent_ui" after asking for an acrylic plugin | `pubspec.yaml`, `spendee_acrylic_surface.dart`, `spendee_test_dashboard.dart`, `spendee_dashboard_interaction_test.dart` | The design menu exposes a Fluent UI Acrylic option for header, avatar, and chart. Selecting each option renders a stable Acrylic surface key and keeps the underlying header/avatar/chart content visible and interactive. | RED/GREEN widget tests for menu keys and selected surface keys; direct dependency check for `fluent_ui 4.13.0`. | DONE |
| GSM-011 | User: "utolsó screenshot egyáltalán nem tűnik üvegszerűnek az avatarok és a chart liquid glass esetében" | `spendee_liquid_glass_surface_stub.dart`, `spendee_liquid_glass_surface_native.dart` | Liquid glass fallback gains stronger glass material cues on avatar and chart surfaces: higher blur/tint, stronger border, inner highlight/glare, and deeper shadow while preserving child layout. | RED/GREEN widget tests assert stable liquid material layer keys under avatar/chart liquid surfaces; `flutter analyze`; restarted webserver. Local post-change screenshot is not available because this container has no Chromium/Chrome binary. | DONE |
| GSM-012 | User: "szedd ki stage 2-ből a két oldalra gombor, helyette oldalra swipepal lehessen triggerelni infinite loopban" | `_BudgetPieStage2Layer`, stage2 page state, widget tests | Stage2 renders no left/right chevron buttons. Horizontal swipe on the stage2 chart layer changes between category and vendor pages, and repeated swipes loop back. | RED/GREEN widget test asserts chevrons `findsNothing`, swipe left/right changes pages, and two same-direction swipes loop. | DONE |
| GSM-013 | User: "rakj egy újabb design beállítást is a header menube: chart lista mode... ne legyen pill, eredeti legyen, c2 css, liquid, és acrylic" | Header design menu, `_BudgetPieRow`, row surface wrappers | Header design menu exposes a `Chart lista mode` section with five row modes: no pill, original, C2 CSS, liquid, Acrylic. Selecting each glass mode wraps chart list rows in the matching stable row surface key while preserving row tap behavior. | RED/GREEN widget tests for menu keys, selected row wrapper keys, and row tap still selecting category. | DONE |
| GSM-014 | User: "szedd ki a hardcoded rózsaszín glow-t aháttérről" | Dashboard background stack | The dashboard no longer renders the hardcoded header/background glow layer behind the header. The active expense button gradient and chart entry selection glow are not changed. | RED/GREEN widget test asserts `spendee-test-header-outer-glow` is absent while the dashboard/header still render. | DONE |
| GSM-015 | User: "utolsó screenshot... rózsaszín glow a header card alja alatt... header liquid glass: lágyítsad, túl fehér, túl erős, illetve rakj a dropdownba egy slidert is, amivel lágyítani lehet" and screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260718-202348.png` | `spendee_liquid_material_layers.dart`, `spendee_liquid_glass_surface_stub.dart`, `spendee_liquid_glass_surface_native.dart`, header design menu in `spendee_test_dashboard.dart` | Header liquid glass defaults to a softer profile and the header design dropdown exposes a live softness slider. The slider scales header liquid material/glare/border/shadow/native intensity only; avatar, chart, and chart-list liquid surfaces keep their default effect unless separately changed later. | Inspect latest screenshot, RED/GREEN widget test checks slider key and header-only `softness` value change, full interaction/regression tests, `flutter analyze`, webserver restart on PID `27170` and HTTP `200`. | DONE |
