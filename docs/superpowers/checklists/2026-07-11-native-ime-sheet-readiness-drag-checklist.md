# Native IME Sheet Readiness and Drag Checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| READY-NO-BLANK | User reports blank white native sheet while content loads | `NativeImeSheetHost`, `NativeImeSheetApp`, `NativeImeSheetBridge` | The native bottom sheet container is not visually revealed until the embedded Flutter content has rendered and sent a ready signal; no empty white sheet is shown during cold start. | Dart widget tests cover transparent startup and `contentReady`; host code inspection completed; GitHub Actions run `29140196295` passed. | DONE |
| PREWARM-REUSE | User says waiting for content after FAB tap is also unacceptable | `NativeImeSheetHost`, `MainActivity` | AddTransaction native sheet engine/content is prewarmed hidden and reused after close so normal FAB open is ready without content-loading wait. | Code inspection completed; native diagnostics added for `prewarm requested`, `engine reused`, no normal-close destroy; GitHub Actions run `29140196295` built `exptv2-debug-05abb8a.apk`. | DONE |
| DRAG-NATIVE | User reports native sheet is not swipeable | `NativeImeSheetHost`, native drag model | Native host owns sheet drag from the header area, applies drag offset itself, and can request dismiss without Flutter moving the host. | Drag math covered by Kotlin unit tests; host code inspection completed; Android unit tests passed in GitHub Actions run `29140196295`. | DONE |
| MAIN-PASSIVE | User reports keyboard and sheet lag together | `NativeKeyboardInsets`, `ExptShell`, main keyboard observer/follower path | While native sheet is opening/open, the main Flutter app suspends global native-keyboard session handling and does not rebuild from old keyboard-follow logic. | Dart unit/widget tests for suspension passed; shell code inspection completed. | DONE |
| CLOSE-TRACE | User needs more debug before further tuning | `NativeImeSheetHost`, bridge logging | Native host logs every phase: open, prewarm, engine/view attach, first frame/content ready, reveal, drag, IME start/progress/end, close reason, timeout, cache/detach. | Code inspection completed; Dart bridge test confirms native `debugLog` reaches Flutter debug console; GitHub Actions run `29140196295` passed. | DONE |
| NO-STALE-HOST | User reports stale white sheet remains in background | `NativeImeSheetHost` teardown/cache path | Normal close hides and caches the host without leaving a visible stale container; dispose removes the overlay and destroys engine only at Activity teardown. | Code inspection completed; close path caches hidden host; GitHub Actions run `29140196295` passed. | DONE |
