# Stats Screenshot UI Polish Checklist

Source reference:

- Android screenshot: `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260708-150041.png`
- User instruction on 2026-07-08: fix the stats screen so the header magnet uses a line instead of a circle, the chip matches the main-menu color, the onscreen debug panel button moves left, stats monthcard backgrounds are configurable, and every stats FastInfo render mode gets a resized chart stack with legends and x/y axis definitions.

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| STAT-POLISH-001 | Screenshot: header magnet strip contains a large circle; user: "vonal helyett" | `MagnetStrip`, `TransactionHeaderCard`, `StatsPage` | Stats custom magnet marker is a thin vertical line on the strip, not a circular puck. Existing main-menu/backheader magnet behavior is preserved. | Widget/painter test for stats marker style and key; code inspection. | NOT DONE |
| STAT-POLISH-002 | User: "a chip olyan színű legyen mint a főmenüben" | `StatsPage`, `TransactionHeaderCard` | Stats header leading chip uses the main-menu chip color `#FBBF24` for all render modes. | Widget test verifies rendered chip color does not vary by render mode. | NOT DONE |
| STAT-POLISH-003 | User: "onscreen debug panel gomb kerüljön bal oldalra" | `DebugFloatingButton`, shell/widget tests | Debug floating button is positioned with `left: 16` and no right anchor. Bottom offset behavior remains unchanged. | Widget test inspects `debug-floating-button-position`. | NOT DONE |
| STAT-POLISH-004 | User: "monthcardok háttere választható legyen a beállításokban (fehér vs a 2 szürke árnyalat)" | `AppThemeSettings`, `ExpenseTheme`, `ThemeOptionsPanel`, `StatsPage`, `StatsYearCalendar` | Settings expose a stats monthcard background option with white, gray100, gray200. The annual stats month cards paint with the selected color. | Unit tests for settings parse/serialize/copy/theme resolve; widget/painter test for calendar color. | NOT DONE |
| STAT-POLISH-005 | Screenshot: top FastInfo chart starts under status bar; user: "felső grafikon teteje ... pár px lejjebb" | `StatsFastInfoGraph` | All render modes use a shared layout where the first title/chart starts lower and no title is clipped by status bar/header top. | Widget/painter layout test via exposed layout spec or keys; screenshot inspection if built. | NOT DONE |
| STAT-POLISH-006 | User: "alsó grafikon alja jó helyen van, de rossz a méretezés" | `StatsFastInfoGraph` | Bottom chart bottom remains at the accepted lower area, while chart heights are redistributed: top chart lower/less clipped, pulse chart compact, bottom chart legible. | Layout spec unit/widget test. | NOT DONE |
| STAT-POLISH-007 | User: "nincs legend sem, nincs az x és y tengely definiálva" | `StatsFastInfoGraph` | Category scope, heatmap, and closing FastInfo charts render legends and x/y axis definitions inside the graph canvas without pills/cards. | Painter/widget test verifies semantic labels/custom painter inputs; visual inspection. | NOT DONE |
| STAT-POLISH-008 | User: "mindegyik render modeban tervezd át" | `StatsFastInfoGraph` and stats tests | Category scope, heatmap, and closing all use the new shared layout/legend/axis system, with mode-specific legend text and colors. | Widget tests switch through all render modes; targeted stats tests pass. | NOT DONE |
