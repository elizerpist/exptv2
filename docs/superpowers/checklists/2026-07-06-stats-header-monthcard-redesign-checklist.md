# Stats Main Menu Acceptance Checklist

Current approved visual references:

- Local file: `.superpowers/brainstorm/11665-1783356886/content/stats-current-concept-summary-v26.html`
- Browser URL while the HTML server is running: `http://127.0.0.1:8765/stats-current-concept-summary-v26.html`
- Local file: `.superpowers/brainstorm/11665-1783356886/content/stats-heatmap-render-mode-v28.html`
- Browser URL while the HTML server is running: `http://127.0.0.1:8765/stats-heatmap-render-mode-v28.html`
- Local file: `.superpowers/brainstorm/11665-1783356886/content/stats-closing-render-mode-v29.html`
- Browser URL while the HTML server is running: `http://127.0.0.1:8765/stats-closing-render-mode-v29.html`

Every approved HTML listed here is mandatory implementation input. Before editing Flutter code for the stats main menu, re-open the relevant files and compare the implementation against layout, colors, graph structure, legends, category sheet states, and explanatory rules. HTML references are not inspirational mockups.

ID ranges:

- `STAT-SHARED-*`: shared annual stats shell, header, SummaryPill, type pills, joystick, monthcard behavior.
- `STAT-CAT-*`: accepted `Kategória scope` render mode.
- `STAT-HEAT-*`: accepted annual `Hőtérkép` render mode. Month focus/detail remains deferred.
- `STAT-CLOSE-*`: accepted annual `Hózárás` render mode. Month focus/detail remains deferred.

Scope currently covered: annual stats main menu shared behavior, the accepted `Kategória scope` render mode, the accepted annual `Hőtérkép` main-menu render mode, and the accepted annual `Hózárás` main-menu render mode. Month focus/detail is deferred until explicitly approved later.

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| STAT-SHARED-001 | User accepted `stats-current-concept-summary-v26.html`; user: "html alapján fogsz implementálni" | All stats implementation files | The implementation must use the approved HTML references as primary inputs and must not replace accepted chart/header/sheet designs with different interpretations. | Re-open relevant HTML before implementation; compare screenshots and code constants against the HTML. | NOT DONE |
| STAT-SHARED-002 | User: "egy biztos, hogy kell az éves month card nézet. 12 monthcarddal"; approved HTML annual view | `lib/features/stats/stats_page.dart`, `lib/features/stats/widgets/stats_year_calendar.dart` | Annual stats main view shows the header, type pills, SummaryPill, and exactly 12 month cards. Month focus is not part of this checklist. | Widget test renders `StatsPage`; screenshot comparison against approved HTML. | NOT DONE |
| STAT-SHARED-003 | User: "header card ... 1:1"; approved HTML header | `TransactionHeaderCard`, `StatsPage` | Stats header keeps the main-menu header-card geometry and styling: gray header card, 188 px height, bottom radius, magnet strip area, left chip, right category button, and main-menu visual language. | Direct code inspection against `transaction_header_card.dart`; screenshot comparison. | NOT DONE |
| STAT-SHARED-004 | User: "summary pill ... kiválasszuk az időszakot"; approved HTML | `StatsPage`, `SummaryPill` | SummaryPill stays visually identical to the main menu and owns the selected annual period amount. Header does not duplicate the SummaryPill amount. | Widget test and screenshot comparison. | NOT DONE |
| STAT-SHARED-005 | User: "bevétel kiadás gombokkal pedig a tranzakció típust"; approved HTML | `StatsPage`, `TransactionTypePills`, stats data builder | `Bevétel` / `Kiadás` pills remain and select the active transaction side for every stats calculation and render mode. | Unit tests for active type filtering; widget test for pill state. | NOT DONE |
| STAT-SHARED-006 | User: "joystick tap render mode selector lesz" | `CalendarValueSliderPanel`, `StatsPage` | Joystick tap opens the render-mode selector. Joystick drag/long-press remains threshold control. Render modes are not exposed as three foreground pills. | Widget test taps joystick and verifies selector; gesture test for threshold update. | NOT DONE |
| STAT-CAT-001 | Approved HTML: header chip row; user accepted ALL/custom logic | `StatsPage`, scope state model | Header chip is passive state only. It shows `ALL` when every active-type category is included, or a number when a custom subset is active. It is not the category button and not the render selector. | Widget test for all/single/multi/all-selected states. | NOT DONE |
| STAT-CAT-002 | Approved HTML: real category sheet section | `lib/features/stats/widgets/stats_category_scope_sheet.dart` | Header category button opens a real main-menu-style category sheet adapted for multi-select: top grabber, title, two-column 150 px category cards, avatar at top, name/status at bottom, active border/background, bottom `Szűrőbeállítás` pill. | Screenshot comparison against HTML sheet states; widget tests for sheet elements. | NOT DONE |
| STAT-CAT-003 | User: "alapállapotban ... ne legyen üres, hanem all"; approved HTML first sheet | Scope state model, category sheet | Opening the sheet in default state shows an explicit active `ALL / Minden kategória` card. There is no visually empty selection state. | Widget test opens sheet with all scope and verifies ALL card active. | NOT DONE |
| STAT-CAT-004 | User: "ha a user mindet egyessével jelöli ki"; approved HTML third sheet | Scope normalization logic | Scope normalization rules: `0 selected -> ALL`, `N selected where N == available active-type categories -> ALL`, `1..N-1 selected -> custom`. All-selected-by-hand never remains a custom state. | Unit tests for scope normalization; widget test for chip returning to `ALL`. | NOT DONE |
| STAT-CAT-005 | Approved HTML: custom scope sheet | Scope state model, category sheet | Single-category and multi-category custom scopes are both valid. In custom mode the `ALL` card is inactive, selected category cards show `scope aktív`, and the header chip shows the selected count. | Widget tests for 1 selected and 3 selected states. | NOT DONE |
| STAT-CAT-006 | User: scope must prevent unrelated purchases from distorting fast-food measurement | `StatsYearData` or equivalent stats data builder | `scopeAmount(day)` sums only active-type transactions in the active scope. In `ALL`, all active-type categories are included plus uncategorized active-type records. In custom scope, only selected category IDs are included. | Unit tests with fast-food + clothing same day; verify clothing is excluded from fast-food scope. | NOT DONE |
| STAT-CAT-007 | User: graphs should end at actual transaction range, not flat to year end | Stats data builder, FastInfo graph painter | Graph domain starts at the first active-type transaction in the selected year and ends at the last active-type transaction in that year. It does not draw a flat empty tail to December when the active data ends earlier. If no active-type transaction exists, fall back to the full year empty state. | Unit test for first/last transaction domain; graph painter inspection. | NOT DONE |
| STAT-CAT-008 | Approved HTML: chart title `1. Előfordulás vs értékindex` | Category-scope graph data | Top chart x-axis is the active graph domain timeline with month ticks. Y-axis is normalized 0-100, not HUF. It contains two lines: occurrence and value index. | Unit tests for normalized series; screenshot comparison. | NOT DONE |
| STAT-CAT-009 | User: "line chart előfordulás, tehát nem mutathat összeget" | Category-scope graph data | Occurrence line is red (`#EF4444`) and measures threshold-hit frequency, not money. Formula: for each day, `hit(day) = scopeAmount(day) >= threshold`; occurrence series is a rolling hit rate over the graph window, scaled 0-100. | Unit test proves high amount alone does not change occurrence unless it is a hit day count. | NOT DONE |
| STAT-CAT-010 | User: "értékindex"; approved HTML explanation | Category-scope graph data | Value index line is turquoise (`#0EA5A4`) and represents normalized financial pressure, not a direct HUF label. Formula: `hitValue(day) = hit(day) ? scopeAmount(day) : 0`; value index is the rolling sum of `hitValue` normalized to the max rolling value in the active graph domain, scaled 0-100. | Unit test where fewer but larger hit days lowers occurrence but can keep/rise value index. | NOT DONE |
| STAT-CAT-011 | User: trend background must be analog, not monthly blocks | Category-scope graph painter | Top chart background risk segments are drawn between adjacent sampled points and can change multiple times inside one month. They are not month-width blocks. | Painter unit/golden test or screenshot comparison with wavy lines and narrow colored segments. | NOT DONE |
| STAT-CAT-012 | User: "trendeket jelölni, az oldalazást nem jelölni"; approved legend | Trend classifier | Trend segment rules: green (`rgba(16,185,129,.55)`) when occurrence and value index both improve; red (`rgba(239,68,68,.55)`) when both worsen; orange (`rgba(249,115,22,.55)`) when the signals are mixed/diverging; no background when both lines are inside the flat/noise threshold. | Unit tests for slope combinations and flat/noise threshold. | NOT DONE |
| STAT-CAT-013 | User: "a legend ne legyen külön pillben"; approved HTML legend | Graph painter/widgets | Top chart legend sits inside the graph under the title, uses tiny swatches/line samples on transparent background, and is not rendered as a pill/card. It includes occurrence, value index, javul, vegyes, romlik. | Screenshot comparison and widget inspection. | NOT DONE |
| STAT-CAT-014 | User requested performance indicator; approved HTML `40/100 Kontroll` | Header data + painter | Kontroll score is a derived 0-100 value from both top-chart lines. Formula: `pressure = 0.4 * occurrence + 0.6 * valueIndex`; compare recent pressure against the prior same-length window, penalize worsening, and clamp to 0-100. Bad value-index movement must prevent a false high score even if occurrence improves. | Unit tests for both-lines-improve, occurrence-only-improves, both-worsen cases. | NOT DONE |
| STAT-CAT-015 | User: score must not overlap magnet strip | Header painter/widget | Header shows score as a separate readout like `40/100` with label `Kontroll`. The magnet strip contains no number and no trend text. | Screenshot comparison; widget tree inspection. | NOT DONE |
| STAT-CAT-016 | Approved HTML magnet design | Header painter/widget | Magnet strip is only visual: red (`#EF4444`) to orange (`#F97316`) to green (`#10B981`) gradient with a white marker. It is not a pill and does not contain text. | Screenshot comparison against HTML. | NOT DONE |
| STAT-CAT-017 | Approved HTML: `Alt: Behavior MACD` | MACD data + graph painter | Behavior MACD is calculated from the same pressure index as the score: `P = 0.4 * occurrence + 0.6 * valueIndex`, `MACD = EMA(P, 7) - EMA(P, 21)`, `signal = EMA(MACD, 5)`, `histogram = MACD - signal`. | Unit tests for EMA/MACD known input and histogram sign. | NOT DONE |
| STAT-CAT-018 | User: "macd olyan széles legyen, mint a grafikon szélessége"; approved HTML | FastInfo graph layout | MACD plot width aligns with the top chart plot area, not just the card width. It uses the same x-domain as the top chart. | Screenshot/golden comparison; layout test. | NOT DONE |
| STAT-CAT-019 | Approved HTML MACD interpretation | MACD painter | For expense mode, positive histogram means worsening pressure and is red; negative histogram means improving pressure and is green. Shrinking positive bars show weakening worsening; crossing below zero is a confirmed green turn. Income mode must mirror this only if a later approved income rule requires it. | Unit tests for sign-to-color mapping. | NOT DONE |
| STAT-CAT-020 | Approved HTML: chart title `2. Havi scope Ft + impact line` | Bottom chart data + painter | Bottom chart x-axis is monthly. Left y-axis is HUF monthly scope total. Bars are stacked monthly totals, not daily occurrence. | Unit tests for monthly aggregation; screenshot comparison. | NOT DONE |
| STAT-CAT-021 | User: "top 3 + Egyéb"; approved HTML legend | Bottom chart aggregation/painter | If more than 3 categories are visible in scope/domain, only top 3 by total HUF get individual colors and every other category is grouped into `Egyéb` gray (`#CBD5E1`). Total visible bar height remains the full monthly scope total. | Unit test with 5+ categories verifies top 3 + other aggregation. | NOT DONE |
| STAT-CAT-022 | Approved HTML bottom legend | Bottom chart legend | Bottom legend is inside the graph under the title, not a separate pill. It shows up to 3 category swatches, `egyéb`, and orange line swatch `Ft/kiugrás`. | Screenshot comparison. | NOT DONE |
| STAT-CAT-023 | User: "18.4 átlagkiugrás? rakd be a 2. grafikon szélére" | Bottom chart data + painter | Orange `Ft/kiugrás` line is `monthly scope Ft / monthly threshold-hit days`. The latest non-null line value is rendered as an orange right-edge label, e.g. `18.4k`. It is not a separate FastInfo row. | Unit test for division and null months; screenshot comparison. | NOT DONE |
| STAT-CAT-024 | User: FastInfo should have no boxes/pills, just graph area | `StatsFastInfoGraph`, `HeaderFastInfoSurface` | Stats FastInfo area is passive graph canvas only: no cards, no filter chips, no interactive category buttons, no explanatory paragraph. The accepted category-scope FastInfo stack is top chart, MACD, bottom chart. | Widget test verifies absence of pill/card controls; screenshot comparison. | NOT DONE |
| STAT-CAT-025 | User: "hosszabb szöveghez fastinfo area kevés"; approved HTML | Header/FastInfo copy | Do not add long human explanatory text into FastInfo. Header uses compact labels only; chart meanings are carried by legends and visuals. | Code inspection and screenshot comparison. | NOT DONE |
| STAT-HEAT-001 | User selected `M1, S1, S2, H1`; approved HTML: `.superpowers/brainstorm/11665-1783356886/content/stats-heatmap-render-mode-v28.html` | All heatmap render-mode implementation files | The annual heatmap render mode uses the approved `M1 + S1 + S2 + H1` stack from the HTML. Other drawn options (`M2-M6`, `S3-S6`, `H2-H5`, coloring B/C) are documented alternatives only and must not be implemented unless the user explicitly changes the selection. | Re-open v28 HTML before implementation; compare the selected card markings and screenshot against Flutter. | NOT DONE |
| STAT-HEAT-002 | Approved HTML v28; user: heatmap uses same category scope and active type | `StatsPage`, stats scope state, stats data builder | Heatmap uses the same active `Bevétel` / `Kiadás` side and same category scope model as category-scope mode. Header scope chip keeps the same `ALL`/custom count rule. SummaryPill remains the period money display. | Unit tests for type/scope filtering; widget test for chip and SummaryPill state. | NOT DONE |
| STAT-HEAT-003 | User: "a hónapkártyák napcellák színezése a jelenlegi marad az appban, ami már implementálva van"; current code: `stats_year_calendar.dart` | `StatsYearCalendar`, `StatsYearData` | Do not redesign annual month-card day-cell coloring for heatmap. Preserve the current app implementation: `heatmapIntensity = clamp(scopeAmount(day) / threshold, 0..1)`. In stats month cards, if intensity is `> 0`, draw a rounded overlay inside the day cell. For `Kiadás`, base color is `AppColors.primary` `#06B6D4`; for `Bevétel`, base color is `AppColors.income` `#22C55E`. Overlay alpha is `intensity * 0.8`; then draw `AppColors.white` `#FFFFFF` over it with alpha `(1 - intensity) * 0.4`. If intensity is `0`, draw no heat overlay and keep the existing base day-cell appearance. `forró nap` counts still use `scopeAmount >= threshold && scopeAmount > 0`. | Code inspection of `lib/features/stats/widgets/stats_year_calendar.dart` and `lib/features/stats/data/stats_year_data.dart`; regression screenshot to ensure existing day cells did not change. | DONE |
| STAT-HEAT-004 | User selected `M1`; approved HTML v28 `M1 · Koncentráció marker` | Header magnet painter/widget | Heatmap header magnet uses M1 only. It is a thin visual strip, not a pill and not interactive text. Background is a left-to-right concentration gradient: gray `#E2E8F0` -> white `#FFFFFF` -> pale heat blue `#DDF8FD` -> cyan `#67E8F9` -> primary blue `#06B6D4`, with a subtle white repeating texture. The marker is white `#FFFFFF` with a subtle dark outline/shadow. Left means scattered/low concentration, right means clustered/high concentration. No number, label, score, or verdict is placed inside the strip. | Screenshot comparison against v28 M1; widget/painter inspection for no text inside magnet. | NOT DONE |
| STAT-HEAT-005 | User selected `S1`; approved HTML v28 `S1 · Klaszter-sűrűség` | Heatmap FastInfo top/spec chart data + painter | S1 is the upper special function. X-axis is the active graph domain from first active-scope transaction to last active-scope transaction. Y-axis is rolling hot-day ratio, not HUF. Formula: `hot(d) = scopeAmount(d) >= threshold ? 1 : 0`; `density(t) = sum(hot, W) / activeDays(W)`. Window `W` is dynamic: 7 days for short active ranges, 14 for medium ranges, 21 for full-year ranges. Draw the density line in primary blue `#06B6D4`; draw the area under it in pale cyan/blue using `#67E8F9` with transparency; draw cluster background zones in transparent primary blue `rgba(6,182,212,.13)`; draw hot-day rug ticks in `#06B6D4`. | Unit tests for `density(t)` and dynamic window selection; screenshot comparison against S1 in v28. | NOT DONE |
| STAT-HEAT-006 | User selected `S2`; approved HTML v28 `S2 · Heat Pulse` | Heatmap FastInfo small indicator data + painter | S2 is the small complementary function under/near the special chart. It is a confirmation indicator, not a separate decision chart. Formula: `over(d) = hot(d) ? min(scopeAmount(d) / threshold, 3.0) : 0`; `pulse(t) = EMA_short(over) - EMA_long(over)`. EMA windows are dynamic: 3/7 for short active ranges, 5/14 for medium ranges, 7/21 for full-year ranges. Positive/up bars mean recent heat pressure is above baseline and are blue `#06B6D4`. Negative/down bars mean cooling and are gray `#CBD5E1`. Zero line is muted gray `rgba(100,116,139,.50)`. | Unit tests for EMA/pulse sign and dynamic windows; screenshot comparison against S2 in v28. | NOT DONE |
| STAT-HEAT-007 | User selected `H1`; approved HTML v28 `H1 · Havi heat load` | Heatmap FastInfo monthly chart data + painter | H1 is the lower monthly/holistic function. X-axis is months in the active year/domain. Y-axis is monthly threshold-unit load, not direct HUF. Formula: `over(d) = hot(d) ? min(scopeAmount(d) / threshold, 3.0) : 0`; `heatLoad(month) = Σ over(d)`. Each monthly bar height is frequency plus severity together. Bar stack colors represent threshold overshoot bands: high/3x cap uses primary blue `#06B6D4`, middle/2x uses cyan `#67E8F9`, low/1x uses pale blue `#DDF8FD`, empty months use gray `#E2E8F0`. Hot-day count labels above bars use gray text `#64748B`. | Unit tests for monthly `heatLoad`; screenshot comparison against H1 in v28. | NOT DONE |
| STAT-HEAT-008 | Approved HTML v28 FastInfo structure; user rejected chaotic grouping | `StatsFastInfoGraph`, heatmap chart layout | Heatmap FastInfo implements only the selected graph stack: top S1 cluster-density chart, small S2 Heat Pulse indicator, lower H1 monthly heat-load chart. It remains a passive graph canvas: no cards, no filter chips, no category controls, no long explanatory text. | Widget test verifies selected stack and absence of controls/text blocks; screenshot comparison. | NOT DONE |
| STAT-HEAT-009 | Approved HTML v28 color reference; user: "a a helyes színezés" | Heatmap widgets/painters | Heatmap design palette is app-exact white-blue for expense-side visuals: primary `#06B6D4`, pale `#DDF8FD`, cyan `#67E8F9`, strong blue `#0891B2` only where an emphasized blue is needed. Do not use orange/red/yellow for heatmap mode coloring or risk states. Orange/red remain category-scope-specific and must not leak into heatmap. Income-side month-card day cells continue to use existing `AppColors.income` `#22C55E` per STAT-HEAT-003. | Code scan for forbidden heatmap orange/red/yellow constants; screenshot comparison against v28. | NOT DONE |
| STAT-CLOSE-001 | User accepted `close pulse kell`, `havi zárás threshold pontok`, `mágnescsík zárási drift`; approved HTML: `.superpowers/brainstorm/11665-1783356886/content/stats-closing-render-mode-v29.html` | All closing render-mode implementation files | Annual `Hózárás` uses the selected v29 stack: header magnet `M1 · Zárási drift`, FastInfo `S2 · Close Pulse`, and `H1 · Havi zárás + threshold pontok`. The v29 `S1 · Zárási pálya` drawing remains a reference alternative/context only and must not be implemented as a required third FastInfo chart unless the user explicitly reselects it. | Re-open v29 HTML before implementation; compare selected stack, chart titles, colors, and monthcard threshold dots against Flutter. | NOT DONE |
| STAT-CLOSE-002 | Approved HTML v29; user: active side still controls stats; previous scope rules accepted | `StatsPage`, stats scope state, stats data builder | Closing mode uses the same active `Bevétel` / `Kiadás` side and the same category scope state as the other render modes. SummaryPill remains the annual period amount owner. Header label is `HÓZÁRÁS`; header value is compact annual feedback, not a duplicated money total. Scope chip keeps the shared `ALL`/custom count rule. | Unit tests for active type and scope filtering; widget tests for SummaryPill, label, value, and chip. | NOT DONE |
| STAT-CLOSE-003 | User: `hózárás` shows how the month closed; current code in `stats_year_calendar.dart` | `StatsYearCalendar`, `StatsYearData` | Preserve the annual monthcard closing amount behavior. `closing(month) = activeTypeTotal(month)`. Monthcards show `+formatHuf(closing)` for income and `-formatHuf(closing)` for expense. Income text color is `#059669`; expense text color is `#DC2626`. Months with active transactions get a subtle side overlay: income `#22C55E` at alpha `0.08`, expense `#EF4444` at alpha `0.08`. This amount is not scope-filtered; it is the active side's whole monthly close. | Code inspection plus widget/golden test for income and expense monthcards. | PARTIAL |
| STAT-CLOSE-004 | User: `havi zárás threshold pontok`; current code in `stats_year_calendar.dart` | `StatsYearCalendar`, `StatsYearData` | Preserve and test the closing threshold point on day cells. `scopeAmount(day)` is still scope-filtered. A threshold point is drawn only when `scopeAmount(day) >= threshold && scopeAmount(day) > 0`. The point sits at the top of the day cell (`Offset(cell.center.dx, cell.top + 1)`) with radius `2.5`. Expense point color is `#EF4444`; income point color is `#22C55E`. These dots answer "which days crossed the selected scope threshold" while the monthly closing amount answers "how the active side closed the month." | Unit tests for scoped threshold days; painter/golden inspection for point position and color. | PARTIAL |
| STAT-CLOSE-005 | User: `mágnescsík zárási drift`; approved HTML v29 `M1 · Zárási drift` | Header magnet painter/widget, closing summary data | Closing magnet uses M1 only. It is a thin strip, not a pill and not text. It shows annual closing drift across active months only, with no empty future tail. Formula: `delta(m) = closing(m) - previousActiveClosing`; `improvementDelta(m) = activeType == expense ? -delta(m) : delta(m)` so positive means better for both sides. Normalize by `maxAbsDelta = max(abs(delta(m)))`; `drift = average(improvementDelta(m) / maxAbsDelta)` over adjacent active months; marker position is `clamp(0.5 + 0.5 * drift, 0, 1)`. Left means worsening drift, center stable, right improving drift. Gradient: red `#EF4444` -> pale red `#FEE2E2` -> white `#FFFFFF` -> pale green `#DCFCE7` -> green `#22C55E`; marker is white `#FFFFFF` with subtle outline/shadow. No number or trend text may overlap the strip. | Unit tests for income/expense drift sign and active-month-only domain; screenshot comparison against v29 M1. | NOT DONE |
| STAT-CLOSE-006 | Approved HTML v29 `S2 · Close Pulse`; user: `close pulse kell` | Closing FastInfo pulse data + painter | Close Pulse is the small momentum indicator for monthly closing deterioration/improvement. It uses active months only. `pressureDelta(m) = activeType == expense ? delta(m) : -delta(m)` where positive means worsening pressure for both sides. `closePulse(m) = EMA_short(pressureDelta) - EMA_long(pressureDelta)`. Use dynamic windows by active-month count: `1/3` for fewer than 5 active months, `2/4` for 5-8 active months, `3/6` for 9-12 active months. Positive bars are red `#EF4444`; negative bars are green `#22C55E`; zero line is `rgba(100,116,139,.50)`; grid uses `#E2E8F0`/`#CBD5E1`. | Unit tests for pressure sign, dynamic windows, EMA values, and color mapping; screenshot comparison against v29 S2. | NOT DONE |
| STAT-CLOSE-007 | Approved HTML v29 `H1 · Havi zárás + hit napok`; user: `havi zárás threshold pontok` | Closing FastInfo monthly chart data + painter | H1 is the lower monthly/holistic function. Bars show `closing(month) = activeTypeTotal(month)` for each active month in the graph domain. It must not stretch to inactive future months. Threshold markers on/above each bar show `thresholdHitDays(month) = count(days where scopeAmount(day) >= threshold && scopeAmount(day) > 0)`. Expense closing bars use red `#EF4444` with light red `#FCA5A5`; income bars use green `#22C55E` with light green `#86EFAC`. The threshold point color follows active side: expense `#EF4444`, income `#22C55E`; count labels use gray `#64748B`. If a month improves versus previous active month, the chart may use the accepted green improvement accent from v29, but bar height always remains the actual monthly closing amount. | Unit tests for active-month domain, monthly close totals, threshold-hit counts, and income/expense colors; screenshot comparison against v29 H1. | NOT DONE |
| STAT-CLOSE-008 | User: graphs must not end at year end if data stops earlier | Closing data builder and FastInfo painters | Closing FastInfo graph domain starts at the first active-type transaction month and ends at the last active-type transaction month. Annual 12 monthcards remain visible, but header drift, Close Pulse, and H1 monthly chart do not draw a flat empty tail for months without active transactions. If no active-type transaction exists, show the defined empty state over the full year. | Unit tests for first/last active transaction month; screenshot comparison with mid-year data. | NOT DONE |
| STAT-CLOSE-009 | Existing annual closing header behavior; user: header should show render-specific textual feedback | `StatsYearData`, `StatsPage` | Header value for closing remains annual feedback: `N romló hónap idén`. Worsening count compares adjacent active months: expense is worse when `closing(current) > closing(previous)`; income is worse when `closing(current) < closing(previous)`. It must not say "this month caused it" in annual view; month-specific explanation belongs to future focus mode. | Unit tests for worsening count in income and expense modes; widget test for annual header copy. | NOT DONE |
| STAT-CLOSE-010 | User: FastInfo is graph-only and text would overcrowd it | `StatsFastInfoGraph`, closing chart layout | Closing FastInfo remains passive graph canvas only: no cards, no filter chips, no category controls, no long explanatory text. It shows the selected Close Pulse and H1 monthly closing/threshold-dot charts using the v29 visual language. | Widget test verifies absence of controls/text blocks; screenshot comparison against v29. | NOT DONE |
