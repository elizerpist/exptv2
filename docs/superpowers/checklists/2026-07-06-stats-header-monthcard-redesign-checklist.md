# Stats Header Monthcard Redesign Checklist

| ID | Source instruction / reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| STAT-DES-001 | User: "exact designt ... header card ... 1:1 arányú tökéletes másolat" + `lib/features/transactions/widgets/header_card/transaction_header_card.dart` | Brainstorm HTML mockup, later `StatsPage` header implementation | Header mockup uses the real header card structure: 188 height, bottom radius 24, title at top 35/left 30, magnet at top 41/height 157.5, wallet chip at top 62/left 30, balance label at top 112, balance value at top 134, menu button top 112/right 25, 48x48 radius 24. | Direct code inspection against source files and visual mockup review | DONE |
| STAT-DES-002 | User: "kell az éves month card nézet. 12 monthcarddal" + `calendar_canvas_layout.dart` | Brainstorm HTML mockup, later stats calendar body | Annual view shows exactly 12 month cards in the current layout model: 2 columns, 6 rows, card width 48% of content width, gap 4%, card height 200, row gap 15. | Direct code inspection and mockup visual review | DONE |
| STAT-DES-003 | User: "exact designt ... monthcard" + `calendar_canvas_painter.dart` | Brainstorm HTML mockup, later month card implementation | Month card visual matches current painter tokens: gray50 fill, gray200 1px border, radius 15, subtle black shadow, centered month title at top+14, optional summary at top+30, weekday row at top+44, day grid at top+54. | Direct code inspection and mockup visual review | DONE |
| STAT-DES-004 | User: "ha a header cardon lennének az évi statisztikák" | Brainstorm concept and later stats header content | Yearly stats are placed inside the existing header-card geometry without changing its surface language; any new stat text replaces/adapts existing header content positions instead of inventing unrelated cards. | Mockup review against header source geometry | PARTIAL |
| STAT-DES-005 | User: "a bevétel kiadás gombokkal pedig a tranzakció típust" | Brainstorm concept and later stats state model | Income/expense controls remain visually identical to the main menu `TransactionTypePills` and select transaction type only. | Concept review with user | DONE |
| STAT-DES-006 | User: "nem összevissza rakott random elemeket" | Brainstorm HTML mockup | Mockup avoids arbitrary new controls, random card grids, or non-source styling; if a control is not present in current UI, it is explicitly marked as a proposed content replacement within the existing component geometry. | Mockup self-review before sharing | DONE |
| STAT-DES-007 | User: "a summarypillel kiválasszuk az időszakot ... a summary pill mutatja az összeget" + `lib/features/transactions/widgets/summary_pill.dart` | Brainstorm HTML mockup, later stats time-scope state | SummaryPill remains in its exact current visual form and is used as the stats time-scope navigator and amount display: month/year/all scope, previous/next period swipe, reset gesture, right-side amount value. It must not replace transaction type selection. | Direct code inspection and concept review with user | DONE |
| STAT-DES-008 | User: "a 3 nézetre találj ki valamit, ne 3 pill legyen, ne legyen ennyire előtérbe helyezve" + later: "joystick tap render mode selector lesz" | Brainstorm HTML mockup, later stats view-mode control | The three visualization modes are secondary render modes, not a foreground pill row and not the header category button. Joystick tap opens the render-mode selector; joystick long-press/drag still adjusts threshold. | Mockup visual review with user | DONE |
| STAT-DES-009 | User: "a summary pill mutatja az egész évest, a sum mode pedig minden hónapcardra a hó végi balanceot mutatja. azt írja ki, hogy zártuk a hónapot" | Brainstorm HTML mockup, later stats render mode semantics | The former summary render mode is not removed and not treated as the same thing as SummaryPill. It is defined as `Hózárás`: each month card displays that month’s closing balance / how the month closed, while SummaryPill displays the selected period total. | Concept review with user | DONE |
| STAT-DES-010 | User: "ez lesz a logika..." | Brainstorm concept, later stats filtering/render state | Transaction type is the primary data side for every stats render mode. If income is active: SummaryPill shows selected-period total income; dominant category uses income categories/sources with joystick threshold; heatmap uses income intensity; closing mode colors income days and shows monthly closing amount. If expense is active, all same render modes specify the expense side. | Concept review with user | DONE |
| STAT-DES-011 | User: "ha a user egy hónapra tappel,, akkor az adott hónap fókuszba kerül, megnyílik a réstletes statisztika..." | Brainstorm HTML mockup, later focused month stats view | Tapping a month card opens a focused month detail state. Header card and SummaryPill remain visually consistent; SummaryPill shows the selected month + active transaction type + amount. Full Bevétel/Kiadás pill row is not shown in month focus; transaction type remains available in the header menu to preserve function without taking primary vertical space. All existing monthly stat modules remain available, grouped rather than removed. | Mockup review with user | PARTIAL |
| STAT-DES-012 | User: "a kategória gomb ugyanaz a sheet lesz, mint a főmenuben..." + later: "joystick tap render mode selector lesz" | Brainstorm HTML mockup, later stats threshold/scope/render interaction | Header right category button opens the main-menu-style category sheet adapted for multi-select scope and an apply/filter bottom pill. Joystick long-press/drag adjusts threshold. Joystick tap opens the render-mode selector. Pull-down/offscreen header/FastInfo area is passive only unless later changed to a pinned interactive sheet. | Mockup review with user | DONE |
| STAT-DES-013 | User: "a korábbi sum mode a tresholdot a napcell@k feletti pontokkal színezze, mint ielenleg" | Brainstorm HTML mockup, later `Hózárás` month-card painter behavior | In `Hózárás` mode, threshold hits are shown with colored dots above the affected day cells, preserving the current visual language instead of replacing it with filled day cells or a new marker style. | Mockup review and later painter inspection | PARTIAL |
| STAT-DES-014 | User: "stat menuben ha a user bármelyik render modeot állítja az szöveges feesback legyen a header cardon" | Brainstorm HTML mockup, later stats header insight resolver | Each annual render mode defines a header-card text feedback state. The header does not duplicate SummaryPill totals; it explains the active render mode, threshold, scope, and yearly-level conclusion. | Mockup review with user | PARTIAL |
| STAT-DES-015 | User: "ha lehúzza, azaz a fastinfot triggereli, egy alternatív fastinfo legyen, ahol nincsenek boxok és pillek, egy üres terület, amire grafikonokat teszel" | Brainstorm HTML mockup, later stats FastInfo variant | Stats FastInfo is a passive graph canvas variant: no cards, no pills, no filter buttons, no setting controls. It shows graphs appropriate to the active render mode inside a separate clearly marked FastInfo container. | Mockup review with user | PARTIAL |
