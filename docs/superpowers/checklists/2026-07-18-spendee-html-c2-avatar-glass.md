# Spendee HTML C2 Avatar Glass Acceptance Checklist

Date: 2026-07-18

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| C2G-001 | User: "colour lab html c2 screenje... a html a source of truth... 1:1 azt" and later user update: "ne fade out legyen, hanem az alja is legyen rendes üveg. az avatar containernek is." | `lib/features/transactions/widgets/experimental/spendee_test_dashboard.dart` stage1 avatar surface | App has a third selectable avatar surface matching the HTML C2 `common-focus-budget-stack`: radius `17`, radial gloss at `14% 8%` alpha `.62`, linear white `.36 -> .16`, inset top highlight `.46`, outer shadow `0 6 18 rgba(15,23,42,.08)`, backdrop blur `12`, overflow clipping, and no bottom fade mask for the current selectable C2 avatar surface. | Widget test inspects selected surface key, `ClipRRect`, `BackdropFilter`, `CustomPaint`, absence of the C2 `ShaderMask`, geometry, and a rendered cyan/non-white pixel sample. | DONE |
| C2G-002 | User: "választani lehessen. nincs háttér, régi és új glass container" | Header design menu state/actions | Avatar menu exposes exactly the three avatar surface choices: no background, old glass, and HTML C2 glass. Existing chart glass/background choices remain separate and unchanged. | Widget test opens menu and selects `spendee-test-avatar-surface-html-c2-glass`; existing chart test remains green. | DONE |
| C2G-003 | Same HTML reference `docs/prototypes/color_lab.html:2208`-`2217`, `13817`-`13825` | `_BudgetExtendedInfo` avatar area | The HTML C2 glass option preserves the C2 avatar-only content area: avatar gesture/carousel stays inside the selected glass surface with `left/right 10`, full height, and the existing five-slot avatar belt behavior remains intact. | Widget geometry assertions plus existing avatar belt interaction test. | DONE |
| C2G-004 | User correction: previous implementation was not the requested design | Regression protection | The legacy simplified glass remains available under its own option and is not silently treated as the HTML C2 source-of-truth surface. | Widget test asserts old glass and HTML C2 glass have different keys and menu entries. | DONE |
| C2G-005 | Project workflow | Test and analyzer suite | New behavior is implemented test-first and does not regress Spendee interaction tests or analyzer. | Red/green targeted test; `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart`; targeted regression tests; `flutter analyze` through Ubuntu proot. | DONE |
| C2G-006 | Latest screenshot comparison: expected `Screenshot_20260718-160914.png`, received `Screenshot_20260718-160852.png` | `_SpendeeBudgetHeaderCard`, `_BudgetExtendedInfo` | Selecting HTML C2 glass uses the C2 source-of-truth layout and suppresses the later app-only stage1 additions: no `Elköltve/Maradt` partition summary and no selected avatar name inside the avatar glass panel. The old glass option keeps those app additions. | RED/GREEN widget assertion in `header menu selects exact HTML C2 avatar glass container`; full interaction regression test. | DONE |
| C2G-007 | User: "semmi köze az implementációdnak a html hez"; CSS source-of-truth `radial-gradient(...), linear-gradient(...)` paint order | `_HtmlC2BudgetStage1Glass`, `_HtmlC2BudgetStage1SurfacePainter`, `_HtmlC2BudgetStage1ShadowPainter` | HTML C2 no longer uses separate generic Flutter `DecoratedBox` gradient layers; one painter draws CSS order exactly: linear base first, radial highlight above it, inset top line, with shadow outside the clipped surface. | RED/GREEN widget test asserts old `html-c2-linear/radial` widget keys are absent, painter key exists, and sampled rendered pixel remains cyan-biased instead of white-card biased. | DONE |
