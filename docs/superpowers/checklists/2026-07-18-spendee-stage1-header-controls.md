# Spendee Stage 1 Header Controls Acceptance Checklist

Date: 2026-07-18

| ID | Source instruction / reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| CAR-001 | User: stage1 category avatar carousel should behave like a belt, not shift the whole row left/right | `lib/features/transactions/widgets/experimental/spendee_test_dashboard.dart` stage1 avatar layer | During horizontal drag, the carousel widget itself has no whole-row horizontal transform; avatar positions are computed per slot inside the stage1 area. | `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart`; analyzer. | DONE |
| CAR-002 | User initial wording refined by follow-up: the belt center anchor stays stable while avatars move through it | Same stage1 avatar layer | Partial drag keeps the carousel's center anchor stable with no whole-row transform; the outgoing selected avatar can move outward and shrink. | `stage 1 avatar belt moves avatars through a fixed center anchor` widget test. | DONE |
| CAR-003 | User: outgoing center avatar shrinks progressively to side size | Same stage1 avatar layer | A partial swipe renders the outgoing selected avatar smaller than center size and larger than near-side size until it ticks to the next category. | `stage 1 avatar belt moves avatars through a fixed center anchor` widget test. | DONE |
| CAR-004 | User: incoming avatar from the side grows as it moves toward center | Same stage1 avatar layer | A partial swipe renders the incoming side avatar larger than near-side size and smaller than center size until it becomes selected. | `stage 1 avatar belt moves avatars through a fixed center anchor` widget test. | DONE |
| CAR-005 | Screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260718-101949.png` | Same stage1 avatar layer | The five-slot avatar belt remains contained in the stage1 header area and does not leave a clipped avatar at the right edge during ordinary partial swipes. | Widget geometry assertion on 412x892 viewport. | DONE |
| DES-001 | User: stage0 top-right glass hamburger button should become the header-specific settings button | `_SpendeeBudgetHeaderCard` menu button wiring | Tapping the header glass hamburger opens a header design dropdown instead of doing only haptic feedback. | `header menu opens design dropdown for avatar and chart surfaces` widget test. | DONE |
| DES-002 | User: implement first avatar design option: avatars on glass container or background | `_BudgetExtendedInfo` stage1 surface | Dropdown exposes an avatar surface choice and selecting background removes the stage1 inner glass container while preserving the carousel gesture area. | `header menu opens design dropdown for avatar and chart surfaces` widget test. | DONE |
| DES-003 | User: implement separate chart option: chart on glass container or background | `_BudgetPiePanel` / stage2 chart layer | Dropdown exposes a separate chart surface choice and selecting background removes the stage2 chart glass container while keeping chart content visible. | `header menu opens design dropdown for avatar and chart surfaces` widget test. | DONE |
| DES-004 | Existing app behavior | Existing controller and stage snapping | Stage0, stage1, stage2 snapping, carousel tick selection, stage2 pie category selection, and existing default glass appearance remain intact. | `flutter test test/spendeetest/spendee_center_carousel_inertia_test.dart test/spendeetest/spendee_dashboard_foundation_test.dart test/spendeetest/spendee_dashboard_interaction_test.dart`; `flutter analyze`. | DONE |
| CAR-006 | User follow-up: copy the working `centerBadgeBudget` colored backheader belt behavior | `lib/features/transactions/widgets/experimental/spendee_test_dashboard.dart` stage1 avatar belt | Partial swipe advances every avatar by fractional logical slot offset: outgoing center moves outward and shrinks; incoming side moves toward center and grows before tick; the two avatars do not overlap. | `stage 1 avatar belt moves avatars through a fixed center anchor` widget test; 29 targeted tests; analyzer. | DONE |
| TXT-001 | User follow-up: stage1-stage2 avatar should show its name below | Stage1 avatar belt and stage2 header content | The selected category name is visible below the avatar belt in stage1 and remains visible in stage2. Stage0 does not show that avatar-name label. | `stage 1 and 2 show avatar name and partition summary below progress` widget test. | DONE |
| TXT-002 | User follow-up: label under partition progress bar: spent x, remaining y percent | Header core partition area | Stage1 and stage2 show a compact label under the partition bar with spent amount and remaining percent. Stage0 does not show that label. | `stage 1 and 2 show avatar name and partition summary below progress` widget test. | DONE |
| TXT-003 | User follow-up: "az elköltve legyen a százalék, a maradt pedig összegszerű" and "legyenek egymástól kicsit távolabb" | Header core partition summary | Stage1 and stage2 summary shows spent as percent and remaining as Ft amount, rendered as separated text runs with visible horizontal spacing so the numbers do not merge. | Widget text and geometry assertions in `stage 1 and 2 show avatar name and partition summary below progress`. | DONE |
| CAR-007 | User follow-up: "az avatarokat is helyezd pár px feljebb, de a név maradjon a jelenlegi pozícióban" | Stage1 avatar belt layout | The avatar belt is raised a few pixels above the carousel vertical center, while the selected category name remains anchored to the same bottom offset. | Widget geometry assertion in `stage 1 raises avatar belt without moving the selected name`. | DONE |
| TXT-004 | User follow-up: "elköltve maradt szöveg és a partition progress bar közt kicsit nagyobb legyen a padding" | Header core partition summary positioning | Stage1/stage2 summary label starts at least 5 px below the partition progress bar, increasing the previous tight spacing without changing stage0. | Widget geometry assertion in `stage 1 and 2 show avatar name and partition summary below progress`. | DONE |
| TXT-005 | User follow-up: "partition progress bar legyen szélesebb... jobb oldalánál annyi legyen a padding, mint most a bal oldalánál" and "maradt összeget pedig jobbra igazítsd" | Header core partition bar and summary row | Partition progress bar uses matching 20 px left/right header padding, and the remaining amount is right-aligned to the summary row's right edge. | Widget geometry assertions in `stage 1 and 2 show avatar name and partition summary below progress`. | DONE |
| ST2-001 | User: "stage2-nek csinálsz egy másik oldalt is... ugyanaz lesz, mint a jelenlegi category chart plusz lista... csak az adat" | Stage2 budget chart/list layer | Stage2 has a second vendor page that reuses the current chart/list panel design while changing the data source to vendors. | `stage 2 chevrons switch to live vendor share for selected category` widget test. | DONE |
| ST2-002 | User: "élőben bekötöd, nem mockup... az adott időszakban, az adott kategóriában" | Vendor aggregation from `TransactionStore.windowedTransactions` | Vendor page lists vendors from the active summary period and selected category, using live transaction data and `displayMerchant`. | Widget test with multiple same-category vendors in the repository fixture. | DONE |
| ST2-003 | User: "a kategórián belüli egymáshoz viszonyított arányt mutassa. chart plusz lista" | Vendor chart/list data model | Vendor chart and rows calculate percentages relative to total spend in the selected category. | Widget assertions for vendor percent and amount labels. | DONE |
| ST2-004 | User: "ugyanolyan üvegcontainerben legyen benne, és a hamburger gomb beállítás, chart háttér/üvegcontainer ezt is vezérelje" | Stage2 panel wrapper and header design menu | Existing chart glass/background setting controls both the category page and vendor page. | Header menu widget test selecting chart background then switching to vendor page. | DONE |
| ST2-005 | User: "stage2 contentet pedig oldalon nyilak váltsák (chevronok)" | Stage2 content controls | Stage2 exposes side chevron buttons that switch between category and vendor pages without leaving stage2. | Widget tap assertions on stage2 chevron keys. | DONE |
