# Center Badge Belt Carousel Fix Checklist

Source:
- User correction on 2026-07-06 after `c404457`: current carousel is worse because the dragged element still drops/tilts out of the rail, the belt does not move freely while dragging, and release incorrectly fires delayed multi-step jumps.
- Debug log evidence: `visual=-64.0` stays clamped while `totalDx` continues past `-250`, then release logs `plan steps=4`; this proves the visible belt stops after one slot and the extra movement is applied only after release.
- User requirement: the carousel must behave like a draggable belt/wheel. If the user drags left/right, icons keep moving left/right while the finger moves. If the user flicks, the belt continues for a few slots and gradually slows. The tick/haptic happens when an icon reaches the center coordinate.
- User requirement: 9 badges visible at once. Do not change the 3 center badges' position/size behavior; add the extra badges into the outer belt and adjust outer sizing/opacity.
- User requirement: opacity falloff must be more gradual; far badges must not become suddenly barely visible.
- Workflow requirement: keep this checklist updated, work in small steps, no partial commit, no GitHub build until all bugfix + 9-badge work is complete and verified.
- RED verification: `flutter test test/transactions/category_budget_stage_test.dart --reporter compact --plain-name centerBadgeBudget` failed before production changes on missing `backheader-center-preview-next-4`, active badge Y drift during drag, and no drag-held active ticks.
- GREEN targeted verification: the same centerBadgeBudget run passed 17/17 after the belt model, 9-slot geometry, active badge rail fix, inertia release, and log throttling changes.
- Local regression verification: full `flutter test test/transactions/category_budget_stage_test.dart --reporter compact` passed 60/60; `git diff --check` passed; `flutter analyze` passed with `No issues found`; full `flutter test --reporter compact` passed 579/579.

| ID | Source Instruction | Intended Code Area | Acceptance Condition | Verification Method | Status |
| --- | --- | --- | --- | --- | --- |
| CBB-01 | "apró lépésekre bontsd... checklistet kell vezetned" | This checklist + work plan | Every implementation step maps to checklist IDs and statuses are updated honestly before final commit/build. | Direct checklist inspection before commit. | DONE |
| CBB-02 | Log shows `visual=-64.0` while `totalDx` keeps growing; user says belt should move while dragging | `CategoryBudgetStage` center drag state and `_applyCenterSurfaceDragDelta` | Center drag offset is not clamped to a single slot; as drag crosses each slot distance, active index changes immediately and residual offset continues from the crossing point. | Widget test drags past multiple slots while held and observes active title/callback changing before release. | DONE |
| CBB-03 | "ha user balra swipeol, akkor addig mennek balra az ikonok amíg swipeol" | `_CenterBadgeWheel` slot layout and stage index updates | During a long held drag, the visible row keeps moving slot by slot; it does not freeze at +/-64 px and then jump only on release. | Widget test asserts a long held drag selects multiple items before pointer up and `DebugConsole` logs tick entries before `up`. | DONE |
| CBB-04 | "ha gyorsan pöccint... párat, egyre lassulva, mintha egy kereket löknénk meg" | Center release/fling handling | Release uses residual offset plus recent release velocity for decelerating inertia; it does not compute delayed steps from total drag distance after a clamped visual. | Widget test flicks once, observes at least one post-release tick with decreasing step cadence/residual movement, then settles. | DONE |
| CBB-05 | "ne kerüljön le a középről kimenő ikon" | `_CenterBadgeWheel._buildSlot` vertical geometry | All badge centers stay on the same rail while moving in/out; vertical top changes only by size centering, not by item identity or stale preview state. | Widget test compares center Y for active and adjacent slots before and during drag/tick. | DONE |
| CBB-06 | "9 badge legyen egyszerre látható" | `BackheaderStyleSurface`, `_CenterBadgeBudget`, `_CenterBadgeWheel` | The center badge wheel renders 9 logical slots: offsets -4, -3, -2, -1, 0, +1, +2, +3, +4 when enough items exist. | Widget test asserts all 8 preview keys plus center exist. | DONE |
| CBB-07 | "a 3 középsőhöz nem nyúlsz" | `_CenterBadgeWheel` geometry constants | Offset -1, 0, +1 keep their existing center spacing and size behavior; new badges are added into the outer belt positions. | Widget test compares center-to-neighbor distance and inner preview sizes against previous accepted values. | DONE |
| CBB-08 | "a többi közé csúsztatod... méretezést" | `_CenterBadgeWheel` outer slot positions/sizes | New outer slots fit inside the rail without overlapping center/inner badges; outer sizes decrease smoothly from inner to farthest. | Widget test asserts monotonic sizes and no incoherent overlap across 390 px width. | DONE |
| CBB-09 | "fokozatosabb opacity effect" | `_CenterBadgeWheel._opacityForDistance` | Opacity decreases gradually across distances 0..4, with farthest previews still legible on colored backgrounds. | Widget test asserts monotonic opacity with higher farthest floor. | DONE |
| CBB-10 | "ne legyen részkommit... csak ha befejezted" | Git/build workflow | No commit/push/build occurs until CBB-01..CBB-09 are DONE and local verification passes. | `git status`, checklist review, and final local verification notes before commit/build. | DONE |
