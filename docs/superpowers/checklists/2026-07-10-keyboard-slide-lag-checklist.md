# Keyboard Slide Lag Checklist

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| KBD-LAG-001 | User log 2026-07-10 19:27: `native raw=9.5 fallback=251.8` | `lib/core/keyboard/native_keyboard_insets.dart` | A lagging fresh native progress sample cannot directly win the visual keyboard inset when fallback geometry is already far ahead. | `flutter test test/core/keyboard/native_keyboard_insets_test.dart`; `motion coordinator rejects fresh native sample that lags fallback`. | DONE |
| KBD-LAG-002 | Perplexity recommendation approved by user | `lib/core/keyboard/native_keyboard_insets.dart`; `android/app/src/main/kotlin/com/exptv2/app/NativeKeyboardInsetChannel.kt` | Native keyboard events expose an animation session model with start inset, end inset, duration, phase, and timestamp metadata. | `flutter test test/core/keyboard/native_keyboard_insets_test.dart`; `native IME session event is parsed with geometry and timing`. | DONE |
| KBD-LAG-003 | Perplexity recommendation approved by user | `lib/core/keyboard/keyboard_inset_follower.dart` | `KeyboardInsetFollower` drives active IME motion from a local controller/ticker value, not per-frame EventChannel samples. | `flutter test test/core/keyboard/native_keyboard_insets_test.dart`; `keyboard follower interpolates active IME session locally`. | DONE |
| KBD-LAG-004 | User: "masszív keyboard slide lag" | `lib/features/transactions/widgets/slide_up_menu_card.dart` | Keyboard ticks update only the transform path and keep the sheet child stable. | `flutter test test/transactions/slide_up_menu_card_test.dart`; `slide card follows local IME session without rebuilding content`. | DONE |
| KBD-LAG-005 | User log shows repeated keyboard trace lines during transitions | `lib/core/keyboard/*`, `slide_up_menu_card.dart` | Per-frame keyboard logs are not emitted by default; summary logs remain available for diagnosis. | `slide card follows local IME session without rebuilding content` asserts one local session lift log; direct code inspection of same-session log dedupe. | DONE |
| KBD-LAG-006 | User log 2026-07-10 20:27: `local-ime-session` appears once, then `flutter-viewInsets` jumps to 238/332dp | `lib/core/keyboard/keyboard_inset_follower.dart` | A delayed native IME session consumes the full remaining native duration locally; it must not complete early and hand visual motion back to stepped `viewInsets`. | `flutter test test/core/keyboard/native_keyboard_insets_test.dart`; delayed-session controller timing regression test; `flutter test test/transactions/slide_up_menu_card_test.dart` delayed transform test. | DONE |
| KBD-LAG-007 | Project Flutter workflow | GitHub Actions | Analyze, Flutter tests, Android unit tests, debug APK build, release publish pass on the final single bugfix commit. | Local proot analyze and targeted tests passed; GitHub Actions build validates the final pushed commit and publishes the debug APK. | DONE |
