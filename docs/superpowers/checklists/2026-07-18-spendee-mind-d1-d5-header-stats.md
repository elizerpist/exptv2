# Spendee Mind D1-D5 Header Stats Acceptance Checklist

Date: 2026-07-18

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| MDH-001 | User: "még mindig ott a header alatt a rózsaszín glow szedd ki végre" and prior Spendee screenshots | `spendee_header_visual_spec.dart`, `spendee_header_glass.dart`, `spendee_test_dashboard.dart`, interaction tests | No header card mode renders pink/purple glow below the header. Header shadows, if any remain, are neutral and cannot tint the area below the card pink. | RED/GREEN widget test inspects header glass/card shadow colors; full `spendee_dashboard_interaction_test.dart` passed 25/25; `flutter analyze` passed. The latest screenshot's separate type-pill glow source is fixed by MDH-010. | DONE |
| MDH-002 | User: "új elemet implementálj. d1, d2 d3g, d4 d5 a colour labon" and `docs/prototypes/color_lab.html:417-542`, `:1787-1821`, `:2806-2925`, `:3262-3385`, `:14135-14255`, `:14315-14483` | `spendee_test_dashboard.dart`, `spendee_mind_stats_adapter.dart` | Header background gains a new D1-D5 selectable/loopable element sourced from the Color Lab D designs, not approximate recreated visuals. | Widget tests cover D1-D5 keys and loop order; code maps Color Lab D1/D2/D3G/D4/D5 structure. Pixel/screenshot 1:1 verification is still not done. | PARTIAL |
| MDH-003 | Earlier user instruction: "a header backgroundon az oldalra swipe vált infinite loopban"; superseded by latest user instruction in MDH-012 | Header gesture/state layer in `spendee_test_dashboard.dart` | This earlier swipe requirement is no longer active. The current accepted behavior is tap-based D mode selection through the header glass menu. | Superseded by MDH-012; widget test now asserts no `spendee-test-header-background-swipe` target is rendered in Mind mode. | DONE |
| MDH-004 | User: "ugyanúgy vannak stage 2-ben és stage 3-ban containerek, az összes kapjon customizálási lehetőséget, mint a budget esetében is, nincs container mindenféle glass beállítás" | Header design menu, D stage containers, shared glass surface wrappers | Every D stage 2/stage 3 container exposes the same surface choices as Budget: no container/background, old glass/original, C2 CSS glass, liquid glass, Acrylic. | Widget tests select liquid for Budget and Mind stage containers; menu includes no/background, old glass, C2, liquid, Acrylic for Mind stage1/stage2. Inner heatmap/card subcontainers are not individually configurable yet. | PARTIAL |
| MDH-005 | User: "a budgetbe és ide is mindenhova tegyél lágyító slidert" | Header design menu, shared glass surface config state | Every liquid/glass configurable container in Budget and D screens has a softness slider scoped to that container/effect, with no cross-container side effects unless explicitly selected. | RED/GREEN widget test moves Budget avatar, Budget chart, and Mind stage1 sliders and asserts scoped liquid softness. Non-liquid old/C2/Acrylic softness controls are not implemented yet. | PARTIAL |
| MDH-006 | User: "a stage 1 és 2 nézet attól függ, hogy melyik nézet aktív a summary pillben (éves havi sum)" | Transaction summary window integration and D content stage selection | The visible D stage 1/stage 2 content follows the active summary pill mode: monthly, yearly, or sum/all-time. | Widget test switches summary window through store mutation and asserts sum/year/month stage keys. | DONE |
| MDH-007 | User: "a 2 chart is valós legyen élő adatot mutasson az időszaknak, és a szűrésnek megfelelőt" | Stats/transaction data adapters, D chart widgets | Both D charts render live data derived from the active period and filters, not hardcoded HTML numbers. | `spendee_mind_stats_adapter_test.dart` asserts summary scope mapping, vendor filter effect, and non-empty live income/expense series. | DONE |
| MDH-008 | User: "a score grafikon ott van az appban azbis legyen élő... bevéteki és a kiadási score" | `stats_category_scope_series.dart`, `stats_fast_info_graph.dart`, D score graph adapter | D score graph uses the existing live income/expense score series already present in Stats, with the Color Lab design shell. | Adapter/widget tests assert real `scoreLine`, `kontrollScore`, and rendered score value. | DONE |
| MDH-009 | User: "a statisztikában ott van a bevételi volumenhasábok, kiadási volumenhasábok, és a mintahasábok is" | Existing Stats chart data models and D chart adapter | D chart layout reuses existing income volume bars, expense volume bars, and pattern/helper bars from Stats; no duplicate fake data pipeline is introduced. | Adapter test asserts `incomeComparisonBars` and `helperBars`; code inspection confirms D mini charts consume those existing series. | DONE |
| MDH-010 | User: "utolsó screenshot, a kiadás alatt még mindig ott van egy rózsaszín glow, ezt szedd ki" and `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260718-223205.png` | Active income/expense type pill decoration in `spendee_test_dashboard.dart` | The `Kiadás` pill must not shadow with pink/magenta/rose/purple colors; the visible area under the pill must not receive a pink glow. | RED/GREEN widget test inspects the `Kiadás` pill shadow colors in inactive and active states; full `spendee_dashboard_interaction_test.dart` passed 25/25; `flutter analyze` passed. | DONE |
| MDH-011 | Previous interpretation of "score chart úgy rajtoljon, mint a statisztikabheader cardban"; latest user correction points to FastInfo, not magnet strip | Mind D header score area in `spendee_test_dashboard.dart` | The earlier MagnetStrip score ribbon is removed; the active requirement is the FastInfo score graph tracked by MDH-013. | `rg` confirms no `MagnetStrip`/`spendee-test-mind-score-ribbon` remains in `spendee_test_dashboard.dart`; full `spendee_dashboard_interaction_test.dart` passed 25/25; `flutter analyze` passed. | DONE |
| MDH-012 | User: "nem lehet swipepal modeot váltani, ehelyett belerakod a header üveg gombjába, user tappel választ modeot" | Mind D header mode state, header glass menu in `spendee_test_dashboard.dart` | D1/D2/D3G/D4/D5 mode switching must not be triggered by horizontal swipe; the header glass menu exposes tap targets for each D mode and selecting one changes the visible Mind page. | RED/GREEN widget test asserts no `spendee-test-header-background-swipe` gesture target is rendered in Mind mode, menu items D1-D5 exist, and tapping D4 changes the page; full `spendee_dashboard_interaction_test.dart` passed 25/25. | DONE |
| MDH-013 | User: "ott van a statban a score grafikon a fastinfo területen... nézd ki a konkrét kiadási score grafikont" and `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260718-232832.png`, `lib/features/stats/widgets/stats_fast_info_graph.dart` | Mind D header score chart in `spendee_test_dashboard.dart`, reusable FastInfo score chart drawing | The Mind header score chart uses a FastInfo-style score graph: soft red/yellow/green zones, 100/50/0 grid, segmented score path, endpoint dot and score badge, not a magnet strip or placeholder lines. | RED/GREEN widget test asserts the Mind score area contains the FastInfo score chart widget/painter and no `MagnetStrip`; code inspection ties painter geometry/colors to `StatsFastInfoGraph` pattern; full `spendee_dashboard_interaction_test.dart` passed 25/25. | DONE |
| MDH-014 | User: "ha a bevétel pill aktív, akkor bevételi grafikon ha a kiadási akkor koadási grafikon" | `SpendeeMindStatsFrame`, active transaction type routing, Mind score chart widget | The Mind score chart follows the active dashboard type: `Kiadás` uses the expense score series; `Bevétel` uses an income score series drawn with the same score graph pattern. | RED/GREEN widget test toggles the type pill and asserts the score chart receives expense then income score values matching the live series; `spendee_mind_stats_adapter_test.dart` passed 3/3; full `spendee_dashboard_interaction_test.dart` passed 25/25. | DONE |
| MDH-015 | User 2026-07-19: "mind modeban a header stílusa nem változik, ha választok pl acryl etc-t" | Header surface frame and Mind header background layering in `spendee_test_dashboard.dart` | Header surface choices must visibly affect Mind mode too: normal keeps the original header glass, C2/liquid/acrylic wrap the Mind content with their own surface layer, and the Mind gradient must sit behind the selected glass/acrylic effect instead of covering it. | RED/GREEN widget test selects Mind mode and header Acrylic/Liquid, then asserts the selected surface wraps the Mind foreground while the opaque Mind page background stays behind it; full `spendee_dashboard_interaction_test.dart` passed 27/27; `flutter analyze` passed; `git diff --check` passed. | DONE |
| MDH-016 | User 2026-07-19: "summary pill oldd meg, hogy reagáljon... kapja meg a visual feedbackez is mint a régi verzióban azaz oldalra eltolódik a swipe hatására" | Summary pill gesture handling in `_SpendeeHomeContent` / summary pill widget | The summary pill must respond to horizontal drags by translating sideways during the drag, then shifting the active period on sufficient displacement and springing back after release. Vertical drag must still cycle monthly/yearly/sum, tap must still open month picker, double tap must still reset. | RED/GREEN widget test performs a slow horizontal drag, verifies the pill transform moves while dragging, verifies period changes after release, verifies the transform returns to zero, and verifies vertical drag changes the summary window; full `spendee_dashboard_interaction_test.dart` passed 27/27; `flutter analyze` passed; `git diff --check` passed. | DONE |
| MDH-017 | User 2026-07-19: "Mind modeban ne legyen D1/D2/D3G/D4/D5 variáció... egyetlen Mind mód maradjon" | `_MindHeaderPage`, header design menu actions, Mind background key in `spendee_test_dashboard.dart` | The dashboard exposes exactly one Mind background/menu item; D1/D2/D3G/D4/D5 mode menu entries and page variants are removed; the rendered Mind page key is stable as `spendee-test-mind-page-mind`. | RED/GREEN widget test asserts the menu has one Mind background option, no D1-D5 Mind mode items, no old D page keys, and the single Mind page key is rendered; `spendee_dashboard_interaction_test.dart` passed 30/30; `flutter analyze` passed. | DONE |
| MDH-018 | User 2026-07-19: "A container style választás valóban az adott objektum containerét cserélje... none/background után nincs glass/acrylic/liquid wrapper... Ne legyen duplikált container" | `_wrapPanelSurface`, `_HeaderSurfaceFrame`, `_MindStage1BoxedGraphs`, `_MindStage2Panel` in `spendee_test_dashboard.dart` | Selecting `none/background` for Mind stage 1 or stage 2 leaves only the object's background keyed subtree, with no glass/C2/liquid/acrylic wrapper under that object and no duplicate selected wrappers. | RED/GREEN widget test selects Mind stage background after a liquid/acrylic choice and asserts selected object descendants contain no glass/C2/liquid/acrylic keys and only the background wrapper remains; `spendee_dashboard_interaction_test.dart` passed 30/30; `flutter analyze` passed. | DONE |
| MDH-019 | User 2026-07-19: "Mind két grafikonját a Stats FastInfo-ból kell kinézni... Bal grafikon: volumen... aktív pillnél csak bevételi/költségi volumenhasábok... Skála alja 0" | Mind stage 1 chart adapter/widgets and `stats_fast_info_graph.dart` chart drawing pattern | The left Mind chart is an active-type volume chart derived from the current summary/filter FastInfo series: income uses income volume bars, expense uses expense volume bars, and bar height grows upward from zero as amount grows. | RED/GREEN adapter/widget tests assert the volume chart receives income-only data when `Bevétel` is active and expense-only data when `Kiadás` is active; `spendee_mind_stats_adapter_test.dart` passed 5/5; `spendee_dashboard_interaction_test.dart` passed 30/30; `flutter analyze` passed. | DONE |
| MDH-020 | User 2026-07-19: "Jobb grafikon: minták... FastInfo alsó bevételi/kiadási minta grafikonját használd... aktív pill szerint" | Mind stage 1 chart adapter/widgets and `stats_fast_info_graph.dart` helper/pattern bar drawing pattern | The right Mind chart is an active-type pattern chart derived from the current summary/filter FastInfo helper bars: income uses income pattern bars and expense uses expense pattern bars. | RED/GREEN adapter/widget tests assert the pattern chart receives income-only data when `Bevétel` is active and expense-only data when `Kiadás` is active; `spendee_mind_stats_adapter_test.dart` passed 5/5; `spendee_dashboard_interaction_test.dart` passed 30/30; `flutter analyze` passed. | DONE |
| MDH-021 | User 2026-07-19: "Header stage 0: Adaptív score chart... aktív pill... aktuális summary pill időszak/filter szerint" | `SpendeeMindStatsFrame.fromStore`, `_MindFastInfoScoreChart`, Mind stage 0 header content | The stage 0 score chart follows the active transaction type and changes when the active summary period/filter changes. | RED/GREEN widget/adapter tests toggle active type and summary scope/filter and assert the score chart painter receives the matching live FastInfo score series; `spendee_mind_stats_adapter_test.dart` passed 5/5; `spendee_dashboard_interaction_test.dart` passed 30/30; `flutter analyze` passed. | DONE |
| MDH-022 | User 2026-07-19 latest screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260719-032703.png`: "nincs a chartoknak glass beállítva, de mégis ott van... egyik komponensnek sem kell semmilyen container... közvetlen a header hátterén üljenek" | `_MindStage1BoxedGraphs`, `_MindGraphCard`, `_MindStage2Panel`, `_MindHeatmapSurface`, `_MindMonthCard`, `_MindYearSummaryCard` in `spendee_test_dashboard.dart` | When Mind stage1/stage2 surface is `background`, the stage content renders directly on the Mind header gradient: no graph-card surface, no heatmap panel surface, and no month/year summary card surface remain behind the components. | RED/GREEN widget test selects Mind stage1/stage2 background and asserts no white/translucent card/panel container decorations remain under those Mind components; `spendee_dashboard_interaction_test.dart` passed 31/31; `spendee_mind_stats_adapter_test.dart` passed 5/5; `flutter analyze` passed. | DONE |
| MDH-023 | User 2026-07-19: "a headert, azaz magát a színes stage0-2 containert még mindig nem lehet rárakni az üveg effectet a mind modeban... legyen a beállítások közt, az összes glass lehetőséggel, és működjön, mint a budget esetében" | `_HeaderSurfaceFrame`, header design menu, Mind background layering in `spendee_test_dashboard.dart` | In Mind mode, the header menu exposes the same header surface choices as Budget; selecting C2, Liquid, or Acrylic applies the effect to the full colored Mind header container, with the Mind gradient/background inside the selected surface wrapper rather than behind it. | RED/GREEN widget test selects Mind mode and each header glass option, then asserts the selected header wrapper contains the `spendee-test-mind-page-mind` colored background and Mind foreground; `spendee_dashboard_interaction_test.dart` passed 31/31; `spendee_mind_stats_adapter_test.dart` passed 5/5; `flutter analyze` passed. | DONE |
| MDH-024 | User 2026-07-19 latest screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260719-090613.png`: "headermind esetében liquid glassra van állítva, de mégsem jó... csak a contentek... nem jó a mind glass rajzolási logika" | `SpendeeLiquidMaterialLayers`, `_HeaderSurfaceFrame`, Mind header liquid wrapping | Liquid glass in Mind mode must render the colored Mind header as the base content and paint the liquid glare/edge layers above it, so the effect applies to the full header container rather than sitting behind the content. | RED/GREEN widget test selects Mind header liquid and asserts the Mind background is before the liquid glare in the material layer stack, and both are inside the header liquid wrapper. | DONE - RED failed on liquid glare order; GREEN passed in `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --reporter expanded`. |
| MDH-025 | User 2026-07-19: "nem közös containereket akarok stage1-ben és stage 2-ben, hanem szeperáltat... hátterük mindig separált legyen... kiszeded a gyári hátterüket" | `_MindStage1BoxedGraphs`, `_MindGraphCard`, `_MindStage2Panel`, `_MindMonthCard`, `_MindYearSummaryCard`, `_wrapPanelSurface` | Mind stage1/stage2 surface choices must not create a shared/common container. Each graph card and each stage2 card receives its own selected surface wrapper; if the selection is background, that individual component has no backing container. Built-in white factory backgrounds are removed. | RED/GREEN widget test selects Mind stage1 liquid and stage2 acrylic, asserts no shared `spendee-test-mind-stage1-*`/`stage2-*` wrapper exists, and asserts separate per-object surface keys exist. | DONE - RED failed on shared Mind stage wrapper; GREEN passed in `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --reporter expanded`. |
| MDH-026 | User 2026-07-19: "a felső traffic light chartnak egyáltalán ne legyen háttere, csak a line legyen a stage0-ban" | `_MindFastInfoScoreChart`, `_MindFastInfoScorePainter` in `spendee_test_dashboard.dart` | In header stage0, the top score/traffic-light chart draws only the score line; it does not draw soft zones, grid, axis labels, badge, card fill, or other chart background. | RED/GREEN widget test renders Mind stage0 and asserts the score painter is in bare-line mode. | DONE - RED failed before the bare-line painter mode existed; GREEN passed in `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --reporter expanded`. |
| MDH-027 | User 2026-07-19: "egyik header sem kapja meg a designokat... sem az akrilt, sem a c2 designt... deep analyse" | `_HeaderSurfaceFrame`, `_C2GlassSurface`, `SpendeeAcrylicSurface`, `SpendeeLiquidMaterialLayers` | In Mind mode, every header surface design renders above the colored Mind header, not hidden behind it: C2 paint overlay, Acrylic highlight overlay, and Liquid glare overlay must all be after the Mind background/content in their paint stack. | RED/GREEN widget test selects Mind header C2/Acrylic/Liquid and asserts each effect overlay key is painted after `spendee-test-mind-page-mind` in the relevant surface stack. | DONE - RED failed on C2 background painter and missing Acrylic overlay key; GREEN passed in `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --reporter expanded`. |
| MDH-028 | User 2026-07-19: "mind stage1-ben ne legyen ott a 2026 minden kategória felirat, nem kell jelölni a szűrést, azt a summary pill végzi" | `_SpendeeMindHeaderContent`, `_MindStage1BoxedGraphs` | In Mind stage1, the inline period/filter label under the score is hidden; filtering and period state remain represented only by the summary pill outside the header. | RED/GREEN widget test switches to Mind stage1 and asserts the combined period/scope label is absent while the summary pill remains present. | DONE - RED failed while `2026 · Minden kategória` was still rendered in the header; GREEN passed in `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --reporter expanded`. |
| MDH-029 | User 2026-07-19: "a stage 1 chartja legyen magasabb... érjen a header tetejéig... legyen szélesebb is... annyi padding legyen a score szám és a chart bal széle közt, mint... a chart jobb széle és header jobb oldala közt" | `_SpendeeMindHeaderContent` score chart positioning | In Mind stage1, the top score chart uses equal top/right/score-gap inset, reaches close to the header top, grows wider to the left, and remains aligned with the same right padding from the header edge. | RED/GREEN widget test measures `spendee-test-mind-score-chart`, `spendee-test-header-card`, and the score painter plot inset in stage1 and asserts equal top/right/score-gap inset plus increased chart height. | DONE - RED failed on missing score key and overlapping chart plot; GREEN passed in `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --reporter expanded`. |
| MDH-030 | User 2026-07-19: "nagyon laggol... a budget avatar slide is nagyon laggol... a régi designban... smooth" | `_SpendeeTestDashboardState`, `_SpendeeBudgetHeaderCard`, `SpendeeMindStatsFrame.fromStore` | Budget/background interactions must not build Mind stats frames. The expensive Mind stats adapter is invoked only while the Mind header background is active. | RED/GREEN widget test instruments `SpendeeMindStatsFrame.fromStore` and drags the Budget avatar/header, asserting zero Mind frame builds while Budget is active. | DONE - RED showed 4 Mind stats frame builds during Budget header/avatar motion; GREEN passed in `flutter test test/spendeetest/spendee_dashboard_interaction_test.dart --reporter expanded`. |
| MDH-031 | User 2026-07-19: "minden laggol, a váltás a header nézetek közt, a drag expand is, a váltás bevétel kiadás közt. optimalizáld" | `_SpendeeTestDashboardState`, Mind stats cache key, store/filter/summary inputs | In Mind mode, repeated header stage drag updates and rebuilds reuse the last Mind stats frame while transaction/filter/summary inputs are unchanged; changing active type, summary window/period, category filter, merchant filter, or query invalidates the cache. | RED/GREEN widget/adapter test counts Mind frame builds across drag updates and type/summary changes. | DONE - RED showed uncached direct Mind frame builds; GREEN passed with zero builds during geometry-only Mind header drag and one rebuild for active type/summary changes. |
| MDH-032 | User 2026-07-19: "ha kész vagy, adj hozzá debug logokat, ha még mindig akadna az új build után be tudjam neked másolni" | Debug logging around Mind stats cache/builds and interaction-frame rebuilds | The app emits copyable `[Perf] SpendeeTest ...` debug logs for slow Mind stats builds, cache misses, and high-frequency header/carousel interaction rebuilds, including mode, stage, active type, summary scope, transaction count, and elapsed time. | Widget/unit tests install a debug log sink and assert relevant log labels are emitted on cache miss and interaction threshold conditions. | DONE - GREEN widget test asserts `[Perf] SpendeeTest MindStats cache_miss` and `[Perf] SpendeeTest header_drag` logs; carousel drag summary logs were added for Budget avatar slide diagnosis. |
| MDH-033 | User pasted 2026-07-19 perf log: `FastInfo metrics build ... elapsed=927ms`, `HomeBuild frame ... elapsed=1878ms`, while `DashboardDesignMode.spendeeTest` is active | `TransactionHomePage.build` spendeeTest branch | SpendeeTest mode must return the experimental dashboard before computing classic-home-only `fastInfoMetrics`, `visibleTransactions`, `visibleGhostTransactions`, and classic `visibleDisplayLogEntries` for HomeBuild logging. | RED/GREEN widget test uses a counting store/repository in SpendeeTest mode and asserts the classic HomeBuild path does not request FastInfo metrics or classic visible log inputs before returning `SpendeeTestDashboard`; `spendee_dashboard_interaction_test.dart` passed 39/39. | DONE |
| MDH-034 | User pasted 2026-07-19 perf log: repeated type switch lines show `MindStats cache_miss ... type=income/expense elapsed=15-16ms` | `_MindStatsFrameCacheKey`, `_mindStatsFrameFor`, `SpendeeMindStatsFrame` active frame selection | Toggling the Bevétel/Kiadás pill in Mind mode must not rebuild expensive income/expense StatsRenderFrames when summary/filter inputs are unchanged; it should reuse the cached pair and only switch the active frame. | RED/GREEN widget test switches active type repeatedly after the first Mind frame and asserts zero `SpendeeMindStatsFrame.fromStore` expensive builds while the score/chart active type still changes; `spendee_dashboard_interaction_test.dart` passed 39/39. | DONE |
| MDH-035 | User pasted 2026-07-19 perf log: header drag still feels unsmooth; `header_drag ... updates=73 elapsed=1073ms`, and HomeBuild jank appears around store-driven changes | `_SpendeeTestDashboardState`, `_SpendeeHomeContent`, `_SpendeeLogList` | Header stage drag must rebuild only the header geometry/surfaces, not rebuild/requery the SpendeeTest content/log list. The transaction log list is hidden before its store list is requested when stage2 covers it. | RED/GREEN widget test instruments SpendeeTest log-list input access during header drag to stage1/stage2 and asserts no new visible-log access occurs for geometry-only drag, with a debug log if a content rebuild exceeds threshold; `spendee_dashboard_interaction_test.dart` passed 39/39. | DONE |
| MDH-036 | User 2026-07-19: "még lehetne jobb a teljesítmény, főleg a kategória avatarok slideolása közben" plus code root cause: carousel calls `setCategoryFilter` on every tick | `_SpendeeTestDashboardState` carousel drag handlers, `TransactionStore.setCategoryFilter` boundary | Budget avatar/category carousel slide must not call store filter/prewarm on each tick; drag changes only local header selection and publishes the selected item once on tap/release. | RED/GREEN widget test instruments category filter calls during carousel drag and asserts zero calls before release plus at most one publish after release; `spendee_dashboard_interaction_test.dart` passed 45/45. | DONE |
| MDH-037 | User 2026-07-19: "budget header nem kapja meg a budgetavatarokat, csak a kategóriákat, kapja meg azokat is, legyenek ott a listában" | Budget stage1 carousel data model and avatar renderer in `spendee_test_dashboard.dart` | Budget stage1 carousel uses `backheaderBudgetItems`, so overview budget avatars (Budget / income goal as applicable) appear in the same list as category avatars and can be selected. | RED/GREEN widget test expands budget stage1 and asserts an overview budget avatar key is present alongside category avatar keys and can become the selected center item; `spendee_dashboard_interaction_test.dart` passed 45/45. | DONE |
| MDH-038 | User 2026-07-19 latest screenshot: "ne legyen cím, a chart legyen legfelül, és mindig legyen, ne legyen a scroll része, csak a lista alatta. a cím fentről pedig kerüljön jobb oldalra" | `_BudgetPieStage2Layer`, `_BudgetPieContent` | Budget stage2 has no separate top title/eyebrow above the pie. The fixed top row contains the donut chart and right-side focus text; only the list below scrolls. | RED/GREEN widget test asserts old heading text is absent, fixed pie/focus row key remains outside the scroll view, and list rows are under a dedicated scroll view; `spendee_dashboard_interaction_test.dart` passed 45/45. | DONE |
| MDH-039 | User 2026-07-19: "a szöveghez a pie chart cikkei is legyenek tappelhetőek... közepe is legyen tapelhető, olyankor a budget avatarra ugrik" | Budget donut hit testing and selection bridge to carousel | Tapping a donut slice selects the matching category and animates the carousel to it; tapping the donut center selects the overview budget avatar. | RED/GREEN widget test taps a donut slice and center and asserts the stage2 focus plus stage1 carousel selection jump to the expected category/overview item; `spendee_dashboard_interaction_test.dart` passed 45/45. | DONE |
| MDH-040 | User 2026-07-19: "limitet állítani... longtappel egy avatart... felfele swipe... 1000/10000hufonként... minden tick után ment... very long tap törlés... 0-ra is kiszedi" | Budget avatar long-press gesture, optimistic limit preview, store/repository limit save | Long press activates avatar limit edit with haptic; vertical swipe ticks adjust the selected avatar limit by 1,000 or 10,000 Ft based on drag magnitude, save each tick, update header value/progress live, release exits, and very-long-press or reaching 0 clears the limit. | RED/GREEN widget tests simulate long press + vertical drag and very-long-press, asserting saved payloads, live header value, and zero-limit deletion semantics; `spendee_dashboard_interaction_test.dart` passed 45/45. | DONE |
| MDH-041 | User 2026-07-19: "mind mode: a grafikon akkora legyen mindegyik stage-ben mint stage0-ban, és ugyanabban a pozícióban legyen... stage 0 ne írja ki a szűrt nézetet a score alatt" | `_SpendeeMindHeaderContent` score chart positioning and stage0 label | Mind score chart uses the stage0 rectangle for every header stage, and stage0 does not render the period/filter label below the score. | RED/GREEN widget test measures score chart rect across stage0/stage1/stage2 and asserts equality; it also asserts the stage0 period/filter label is absent; `spendee_dashboard_interaction_test.dart` and `spendee_mind_stats_adapter_test.dart` passed. | DONE |
| MDH-042 | User 2026-07-19: "ha user longtappel egy avatart, annak legyen visual feedbackje, legyen egy shrink effect... kb 20%-ot csökkenjen" | Budget avatar long-press gesture/rendering in `spendee_test_dashboard.dart` | Long-pressing any budget/category avatar scales that avatar to about 80% while limit edit is active, then returns it to full size on release. | RED/GREEN widget test long-presses an avatar, inspects the avatar press-scale widget, then releases and verifies the scale returns to 1.0; `spendee_dashboard_interaction_test.dart` passed 50/50; `flutter analyze` passed. | DONE |
| MDH-043 | User 2026-07-19 earlier: "ami most a progress bar... alulról egy félkör ív... meghagyod, a header menube rakod mint opciót avatar 3d..."; superseded by latest MDH-049 | Header design menu and budget/category avatar painters | This earlier interpretation is no longer active. The current accepted behavior is MDH-049: no separate lower decorative arc and no `Avatar 3D` overlay/menu item. | Superseded by MDH-049; RED/GREEN now asserts no `spendee-test-avatar-3d-effect-*` overlay and no `spendee-test-avatar-effect-3d-toggle` menu item. | DONE |
| MDH-044 | User 2026-07-19: "budget avatar nem kapja meg a felső világos félkörív effectet... legyen rajta... avatarok felső világos félkörét is tedd a header dropdownban opcionálissá" | Header design menu, `_OverviewBudgetAvatar`, `GlossyCategoryAvatar` usage | Budget overview avatars receive the same top light semicircle as category avatars; the top highlight is exposed as a header menu toggle and applies consistently to both avatar types. | RED/GREEN widget test asserts the top-highlight toggle exists, budget and category avatars have the highlight by default, and both stop rendering it when disabled; `spendee_dashboard_interaction_test.dart` passed 50/50; `flutter analyze` passed. | DONE |
| MDH-045 | User 2026-07-19: "circle progress bar... halvány fehér körvonal... minél több pénzt költött a limithez képest... 0% halvány... 100% teljes fehér kör" | Budget avatar progress painter and limit/spent calculation | The real avatar progress bar is a white circular outline: a faint full base ring at 0%, plus a stronger translucent white arc that fills according to spent/limit up to 100%. | RED/GREEN widget test inspects the progress painter for a limited category, verifying progress value, white progress color, and base-ring visibility; `spendee_dashboard_interaction_test.dart` passed 50/50; `flutter analyze` passed. | DONE |
| MDH-046 | User 2026-07-19: "carousel 5 és fél avatart mutat... ez ne legyen fixen 5-öt mutasson" | `_ContextAvatarBelt` slot layout and clipping | The budget avatar carousel renders exactly five avatar slots at a time with no clipped half-avatar entering from the left edge. | RED/GREEN widget test counts rendered carousel slot keys and verifies the leftmost slot stays inside the carousel bounds; `spendee_dashboard_interaction_test.dart` passed 50/50; `flutter analyze` passed. | DONE |
| MDH-047 | User 2026-07-19: "ha a budget avatar aktív, akkor ne legyen vendor, csak minden kategoria chart. ha egy kategória aktív, akkor pedig csak vendorlista lehet alatta... megszűnik a kétoldalas chart mode" | Budget stage2 pie/chart selection logic | Budget stage2 is selected-object driven, not page driven: overview budget avatar shows only the all-category chart/list; category avatar shows only the vendor chart/list. Horizontal chart paging is removed. | RED/GREEN widget test selects overview and category avatars, asserts only the matching stage2 content key is rendered, and verifies swiping the chart does not switch to the other mode; `spendee_dashboard_interaction_test.dart` passed 50/50; `flutter analyze` passed. | DONE |
| MDH-048 | User 2026-07-19: "tehát olyan chart nem lehet sehol hogy nincs adat" | `_BudgetPiePanel` empty-data handling | Empty budget stage2 datasets do not render an empty donut/list chart; the chart area is hidden instead of showing a no-data chart shell. | RED/GREEN widget test selects a stage2 target with no entries and asserts neither category nor vendor chart content is rendered; `spendee_dashboard_interaction_test.dart` passed 50/50; `flutter analyze` passed. | DONE |
| MDH-049 | User 2026-07-19 latest: "tettél bele több extra komponenst... tettél bele egy felesleges alsó ívet... szedd ki az alsó díszítő ívet" and `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260719-192324.png`; superseded by MDH-053 | `_ContextAvatar`, `_BudgetAvatar3DEffectPainter`, header avatar-effect menu in `spendee_test_dashboard.dart` | This earlier correction removed the lower 3D overlay entirely. The current accepted behavior is MDH-053: the 3D effect may return as an optional colored lower depth effect, but never as a white lower decorative arc. | Superseded by MDH-053; new RED/GREEN will assert the `Avatar 3D` toggle exists and the lower painter exposes `drawsWhiteArc == false`. | DONE |
| MDH-050 | User 2026-07-19 latest: "ne extra kört kértem... már ott van egy halvány körvonal az avataron kívül... azt akarom, hogy az avatar körüli kör legyen a progress bar" | `_BudgetAvatarOuterHaloProgressPainter`, `_ContextAvatar`, selected avatar halo composition | The limit progress uses the selected avatar's existing outer glass halo area: no inner progress circle and no immediate avatar-edge progress ring are drawn. The faint outer halo remains at 0%, and a stronger translucent white arc fills that same outer halo clockwise from 12 o'clock as spent/limit increases. | RED failed with 5 legacy `spendee-test-avatar-progress-*` painters; GREEN passed with the selected `spendee-test-avatar-outer-halo-progress-*` painter exposing outer-halo, clockwise, 12 o'clock start, and no-inside-body contract; `spendee_dashboard_interaction_test.dart` 51/51 passed. | DONE |
| MDH-051 | User 2026-07-19 latest perf log: limit ticks trigger repeated `Stats frame prewarm`, `Store budget-bars`, `content_frame ... jank=true`; "laggol az app, nem lehet limitet állítani... grow shrink laggol, optimalizáld" | Budget limit long-press/tick save path in `spendee_test_dashboard.dart` and `TransactionStore.save*LimitInline` usage | Limit editing remains live and saves every tick, but tick saves do not notify the whole store or trigger Stats prewarm/content rebuild storms. Local header value/progress updates immediately; one store notification may happen when the edit finishes. Press shrink uses a cheap repaint boundary. | RED failed with 2 store notifications before release; GREEN passed with repository tick saves and 0 store notifications before release, <=1 after release; `spendee_dashboard_interaction_test.dart` 51/51 and `flutter analyze` passed. | DONE |
| MDH-052 | User 2026-07-19 latest: "mind header függvénye nem úgy rajzol, ahogy a statban... utolsó screenshot... utolsó előtti a stat valós színezése" and `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260719-192803.png`, `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260719-192728.png`, `lib/features/stats/widgets/stats_fast_info_graph.dart` | `_MindFastInfoScoreChart`, `_MindFastInfoScorePainter` | Mind header score graph uses the Stats FastInfo score rendering style: soft score zones/grid, segmented red/yellow/green score line, endpoint dot, and score badge from the live active type/summary data, instead of a bare single line. | RED failed while `drawsBackground == false`; GREEN passed after enabling full FastInfo style and Stats score color thresholds; `spendee_dashboard_interaction_test.dart` 51/51 and `spendee_mind_stats_adapter_test.dart` 5/5 passed. | DONE |
| MDH-053 | User 2026-07-19 latest: "az alsó ívnek valahogy volt belső árnyéka... nézd ki mi volt... 4771ca2"; correction: "alul ne legyen fehér ív, sehol, csak felül legyen díszív" | Header avatar-effect menu, `_BudgetAvatar3DEffectPainter`, `_ContextAvatar` | The header menu includes an `Avatar 3D` toggle. When enabled, budget/category avatars may render only a colored lower depth/inner-shadow effect using the avatar accent. No lower white decorative arc is drawn anywhere; white decorative arcs are reserved for the top highlight. | RED failed because the 3D key/toggle were missing; GREEN passed with `Avatar 3D` toggle, colored accent painter, `hasInnerDepthShadow == true`, and `drawsWhiteArc == false`; `spendee_dashboard_interaction_test.dart` 51/51 and updated widget goldens passed. | DONE |
| MDH-054 | User 2026-07-19 latest: "a felső színes ív a kategóriáknál a budgettel egyezzen, és ne a budgetet igazítsd a kategóriákhoz" | `_ContextAvatar`, `GlossyCategoryAvatar`, `_BudgetAvatarTopHighlightPainter` | Category avatars in the budget carousel use the same external top-highlight painter and geometry as the budget overview avatar. The built-in category avatar top ring is disabled there; the existing Budget top-highlight painter remains the source of truth. | RED failed while category avatars used their internal `GlossyCategoryAvatar` ring; GREEN passed with external `spendee-test-avatar-top-highlight-category-*` keys and `GlossyCategoryAvatar.showTopHighlight == false` in the budget carousel. | DONE |
| MDH-055 | User 2026-07-19 latest: "a shrink animáció is legyen responsívabb... longtap shrink ne csak akkor triggereljen, amikor már a tick megjelent... a gyorstap ne triggerelje" | `_ContextAvatar` press/hold gesture state | A quick tap does not shrink the avatar, but a sustained tap-and-hold shrinks the avatar before the long-press limit edit tick starts. Releasing restores the avatar size. | RED failed because 180 ms hold still showed scale 1.0; GREEN passed after pointer-down hold preview starts shrink before long-press activation while quick tap remains scale 1.0. | DONE |
| MDH-056 | User 2026-07-19 latest: "nem kell bele sem a felső díszcsík, sem a 3d, ezeket szedd ki" | Header avatar-effect menu, `_ContextAvatar`, `_BudgetAvatarTopHighlightPainter`, `_BudgetAvatar3DEffectPainter` | The header dropdown no longer exposes `Avatar 3D` or top decorative arc controls, and budget/category avatars render no separate top decorative arc or 3D/lower depth painter. | RED failed on existing `spendee-test-avatar-top-highlight-*`/`3d-effect-*` keys; GREEN passed in `spendee_dashboard_interaction_test.dart` 53/53 and `test/widget_test.dart` 64/64 with updated goldens. | DONE |
| MDH-057 | User 2026-07-19 latest: "budget bal felső felén fehéres az avatar, de a többi nem... user a dropdownban tudja választani... slider is" | Header design menu, `GlossyCategoryAvatar`, `_OverviewBudgetAvatar`, budget carousel avatar composition | The avatar body top-left glossy highlight is consistent across overview and category avatars: the user can toggle it on/off from the header dropdown and tune its intensity with a slider. | RED failed on missing body-highlight controls/keys; GREEN passed with `spendee-test-avatar-body-highlight-toggle` and strength slider in `spendee_dashboard_interaction_test.dart` 53/53. | DONE |
| MDH-058 | User 2026-07-19 latest: "a progress circle jó, de méretváltozása nem követi le rendesen az avatarok méretváltozását... 75%-nál sárgás... 90 felett piros" | `_ContextAvatar`, `_BudgetAvatarOuterHaloProgressPainter`, avatar press/selected transform | The outer halo progress circle shares the exact same transform as the avatar body during selected/press shrink states, and its progress color becomes yellow at >=75% and red at >=90%. | RED failed while progress remained white and category avatars used internal selected scaling; GREEN passed with red/yellow threshold assertions and `scaleSelection == false`; `spendee_dashboard_interaction_test.dart` 53/53. | DONE |
| MDH-059 | User 2026-07-19 latest: "nem kell sima tap pulse, csak shrink legyen... side avatar tap... tickenként ugrik középre" | Budget carousel tap/selection handlers and press-scale state | Quick taps do not trigger any pulse scale. Tapping a side avatar recenters through intermediate tick steps instead of jumping directly to the tapped item. Shrink feedback is reserved for sustained press/limit edit. | RED failed while live ticks still pulsed and side taps jumped; GREEN passed with no `pulsing` state and ordered `carousel_tick` logs for category-2 then category-3; `spendee_dashboard_interaction_test.dart` 53/53. | DONE |
| MDH-060 | User 2026-07-19 latest: "rakd vissza a korábbi kettős chartot... mindenhol legyen az összes category chart, swipe oldalra-vendor chart... budgetnél... összes vendort listázza ami a scopeban van" | `_Stage2BudgetPage`, `_BudgetPieStage2Layer`, category/vendor entry builders, chart swipe handlers | Budget stage2 again has two swipeable pages everywhere: all-category chart/list and vendor chart/list. With overview selected, the vendor page shows all scoped vendors; with a category selected, it shows vendors for that category. | RED failed while stage2 was selected-object driven; GREEN passed with category/vendor swipe tests for category and overview selections; `spendee_dashboard_interaction_test.dart` 53/53. | DONE |
| MDH-061 | User 2026-07-19 latest screenshot: "van kb 1cm a chart és az avatarok közt, hozd feljebb" | Budget header stage2 layout constants and `_BudgetPieStage2Layer` placement | The fixed chart row starts closer to the avatar/name area, reducing the vertical gap and leaving more space for the scrollable pill/list area below. | GREEN verified by updated C2/C3 widget goldens and stage2 fixed-top/list tests; `test/widget_test.dart` 64/64 and `spendee_dashboard_interaction_test.dart` 53/53 passed. | DONE |
| MDH-062 | User 2026-07-19 latest: "longtappeljük az avatart... ne csak a húzásra nőjjön, legyen benne autotick... közel lassú, messze gyorsabb... very long... erősebb" | Budget limit long-press state, auto tick timer, haptic path, inline limit saves | During active limit edit, holding above/below the avatar auto-adjusts the limit without additional movement; tick interval is slower near the avatar and faster farther away. Very-long press clears the limit with a stronger haptic tick. | RED failed because save count did not grow while held still and no strong clear log existed; GREEN passed with auto tick saves and `budget_limit_clear strength=strong`; `spendee_dashboard_interaction_test.dart` 53/53. | DONE |
| MDH-063 | User 2026-07-19 latest: "a normál módban van egy treshold állíttás tickekkel a stat menuben, szedd ki, inaktiváld, lesz benne később, ne töröld" | `StatsPageController`, `_StatsPageState`, stats FAB/threshold sheet wiring | The existing Stats threshold code remains in the source, but normal stats menu/FAB threshold actions are inactive and cannot open or change threshold. | RED failed while the Stats FAB opened `stats-threshold-sheet`; GREEN passed with the sheet closed and vertical FAB threshold drag disabled; `test/widget_test.dart` 64/64. | DONE |
