# Color Lab HTML Prototype Acceptance Checklist

## Source-of-truth files inspected before implementation

- `lib/features/transactions/transaction_home_page.dart`
- `lib/features/transactions/widgets/header_card/transaction_header_card.dart`
- `lib/features/transactions/widgets/header_card/transaction_header_metrics.dart`
- `lib/features/transactions/widgets/header_card/magnet_strip.dart`
- `lib/features/transactions/widgets/transaction_type_pills.dart`
- `lib/features/transactions/widgets/summary_pill.dart`
- `lib/features/transactions/widgets/search_pill.dart`
- `lib/features/transactions/widgets/transaction_log_box.dart`
- `lib/features/transactions/widgets/transaction_log_list.dart`
- `lib/features/transactions/widgets/category_menu/category_menu_panel.dart`
- `lib/features/transactions/widgets/category_menu/category_card.dart`
- `lib/features/transactions/transaction_home_page.dart` (`VendorFilterPanel`, `_VendorFilterRow`)
- `lib/features/transactions/widgets/add_transaction_sheet.dart`
- `lib/features/transactions/widgets/slide_up_menu_card.dart`
- `lib/features/transactions/widgets/slide_up_panel_metrics.dart`
- `lib/features/transactions/widgets/transaction_menu_metrics.dart`
- `lib/features/shell/widgets/expt_fab.dart`
- `lib/features/shell/widgets/expt_bottom_nav.dart`
- `lib/features/shell/widgets/bottom_nav_item.dart`
- `lib/core/theme/app_colors.dart`
- `lib/core/theme/app_dimensions.dart`
- `lib/core/theme/category_color_manager.dart`
- `lib/features/settings/models/app_theme_settings.dart`
- `lib/features/settings/theme/expense_theme.dart`
- `lib/features/settings/theme/expense_surface.dart`
- `lib/features/transactions/slots/category_icon_manager.dart`
- `lib/features/transactions/widgets/category_slot_icon.dart`
- `android/app/src/main/kotlin/com/exptv2/app/expense/ExpenseSeedData.kt`
- `lib/features/stats/stats_page.dart` (`_StatsThresholdControlSheet`)
- `docs/prototypes/stats_common_2025_snapshot_mode.html`
- `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/keyboardtest/lib/main.dart`

## Requirements

| ID | Source instruction / reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| COLOR-LAB-01 | User: `data/data/.../exptv2 színeket fogunk tervezni` | New HTML prototype under `docs/prototypes` | A browser-served color design lab exists in the `exptv2` repo and is reachable on a local HTTP port | File inspection + HTTP fetch | DONE |
| COLOR-LAB-02 | User: `a komponenek designját a kódból fogod kiolvasni... konkrétan ugyanazt, pixelre pontosat` | Prototype CSS/layout constants | Layout constants are copied from inspected Flutter source: 188px header, 192px content top, 52px type pills, 70px summary pill, 46px search pill, 72px logbox rows, 150px category/vendor cards, 80px bottom nav, 66px FAB, 20px horizontal gutters, matching radii/borders/shadows | Source inspection + static assertions | DONE |
| COLOR-LAB-03 | User: `scrollozható széles vászon... komponensek egymás mellett 1:1 arányúak... screen szélességűek` | Prototype canvas layout | The page is horizontally scrollable and contains four screen-width phone panels side by side, each rendered at the chosen app viewport width | Browser/manual inspection + CSS assertion | DONE |
| COLOR-LAB-04 | User: `header card valós méretben a screen tetején, a mágnescsíkkal` | Home panel header | The home panel includes the real-size header card at the top, with source-matched magnet-strip position, title, balance label, balance value, scope chip, and menu button | Source inspection + browser/manual inspection | DONE |
| COLOR-LAB-05 | User: `alatta bevétel/kiadás, alatta summarypill, alatta searchpill` | Home panel content | Type pills, summary pill, and search pill are stacked below the header using source-matched spacing, height, radius, borders, shadows, colors, and typography | Source inspection + browser/manual inspection | DONE |
| COLOR-LAB-06 | User: `alatta logbox area, logboxokkal... mérete layoutja 100% egyezzen` | Home panel log list | Logbox rows use 20px horizontal margin, 4px vertical margin, 72px row height, 25px radius, 16/12px padding, 46px avatar, 12px gaps, matching text styles and amount colors | Source inspection + browser/manual inspection | DONE |
| COLOR-LAB-07 | User: `fab gombot is a helyére rakod. bottom navot is ugyanúgy` | Shell overlay | FAB uses 66px rounded-square shape, 18px radius, right 20px and bottom 104px placement; bottom nav uses 80px height and source-matched items/colors | Source inspection + browser/manual inspection | DONE |
| COLOR-LAB-08 | User: `mellé ... app nyitott category sheet állapotát` | Category sheet panel | A second screen-width panel shows the home header plus opened category sheet from bottom to `overlayTop`, with grabber, title, 2-column grid, 150px cards, apply button, and sheet/card surfaces from `ExpenseTheme` | Source inspection + browser/manual inspection | DONE |
| COLOR-LAB-09 | User: `cardokra valós... avatar színeket és ikonokat raksz (a logboxokra is)` | Category/vendor/logbox data rendering | Category, vendor, and logbox avatars use real seed categories, `CategoryColorManager` slots, and `CategoryIconManager` SVG asset mapping, not invented icons/colors | Source inspection + DOM/static assertions | DONE |
| COLOR-LAB-10 | User: `3.: vendor sheet: a valós vendor sheet, a valós vendor cardokkal` | Vendor sheet panel | A third screen-width panel shows the vendor sheet with grabber, 46px title area, vendor search pill, grouped 2-column 150px vendor cards, footer apply button, and real vendor/category rollup sample data | Source inspection + browser/manual inspection | DONE |
| COLOR-LAB-11 | User: `mellé pedig addnewtransaction sheet. másik header carddal` | Add transaction sheet panel | A fourth screen-width panel shows the add-transaction sheet with header behind it, source-matched 401px closed panel height, grabber, title, pill fields, category field, date/time fields, and save footer | Source inspection + browser/manual inspection | DONE |
| COLOR-LAB-12 | User: `minden layoutja megegyezik az appal, az a source of truth` | Prototype implementation | The prototype keeps source file references and code-derived constants near the CSS/JS definitions so future edits can be traced back to Flutter source | Code inspection | DONE |
| COLOR-LAB-13 | User: `vászon... növelned kell, lefelé... kb 1x1 cm színslotokat` | Palette area | The canvas height extends below the phone panels and includes approximately 1cm x 1cm tappable color slots | Browser/manual inspection + CSS assertion | DONE |
| COLOR-LAB-14 | User: `belerakod az appban található színeket` | Palette data | Palette includes AppColors, theme-derived defaults, category slot colors, component surface colors, magnet colors, FAB/nav shadows, and stats snapshot sheet colors | Static JS/CSS data inspection | DONE |
| COLOR-LAB-15 | User: `további színeket is fogsz javasolni, szürke és fehérárnyalatokat fogsz használni` | Palette data | Palette includes additional proposed white/gray shades clearly marked as suggestions | Static JS/CSS data inspection | DONE |
| COLOR-LAB-16 | User: `a stat menu fabra nyíló snapshotsheet szürke árnyalatát is belerakod` | Palette data | Palette includes `_StatsThresholdControlSheet` background `AppColors.gray100` and related snapshot card/selected shades | Source inspection + palette inspection | DONE |
| COLOR-LAB-17 | User: `user egy színre tappel... highlightolja... ismét tappel... dehighlightolja` | Palette interaction JS | Tapping a color toggles selected/highlighted state; tapping the same selected color clears selection | Browser/manual interaction + JS test | DONE |
| COLOR-LAB-18 | User: `ha ... színre tappelt, majd ... app komponensre, akkor annak színe megváltozik` | Component recolor JS | With a selected color, tapping a registered component applies that color through CSS variables or inline styles | Browser/manual interaction + JS test | DONE |
| COLOR-LAB-19 | User: `bármelyik komponens színe változtatható... app háttér, sheetek háttere, header card, magnet strip, logbox háttérszín, vendorcard háttérszín, category card háttérszín, textpillek színe` | Component target mapping | All named target types are tappable and recolorable: app background, sheet background, header card, magnet strip, logbox background, vendor card background, category card background, type/summary/search/text pills | Browser/manual interaction + DOM/static assertions | DONE |
| COLOR-LAB-20 | User: `ha kész megadod az elérést, és a portot` | Local server | A local HTTP server is started from the repo/prototype directory and the final response includes URL and port | HTTP fetch + final response | DONE |
| COLOR-LAB-21 | User: `rakj bele pinch zoomot is` | Canvas interaction JS/CSS | The wide canvas supports two-finger pinch zoom on touch devices and Ctrl/trackpad wheel zoom, with visible zoom percentage and bounded transform scale | Static JS inspection + HTTP fetch | DONE |
| COLOR-LAB-22 | User: `az alsó gombok ... is módosítható legyen` | Bottom navigation recolor targets | Bottom nav background and individual bottom nav buttons/items are tappable recolor targets, independent from the generic app background | DOM/static assertions | DONE |
| COLOR-LAB-23 | User: `a fab gomb is módosítható legyen` | FAB recolor target | FAB buttons are tappable recolor targets using their own CSS variable, not only inherited primary/accent color | DOM/static assertions | DONE |
| COLOR-LAB-24 | User: `az addnewtransaction sheet háttere is legyen változtatható` | Add transaction sheet recolor target | The add transaction sheet/card background has a dedicated tappable recolor target and CSS variable separate from generic sheet background | DOM/static assertions | DONE |
| COLOR-LAB-25 | User: `keyboardtest project színeit is rakd bele` | Palette data | Palette includes the concrete color values found in `/keyboardtest/lib/main.dart`, including seed/accent, warm background, sheet/handle, debug console, and status/icon colors | Source inspection + static assertions | DONE |
| COLOR-LAB-26 | User: `rakj egy új palettasort a betűknek` | Palette markup | A separate text-color palette row exists for keyboardtest/app text colors and is visually separate from surface/background swatches | DOM/static assertions | DONE |
| COLOR-LAB-27 | User: `ezek csak betűket állítanak` | Palette interaction JS/CSS | Selecting a text palette swatch and tapping a component changes only text color inside that component; background CSS variables are not changed for text-role swatches | JS/static assertions + browser/manual interaction | DONE |
| COLOR-LAB-28 | User: `nevezd el a slotokat, a1 b1 etc` | Palette CSS/JS | Every color swatch receives a visible grid-style slot ID (`A1`, `B1`, etc.) and that ID is also included in accessibility/title text | Static JS/CSS assertions + browser/manual inspection | DONE |
| COLOR-LAB-29 | User: `lehessen színezni a kategória gombot` | Header menu/category button target | The round category/menu button in each header is a dedicated recolor target, independent from the generic primary color | DOM/static assertions | DONE |
| COLOR-LAB-30 | User: `és a kék bevétel gombot is` | Home type pill target | The blue Bevétel type pill is a dedicated recolor target with its own CSS variable, so tapping it visibly changes that button | DOM/static assertions | DONE |
| COLOR-LAB-31 | User: `a header card egyenleg és alatta a számot` | Header text targets | The `Egyenleg` label and the balance number are separate text-color targets, not one shared header target | DOM/static assertions | DONE |
| COLOR-LAB-32 | User: `és az e pense tracker fekiratot külön lehessen színezni` | Header title text target | The `ExpenseTracker` header title is a separate text-color target | DOM/static assertions | DONE |
| COLOR-LAB-33 | User: `a vendor list search pillt lehessen színezni, jelenleg nem lehet` | Vendor sheet search pill target | The vendor list search pill uses a CSS variable that its own background actually reads, and it has a dedicated recolor target independent from the generic pill target | Static assertions + HTTP fetch | DONE |
| COLOR-LAB-34 | User: `legfelülre kell egy szelektált sor: d1, e1, f1, n5, o5, p5,a6, b6. sötétség alapján rakd sorrendbe`; later: `fordított legyen a sorrend, és rakd bele n3-at is` | Palette selected row | The palette starts with a selected-source row containing D1, E1, F1, N3, N5, O5, P5, A6, B6 duplicated from the existing slot colors and sorted lightest-to-darkest by relative luminance without changing the original slot ID assignment | Static assertions + HTTP fetch | DONE |
| COLOR-LAB-35 | User: `felül vannak a jelenlegi menuk alatta a színpaletta. ez a régi design` | Prototype section structure | The existing four-menu design and its existing palette remain in the upper/legacy section, with explicit markup identifying it as the old design | Static assertions + browser/manual inspection | DONE |
| COLOR-LAB-36 | User: `csinálsz majd egy alternatív designt... a színpaletta alatt lesz, ugyanezzel a logikával, tehát kvázi duplikálod a menuket, színeket, mindent` | Prototype alternate section | Below the old palette there is a separate alternative design section containing a duplicated set of the four current app menu/sheet states using the same source-derived layout and recolor targets | Static assertions + browser/manual inspection | DONE |
| COLOR-LAB-37 | User: `fontos, hogy az app kódjából legyen a design valós színekkel, valós méretekkel, valós layouttal` | Prototype CSS/layout constants and alternate markup | The alternative section reuses the same CSS variables, component classes, source-derived dimensions, AppColors, CategoryColorManager colors, and tappable component targets as the current design | Source inspection + static assertions | DONE |
| COLOR-LAB-38 | User: `az új layout ... alatt a színpaletta tagolva legyen. felső sor app színek ... alatta colour slot színek` | Alternative palette markup | The alternative section has a dedicated structured palette below it: first row/section is app white/gray colors and known app shades, followed by a separate colour slot row/section containing CategoryColorManager slot colors | Static assertions + browser/manual inspection | DONE |
| COLOR-LAB-39 | User: `fehér ikonokat is rakj a logbox avatarokba` | Home and alternative home logbox avatars | Every logbox avatar in both the legacy and alternative home screens contains an explicit white SVG-mask icon marked as a logbox avatar icon, while keeping the existing avatar slot colors and 46px/28px sizing | Static assertions + browser/manual inspection | DONE |
| COLOR-LAB-40 | User: `még mindig nem raktad be a random fehér ikonokat, amik az appban vannak, a színes kategóriakörök, a vendorkörök, a logboxoknak kell ikon` | All colored avatar circles and HTTP serving | Every colored category circle, vendor circle, logbox avatar, and add-sheet category circle in both legacy and alternative sections contains a white app SVG-mask icon from `assets/icons/lucide`; icon URLs use repo-root `/assets/...` paths so the served prototype loads them in the browser | Static assertions + HTTP asset fetch | DONE |
| COLOR-LAB-41 | User: `először a colour slotokat fogod áttervezni. gradiens kell. emulated 0 spendee layout avatar colour gradient képet analizáld` | Lower Spendee gradient colour slot tokens and palette row | The lower/Spendee colour slot palette uses 21 gradient tokens derived from `/storage/emulated/0/spendee/layout  avatar colour gradient.png`, replacing only the lower flat slot swatches with Spendee-style multi-stop gradients | Reference image inspection + static assertions | DONE |
| COLOR-LAB-42 | User: `ott láthatod hogy a logboxok avatarjainak gradiense van, tervezd át a slot színeket ezek alapján` | Lower Spendee avatar preview colors | Lower/Spendee logbox, vendor, and category avatar preview circles use the new gradient slot tokens as their background source, while keeping white SVG icons visible on top | Static assertions + browser/manual inspection | DONE |
| COLOR-LAB-43 | User: `oldd meg, hogy külön szímezhetőek legyenek a tapre a vendorboxok avatar köre, a logboxok avatar köre, és a kategorycardok avatar köre` | Lower Spendee palette interaction target mapping | Lower/Spendee vendor card avatar circles, logbox avatar circles, and category card avatar circles are separate tappable recolor target groups with separate CSS variables, so applying a selected swatch to one group does not recolor the other two groups | Static JS/DOM assertions + browser/manual interaction | DONE |
| COLOR-LAB-44 | User correction: `a felső stekció az eredeti, amhez nem nyúlsz... alul van az új... az alsó szekció lesz a "spendee"... az alsó 21 színslotba 21 gradienst akarok látni` | Legacy/Spendee section boundary | The upper legacy section does not contain Spendee gradient slot references or the new avatar-circle recolor targets; the lower section is labelled Spendee and contains exactly 21 gradient slot swatches | Static assertions + browser/manual inspection | DONE |
| COLOR-LAB-45 | User: `g8 i7 l8 nagyon hasonló, jobban el kell különíteni` | Lower Spendee gradient slot differentiation | Rendered slots `I7`, `G8`, and `L8` map to separate Spendee gradient tokens and are visually separated into different colour families instead of three similar warm red/orange/pink gradients | Static slot-map assertions + browser/manual inspection | DONE |
| COLOR-LAB-46 | User correction: `különítsd el, az összes slotot... a slotokat 1-21 átnevezed... ami színt már használsz valahol, máshol nem használhatod` | Lower Spendee slot numbering and gradient isolation | The lower/Spendee 21 colour slots are visibly fixed as `1` through `21`, are excluded from the automatic A1/P grid relabeler, each gradient token uses unique hex stop colors, and no gradient is an exact duplicate or inverse of another slot | Static slot-number/gradient uniqueness assertions + browser/manual inspection | DONE |
| COLOR-LAB-47 | User correction: `1/15 majdnem ugyanaz, 19 csúnya más legyen helyette, 10/4 majdnem ugyanaz, 6/20 majdnem ugyanaz. a paletta 3 sor 7 oszlop elrendezésben legyen, és szivárvány szerint nőjenek` | Lower Spendee rainbow grid and pair separation | The lower/Spendee slot palette is a fixed 3×7 grid ordered by increasing rainbow hue from slot 1 to 21; pairs 1/15, 4/10, and 6/20 are separated into different hue families; slot 19 is replaced with a non-brown/non-ember rainbow-family gradient | Static CSS/layout/hue assertions + browser/manual inspection | DONE |
| COLOR-LAB-48 | User: `ez jó, azonban rajzold vissza a korábbi palettát is, új komponensként a slotok alá, ezekkel is szeretném tesztelni, a jelenlegi jó, ez alá rajzold új palettaként` | Restored previous Spendee test palette | The current 3×7 rainbow slot palette remains unchanged, and directly below it a new separate previous-palette component renders the earlier 21 gradient slots as tappable swatches using separate `--previous-slot-gradient-*` tokens | Static DOM/token assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-49 | User: `a bevétel kiadás gombok legyenek izoláltan külön színezhetők, a searchpill, és a summarypill is` | Home pill target isolation | In both legacy and lower Spendee home panels, the `Bevétel` button, `Kiadás` button, home summary pill, and home transaction search pill each use their own `data-color-target` and CSS variable so recoloring one does not recolor the others or generic text pills | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-50 | User: `legyen a fabhoz hasonló kékkel kombinált gradiens sor, csinálj többet. pl lilával türkizzel kombinált verzió is legyen, ez újabb paletta sor lesz legalul` | Bottom FAB-blue gradient palette row | A new bottom-most palette section renders multiple FAB-primary-blue combined gradient swatches, including purple/turquoise combinations, using separate `--fab-blue-gradient-*` tokens without changing the current rainbow or previous slot palettes | Static DOM/token assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-51 | User: `legyenek rózsaszín árnyalatok is` | Bottom FAB-blue/pink gradient variants | The bottom FAB-blue gradient palette is extended with pink/rose shade variants while keeping the existing blue, turquoise, purple and purple/turquoise options | Static DOM/token assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-52 | User: `átalakítod a header cardot. emulated 0 spendee dashboard. png... header egy színes kártya üvegszerű gradiens glow-val... alja megegyezik a régi header card aljával... felette ... logo ... spendee, your personal financial trainer` | Lower Spendee dashboard header redesign | Only the lower Spendee section uses the dashboard-derived inset glass gradient header card; its bottom aligns with the old 188px header bottom, its top is calculated at 84px with 104px height, and the free area above it contains the source Spendee logo plus title/tagline from `/storage/emulated/0/spendee/final_spendeevector.svg` | Reference inspection + static CSS/DOM assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-53 | User: `fontos, hogy a felső szekció ne változzon, csak az alsó, a felső az eredeti layout lesz, mert az lesz a viszonyítási alap` | Legacy section protection for Spendee redesign | The upper/legacy design remains the original layout and does not receive the new Spendee dashboard header, logo lockup, Spendee class names, or lower-section gradient/avatar target changes | Static section-boundary assertions + browser inspection | DONE |
| COLOR-LAB-54 | User: `a legelső színpalettát is rakd vissza... egy új palettaként` | Restored first-pass Spendee palette | A separate palette row restores the first generated 21 Spendee gradient slot set, using `--original-slot-gradient-*` tokens and tappable `original-colour-slots` swatches, placed above the bottom FAB-blue row | Static DOM/token assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-55 | User: `a színpaletta mindegyik szín szélessége annyi legyen mint a kis színslotoké... a színslotok jók` | Palette swatch fixed sizing | Every palette swatch across all palette rows uses the same fixed 38px width/height as the accepted small color slots instead of stretching to fill the wide palette area | Static CSS assertions + browser inspection | DONE |
| COLOR-LAB-56 | User: `nem lehet a bevétel kiadás gombokat színezni, oldd meg` | Lower Spendee type pill CSS cascade | The lower Spendee `Bevétel` and `Kiadás` buttons visibly read `--spendee-income-button-bg` and `--spendee-expense-button-bg` through selectors specific enough to beat the legacy/generic type-pill rules | Root-cause CSS inspection + static cascade assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-57 | User: `csökkentsd a magasságukat... a logo legyen 20% nagyobb... lejjebb tolja alatta a contentet` | Lower Spendee logo/header/content spacing | The lower Spendee logo is 20% larger than the prior 46px asset size, the Spendee header/content stack moves down, and the type pill height is reduced so the following content moves down only moderately | Reference inspection + static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-58 | User: `a tranzakciós kártyáknak, a searchpill, és summarypillnek olyan borderje/minimális árnyéka legyen, mint a referenciaképen, talán borderje nincs, csak minimális árnyéka` | Lower Spendee soft card surfaces | The lower Spendee summary pill, search pill, and logbox transaction cards use borderless white card surfaces with minimal soft shadow matching the dashboard reference direction | Reference inspection + static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-59 | User: `a logboxok magasságát csökkentsd 10%-al, de a köztük a paddingot ne` | Lower Spendee logbox compact height | Lower Spendee logboxes are 64.8px high, exactly 10% lower than the source 72px height, while the existing 4px vertical spacing between cards is preserved | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-60 | User: ongoing boundary: upper section remains comparison baseline | Legacy section protection for the new spacing pass | The upper legacy section keeps the original header/content/type-pill/logbox sizing and does not receive the lower Spendee spacing or soft-card overrides | Static section-boundary assertions | DONE |
| COLOR-LAB-61 | User: `a logo legyen 20% nagyobb` and latest screenshot correction | Lower Spendee brand lockup sizing | The lower Spendee logo is now enlarged to 79.5px, and the lower Spendee header/content tokens are adjusted without changing legacy sizing | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-62 | User: `a flirat a logboxokban a betűtípus egyezzen a spendee betűkkel` | Lower Spendee logbox typography | Lower Spendee logbox title, meta, amount and time typography use the same dark Spendee text color, tight letter spacing and stronger weights as the Spendee lockup direction | Static CSS assertions + browser inspection | DONE |
| COLOR-LAB-63 | User: `nem kapták meg a logboxok a kért kicsi árnyékot... referencia képen nem ezt látod` | Lower Spendee reference-like logbox surface | Lower Spendee logboxes are forced to white borderless cards and use a slightly wider, soft two-layer reference-like shadow rather than the previous weak shadow token | Reference inspection + static CSS assertions + browser inspection | DONE |
| COLOR-LAB-64 | User: `a header cardnak legyen 1 px borderje is fehér`; previous: `bevétel kiadás gomboknak se legyen borderje` | Lower Spendee header/button borders | Lower Spendee header card has a 1px white border per latest correction, while the income/expense buttons remain borderless and read their independent color variables | Static CSS assertions + browser inspection | DONE |
| COLOR-LAB-65 | User: `a header card glowja automatikus legyen... a glow a header cardba rakott színből jön` | Lower Spendee automatic header glow | Palette selection alone does not change any background/glow; only applying a color to the Spendee header card updates the projected glow from that header color/gradient | Static JS/CSS assertions + browser interaction inspection | DONE |
| COLOR-LAB-66 | User: `a corner radius a summary pillnek, és a logboxoknak, illetve a header cardnak olyan legyen mint a referencia képen, ott kisebb az ív` | Lower Spendee smaller corner radii | Lower Spendee header card, summary/search pill and logbox radii are reduced to reference-like smaller values without changing upper legacy component radii | Reference inspection + static CSS assertions + browser inspection | DONE |
| COLOR-LAB-67 | User: `ha egy színre tappelek változik a háttér, illetve nagyon laggol... nem tudom a háttér színét állítani külön` | Palette selection side effects and background isolation | Tapping a palette swatch only selects it and never changes Spendee background/glow; the lower Spendee screen background reads the normal `--app-bg` app-background variable so it can be changed only by tapping the app background target | Static JS/CSS assertions + browser interaction inspection | DONE |
| COLOR-LAB-68 | User: `a logó nem lett nagyobb... rengeteg hely van a logo és a header card között` | Lower Spendee logo and header gap | The lower Spendee logo is increased to 79.5px, the brand lockup is moved upward, and the header card is pulled up to reduce the excessive gap seen in the latest screenshot | Screenshot inspection + static CSS assertions + browser inspection | DONE |
| COLOR-LAB-69 | User: `a referencia képről a bottom nav designt is állítsd be... rounded corner box komponens a háttéren, mint a summarypill` | Lower Spendee floating bottom nav | The lower Spendee home bottom nav is a floating rounded white card on the background, with soft shadow and rounded active item, not a full-width bottom strip | Reference inspection + static CSS/DOM assertions + browser inspection | DONE |
| COLOR-LAB-70 | User: `utolsó screenshot... van egy hardcoded glow is a háttéren... ekkora legyen a header card glow` | Lower Spendee header glow sizing/source | The hardcoded cyan/purple screen background glow layers are removed, and the automatic header-card-derived glow is enlarged to the former hardcoded glow scale behind the header | Latest screenshot inspection + static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-71 | Prior user instruction: `a bottom nav meg olyan széles legyen mint a logboxok`; superseded by COLOR-LAB-73 | Lower Spendee bottom nav width history | The earlier same-width requirement is explicitly superseded by the latest screenshot correction, which requires the nav corners to sit outside the logbox column | Checklist cross-reference + COLOR-LAB-73 static assertion | DONE |
| COLOR-LAB-72 | User: `a glowot meg tudod csinálni, hogy belülről kifele csökken az opacity? túl erős` | Lower Spendee header glow falloff | The automatic header-card glow keeps its large footprint but uses a radial opacity mask so it fades from inside to outside, with reduced total opacity to avoid overpowering the screen | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-73 | User: `bottom nav legyen kicsit szélesebb, hogy az ív kijjebb essen, hogy a logboxok a fehér bottom navból jöjjenek annak felső oldalából` | Lower Spendee bottom nav corner bleed | The lower Spendee bottom nav is wider than the 20px-gutter logboxes, with its 24px rounded corners shifted outside the transaction column so the last logbox visually emerges from the nav card's white top edge instead of the gray background | Latest screenshot inspection + static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-74 | User: `a glow ne ilyen hamar halványuljon` | Lower Spendee header glow slower falloff | The automatic header-card glow keeps the same overall opacity and footprint but its radial mask fades more slowly, with the final transparent stop pushed from 78% to 100% so the glow remains visible farther outward | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-75 | Prior user correction: `a status bar magasságában már 0 opacity kell, illetve ne csak felfele legyen, hanem minden irányba egyformán`; refined by COLOR-LAB-79 | Lower Spendee header glow symmetry and status clipping history | The no-status-bar-glow and all-direction-fade requirement is now fulfilled by making the glow layer itself fade from the header edge to the outer glow bounds, without a separate status cover layer | Static CSS assertions + COLOR-LAB-79 cross-reference | DONE |
| COLOR-LAB-76 | User correction: `bottom nav túl széles, legyen padding a screen széle és a nav közt, és ne 4 gombos legyen hanem 3, mint az app` | Lower Spendee bottom nav padding and app tab count | The lower Spendee bottom nav has visible 8px side padding from the screen edge, remains a floating rounded card, and renders the three app-built items: Főoldal, Stats, Beállítások, without Értesítések | Flutter source inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-77 | Prior correction: `a status bár magasságánál éles a határ ... átmenetes legyen, ott már nullának kell lennie az opacitynek`; superseded by COLOR-LAB-79 | Lower Spendee status glow soft clip history | The separate suppressing layer approach is explicitly superseded because it still created an edge; the active solution is a glow-native opacity mask from header edge to glow end | Latest screenshot inspection + COLOR-LAB-79 cross-reference | DONE |
| COLOR-LAB-78 | User correction: `a spendee ikont és feliratot helyezd lejjebb kicsivel` | Lower Spendee brand lockup vertical adjustment | The lower Spendee logo/title lockup moves slightly downward from 26px to 34px while keeping the same logo size and lower-section-only scope | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-79 | Misstated/interrupted interpretation: `100% a header széle, 0% a jelenlegi glow vége`; superseded by COLOR-LAB-80 | Lower Spendee glow-native edge fade history | The header-edge-to-outer-edge interpretation was rejected before completion; the active requirement is status-bar-bottom 0% to current visible glow edge 100% | Checklist cross-reference | DONE |
| COLOR-LAB-80 | User correction: `jelenlegi glow széle... 100%-nak, és a statusz bar alja... 0%-nak... fade outot akarok` | Lower Spendee status-to-glow fade | The glow uses no separate cover layer; the glow layer itself is masked from 0 opacity at the status-bar bottom to the current visible glow strength at the existing glow edge, then keeps the established radial falloff | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-81 | User: `a logó és felirat kerüljön lejjebb, azaz csökennyen alattuk a padding` | Lower Spendee brand lockup lower position | The lower Spendee logo/title lockup moves further down from 34px to 42px, reducing the gap below the oversized logo before the header card while keeping logo size, header position, and upper legacy section unchanged | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-82 | User: `a mágnescsík kikerült, az egész header lesz a csík, dinamikus reaktív színezést fog kapni` | Lower Spendee reactive header strip | The lower Spendee header card remains the full reactive color surface and is live-updated as a gradient; the lower Spendee section does not reintroduce a separate magnet strip | Static DOM/CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-83 | User: `rajzolsz a paletta fölé de a screen alá egy csíkot... szélessége dupla screen szélességű` | Balance/header scale lab placement and size | A dedicated balance/header scale lab is rendered below the lower Spendee phone screens and above the Spendee palette, with a horizontal track exactly twice the app screen width | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-84 | Prior user instruction: `a színskála spendee colour slot gradiensek legyenek 1-9-ig...`; superseded by COLOR-LAB-87 | Balance/header scale color source history | The original 9-color scale is superseded by the corrected 10-color scale while keeping the same red-to-green neutral-yellow intent | Checklist cross-reference | DONE |
| COLOR-LAB-85 | User: `van egy középpontja... két oldala... oldalai draggolhatóak... longtap után... szélességét állítja` | Balance/header scale window interaction | The scale contains a draggable center indicator that moves the window and two edge handles that, after long press, resize the window symmetrically around the center | Static JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-86 | User: `a header card pedig élőben lereagálja az állításokat` | Balance/header live header update | Dragging or resizing the balance window recomputes the sampled left/center/right colors and writes a live Spendee header gradient plus matching glow | Static JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-87 | User: `a lényeget érted, úgy skálázz, hogy 10 szín legyen` | Balance/header 10-color scale | The balance/header scale uses exactly 10 color segments, mapped to Spendee gradient slots 1 through 10, preserving the red → orange → yellow-neutral → green direction | Static DOM/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-88 | User: `mutasd` after category button design recommendation | Lower Spendee category button glass redesign | Only the lower Spendee header category/menu button is shown as a smaller translucent glass squircle with subtle dark bars, soft shadow, dedicated Spendee recolor variable, and no change to the upper legacy section | Static CSS/DOM assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-89 | User: `a 3 vonal ikon is legyen glossy... ne fekete legyen... szürke glowja... piszkosnak hat` | Lower Spendee glossy menu bars and clean glass shadow | The lower Spendee three-line menu icon uses light glossy raised-glass ridges with a fine highlight, and the menu button removes the previous gray external glow so the header gradient below stays clean | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-90 | User: `kategória gomb 20% kisebb legyen` | Lower Spendee category button 20% smaller | The lower Spendee header category/menu glass button shrinks from the previous 42px to 33.6px, with proportional radius and icon bar spacing, while legacy buttons stay unchanged | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-91 | User: `nem jó a zöld skála, utolsó szín világosabbnak hat mint az előtte lévő` | Balance green endpoint correction | The red-to-green balance scale uses a dedicated 10-stop scale where the last green endpoint is darker/more saturated than the previous green stop in both CSS and JS sampling | Static CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-92 | User: `a limiteknek is hasonló skálát rajzolj fel, a piros zöld alá, ugyanilyen reaktív csúszka, csak ez lila-rózsaszín legyen` | Limits purple-pink reactive scale | A second limits scale is drawn directly under the red-to-green balance scale, uses 10 purple-to-pink stops, and has the same draggable center/window and long-press symmetric resize interaction with its own readout/preview | Static DOM/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-93 | User: `nem színez a lila skála, ha az alsó csúszkát húzom, akkor az színezzen, ha a felsőt akkor az` | Active scale drives header color | Dragging the upper balance slider writes the red-green header gradient/glow; dragging the lower limits slider writes the purple-pink header gradient/glow, and each drag uses its own slider state | Static JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-94 | User: `spendee ikon és felirat kicsit kerüljön lejjebb` | Lower Spendee brand lockup lowered slightly | The lower Spendee logo/title lockup moves slightly lower by changing `--spendee-brand-top` from 42px to 48px without changing the upper legacy section | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-95 | User correction: `rosszul mondtam a színezést: bal oldalt lila- halványodik - halványrózsaszín középen- rózsaszín erősödik... jobb szélen legerősebb rózsaszín` | Limits scale corrected color progression | The limits scale and JS sampler progress from purple on the left through pale pink at the center to the strongest pink on the right | Static CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-96 | User correction: `log area legyen magasabban... log window magassága nő (alja továbbra is a bottom menu teteje marad)` | Lower Spendee log window raised | Only the lower Spendee log area starts higher via a smaller top value and reduced date-header top padding, while the bottom/inherited legacy baseline is not moved | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-97 | User: `balance összegre mit javasolsz... világos és sötét színeken is jól látható legyen... anélkül hogy saját pillbe raknád` + `mutasd` | Lower Spendee adaptive balance ink | The lower Spendee balance amount stays text-only, with no separate pill, and automatically switches dark/light ink, shadow and micro-stroke from the active header slider center color so it remains readable on light and dark gradients | Static CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-98 | User correction: `maradjon fekete, ne váltson színt, de a fehér árnyék jó körülötte` | Lower Spendee fixed dark balance ink | The lower Spendee balance amount and visibility icon stay fixed dark/navy on every header color, with the white shadow and micro-stroke retained for readability, and no luminance-based white text switching | Static CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-99 | User: `a lila-rózsa skála 2 és 9 közt legyen, azaz a két szélsőt elveted, és a 2-9 közt alakítasz ki 10 színes skálát` | Limits 2-9 rescaled purple-pink stops | The lower limits scale discards the previous outer extremes and redraws a 10-stop purple-to-pink scale inside the previous stop-2 through stop-9 range, keeping the pale pink center and stronger pink right edge | Static CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-100 | User: `az ablak szélességet lehessen szövegesen is beírni, ne csak sliderrel` | Reactive scale window numeric width input | Each lower reactive scale exposes a numeric window-width percentage input that writes through the same clamped state path as dragging/resizing, so balance, limits and opacity windows can be set by text as well as the slider | Static DOM/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-101 | User: `legyen egy 3. skála is, ezzel opacityt lehet állítani` | Third opacity reactive scale | A third reactive scale is drawn under the limits scale, uses the same center/window/input controller, and live-writes the lower Spendee header opacity variable so opacity can be tuned interactively | Static DOM/CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-102 | User correction: `a kategória gombnak, a balance feliratnak, és a balance összegnek ne változzon az opacityje, ez csak a háttére vonatkozzon a headerben` | Header opacity applies to background layer only | The opacity scale affects only the lower Spendee header background/glass layer, while the category/menu button, balance label, balance amount and visibility icon remain fully opaque above that layer | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-103 | User: `a skálák szélessége és a magassága is fele akkora legyen` | Reactive scale lab half-size tracks | The lower balance, limits and opacity scale tracks are half their previous width and height, with the window, center marker and edge handles reduced proportionally so the controls fit the smaller tracks | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-104 | User: `az egyenleg felirató pillbis üveg legyen` | Lower Spendee balance label glass pill | The lower Spendee `Egyenleg` label pill uses the same glass language as the header controls: translucent fill, white border, inner highlight and blur, while remaining fully opaque above the header background opacity layer | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-105 | User: `nem kell szöveges felirat a slider boxokba feleslegesen magasak` | Reactive slider window labels removed | The lower balance, limits and opacity slider window boxes contain no internal text labels (`header window`, `limits window`, `opacity window`), leaving only the handles/center marker on the compact track | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-106 | User: `a pill üveges lett, de zavaró, hogy a felirat színes, olyan legyen mint a dombornyomás az üvegen` | Embossed glass balance label text | The lower Spendee `Egyenleg` glass pill no longer uses colored teal text/dot; its text and dot use a subtle white embossed glass effect with highlight/shadow | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-107 | User: `a tranzakciós ablak, még lehet kicsit magasabb, van padding elég a tranzakciók felirat és a dátum közt` | Lower Spendee transaction window slightly taller | The lower Spendee log area starts slightly higher so the transaction list window grows while the bottom remains aligned above the bottom nav | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-108 | User correction: `nem jó, nehéz látni, legyen sötétszürke` | Balance label dark gray readability | The lower Spendee `Egyenleg` glass pill keeps its glass background but uses dark gray text and dot for readability instead of the low-contrast white embossed variant | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-109 | User + latest screenshot `Screenshot_20260712-191155.png`: `felesleges a szöveg... túl nagy a terület amit foglal... szedd ki... a skála magasságát is felezd meg` | Compact textless reactive scale lab | The lower reactive scale lab removes the visible title, explanatory paragraph, readout and `WINDOW %` text from all three scales, keeps only compact numeric width inputs, and halves the track height again from 48px to 24px with proportionally smaller handles/center markers so the app stays visible while tuning | Latest screenshot inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-110 | User approved recommendation: `ok` after `Egyenleg` pill replaced by a small balance vs budget & limits/backheader icon button | Lower Spendee backheader icon button | The lower Spendee header removes the `Egyenleg` label pill and uses a small category-button-like glass icon button on the left to trigger the balance vs budget/limits backheader, with no text label and a finance icon | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-111 | User: `nem tetszik, ha halvány a háttér, akkor alig látszik, mit javasolsz még?` + approved `mutasd` | Lower Spendee custom dark stacked-card backheader icon | The lower backheader toggle keeps the glass button but replaces the low-contrast piggy icon with a custom dark stacked-card/header-layer icon that stays visible on pale header gradients | Static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-112 | User: `az opacity beállítás ne ablakszerű legyen, csak egy kis handle kell rá` | Opacity scale single handle | The opacity control is no longer a window with two edges or a typed window width; it is a single draggable handle on the opacity scale that writes the header opacity from its center position | Static DOM/JS/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-113 | User: `stage 0- lefele vagy felfele swipe -stage 1... stage2 kizárólag ikontappel triggerelhető... most swipeot nem akarok modellezni a html-ben` | Lower Spendee stage model documentation/prototype | The lower prototype explicitly represents the three-state model without swipe simulation: normal header, backheader icon-list state, and focused inline limit state; tap is used only to enter/leave the focus mockup | Static DOM/JS assertions + browser/manual inspection | DONE |
| COLOR-LAB-114 | User: `a jelenlegi főmenüt duplikálod... mellé beraksz még egy screent, de itt már a backheader lesz, nem a header, ehhez kinézed, a kódból hogyan kell kinéznie` | Lower Spendee duplicate backheader screen | A new lower screen is placed next to the current alternative main menu, duplicates the main-menu shell, and replaces the normal header with a code-informed colored backheader using the app's center badge/category budget structure | Source inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-115 | User: `ez nem színezódik a skálával, itt user select van, mert ez a kategória színe` | Backheader category-color isolation | The new backheader background uses its own category-color CSS variables and is not targeted by the balance/limits header color scale variables | Static CSS/JS assertions + browser/manual inspection | DONE |
| COLOR-LAB-116 | User: `de ez alá is kell egy háttér opacity slider` | Backheader background opacity slider | A dedicated backheader background opacity slider is rendered under the new backheader screen and changes only the backheader background opacity, not the normal header or text/icon layers | Static DOM/JS/CSS assertions + browser interaction inspection | DONE |
| COLOR-LAB-117 | User: `oldd meg, hogy a középső ikon tappelhető legyen, ezzel tudok belépni a focus modeba` | Center badge tap enters focus mode | The center category badge in the backheader icon-list mockup is tappable and switches the duplicated screen to the inline focused category state | Static DOM/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-118 | User: `stage2-ből stage1-be kell egy kis chevron... bal fentre... kis üveg gomb` | Focus-mode glass back chevron | The focused inline limit state has a small top-left glass chevron button that returns to the backheader icon-list state | Static DOM/CSS/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-119 | Prior user instruction: `részei legyenek: ... vízszintes progress bar mockup ...`; superseded by COLOR-LAB-130 | Inline limit editor contents history | The earlier horizontal progress bar requirement is superseded by the latest correction: stage2 keeps the icon-ring progress only, plus spending amount, live limit pill, slider, reset/delete and back button | Checklist cross-reference + COLOR-LAB-130 static assertions | DONE |
| COLOR-LAB-120 | User: `ami az appban slide up sheet, itt inline lesz` | Slide-up limit editor translated inline | The prototype adapts the app's `BudgetTargetEditorSheet` semantics into the backheader surface: amount field, slider/partition progress, reset/delete affordance, and active category identity are visible inline | Source inspection + static DOM/CSS assertions | DONE |
| COLOR-LAB-121 | Prior user instruction: `a színes sliderek ablakait csak text beírással lehessen állítani, nem kell a drag oda`; superseded by COLOR-LAB-124 | Text-only colored scale windows history | The previous text-only center/window requirement is explicitly superseded by the latest correction: balance and limits window position is draggable again, while only the window width remains typed in the pill | Checklist cross-reference + COLOR-LAB-124 static assertions | DONE |
| COLOR-LAB-122 | User: `szedd ki a headerből bal felül a gombot, annak már nincs funkciója` | Remove obsolete lower header backheader toggle | The normal lower Spendee headers no longer render the obsolete top-left backheader toggle button, its `data-backheader-toggle` target, or its stacked-card CSS/variables; backheader access is represented by the separate duplicated backheader screen and center-icon focus interaction | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-123 | User: `az opacity slider, ami a backheadert állítja legyen a backheaderes screen alatt` | Backheader opacity control placement | The dedicated backheader opacity slider is scoped inside the A1B backheader screen column directly under the backheader phone screen, not after all duplicated screens or above unrelated scale labs | Static DOM ordering assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-124 | User: `a színes sliderek működését lehessen draggolni, csak a window szélességét lehessen a pillben állítani` | Colored scale drag/window input split | The balance and limits color-scale windows can be dragged along the track to move the center/window, while the only numeric pill input changes the window width; center-position text inputs are removed | Static DOM/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-125 | User: `a backheadert nem lehet színezni... explicit lehessen színezni, furcsán színez` | Explicit backheader category recolor | Applying a selected swatch to the backheader card writes only `--spendee-backheader-category-color`; the backheader gradient has no hardcoded secondary green and is isolated from normal header scale variables | Static CSS/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-126 | User: `most kiesett a backheader stage 2, az ikonon a tap nem hozza elő` | Backheader focus trigger regression | Tapping the center category icon always stops propagation and enters the inline focus/stage2 state, even when a palette swatch is selected | Static JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-127 | User: `oldd meg a brand ikont is: kapjon hasonló gradienst...` then `olvasd ki az svg pathokat... kártya kék, harang szürke körvonal ... zöld-narancs-rózsa gradiens, oszlopok világosszürkék` | Path-targeted Spendee SVG logo coloring | The local Spendee SVG logo keeps its real path geometry but recolors the card/ring paths blue, bell/detail paths with a green-orange-rose gradient plus gray outline, column paths light gray, and preserves the notification dot as a rose accent | SVG path inspection + static SVG assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-128 | Prior user instruction: `logo régi színét látom`; superseded by COLOR-LAB-134 | Logo cache-busted asset reference history | The cache-busted image approach is superseded by inline live SVG previews because the mock app icon must now update as the Logo Editor paths are recolored | Checklist cross-reference + COLOR-LAB-134 static assertions | DONE |
| COLOR-LAB-129 | User: `ha a backheader aktív, akkor annak látszon a glow-ja, ne a normál headernek` | Backheader-specific projected glow | The A1B backheader screen projects glow from `--spendee-backheader-category-color` through a separate `--spendee-backheader-glow` variable and CSS override, while normal Spendee screens continue to use `--spendee-header-glow` | Static CSS/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-130 | User: `ne legyen a stage 2-nek horzontal progress barja, csak a circle az ikon körül` | Stage2 icon-ring-only progress | The focused inline limit state removes the horizontal progress bar DOM/CSS and uses only the circular ring around the focused category icon as the progress visual | Static DOM/CSS assertions + browser inspection | DONE |
| COLOR-LAB-131 | User: `az egyenleg felé pedig legyen balance kiírva` | Lower Spendee balance label text | The lower normal Spendee headers show a small `Balance` label above the balance amount without restoring the old Hungarian `Egyenleg` pill/color target | Static DOM/CSS assertions + browser inspection | DONE |
| COLOR-LAB-132 | User: `latest screenshot, nem jó a logo szín. csinálj egy logo editort az appba... minden komponense külön tappelhető... komponens=path... legalul legyen a colour selectorok alatt, legalább olyan széles legyen mint a screen` | Lower Spendee path-level logo editor | A new bottom-most Logo Editor sits below all alternative colour selector rows, is at least one screen wide, loads the large Spendee SVG, makes each SVG path separately tappable, and applies the currently selected palette color/gradient to the tapped path | Latest screenshot inspection + static DOM/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-133 | User: `az ikonszerkesztőben a harangnak, és a harang feletti rezgés jelző ívnek ne legyen borderje` | Logo editor bell border removal | The editable Spendee SVG has no stroke/border on the bell path and the vibration-arc path, while leaving the paths separately tappable/recolorable | Static SVG assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-134 | User: `az appban a mock ikon élőben kövesse le, ahogy szerkesztem` | Live mock logo preview sync | All five lower Spendee mock logo instances are inline live SVG previews cloned from the same source paths, and recoloring a path in the Logo Editor immediately syncs the matching path in every mock app logo | Static DOM/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-135 | User: `a subheader ikon avatarok glossy legyenek mint a category gomb` | Glossy subheader avatar buttons | Backheader/subheader category avatar circles use the same glossy glass styling language as the category menu button: translucent fill, white border, inset highlights, blur, and a small highlight sheen | Static CSS assertions + browser inspection | DONE |
| COLOR-LAB-136 | Latest screenshot + user: `valami van a belsejükben a korongoknak, egy fehér folt, az felesleges` | Remove internal white avatar spot | Backheader/subheader glossy avatar circles keep their glass border/inset/blur styling but no longer draw an internal white pseudo-element spot over the icons | Latest screenshot inspection + static CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-137 | User: `az ikonban a harang nyelvének ne legyen borderje, jelenleg még van` | Logo bell clapper border removal | The Spendee logo bell clapper path has no stroke/border in the editable logo and all live mock previews | Static SVG assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-138 | User: `az előző circle progress bar rajta volt, az most is legyen a korongon ugyanúgy, a sliderrel lehessen interaktálni` | Stage2 icon-ring progress slider sync | Focus/stage2 keeps the circular progress ring on the category avatar, and the limit slider updates the ring live | Static DOM/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-139 | User: `nem sikerült a kuka gombot berakni rendesen... nincs rajta ikon, a kuka gomb is glossy legyen` | Stage2 glossy trash reset button | Focus/stage2 reset/delete is a visible glossy glass button with a trash icon, not an empty gray circle or text glyph | Static DOM/CSS assertions + browser inspection | DONE |
| COLOR-LAB-140 | User: `a limit input pillt szedd ki. helyette a jobb felső sarok mutassa: x/y összeg, aktuális/beállított limit, a slider ezt állítsa` | Stage2 limit summary replaces input pill | Focus/stage2 removes the numeric limit input pill; the top-right summary shows current spend / configured limit, and the slider changes the configured limit value | Static DOM/JS assertions + browser interaction inspection | DONE |
| COLOR-LAB-141 | User: `a category címe a stage 2-ben, és a slider a category avatar mellett legyen... ez a 2 content nem lehet nagyobb, mint a category avatar... nem raktad vissza a circle progress bart` | Stage2 avatar-adjacent compact controls and visible ring | Focus/stage2 groups the category title and slider in a compact stack beside the category avatar, no taller than the 58px avatar, and the circular progress ring is visibly layered around the avatar instead of clipped/hidden | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-142 | User correction: `nem értettél meg. a category avatar helye vertikálisan középen van, mellette jobbra a név és a slider, a kuka pedig a jobb alsó sarokban` | Stage2 centered avatar row with bottom-right trash | Focus/stage2 positions the category avatar at the vertical center of the backheader, places the title+slider stack immediately to its right on the same center line, and keeps the trash/reset button independently anchored in the bottom-right corner | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-143 | User correction: `az x/y összeg kerüljön vissza a jobb felső sarokba` | Stage2 top-right limit summary | Focus/stage2 keeps the current/limit `x/y` amount as an independent top-right element, separate from the centered category title+slider stack | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-144 | User: `a header alján legyen egy glossy handle... swipepal lefele expandálni` | Fastinfo header pull handle | Lower Spendee normal headers expose a glossy pull handle at the bottom edge to represent the future downward fastinfo expansion gesture | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-145 | User: `az expanzió hasonlóképp nézzen ki mint ... fastinfo.png ... újabb screent ... első, és a backheader közé` | Fastinfo expanded screen insertion | A new lower Spendee expanded-fastinfo screen is inserted directly between the main screen and the backheader screen, references `/storage/emulated/0/spendee/fastinfo.png`, and keeps the backheader screen after it | Reference image inspection + static DOM ordering assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-146 | User: `felső content a partition progress bar legyen, alatta pedig 3 box, mockup chartokkal` | Fastinfo overlay contents | The expanded fastinfo overlay contains a partition progress bar as its upper content and three glossy chart mockup cards beneath it | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-147 | Prior design choice after user question: `printeljen rá ... vagy lefele tolja...` superseded by COLOR-LAB-148 | Fastinfo overlay bounce model history | The earlier overlay/print-over model is explicitly superseded by the current expanded-header pushdown requirement | Checklist cross-reference | DONE |
| COLOR-LAB-148 | User correction: `nem új containert... a header container lesz magasabb... az alatta lévő contentet lejjebb tolja` | Fastinfo expanded header pushdown model | The fastinfo expanded state is a taller header container with the fastinfo content inside the expanded header area, and the type buttons, summary pill, search pill and logboxes are pushed down below it | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-149 | User correction: `a handle pedig egy csík legyen csak` | Fastinfo strip-only handle | The fastinfo handle is rendered as a simple thin strip on the header, not a separate glossy button/pill control | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-150 | User correction: `a fastinfo is 2 stage lesz... kell egy új screen... stage 2... második screennek` | Fastinfo two-stage screen order | The lower Spendee sequence becomes main screen → fastinfo stage 1 → fastinfo stage 2 → backheader, with stage 2 preserving the previously drawn full expanded fastinfo content | Static DOM ordering assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-151 | User correction: `1 tick... megakad... stage 1... alja a bevétel/kiadás pill alja... contentet lejjebb tolja` | Fastinfo stage 1 sticky pushdown height | Stage 1 is represented as a permanent/sticky expanded header state whose bottom aligns with the original income/expense pill bottom and pushes all below-header content downward | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-152 | User correction: `a stage 1 a progress bart tartalmazza csak` | Fastinfo stage 1 progress-only content | Stage 1 contains the partition/progress bar only and does not include the ratio row or three chart cards reserved for stage 2 | Static DOM assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-153 | User: `a partition progress bart próbáld rendesen az app kódjából kinézni` | App-code-based partition bar geometry | The fastinfo partition bar cites and follows `CategoryLimitPartitionBar`, `LimitAllocationManager`, and the magnet/orbit partition sizing logic from the app code instead of the prior generic 3-color mock bar | Source inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-154 | User: `5 kiadási kategória legyen benne, az össz költség 70 százalékát érték el 10-20-30-10 arányban` | Five-category 70% allocation model | The partition bar renders five expense categories chosen from Spendee color slots, with the macro allocation rhythm 10/20/30/10 totaling 70%; the final 10% macro segment is split into two 5% categories to satisfy the five-category requirement | Static DOM/data-fraction assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-155 | User: `mindegyik kategória csak félig van kiköltve (ez az appban max opacity vs kevesebb opacity)` | Used/remaining opacity split per category | Each category is split into a full-opacity used half and a lower-opacity remaining half using the app's used/remaining allocation model; the unallocated 30% remains neutral gray | Static DOM/CSS opacity assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-156 | User: `pill alakú legyen, ne strio` | Pill-shaped partition bar | The app-code-based fastinfo partition bar keeps the 5-category allocation logic but renders as a rounded pill with a continuous white outline, not as a square/full-bleed strip | Static CSS assertion + HTTP/browser inspection | DONE |
| COLOR-LAB-157 | User: `nagyon nyers, rajzold alá a legelső ötletedet összehasonlításbak` | First partition idea comparison bar | Under the current app-code-based fastinfo partition bar, a second comparison bar redraws the first soft 3-zone gradient concept so the raw app-like allocation can be compared against the original visual idea on the same colored header | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-158 | User: `ez az alacsonyabb csík kisebb borderrel jobb` | Lower-profile app allocation pill | The main app-code-based fastinfo allocation pill keeps the 5-category allocation data but adopts the lower comparison bar proportions: 9px height and a subtle 1px translucent white border instead of a taller/heavier outline | Static CSS assertion + HTTP/browser inspection | DONE |
| COLOR-LAB-159 | User: `szedd ki a korábbi mockup minta partition progress stripet, csak az aktuális maradjon` | Remove previous comparison partition strip | The earlier soft three-zone comparison strip is removed from CSS and DOM; only the current app-code-based category-limit partition pill remains in fastinfo/backheader contexts | Static DOM/CSS negative assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-160 | User: `duplikáld akkor a backheaderes mockupot, de az új verzió már magasabb legyen` | Higher duplicated backheader mockup | The existing A1D backheader remains unchanged as reference, and a new A1E duplicated backheader screen is inserted after it with a taller backheader card; the extra lower area's content is defined by the latest category status shelf decision below | Static DOM ordering/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-161 | User: `ugyanúgy lehessen tapelni focus modera` | Scoped focus mode for multiple backheaders | Both backheader mockups can enter and leave inline focus mode via their own center icon/back chevron without cross-triggering the other card | Static JS/controller assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-162 | User selected proposal `1` after: `ne ugyanaz legyen ott az extended backheaderben, akkor mi?... ötletek?` | A1E category status shelf | The A1E extended backheader browse lower area replaces the duplicated partition pill with three glossy selected-category chips: spent percent, remaining budget, and daily budget | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-163 | User selected proposal `1` focus-mode consequence | A1E focus editor-feedback shelf | A1E focus mode no longer leaves the extended area empty; it shows three editor-feedback chips for new limit, remaining budget, and daily budget, updated from the inline limit slider | Static DOM/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-164 | User correction: `nem kell pill, csak annyi kell, hogy elköltve x %, maradt y ft` | A1E plain text status row | The latest A1E extended/backheader focus status removes chip/pill styling and daily/new-limit text; it shows only plain text `Elköltve X%` and `Maradt Y Ft`, with focus values updated from the inline slider | Static DOM/CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-165 | User approved no-limit fastinfo idea: `ok, csinálj egy olyan mockupot is, azaz duplikáld ismét` | Fastinfo stage 1 no-limit duplicate | A duplicated fastinfo stage 1 screen is inserted next to the active-limit stage 1; it keeps the same sticky pushdown header/home shell but replaces the category-limit progress bar with a plain empty state: `Nincs kategórialimit beállítva` and `Állíts be limiteket kategóriákhoz` | Static DOM/CSS negative/positive assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-166 | User approved A2 fullscreen: `ok, rajzold át akkor, mivel fullscreen, már lehet benne searchpill` and reference `/storage/emulated/0/spendee/category.png` | A2 fullscreen category selector route | The lower A2 category selector is redesigned from bottom sheet to fullscreen modal route, includes a search pill, keeps selector-only semantics, and does not render `sheet-card`/`sheet-panel` in the A2 block | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-167 | User reference: `android emulates 0 spendee category.png, innen kell a kártyákat lenézni` | A2 compact reference category cards | A2 cards follow the reference layout: compact 2-column cards, left drag dots, small glossy avatar, title + transaction count, colored dot, and right selected/unselected check circle; bulk/add actions follow the later selector-control requirements | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-168 | User clarified A2 semantics: selector only, dashboard performs filtering/listing | A2 apply selector footer | A2 keeps only selector actions: multi-select cards, `Új kategória` trigger, and sticky footer with selected count + `Alkalmaz`; no settings/filter icon or category-management sheet nesting | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-169 | User correction: `töltsd ki az egész elképzelt képernyőt kategóriakártyákkal` | A2 full-height category grid density | The fullscreen A2 selector fills the available phone viewport with a denser 2-column grid of at least 16 compact category cards, not just the first 8 reference cards | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-170 | User correction: `a felső gombokat nem jól tagoltad. ninvs padding` | A2 segmented top action row | The top selector action controls are separated like independent reference cards, with explicit row padding, internal padding, gap, and no visually merged edges | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-171 | User correction: `a hátteret, a kártyákat, és az avatarokat, és az alsó gombot is lehessen színezni` | A2 independent color targets | A2 exposes explicit tap recolor targets and CSS variables for selector background, category cards, category avatars, and the bottom apply button; avatar/card/button recoloring is not blocked by inline fallback colors | Static DOM/CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-172 | User correction: `mindet kijelölni és egyiket se opció legyen` | A2 bulk select controls | The A2 selector replaces the ambiguous `Minden kategória` selected card with two explicit bulk action cards: `Mindet kijelölni` and `Egyiket se`; the `Új kategória` trigger remains available as a separate dashed add row | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-173 | User correction: `nem kell a kártyákra drag handle, extra pötty se a tranzakciószám után` | A2 clean category card metadata | A2 category cards remove the left drag handle dots and the color dot after the transaction count; the card layout becomes avatar + title/count + check only | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-174 | User correction: `zavaró, hogy a1d től, a canvas sötét, az sem conzisztens` | Light page canvas continuation | The prototype page/canvas background remains light and consistent beyond A1D; the dark body fallback no longer appears behind later screen columns or horizontal scroll areas | Static CSS assertion + HTTP/browser inspection | DONE |
| COLOR-LAB-175 | User correction: `longtap marad, jelenleg is az` | A2 category edit trigger model | A2 category cards explicitly model editing as long-press only, so normal tap remains selection and the edit affordance does not add visible per-card clutter | Static DOM assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-176 | User correction: `az új category card is színezhető legyen` | A2 add-category recolorability | The `Új kategória` add card is recolorable as a card surface and its plus avatar circle is separately recolorable through the category avatar target | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-177 | User correction: `a searchpillben pedig szöveg keresés a kategóriák közt legyen ugyanazzal a betűszínnel mint a dashboard searchpillben` | A2/A3 selector search placeholder style | Category and vendor fullscreen selector search pills use dashboard-style placeholder text and the same placeholder color/typography as the dashboard search pill | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-178 | User correction: `ez alapján tervezd át a vendor képernyőt... ott nincs új vendor, viszont abc rend szerint vannak rendezve` | A3 fullscreen ABC vendor selector | A3 is redesigned from the old vendor sheet into a fullscreen vendor selector matching A2 structure: search, bulk select/none, compact recolorable vendor cards grouped alphabetically, no `Új vendor`/add row, and sticky apply footer | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-179 | User correction: `stage1 backheader: ikonlista tap a fókiszhoz szöveget szedd ki, szedd ki a stage2-ből is` | Backheader helper text removal | A1D/A1E backheader browse/focus states no longer render the secondary helper text `ikonlista · tap a fókuszhoz`; only the category name/status content remains | Static DOM assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-180 | User correction: `stage2-ből szedd ki a törlés gombot, ha 0 lesz, a slider, az lesz a törlés, akkor no limit van` | Inline limit zero-is-no-limit model | Backheader focus/stage2 removes the trash/delete button entirely; the range slider keeps `min=0`, and JS treats value `0` as `No limit`/delete state in the summary and feedback | Static DOM/CSS/JS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-181 | User correction: `a vendor list felső szekciója egyezzen a category menuvel... sem a searchpill mérete sem a tájolás` | A3 vendor top-section parity with A2 | A3 vendor selector header/search/action top section uses the same dimensions, margins, orientation and placement as A2 category selector; vendor search is no longer sized/oriented by the legacy vendor sheet CSS | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-182 | User: `keresd ki az appból az addnewcategory menut, a konkrét kód alapján tervezd meg a fullscreen adnewcategory menut` + source `CategoryEditorPanel`/`CategoryEditorSheet` | A4 app-code-based fullscreen AddNewCategory route | A new lower A4 screen adapts the real Flutter `CategoryEditorPanel` add flow into a fullscreen route: title, name pill, color slots, icon slots, preview pill and save action, with no sheet/grabber/toggle-page structure | Source inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-183 | User: `mivel fullscreen, itt elf3r egymás alatt az ikon és színslot szekció` + source `CategorySlotGrid` | A4 stacked color and icon slot grids | The fullscreen AddNewCategory editor renders the 21 real color slots and 21 real icon slots as two separate one-under-another sections using the app's 7-column slot-grid logic | Static DOM slot-count/order assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-184 | User: `ezt is rakd a vendor menu után` | A4 placement after vendor menu | The fullscreen AddNewCategory screen is inserted directly after the A3 vendor selector and before the add-transaction mockup, so the route sequence remains category selector → vendor selector → add category editor → add transaction | Static DOM ordering assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-185 | User correction: `ez tökéletesen az eredeti design, de igazítsd a layoutot a vendorhoz (bottom ok gomb, etc)` | A4 vendor-style bottom OK footer | The A4 fullscreen AddNewCategory editor keeps the original editor content but replaces the standalone full-width save footer with the same sticky bottom footer structure as the vendor selector: summary copy on the left and a right-aligned recolorable `OK` button | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-186 | User correction: `ebben, a vendor és a kategória menuben inkább dual legyen a bottom gomb, legyen cancel is` | A2/A3 dual Cancel/OK selector footer | The fullscreen category selector and vendor selector keep their selected-count footer copy, but their bottom action area becomes a dual `Cancel` + `OK` button group; the old single `Alkalmaz` action is removed from both selector menus | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-187 | User approved proposal: `mutasd` after AddNewTransaction sheet recommendation | A5 redesigned AddTransaction amount-hero sheet | The A5 alternative add transaction screen shows the proposed sheet layout: compact header with expense badge, large amount hero, name/category/date-time pills, glossy category avatar, and dual bottom `Cancel` + `Mentés` actions; the legacy equal-weight field stack/save-footer is not used in A5 | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-188 | User correction: `kiadás pill nem kell külön bele, illetve csak egy gomb kell alulra, tranzakció hozzáadása, a cancel a swipe down marad` | A5 single-submit AddTransaction sheet | The A5 AddTransaction mockup removes the separate expense/type badge, keeps dismissal as swipe-down only, and replaces the dual footer with one full-width `Tranzakció hozzáadása` CTA | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-189 | User: `most jöhet új screenként az addnewrecurring sheet. úgy rajzold fel ahogy gondolod` + source `RecurringManagerSheet`/`RecurringRuleDraft` | A6 redesigned AddNewRecurring sheet | A new A6 alternative screen is inserted after A5 and before the color scale labs; it adapts the real recurring manager fields into a compact schedule-hero sheet with `Idő/Push` trigger choice, transaction summary, day/time/status schedule fields, and one `Ismétlődés hozzáadása` CTA with swipe-down cancellation | Source inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-190 | User: `most ugyanezt, de a push oldalt egy új screenre` + source `_PushScheduleRow`/`_PushTrainingForm`/`NotificationParserRule` | A7 redesigned AddNewRecurring Push sheet | A new A7 screen is inserted after A6; it keeps the same recurring sheet visual model but switches the trigger to `Push`, shows app picker and month-day schedule fields, push sample training, amount/merchant token selection, parser preview, tolerance controls, and the same single `Ismétlődés hozzáadása` CTA | Source inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-191 | User: `a sheetet a teljes magasságában rajzold le` | A7 full-height push recurring sheet | The A7 push recurring sheet is no longer drawn as a fixed short 586px bottom sheet; it is explicitly marked full-height and stretches from near the top of the phone mock to the bottom edge with `top`, `bottom: 0`, and `height: auto` geometry | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-192 | User: `rajzolj egy új screent ezzel a megoldással a vendor screen mellé`, then corrected by `ok, ez jó, de nem ide fogod alkalmazni` | Superseded floating keyboard edit concept | The floating keyboard edit concept is no longer retained as a vendor-specific screen; it is moved to the backheader limit amount flow in COLOR-LAB-193 | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-193 | User: `tervezd át a screent, és rakd a backheader stage 2 screen mellé, ha user egy limitösszegre tappel... stage 1-és 2-ből is állítani` | A1F backheader limit amount tap-edit state | A new screen sits next to the backheader stage 2 prototype and models tapping the limit amount from either stage1/browse or stage2/focus: the backheader remains fixed, the background is inert/dimmed, a keyboard-safe floating numeric limit editor opens above a numeric keyboard overlay, and the slider remains visible as the secondary adjustment path | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-194 | User: `az jó, azonban legyen mellette az avatar, a kör az ikonnal` | A1F limit amount avatar adjacency | The A1F stage1/stage2 amount tap targets show the category avatar circle with icon directly beside the amount/entrypoint label, so the edited limit remains visually tied to the selected category | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-195 | User: `ott van de csak az ikon, fehéren fehérrel, nincs színes kör... ha nincs limit kiválasztva, akkor ne felirat legyen hanem legyen egy progress bar... minél kisebb a balance anál kisebb` | A1F colored hotspot avatars + no-limit balance progress | The A1F limit hotspot avatars use a real colored category-gradient circle behind the white icon, and the no-limit fastinfo state replaces text copy with a balance-depletion progress bar whose fill width decreases as balance decreases | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-196 | User: `last screenshot, raktál a backheaderbe 2 felesleges pillt... nem színes az avatar a billentyűzet felett... balance progress csík ugyanabban a pozícióban legyen mint a partitioned, és ugyanolyan magas, fehér legyen` | A1F overlay cleanup + white no-limit progress geometry | The A1F keyboard edit screen removes the two extra stage entrypoint pills from the backheader, the floating editor avatar above the keyboard is a colored category circle with the white icon, and the no-limit balance progress reuses the partition bar vertical position/9px height with a white decreasing fill | Screenshot inspection + static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-197 | User correction: `a limitösszeget is egy felesleges pillbe tetted` | A1F plain inline limit amount tap target | The A1F displayed limit amount is no longer rendered as a pill/button chip; it remains tappable as a transparent inline amount row with a colored avatar and no pill border/background | Static DOM/CSS assertions + HTTP/browser inspection | DONE |
| COLOR-LAB-198 | User correction: `még mindig van egy felesleges ikon a limit mellett a háttérben... balance fehér progress bar felett legyen írva valami magyarázó szöveg... ha backheader stage 2 expanded, és van limit, akkor ez is kapja meg a partition progress bart... ha nincs limit ide írd ki a hozz létre limitet szöveget` | A1F background icon removal + no-limit balance explainer + A1E expanded limit/no-limit lower band | The A1F keyboard-edit background removes the extra icon/avatar beside the inline limit amount while keeping the floating keyboard editor avatar; the no-limit fastinfo balance bar has explanatory copy above it and uses a 0% negative-floor empty bordered bar; the A1E expanded focus lower band shows the category limit partition bar when a limit exists and carries a `Hozz létre limitet` no-limit fallback | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-199 | User correction: `szedd le ezt a szöveget: balance= ez nem jó, egyszerúbb kell... egy szó legyen... backheader stage 1 expanded stage 2 expanded mind a kettő a partition progress bart kapja meg... ugyanabban a magasságban legyen, mint fastinfo stage1-nél (a1b)` | A1B one-word no-limit concept + A1E expanded stage1/stage2 partition bars | The A1B no-limit balance explainer removes the `Balance =` wording and uses the one-word non-negative concept `Tartalék`; the A1E expanded browse/stage1 lower band and focus/stage2 lower band both render the category partition progress bar, aligned to the same 18px side inset, 16px bottom offset, zero margin, and 9px bar height as the A1B fastinfo stage1 partition/progress placement | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-200 | User correction: `a1b: progress bar kért magassága a1e: progress bar lentebb van, nem az a1b magasságában` | A1E progress Y-position matches A1B | The A1E expanded stage1 and stage2 partition progress bars use the same raised internal progress Y-position as the A1B fastinfo stage1 progress instead of sitting lower at the bottom of the backheader card | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-201 | User correction: `kategória vendor sheet addnewcategory: body teteje, a header card tetejénél legyen... body teteje a searchpillek teteje` | Fullscreen selector/editor body start alignment | The fullscreen category selector, vendor selector, add-category editor, vendor editor, and icon selector mark their body/control area as starting at the dashboard header-card top; the selector search pills are the top of that body/control area and downstream pills/cards are pushed below that line | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-202 | User: `hozz létre egy új screent: vendor editor... fullscreen menu... user nevet módosíthat, kategóriát módosíthat... alul ugyanaz a footer mint az addnewcategoryban... content kevés, ezért úgy rajzold fel, hogy kitalálsz bele valamit` | A3B fullscreen vendor editor | A new fullscreen vendor editor screen is inserted next to the vendor selector; it models long-press from a vendor card, lets the user edit vendor name and assigned category, adds useful non-empty vendor stats/rules/preview content, and uses the same bottom footer structure as the add-category editor | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-203 | User correction: `addnewcategory: a színslotok a megbeszélt gradientek legyenek... sok az üres hely, arányosan rendezd el a lapot... body teteje itt is a header card tetejének magassága legyen` | AddNewCategory gradient slots + balanced fullscreen layout | The add-category editor color slots use the active Spendee gradient variables instead of flat app hexes, and the fullscreen content is redistributed from the header-card top to the footer with balanced name, color, icon, and preview spacing | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-204 | User: `új screen: az appban van egy icon selector, ezt is rajzold fel egy új screenbe, fullscreen legyen, ugyanazokkal az eddigi szabályokkal (footer gombok, body pozíció)` + source `IconSelectorSheet` | A4B fullscreen icon selector | A new fullscreen icon selector screen adapts the app's `IconSelectorSheet` into the prototype: 5-column icon grid from `CategoryIconManager.iconOptions`, body starts at the header-card top, and the footer follows the established selector/editor bottom action rule | Source inspection + static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-205 | User: `most jön a statisztika menu... újabb sorban... jelenlegi felé... első screen... header card, bevétel/kiadás, bottom nav... kiadás fülön... score legyen kiírva... slider fogja majd mutatni... mágnescsík helyett az egész card színez` | S1 statistics expense dashboard first row | A new statistics-menu row is inserted above the current lower main-menu row, making the main menu the second row; its first screen shows a Spendee dashboard shell on the `Kiadás` tab with header card, type row, bottom nav, `Score` instead of `Balance`, an in-header score/magnet slider, whole-card header recoloring, and only minimal undecided body content | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-206 | User: `egy alternatív dashboard kezelést fogsz felvázolni... ez lesz a B sor... a1 screen copy paste, to b1... stage 1... backheader tartalma kikerül a frontra... stage 2 pedig a fastinfo... commonheader1.png, commonheader2.png` | B row common expandable dashboard header | A new B row is inserted below the A/main-menu row and before the balance/palette labs with three screens: B1 stage 0 copies the A1 dashboard shell as the collapsed core header state, B2 stage 1 expands the same header to show the former backheader/category carousel content on the front, and B3 stage 2 expands further to show the context selector plus compact fastinfo; the row cites both `commonheader1.png` and `commonheader2.png` references and uses one common header component rather than separate backheader/fastinfo widgets | Reference image inspection + static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-207 | User correction: `rajzold fel a többi header alatti contentet is pontosan... bevétel/kiadás, summarypill, searchpill, logboxok... távolság marad ugyanannyi, mint A1-ben, csak lejjebb kerülnek` | B row full A1 under-header content stack | B1, B2, and B3 each render the complete A1 under-header content stack — income/expense row, summary pill, search pill, transaction count/date header, and five logboxes — with the same internal order/spacing as A1; only each stage's `home-content` top offset changes so the vertical displacement is visible | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-208 | User correction: `ahogyan a header expandál, az az alatti content is lefele slideol ugyanannyit amennyit a header expandál... a bevétel kiadás mindig a header alatt lesz` | B row content anchored to current header bottom | In B1/B2/B3 the under-header content starts at the current common header bottom plus the original A1 4px gap. Stage 1 and stage 2 no longer use arbitrary fixed offsets; their income/expense row moves down exactly by the header expansion delta | Static CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-209 | User correction: `b sorból szedd ki mindenhonnan a bottom navot, stage 2 képben pedig olyan lentre húzd a headert, hogy a searchpill alja a safety zone teteje legyen... csak a summarypill, a searchpill, a bevétel kiadás, és a header látszik` | B row no bottom nav + B3 safety-cropped stage2 | B1/B2/B3 render no bottom navigation. In B3 stage2 the common header and anchored content are shifted down so the search pill visual bottom lands on the safety-zone top, and the content container is clipped there so transaction count/date/logboxes remain in DOM for spacing tests but are not visible | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-210 | User correction after latest screenshot: `b3 header teteje ott van ahol b1 teteje van, az alját ... expandáld... searchpill alja a safety zone teteje... A5 tranzakció hozzáadása gomb alja a safe zone teteje... kapja meg ugyanazt a glow logikát, és színezés logikát, mint a1` | B3 fixed-top expanded header to A5 safe-top | B3 stage2 keeps the same header top as B1/B2 and increases only the header height downward. The height is calculated from the A5 transaction-submit button bottom/safe-top (`screen-h - 18px`) so the unchanged type/summary/search stack lands with the search pill bottom on that safe-top. B common headers reuse A1 header glow/color update logic, including header-card recolor triggering `--spendee-header-glow` | Screenshot inspection + static DOM/CSS/JS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-211 | User correction: `b1,b2 alja, ahol volt a bottom nav üres lett, töltsd ki tranzakciós logokkal... b3-ból töröld a header contentjeit, csak a balance marad` | B1/B2 transaction-filled bottom + B3 balance-only expanded header | B1 and B2 extend their home/log content to the bottom of the phone after bottom-nav removal and render extra transaction rows to fill that area. B3 keeps the expanded header geometry but removes header context/fastinfo/control content so only the Balance label and value remain in the header card | Static DOM/CSS assertions + direct HTML inspection + HTTP/browser inspection | DONE |
| COLOR-LAB-212 | User correction: `nem reagál a 3 új header a sliderre, csak a glowjuk, a színük hardcoded... rajzolj fel még egy skálát... fehér-cyan-sötétkék` | B common headers fully reactive + cool scale | B1/B2/B3 common header cards use the same reactive `--spendee-header-bg` color layer as A1, without an opaque hardcoded gradient; a fourth white→cyan→dark-blue scale is drawn under the current sliders with the same draggable-center and numeric window-width model, and it live-writes the header color/glow/ink path like the existing colored scales | Static DOM/CSS/JS assertions + JS parse + HTTP inspection | DONE |
| COLOR-LAB-213 | User correction: `a palettát jobban szedd szét... 3 focus mode lesz, mindháromnak 3 header állapota... balance mode... budget mode... mind mode... csak a specifikus sliderre` | Three scoped common-header focus modes | The common-header prototype is organized as balance mode, budget mode, and mind mode, each showing the three header sizes/states and a single dedicated slider directly below its row. Balance mode is controlled only by the purple→pink slider, budget mode only by the white→cyan→dark-blue slider, and mind mode only by the red→green slider; each slider live-updates only its own row. The cool scale keeps stops 1–7 and replaces the grayish tail with saturated dark-blue stops. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-214 | User correction: `van egy hardcoded gloss a headerekben... jobb oldali lilát... sárgát koszossá teszi... csak egy gomb legyen... mindegyik focus mode alá tegyél egy opacity slidert... tappel egy bizonyos pontjára` | Reactive glass gloss + one menu button + mode opacity sliders | The common header glass layer keeps the left white highlight but replaces the hardcoded right purple gloss with a reactive, lightened accent derived from the active header color and anchored to the mini-header menu-button position. Every common-header state has exactly one glossy three-bar menu button in the same top-right mini-header position, with no separate eye button. Each focus mode has its own opacity slider under its color slider, scoped to that mode, and all color/opacity sliders jump on track tap as well as drag. | Latest two screenshot inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-215 | User correction: `az app logikáját fogod szétszedni... mind mode stage1... app kiadási score grafikonja... budget stage 1 pedig... partition progress bar... avatarok... longtap... stage2-be is` | Focus mode header B/C/D content redesign | A separate current-app focus-mode note exists. Budget mode stage1 renders current Budget, x/y amount, spent/remaining line, A-row partition progress bar, and bottom-pinned avatar carousel without avatar name/dot indicator; the limit-adjustment interaction is documented as category-avatar longtap + vertical joystick. Mind mode stage1 renders an expense-side score graph based on `StatsFastInfoGraph` logic, restyled with translucent/glass gradients. All common header cards have a small white bottom handle, stage1 height is expanded to 284px, and stage2 also shows the avatar carousel pinned at the header bottom. | App source inspection + note inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-216 | User correction: `d2,b2 nem kell avatar. c2 duolikáltál mindent, csak a progress bart kellett volna a headerbe raknod az elköltve alá, duolikáltál egy containerr, és a feliratokat is` | Focus mode stage1 duplicate cleanup | B2 and D2 stage1 do not render avatar carousels. C2 budget stage1 keeps the existing Budget title, x/y amount, and Elköltve/Maradt subvalue, and inserts only the partition progress bar directly under that existing subvalue; no extra budget container, title, amount, or spent/remaining labels are duplicated. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-217 | User correction: `kell a külön glossy container... revert last delete command... category avatarokat is rakd vissza a stage1-be... kiterjesztett információ mindig glossy containerben legyen (elköltve-maradt-progress bar-avatarok)` | Stage1 glossy extended info restored | The COLOR-LAB-216 avatar/container removal is superseded. B2, C2, and D2 stage1 restore category avatars inside glossy extended-info containers. C2 keeps the existing Budget title and x/y amount outside the container, while the glossy container holds `Elköltve/Maradt`, the partition progress bar, and the avatar strip. D2 keeps the score graph in its glossy container and includes the avatar strip there. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-218 | User correction: `c2: progress bar, és feliratait a container aljára rakd... b2, d2: szedd ki az avatarokat` + `c3-d3-a3 onnan is szedd ki az avatarokat` | Focus-mode avatar cleanup + C2 bottom progress | This supersedes the avatar portions of COLOR-LAB-217. B2 and D2 stage1 render no category avatars. The stage3/fastinfo header screens for the three focus-mode rows render no bottom avatar carousel. C2 keeps the glossy budget extended-info container, but its `Elköltve`/`Maradt` labels and partition progress bar are anchored together at the bottom of that container, not at the top. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-219 | User correction: `d2: ... nagyobb glossy containerben ... kiadási score, legend ... kisebb container ... szedd ki ... rakd bele a nagyobba` + `b2: hasonló glassy containerben a stat bevételi oldalon lévő fő bevétel vs kiadás legyen` | B2/D2 stat glossy graph cleanup | D2 keeps the expense score title/legend/graph inside one large glossy surface and removes the smaller inner chart card/background so the SVG chart scales directly in the parent surface. B2 stage1 gets a matching glossy stats surface based on the app income-side `StatsFastInfoGraph` branch: `Bevétel vs kiadás`, income/expense/null-balance legend, and centerline bars. | App source inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-220 | User correction: `úgy csináld: stage1 tetejénél progress bar, tartalék felirattal, a header aljánál a 3 card, közte pedig balance arány szám... a jelenlegi balance összeg alatt található kisbetűs arányszámokat kiveszed` | B2 reserve layout + B3 graph relocation | B2 stage1 no longer contains the income-vs-expense graph or the small subvalue under the Balance amount. Instead, B2 stage1 is a non-glossy layout: top `Tartalék` progress bar, an intermediate balance ratio area later refined by COLOR-LAB-221, and the same three A1C info cards pinned to the header bottom. The income-vs-expense graph moves to the bottom of B3 stage2. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-221 | User correction from latest screenshot: `rossz helyre raktad a balance számokat... tartalék progress bar és havi kiadás box teteje közt felirat: balra igazítva: balance arány. jobbra igazítva kisebb betűkkel bevétel szám zöld, alatta felirat, mellette piros alatta kiadás felirat` | B2 balance ratio row | B2 stage1 no longer uses the large centered `32 / 68` ratio. Between the `Tartalék` progress bar and the top of the A1C card row, it renders a compact row: left-aligned `balance arány`, and right-aligned mini income/expense columns with green `32%` over `bevétel` and red `68%` over `kiadás`, sized to fit above the cards. | Static DOM/CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-222 | User correction: `emulated 0 spendee scorechart.png, csak a chartot nézd ki, a badge nem kell... tripla szám, nehezen látható skála, felesleges színcsóva... b2 tartalmát duplikáld be b3-ba... stage 2 az stage1+ extra terület mindenhol` | D2 score chart reference cleanup + B3 stage1+extra composition | D2 score chart follows `/storage/emulated/0/spendee/scorechart.png` chart structure only: clean glass chart panel, readable 100/50/0 y-scale and month x-scale, segmented red/yellow/green trend line, one endpoint dot, no score badge and no duplicate chart-internal `82/100`; colored background zones/plumes are removed. B3 stage2 duplicates the full B2 reserve-summary content in a fixed stage1-height layer and adds the income-vs-expense graph as separate extra stage2 area below it. | Reference screenshot inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-223 | User correction from latest screenshot: `a trendet nem sikerült felrajzolnod rendesen. c2-3 avatar cardjai legyenek 20%-al nagyobbak, és kicsit jobban húzd szét, budget mode glassy container magasságát csökkentsd, 10%, de a felső oldal pozícióját ne, így feljebb kerül az alja, értelem szerűen a partition progress bar is` | D2 score trend fill fix + C2/C3 budget geometry | The score chart axis/grid paths explicitly use `fill:none` so no black filled polygon can cover the trend. The budget-mode glossy layer uses a 153px fixed height (10% less than the 170px stage1 layer) with the same top position, moving its bottom edge and partition progress upward. The temporary avatar enlargement portion was superseded by COLOR-LAB-224. | Latest screenshot inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-224 | User correction: `a budget glossy box mérete jó, de az avatarok túl nagyok lettek, az avatarokat visszavonom... a b3 grafikon jó, de ki fogod egészíteni, a glossy container magassága 2x ekkora legyen. 3 graph lesz benne... alatta összevonod a stat menu kiadási és bevételi alsó függvényeket...` | Budget avatar rollback + B3 merged pattern graph stack | Keep the 153px budget glossy layer and score trend fill fix from COLOR-LAB-223, but revert budget avatar geometry to the previous 36/46/66px badges, 17/22/30px icons, 12px gap, and 66px strip height. B3 extra graph glass grows from 132px to 264px and becomes a three-layer graph stack: current income-vs-expense graph on top, plus a merged pattern chart sourced from the stats expense/income lower helper bars where green income pattern bars grow upward from the centerline and red expense pattern bars grow downward from the centerline. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-225 | User correction: `d3-ra a maradék helyre tervezel egy saját heatmapet, az app alapján... 12 hónap, 4 sor 3 oszlopba... stage2 felülete (csak az) scrollozható... új screen... nem olyan magas mint a telefon... budgetavatarok glossy container magasságát tovább kell csökkentsd 10%` | D3 scrollable yearly heatmap + reduced budget glossy layer | D3 Mind mode stage2 keeps the score graph stage1 layer and uses the remaining stage2 space for a scrollable minimalist heatmap based on the app `StatsYearCalendar`/`StatsMonthCard` model. A separate content-height preview screen shows all 12 month cards in a 4×3 grid. The budget glossy layer height is reduced a further 10% to 138px while keeping the same top position, moving its bottom edge and partition progress upward. | Latest screenshot inspection + app source inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-226 | User correction: `kérek még változatokat a hónapcardokra... cellák színezése dinamikus... user színre tappel, majd a hónapcardokra... c2c3 glossy container magasságát még csökkentsd 10%... progress bar magasságát növeld 10%-al... explicit c3 area kapja meg a statisztikából ismert pie chartot... scrollavlenek kell lennie` | Glossy heatmap variants + C3 budget pie stage2 | The heatmap preview offers multiple non-white glossy monthcard variants; heatmap day cells use a dynamic selected category color via tap-on-color then tap-on-monthcard, with per-cell opacity preserving intensity. The budget glossy layer is reduced another 10% to 124px and the C2/C3 budget progress bar is increased to 10px. Budget mode C3 gets a scrollable category pie/donut area based on the app `CategoryDonutChart`/stats donut, with the active category highlighted and a long scrollable category list. | App source inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-227 | User correction: `d3: túl magas a monthcardok headerje, illetve a háttér miatt nehéz átlátni, itt is hasonló legyen a container mint a chart felette, a score számok nem kellenek a headerbe` | D3 heatmap compact glass panel | D3 Mind mode stage2 wraps the heatmap grid in a chart-like glass panel matching the score graph above; monthcard headers are compact month-name-only rows with no score badge/number in the header, and weekday spacing is reduced so the heatmap reads cleaner against the glass container. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-228 | User correction after latest screenshot: `a partitition bar paddingja levágja a progress circle alját, illetve színinkonzisztenciát okoz, 0 legyen az opacityje a glossy container paddingja pedig a circle felső szélét vágja le, túl kicso lett a container, legyen 5% magasabb` | C2/C3 budget avatar clipping fix | The budget glossy layer is increased 5% from 124px to 130px, its avatar area gets more vertical breathing room so the progress circle/ring is not clipped, and the budget partition track/padding visual has zero opacity so it does not add a white/gray band or inconsistent color behind the colored segments. | Latest screenshot inspection + static CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-229 | User correction: `bottom navot teszed vissza erre a koncepcióra, mind a 9 screenre, bal oldal dashboars, középen kilógó fab, jobb oldal beállítások... stage 2... searchpill alja a bottom nav tetején legyen` | B/C/D common-header 3x3 focus-mode screens | All nine common-header concept screens (balance, budget, mind; stages 0/1/2) render the restored bottom nav with left Dashboard, a centered protruding FAB, and right Settings. Stage1 is shortened by the search-pill height and moves its under-header content up with the header. Stage2 uses the bottom-nav top as its safety anchor, so the anchored content stack moves up with the reduced header and the search pill bottom lands on the bottom-nav top. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-230 | User correction with latest screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260713-224646.png`: `a fab gomb a bottom nav közepén legyen, és olyan kilógó legyen, mint az utolsó screenshoton, a bottom nav designja is ilyen legyen, csak 3 gombos, de a koncept ugyanaz` | B/C/D common-header bottom navigation visual design | The common-header bottom nav uses the latest screenshot concept: a white/glass bottom shelf with a central raised notch/bump, the FAB is centered in that notch and protrudes upward from the shelf, and the three-button layout remains Dashboard / centered FAB / Settings without adding extra nav items. | Screenshot inspection + static CSS/DOM assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-231 | User correction: `bottom nav olyan legyen, mint az 1. régi design, csak középen egy kerek fab van, legyen inline` | B/C/D common-header legacy-inline bottom nav | The common-header bottom nav is reverted from the notch/shelf concept to the first legacy bottom-nav design language: a straight inline bottom bar using the legacy nav metrics, with Dashboard on the left, a circular FAB as the center inline slot inside the nav, and Settings on the right. No separate sibling FAB and no nav notch/bump pseudo-element remain. | Legacy HTML/CSS inspection + static DOM/CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-232 | User correction with latest screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260713-230621.png`: bottom nav fills the screen bottom, FAB is blue, and stage2 search pill bottom keeps the same gap from the bottom-nav top as the summary/search gap | Common-header bottom nav + stage2 geometry | The common-header inline bottom nav fills the phone bottom, the inline center FAB is blue, and stage2 safety top is the nav top minus `--common-header-search-top-gap`, reducing stage2 height and moving content up by that gap. | Latest screenshot inspection + static CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-233 | User correction: `B2: balance arány feliratot, Bevétel kiadás feliratot és számot belerakod a jobb oldali fast info boxba` | B2/B3 balance stage1 card layout | Balance mode stage1 removes the standalone balance-ratio row and places `balance arány`, income percent/label, and expense percent/label inside the rightmost A1C fastinfo card; B3 stage2 uses the same stage1 layer. | Static DOM/CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-234 | User correction: `b3 stage 2 része jelenleg még a korábbi layout, ezt módosítod olyanra mint stage1` | B3 balance stage2 extra area | Balance mode B3 no longer renders the old income/pattern graph stack as a stage2 extra area; its stage2 content is the same reserve/card stage1 composition only. | Static DOM assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-235 | User correction: split B3 red/green graphs; red pair goes to D2 and D3 where the score graph was, score function moves to D1/D2/D3 stage0 area | Mind mode expense side | Mind mode keeps a compact red-yellow-green score ribbon in the stage0/core area of D1/D2/D3, removes the old score graph panel, and renders the two red expense graphs in a glossy double-graph stage1 panel for D2 and D3. | Static JS/DOM/CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-236 | User correction: create `D4` stage2 for the income side and put the two green functions into its stage1 section | Mind mode income side D4 | A new D4 full screen is generated for Mind income stage2; it follows the Mind stage2 shell but its stage1 section contains the two green income graphs in the same double-graph structure as the expense side. | Static JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-237 | User correction: `partition progress bar kerüljön ki a glossy containerből a stage 0 areába, de legyen fele ilyen magas, és ne legyen felirat (c1-c2-c3) ... avatarokat a glossy sheet közepére vertikálisan`, plus prior correction that the category-badge glossy container lower quarter fades out | Budget core partition + centered avatar sheet | Budget mode C1/C2/C3 render the category partition progress bar in the shared stage0/core area, outside the glossy category-badge sheet, at half the previous height and without spent/remaining labels. The glossy category-badge sheet contains only the vertically centered avatars and fades its lower quarter out to transparent without clipping the badges. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-238 | User correction: `partitioned bar nem került fel eléggé. a szám alá kerüljön. maradt belőle a glossy containerben is maradvány... a balanceba pedig szintén kell egy fekete, ami balance historyt mutat` | Budget partition under value + balance black history visual | Budget C1/C2/C3 core partition sits directly under the header number, aligned to the number width and outside the glossy avatar sheet with no bar-source remnants. Balance B2/B3 stage1 gets a black balance-history visual between reserve progress and the bottom fastinfo cards. | Static DOM/CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-239 | User correction: `balance insight csak egy grafikon legyen, csak egy line, a stage 0-ba rakd... balance arány... fehér progress bar helyére... boxba placeholder... d2 piros hasábjai egy glossy boxban... felfele... d3 ... egyesítettet bele... D4 hasonló mint d3... zöld hasábok, alatta heatmap` | Balance core line + ratio slot + unified mind charts | Balance mode moves the single-line balance insight into the stage0/core header area and removes the stage1 black history card. Balance ratio replaces the white reserve progress slot, while the right fastinfo card becomes a placeholder. Mind D2/D3 use one compact glossy upward red bar chart, D3 removes stacked chart containers, and D4 uses the matching compact green chart with heatmap below. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-240 | User correction: `a bottom nav olyan széles legyen, mint a logbox... b3 contentjét igazítsd b2-höz... kiadási függvényeket felezd meg... egy glowy boxba... egyik a havi kiadás, másik a minták... zöld függvény... d2 mérete a jó... d3 helyett ugyanezt a téglalapot` | Common nav width + B3/B2 alignment + split half-height mind charts | The common-header bottom nav uses the same 20px side gutters as the logboxes. B3 stage2's stage1 content is a direct B2-aligned clone with the same stage1 geometry. Mind D2/D3/D4 use the D2-sized glossy chart rectangle; inside it the expense/income functions are split into two half-height sections (`Havi kiadás`/`Minták` for red expense and income equivalents for green), with both red and green sides present and upward bars contained in the glossy box. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-241 | User bug report: `c3-ból eltűntek az avatarok` | C3 budget stage2 avatar restoration | Budget mode C3 keeps the cloned budget stage1 avatar glossy sheet visible in stage2. The cloned C3 budget avatar layer is explicitly identified as a budget stage2 clone, keeps the 130px avatar-sheet geometry, and stacks above the C3 pie/scroll layer so the avatars cannot disappear behind stage2 content. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-242 | User approval after screenshot review: `mutasd` for the realistic Balance stage2 proposal | B3 balance diagnostic stage2 preview | B3 keeps the B2-aligned balance reserve/card stage1 clone at the top, and shows a separate scrollable glossy Balance diagnostics panel below it. The diagnostics panel uses the second latest screenshot concept in a compact, feasible form: header, six metric rows (`Reserve`, `Balance Ratio`, `Savings Rate`, `Buffer`, `Forecast`, `Ghost Income`), icon/status/value affordances, and local scroll inside the stage2 area without changing the accepted bottom nav/search-pill geometry. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-243 | User correction referencing latest screenshot: `bottom nav szélessége ... legyenek egyformán szélesek` | Common-header bottom nav/logbox width parity | The common-header bottom nav outer card uses the same 20px side gutters as the transaction logboxes in the actual CSS cascade. A more specific common-header override prevents the later `.spendee-dashboard-screen .bottom-nav` rule from widening/narrowing it to the Spendee default 8px side gutter. | Static CSS cascade assertion + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-244 | User correction superseding COLOR-LAB-243: `jelenleg a bottom nav sziget jellegű, nem whole screen wide... whole screen wide` | Common-header whole-screen bottom nav | The common-header bottom nav is no longer a floating/island card and no longer matches logbox width. It spans the whole phone screen width with `left:0`, `right:0`, `bottom:0`, the accepted common-header nav height, no rounded island radius, and a cascade-safe override after the later Spendee floating-nav rule. | Static CSS cascade assertion + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-245 | User correction referencing latest screenshot: `a hasábok körül egy kitöltött line van, rakd bele a havi kiadás hasábjaiba. az alsók maradjanak, azok minták` | Mind monthly expense area+bar overlay | The top `Havi kiadás` half-chart in the Mind expense graph gets the screenshot-style filled area plus outline line behind/around the red bars. The lower `Minták` half-chart remains unchanged as a bar-only pattern chart. | Screenshot inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-246 | User request: `d2-höz kérek alternetív screenseket, ahol nem kiadási grafikon van stage2-ben, hanem hasonló megnyitható cardok, mint b3-ban... score-ral vagy kiadással... szűrésre reagálnak` + clarification `minden screent újként teszel be a jelenlegi mellé` | Mind alternative card-based stage2 screens | Add new Mind stage2 alternatives next to the existing D screens without replacing D1/D2/D3/D4. The alternatives are separate screen columns: score cards, expense cards, and action cards. Each uses B3-like openable/chevron cards instead of the expense graph, marks content as filter-reactive, and relates to the current score and/or expense filter context. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-247 | User correction: `rossz helyre raktad, ne stage 2-be rakd, azba heatmap helye, stage1 felületére rakd` | Mind alternative card placement correction | Move the D5/D6/D7 alternative cards out of the lower stage2/heatmap area and onto the stage1 surface inside the stage2 screen. The lower stage2 area remains reserved for the heatmap. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-248 | User request after rejecting current look: `rajzold fel újabb screenekre` | Mind glanceable alternative screen set | Add three additional Mind stage2 screen columns next to the existing D screens without replacing D1-D7: a score hero insight screen, an expense pressure hero screen, and a compact action stack screen. These new screens use the stage1 surface for glanceable hero/compact content rather than the dense B3-like list, keep the heatmap in the lower stage2 area, and remain filter-reactive. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-249 | User request: `még egy screent csinálj: ugyanolyan boxok legyenek, mint a b3-ban. a grafikonokat tedd a boxokba. a maradék üres tér a boxok felett: előző időszakhoz képest, egy mutatószám legyen, le vagy fel nyíllal` | Mind B3-style boxed graph alternative screen | Add one additional Mind stage2 screen next to the existing D alternatives without replacing them. The new screen uses the stage1 surface, shows a single previous-period KPI with up/down arrow in the free space above, then uses B3-style fastinfo boxes to contain the graphs inside the boxes. The lower stage2 area remains reserved for heatmap. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-250 | User correction: `a 3 card ne legyen benne egy extra containerben, illetve egyik box a score változását mutassa, a másik a kiadási hasábok legyenek az időszakra, a harmadik a kiadási minták volumene legyen` | D11 boxed graph content/layout correction | Update D11 so the three B3-style cards are direct children of the stage1 panel, not wrapped in an extra inner grid/container. The three cards must represent: score change, period expense bars, and expense pattern volume. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-251 | User correction: `2 box legyen... egyikbe volumen másikba időszaki kiadás... ezek külön legyenek a háttéren, ne legyenek még egy extra glossy containerben... az előző időszakhoz képest sem legyen glossy containerben, a háttéren legyen, hasonlóan, mint a balance arány felirat a balance modeban` | D11 two-box direct background layout | Update D11 so it has exactly two wide B3-style cards on the stage1 background: `volumen` and `időszaki kiadás`. Remove the score card. Remove the extra glossy/panel container around the KPI and cards. The previous-period KPI is rendered directly on the background, similar to the balance ratio label treatment in Balance mode. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-252 | User correction: `a boxban a havi kiadás úgy legyen, mint a d3-ban, line+bar` | D11 expense box line+bar chart | Update the D11 expense/period spending box so its mini chart uses a D3-style line+bar composition: filled area/line plus expense bars in the same box, instead of bar-only rendering. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-253 | User request: `d5-d10 töröld, illetve ha a htmlt frissítsük a focus ne ott legyen ahol jelenleg, ne oda ugorjon a keyboardos screenhez, hanem a d1 legyen` | Remove D5-D10 and default refresh focus to D1 | Remove the generated D5-D10 Mind alternative screens while keeping D1-D4 and the accepted D11 boxed-graph screen. Remove the keyboard mockup autofocus that drags refresh focus to the keyboard screen, and add a load-time D1 focus/scroll anchor for the Mind row. | Static DOM/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-254 | User request: `ezt a box layoutut rakd a d2-be is` | D2 uses accepted D11 box layout | Mind D2 stage1 uses the accepted D11 direct-background box layout: previous-period KPI directly on the background and exactly two wide B3-style boxes (`Minták volumene` and `Havi kiadás`), with the Havi kiadás box using the line+bar mini chart. D11 remains as the standalone alternative screen and D5-D10 stay removed. | Static DOM/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-255 | User request: `d4, d11 töröld` | Remove D4 and D11 Mind screens | Remove the generated D4 income-side Mind stage2 screen and the separate D11 boxed-graphs alternative screen. Keep the D2 stage1 box layout as the only consumer of the boxed graph layout, and keep D5-D10 removed. | Static DOM/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-256 | User approval after animation planning: `ok` for `a d sor header touch szenzitív legyen` | D row touch-sensitive portal header | Add a Mind/D-row-only portal interaction layer to common-header cards. The slider-driven `--spendee-header-bg` value layer remains value-locked. A separate overlay provides low-frequency idle vortex motion and pointer-following purple/pink chromatic bloom under the finger, with blur/diffusion and opacity decay on release. Balance/Budget rows must not receive the portal behavior. | Static CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-257 | User correction: `nem örvénylik... touch szín legyen erősebb... ne momentary legyen, hanem delayed fade out... ha a user az ujját húzza, akkor csíkot húzzon... legyen erre a feladatra egy teszt header... d sor alá... swipe ne scrollt triggereljen` | Mind portal trail + drag test header | Strengthen the Mind portal idle vortex so motion is visible, strengthen the purple/pink touch color, and replace momentary touch feedback with pointer trail dots. Dragging must leave a fading/shrinking trail where older points disappear first. Add a standalone test header under the D/Mind row with a label; that header uses the same portal drag behavior and `touch-action:none`/preventDefault so dragging on it does not trigger page scroll. | Static CSS/JS/DOM assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-258 | User correction: `perfect a touch reaction, de nincs nyugalmi örvénylés... bal és jobb oldala más szín... mozogjanak, mintha víz lenne... három rétegű legyen lila-magenta-fehér... elengedi... ne hirtelen eltűnjön` | Mind portal idle water + release fade | Keep the accepted drag/touch reaction, but make the idle portal visibly alive: a three-layer purple-magenta-whitish ambient field with separately colored left/right water-like motion. On release, the touch bloom must fade through a real long opacity transition; do not delay and then instantly zero the gradient color alpha. | Static CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-259 | User correction: `nem a headert kellett volna színezned... header színe mindig a sliderrel kiválasztott... traffic light skála... nyugalmi állapotnak örvénylenie kell... user ujjlenyomatai színsorrendje: belül világos rózsaszín, körülötte lilás, körülötte fehéres` | Mind header/value animation layer correction | Remove the hardcoded purple/magenta/white idle coloring from the portal overlay. The Mind header's idle state keeps the slider-selected A/B traffic-light gradient and only animates that value gradient's geometry/background-position like water. The touch glow alone uses the requested triple radial order: light pink center, purple middle ring, whitish outer ring, with the existing opacity fade behavior. | Static CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-260 | User correction referencing latest screenshot: `ujjlenyomat túl nagy, legyen fele ekkora... trace jó... drag jó... nyugalmi hullámzás kevés... gradiens tengely szögét is forgatni kéne nagyon lassan... zöld bal oldalt átkerül jobbra` | Mind portal touch radius + stronger idle rotation | Reduce only the touch bloom radius to about half of the screenshot size while keeping the drag trail size/behavior unchanged. Increase idle water motion by slowly rotating the Mind header's slider-selected value-gradient axis, so the traffic-light A/B colors drift from left/right/top/bottom over time without changing the selected color state. | Screenshot inspection + static CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-261 | User correction referencing latest screenshot: `nem a gradiens forog, hanem az ablakon belül a szín... a gradiens forogjon... a jobb oldali szín cseréljen helyet lassan a ballal` | Mind true gradient-axis rotation | Replace the oversized rotating pseudo-element approach with true gradient-axis rotation. The Mind header must animate a registered gradient angle variable from 112deg to 292deg to 472deg, using the slider-selected left/center/right traffic-light stops, so the right-side color slowly swaps with the left-side color. Do not use `transform: rotate(...)` on the clipped header pseudo-element for this effect. | Screenshot inspection + static CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-262 | User correction: `touch mérete még mindig túl nagy a csóva méretéhez képest, legyen kisebb, de a csóva... jó. a spread is jó` | Mind smaller touch bloom only | Reduce only the static touch bloom radius further, from the previous 38% transparent stop to a smaller 25% transparent stop. Keep the drag trail/chóva dimensions, colors, 82px dot size, 76% trail fade-out radius, blur/spread, and fade behavior unchanged. | Screenshot inspection + static CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-263 | User request: `van 3 csúszka, bármelyik csúszkát állítom, ezt a teszt headert is színezze, a másik csúszka felülírja az előzőt, de csak erre legyen érvényes, a korábbi funkciók ne vesszenek el` | Three-slider test-header color preview | Any of the three header color scale sliders (Mind/balance scale, Balance/limits scale, Budget/cool scale) updates the standalone portal test header as a last-writer-wins color preview. The update must be scoped only to the test header and must not remove or replace the original mode-specific slider behavior for Mind, Balance, or Budget headers. | Static JS/CSS/DOM assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-264 | User request: `tegyél egy csúszkát a test header card alá, amivel az interakció szín opacityjét állítom` | Test-header interaction opacity slider | Add a range slider directly under the standalone portal test header card. It adjusts only that test header's interaction color alpha via a local CSS variable, affecting the touch bloom and drag trail color opacity while preserving the current trail size/spread/fade and the header drag surface behavior. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-265 | User bug report: `hova raktad az opacity állítót? nincs a test header alatt` | Test-header opacity slider visible below card | Fix the CSS cascade so the standalone test header remains in normal relative flow despite the later generic `.common-header-card { position:absolute; }` rule. The opacity slider must render directly below the test header card rather than being hidden underneath it. | Screenshot/server inspection + static cascade assertion + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-266 | User request: `rajzolj fel d3 mellé egy d4 stage2-t is, ahol havi nézet van, azaz csak egy hónap napcellái látszanak, itt is legyen a heatmap színezhető` | D4 monthly heatmap stage2 beside D3 | Add a new D4 Mind stage2 phone-screen directly beside D3. It must keep the accepted Mind stage2 shell, but its heatmap area shows exactly one month/day-cell heatmap instead of the 12-month year grid. The monthly heatmap must use the same selected-color heatmap mechanism as D3 (`data-heatmap-color-scope`, `data-color-target="heatmap-cell-color"`, and `--heatmap-active-color`). | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-267 | User correction: `a test header nyugalmi animációját kell állítanod, a jelenlegi rotating hullám nem jó, de az ujj animációja jó, ahhoz ne nyúlj` + fluid field/mesh/domain-warp notes | Test-header fluid idle field | Replace only the standalone portal test header's inherited rotating idle gradient with a non-rotating fluid/mesh/domain-warp CSS approximation: fixed A/B/center colors from the active slider, multiple slow-moving radial/linear layers, a warped mixing boundary, and subtle caustic luminance. The accepted touch bloom, drag trail, opacity control, pointer handling, and D-row header behavior must remain unchanged. | Static CSS/DOM assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-268 | User bug report: `nem látok semmit, javíts rajta` after last screenshot | Test-header idle field visible amplitude | Increase only the standalone test header idle field visibility. The fluid mesh must be visibly stronger at rest through higher contrast/saturation, a shorter flow cycle, a clearer warped mixing seam, and stronger caustic luminance. The accepted touch bloom/trail and interaction opacity slider must stay unchanged. | Screenshot inspection + static CSS/DOM assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-269 | User correction after screenshot: `ez víz, csak úgy látom, mintha oldalról lenne, a nívót látom, és nem szemből a felületet, itt a víz felülete kell áramoljon` | Test-header top-down water surface idle | Change only the standalone test header idle field from side-view/niveau water to top-down water-surface flow. Remove sharp side color plateaus and vertical seam behavior; use distributed, soft, whole-surface wave/caustic layers so the visible motion reads as a flowing surface. Preserve touch bloom/trail and interaction opacity behavior. | Screenshot inspection + static CSS/DOM assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-270 | User correction: `hiába csökkentem az opacityt túl éles és túl telített... morfológiája nem változik... ablak alatt legyen 3 signature slider... 3 színslider alatt legyen még egy opacity is a headerbg nek` | Test-header local signature controls + soft morphing surface | Make the standalone test header idle background less sharp and less saturated while increasing visible morphology change. Add three local signature color sliders under the test header that target only that header and overwrite each other, plus a separate header-background opacity slider below those three color sliders. Keep the existing interaction opacity slider for touch bloom/trail unchanged. | Screenshot inspection + static CSS/DOM/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-271 | User bug report: `vízszintes sáv alakul ki, mintha hosszában vágná szét a headert`; plus earlier request to add all suggested fluid-field layers | Test-header canvas fluid surface idle | Replace the standalone test header's visible CSS-gradient idle surface with a canvas-rendered fluid field: multiple moving mesh color sources, FBM/value noise, curl/domain warp, flow-map-like UV displacement, Brownian drift, caustic luminance, ripple/pulse hooks, and soft luminance-preserving color mixing. The header must no longer read as one sharp horizontal/vertical band between A/B colors. Preserve the accepted touch bloom/trail and the local signature/header-bg/interaction opacity controls. | Latest screenshot inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-272 | User correction: `ez inkább láng mint víz... a/b nem áramlik... vándorlást, mint a jinyang... illeszkedik az ujj interakció fade outjához` and `minden animáció minden komponensét felrakod a header alá számozva... slider... manual input` | Test-header yin-yang water idle + animation controls | Replace the flame-like repeated plume field with a slower large-form A/B wandering field: A flows into B, returns, and B can reach the opposite edge without a simple horizontal swim. The motion cadence must visually fit the existing touch fade-out. Add a numbered animation-component control panel under the test header, with each component as a slider plus manual numeric input, scoped only to the standalone test header. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-273 | User correction after latest screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260715-073925.png`: `csak egy pacát tudok belerajzolni... eredeti gradiensre nem tudom visszaállítani... ha mindent resetek, akkor 1 színt látok... az előzőekről is tegyél fel slidert` | Test-header original-gradient restore + previous-layer controls | Add explicit controls for the original base gradient and previous animation layers, not only the new yin-yang water. The standalone test header must have an original-gradient preset, base/water/legacy/CSS blend sliders with manual inputs, and defaults/minimums that keep at least some A/B gradient separation visible instead of collapsing to one flat color. | Latest screenshot inspection + static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-274 | User correction: `a színsliderek mellett legyen ott az ablakméret text input, illetve statikusnak látom, nem látok mozgást` | Test-header signature window inputs + visible motion defaults | Add a manual numeric window-size input beside each local test-header color slider (`Traffic`, `Limit`, `Cool`) and use that value when sampling the A/B color stops. Increase the balanced/default motion values and bounds so the standalone test header visibly moves unless the explicit `Eredeti gradiens` static preset is selected. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-275 | User request: `emlékszel amikor azt mondtam, hogy olyan mint egy oldalról látott nívó? azt is kérem` | Test-header side-view level/nívó layer | Restore the earlier side-view water-level/nívó character as an explicit selectable preset and a separate numbered slider/manual input layer. It must be optional, combinable with the other layers, and not replace the current balanced water/yin-yang controls. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-276 | User correction: `a nívó vertikális volt... amelyik típus aktív annak a csúszkáit akarom látni csak... nívó esetében lehessen állítani a szögét, annak kilengésének erősségét etc` | Test-header vertical level/nívó controls and active slider filtering | Change the `level-view` preset from a horizontal side-view waterline to a vertical, angle-adjustable level boundary. The level preset exposes its own numbered controls for level amount, angle, oscillation amplitude, wave frequency, and band thickness. Preset selection filters the control panel to the active type's relevant controls, while `Balanced water` can show all controls because its composition is layer-combinable. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-277 | User request: `kell egy rotáció is, ami az összes módot állítja. kell globális sebesség is... rotáció sebesség... oszcilláció... user beírja a szöget... kör... oszcillációs szög közepe... oszcilláció sebesség... mozgás mindig balról jobbra` | Test-header global motion transform controls | Add global controls visible for every active animation type: global speed, global rotation amount, rotation speed, oscillation angle/amplitude, oscillation speed, and oscillation center angle. Provide both numeric angle input and a circular ray control for the oscillation center. Apply the global transform before all effect fields so base, water, level, and legacy modes rotate together; global speed scales the shared timebase; rotation/pan direction stays left-to-right only. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-278 | User correction: `a sliderek containerén a scrollt ne tiltsd le, csak abheader testpanelen` | Test-header slider panel scroll behavior | Keep scroll blocking only on the standalone test header drag surface. The slider panels and their range controls under the header must allow vertical page scrolling (`touch-action: pan-y`) instead of horizontal-only gesture capture, while preserving slider/manual input behavior. | Static CSS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-279 | User request: `legyen benne egy normál mód is, a jelenlegi alapgradiens, animálás nélkül, és én állítom a rotációt/oszcillációt, azaz csak az azzal kapcsolatos dolgok legyenek ebben az esetben` | Test-header normal gradient rotation-only preset | Add a `Normál` preset for the standalone test header that renders only the current base gradient with all automatic water/mesh/level animation layers disabled. The preset exposes only the rotation/oscillation-related controls, including the circular angle pad, and defaults to no automatic motion until the user changes rotation or oscillation speed/angle. | Static DOM/CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-280 | User bug report: `nem egyeznek az árnyalatok a valódi színekkel... normálra, mintha a tesztablakon lenne egy szürke veil` | Test-header normal mode color fidelity | Remove the gray/white veil from `Normál` mode. The normal preset must render the current real header gradient at full opacity with no fixed white-wash/luminance veil, while animated water modes may still use caustic/soft-light styling. | Static CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-281 | User correction: `mégis sötét a tesztablak, nem élénk, nem egyezik` and `ha a rotációs slider az összes 0, és a körben állítom a szöget, manuálisan forogjon... ha a sebesség csúszka több mint 0, az felülírja` | Test-header direct normal gradient and manual rotation pad | In `Normál`/color-true mode, do not use weighted mesh color mixing or canvas desaturation. Sample the current A/B/center gradient directly so the test header matches the real header shades. The circular angle pad must manually rotate the gradient even when all other rotation sliders are zero; rotation speed above zero switches back to automatic rotation and overrides that manual angle. | Static CSS/JS assertions + RED/GREEN test + JS parse + HTTP inspection | DONE |
| COLOR-LAB-282 | User bug report: `a többi nézetben, alig van effectje a csúszkáknak... korábban ez jó volt a rotáció implementálása előtt... sem lángcsóvákat nem tudok előidézni, sem nívót nem látok` | Test-header global drift keeps effect layers in field | The rotation/global left-to-right motion must not push the shared sampling coordinates out of the 0..1 header field over time. Water, level/nívó, and legacy plume layers must remain in the visible field so their sliders keep strong response after the rotation controls are used or the page has been open. | Static regression assertion + JS parse + HTTP inspection | DONE |
| COLOR-LAB-283 | User bug report/request: `legyen egy rotatio ki ba kapcsolás, mintha nem is lenne... jelenleg a sebesség változtatja a szöget, nem a változás sebességét... elforgatja a színt az ablakban... éles határ` | Test-header rotation toggle and continuous phase | Add a rotation on/off control. When OFF, the global rotation transform returns identity coordinates as if rotation did not exist. When ON, rotation speed changes only the future phase rate without jumping the current angle. Rotated sampling must wrap both X and Y so finite-window rotation cannot expose missing-content hard seams inside the header. | Static regression assertions + JS parse + HTTP inspection | DONE |
| COLOR-LAB-284 | User bug report/request: `nincs ott a kért on off gomb... a rotációt tedd egy másik containerbe... a három módot akarom csak látni ebben a containerben... az eredeti gradiensben nem reagál semmi... balanced illetve nívó reagál, azonban fakó` | Separate rotation panel + vivid responsive modes | Keep the main motion panel scoped to exactly three mode presets: Original gradient, Balanced water, and Level view. Move all rotation controls, including the on/off toggle, into a separate adjacent rotation panel controlling the same test header without changing the selected mode. Original gradient mode must stay color-true but expose visible movement controls; Balanced and Level modes must render without the fixed gray/white fade. | Static regression assertions + JS parse + HTTP inspection | DONE |
| COLOR-LAB-285 | User bug report: `nem lehet scrollozni, ha a slider containereket swipeoljuk, oldd meg, hogy lehessen` | Slider panels vertical swipe scroll passthrough | Swiping vertically from any slider inside the test-header signature, motion, rotation, or opacity panels must scroll the page instead of being trapped by the native range input. Horizontal gestures must still operate the slider. The standalone header itself remains non-scrollable for drag/touch testing. | Static regression assertions + JS parse + HTTP inspection | DONE |
| COLOR-LAB-286 | User reset request: `amióta betetted a rotációt... kiveszel mindent... az ablak legyen statikus... touch effect jó, ahhoz ne nyúlj` | Clean static portal baseline | Remove the old shared rotation, level, legacy, and composite idle-animation controls/engine from the standalone portal test header. Default to an exact static left-A/right-B gradient with no canvas veil or idle motion. Preserve the accepted touch bloom, drag trail, pointer behavior, and release fade unchanged. | Source-boundary regression assertions + deterministic static color sampling + screenshot comparison + JS parse | PARTIAL |
| COLOR-LAB-287 | User request: `kizárólag a színezősliderek legyenek ott, és az interakció opacity` | Minimal static-mode controls | In static mode, retain the local Traffic, Limit, and Cool palette sliders plus the existing interaction-opacity control. Remove header-background opacity, rotation pad, and all old animation controls. The row-level window-size inputs are intentionally reintroduced by `COLOR-LAB-293`. The most recently moved palette slider remains last-writer-wins for the test header only. | Static DOM assertions + slider event regression tests + screenshot inspection | PARTIAL |
| COLOR-LAB-288 | Approved portal concept: `portal... energiamező... bal oldal A szín, jobb oldal B szín... vándoroltatni kell a gradiens színeit, és az arányukat alakítani` | Color-faithful energy-field architecture | Keep a non-animated exact A/B CSS reference beneath the canvas. Animated renderers may change only geometry, transition shape, energy luminance, and visible A/B share; they must interpolate the exact selected A/B endpoints without rotation, gray/white veil, or hidden-content seams. Animation strength zero must equal the static reference. | Deterministic renderer unit assertions + endpoint pixel comparison + static/animated screenshots | PARTIAL |
| COLOR-LAB-289 | User extension: `legyen több ötlet is, több alternatíva` + approval `ok` | Five portal modes | Provide one static reference mode and four independently selectable animated alternatives: Kettős árapály, Mágneses membrán, Lélegző lencse, and Celluláris mező. Each mode must have a distinct renderer and preserve its own settings when the user switches modes. | Static DOM/JS assertions + deterministic per-mode sample tests + five-mode screenshot set | PARTIAL |
| COLOR-LAB-290 | User extension: `mindegyiknél maximális kontroll sliderrel` | Complete per-mode controls | Expose every meaningful shared and mode-specific animation component as a numbered range slider with synchronized manual numeric input. Show only the active animated mode's controls, retain per-mode edits across switching, and provide a mode-local reset that never changes palette selection or interaction opacity. | Control-schema coverage test + slider/number synchronization tests + state-switch/reset tests | PARTIAL |
| COLOR-LAB-291 | User extension: `a slider container scrollozható legyen` | Internally scrollable active controls | Put the active animation controls in a fixed-max-height, momentum-enabled vertical scroll viewport. A vertical gesture beginning on a range input scrolls the internal viewport; a horizontal gesture changes the slider; boundary scrolling may continue to the page. The header drag surface remains touch-only and non-scrollable. | Static CSS/DOM assertions + gesture-routing JS tests + mobile browser interaction inspection | PARTIAL |
| COLOR-LAB-292 | Approved design spec `docs/superpowers/specs/2026-07-15-portal-energy-field-alternatives-design.md` | Portal lab performance and verification | Use one resize-aware canvas and one animation loop, pause idle painting in static/offscreen/disconnected states, honor reduced motion, bound all renderer output, and visually verify broad coherent motion without rotation seams, flame bands, or touch regressions. | Deterministic boundary tests + JS parse + HTTP runtime smoke + screenshots for static and four alternatives | PARTIAL |
| COLOR-LAB-293 | User correction: `eddig jó, de tedd a színsliderek mellé az ablak méret text inputot` | Signature window inputs beside color sliders | Put one manual numeric window-size input beside each standalone portal Traffic, Limit, and Cool color slider. Default to the previous 28% window, clamp to 10-68%, and use that value for A/B endpoint sampling. Keep touch/trail behavior unchanged and keep removed header-opacity, old motion, and rotation controls out of the rebuilt portal. | RED/GREEN static DOM/CSS/JS assertions + JS parse + HTTP inspection + screenshot inspection | PARTIAL |
| COLOR-LAB-294 | User correction: `a beállítás container szélessége akkora legyen, mint a sliderek szélessége... a box se legyen ilyen magas... telefonon nézem... alatta a contentnek ki kell férnie... selectort is(dropdown menu)` | Compact phone-width portal settings selector | Make the portal settings/mode panel the same width as the color slider panels, replace the wide horizontal mode button strip with a compact dropdown selector, and lower the active controls viewport height so the header lab behaves like one phone screen with room for content below. Keep vertical slider-panel scrolling and existing touch/trail behavior unchanged. | RED/GREEN static DOM/CSS/JS assertions + JS parse + HTTP inspection + Android screenshot inspection | PARTIAL |
| COLOR-LAB-295 | User request: `a balance mode színösszeállítását... az a1-ben található türkiz-rózsaszín... 4 slider lesz alul 3 helyett` + approved design `docs/superpowers/specs/2026-07-16-balance-money-flow-portal-design.md` | Fourth Money-flow signature and A1 five-stop palette | Add a fourth standalone portal signature row named `Money flow`. It uses the ordered palette `#49cfc5` → `#8defe5` → `#f8e8f3` → `#f7b2f5` → `#d8b4fe`. The existing Traffic, Limit, and Cool signatures keep their current colors and behavior. | RED/GREEN pure palette + static DOM/JS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-296 | User request: `nem 0-100... money flow arány... 100-0, 50-50, 0-100... mindig látható a header 2 oldalának színei` + user approval: `legyen 8%` | Semantic income/expense ratio geometry | Interpret the fourth slider as income percentage and display `income–expense`. Map semantic 0–100 to a visual split of 8–92%, so turquoise/income always remains on the left, pink-purple/expense always remains on the right, and each side retains at least an 8% visible band. Synchronize a manual ratio input with the slider. | Pure ratio mapping/order tests + RED/GREEN DOM/JS synchronization assertions + 0/50/100 screenshots | PARTIAL |
| COLOR-LAB-297 | Approved option: `Balance membrán` | Ratio-locked Balance membrane field | Append a Balance membrane mode whose vertically varying low-frequency boundary moves around the selected ratio while preserving the time-averaged split and semantic side ownership. Strength zero exactly matches the static five-stop Money-flow gradient. | Deterministic sampler/control coverage + boundary invariant tests + animated Android screenshot | PARTIAL |
| COLOR-LAB-298 | Approved option: `Balance ellenáram` | Compensated Balance counterflow field | Append a Balance counterflow mode with alternating turquoise and pink/purple lobes that locally intrude across the seam in compensated counterphase, without reversing sides or silently changing the selected overall ratio. | Deterministic sampler/control coverage + side-swap/area-compensation tests + animated Android screenshot | PARTIAL |
| COLOR-LAB-299 | Approved option: `Balance töltések` | Side-local Balance charge field | Append a Balance charges mode with independent darker/lighter energy pockets moving within each semantic side and only subtle seam drift. A charge may modulate its own palette side but may not inject the opposite side's semantic color. | Deterministic sampler/control coverage + side-local color tests + animated Android screenshot | PARTIAL |
| COLOR-LAB-300 | User constraint: `természetesen a többi modehoz nem nyúlsz... ezt extraként tedd bele... újabb animációk a dropdownban` | Additive isolation and existing interaction preservation | Append the fourth signature and three new modes without changing the five existing mode labels/order/output, the three existing signature results, the accepted touch bloom/trail/ripple/fade, or their interaction-opacity control. Reuse the existing single canvas/lifecycle and scrollable active-control viewport. | Existing regression suite + source-contract hashes/assertions + JS parse + HTTP smoke + mobile touch/scroll inspection | PARTIAL |
| COLOR-LAB-301 | User bug report: `ha egyszer váltunk a money flow-ra, már nem tudjuk visszaállítani, a többi színezés nem írja felül a money flow-t` | Money-flow signature escape hatch | After Money flow or any Balance-only animation mode has been selected, moving Traffic, Limit, or Cool must clear the money-flow semantic state and leave Balance-only rendering so the selected non-money A/B colors visibly own the standalone test header again. Money flow must remain available afterward and existing touch/trail behavior must stay unchanged. | RED/GREEN static JS regression + JS parse + HTTP smoke + Android screenshot/manual interaction | PARTIAL |
| COLOR-LAB-302 | User request: `d4: a monthcard szélessége nem ér végig a glossy containeren... focused month mode... monthcardnak szélesebbnek kell lennie, mint a d3-ban` | D4 focused monthly heatmap card width | In D4 monthly stage2, the single month-card must span the monthly glossy panel width instead of inheriting the D3 three-column year-grid width. It remains one colorable month view and is visibly wider than D3 cards. | RED/GREEN static CSS assertion + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-303 | User request: `d3: a monthcardokról szedd ki a kékes glow-t a jobb felső sarokból` | D3 month-card top-right blue glow removal | D3 yearly stage2 monthcards must not use the aurora/cyan top-right glow variant. Their glossy styling may remain, but the right-top blue radial highlight must be absent from D3 cards. | RED/GREEN static CSS/JS variant assertion + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-304 | User request: `a category gombot kiveszed mindenhonnan a b c d sorból (jobb felső sarok üveg gomb)` | Remove B/C/D header category glass buttons | Remove the top-right glossy category/menu button from all common-header B, C, and D row screens. The source Balance row and runtime Budget/Mind clones must not render `.common-header-menu`, while unrelated A-row/dashboard buttons may remain. | RED/GREEN static DOM assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-305 | User request: `megtervezel egy új screent... c sor alá... fullscreen menu... query menu... preview card... nézetválasztó... kategória... vendor... szűrőfinomítás... expand-collapse... alkalmaz pill` + screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260716-084152.png` | C-after Query Menu fullscreen filter screen | Insert one new phone-screen row after the C/Budget mode and before the D/Mind mode. The screen is a fullscreen Query Menu based on the latest screenshot: scrollable content, stage0-style preview card, Sum/Year/Month selector, view-specific multi-select controls, collapsible Category and Vendor sections, a refinement section with thresholds and extra filter options, and a sticky bottom Apply pill. It must be a separate row that pushes the D row, portal test header, and palette down. | RED/GREEN static DOM/CSS order assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-306 | User request: `legyen interaktív, tappelhető kijelölhető elemekkel` | Query Menu tappable selectable controls | The Query Menu view selector, period chips, category chips, vendor rows, and refinement rows must be real tappable/selectable controls. Tapping them toggles visual selected state, `aria-pressed`, checkmarks where relevant, section active counters, and the sticky active-filter summary without breaking section collapse or scroll. | RED/GREEN static JS/DOM assertions + HTTP inspection + Android manual tap test | PARTIAL |
| COLOR-LAB-307 | User request: `a5-öt, a6-ot duplikáld be mellé... egyelőre csak rakd be a query mellé, egy sorban legyenek` | Query row A5/A6 duplicates | Keep the original A5 and A6 screens in their existing A-row location, and add duplicated A5/Add transaction and A6/Add recurring phone screens as siblings beside the Query Menu screen in the same `data-query-menu-row`. The row order must be Query, A5 duplicate, then A6 duplicate, with unique duplicate `data-screen` IDs so later app-logic redesign can target them independently. | RED/GREEN static DOM/order assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-308 | User request: `újabb screent rajzolsz ezek mellé, pontosabban hármat... popup wizard 3 elemét egymás mellé... válassz nevet, válassz színt, válassz ikont... infinite wheel... középső nagyobb, szélső kisebb... összes ikon a carousel része` | Category popup wizard replacement screens | Add three more Query-row sibling screens after Query/A5/A6: a popup wizard name step, color step, and icon step. They must represent the replacement for the fullscreen CategoryManager/category-card page and the old 3×7 slot color/icon selector UI. Color data still references the existing slot-gradient logic, but visually uses an infinite ticking wheel with a larger centered item and smaller side items. Icon selection uses the same wheel mechanic and marks all app icons as carousel content instead of 21 slot-bound user-selected icons. | RED/GREEN static DOM/CSS order assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-309 | User approval after design question: `igen` for `Q4: Válassz színt; Q5: Válassz ikont; Q6: Nevezd el + teljes preview + Kész` | Category wizard color-icon-name flow | Reorder the category popup wizard so visual identity is chosen before text entry: Q4 is color wheel, Q5 is icon wheel, and Q6 is the final name step. The final name popup must show the selected color + selected icon + name as a complete preview and end with the `Kész` action. | RED/GREEN static DOM/order assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-310 | User request: `d1-ben mutasd be` after logbox/header glassmorph design discussion | D1 header-glass/logbox-soft demo | In Mind D1 only, demonstrate the hierarchy decision: the header is the dominant strong glass/energy surface, while transaction logboxes stay readable soft-frosted cards rather than full glassmorph surfaces. The demo must be scoped to D1 and must not turn all B/C/D logboxes into heavy glass. | RED/GREEN static DOM/CSS/JS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-311 | User correction: `a logboxokban nem látok eltérést` | D1 logbox visible soft-frost distinction | Strengthen only the Mind D1 demo logboxes so they are visibly different from the normal white logboxes while remaining readable soft-frosted cards, with a cascade-safe override after the generic Spendee logbox rule. | RED/GREEN static CSS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-312 | User request: `az a7-et is rajzold fel ebbe a sorba, a q3 után` | Query row A7 duplicate after Q3 | Keep the original A7 push-recurring screen in its A-row location, and add a unique Q4 duplicate directly after Q3/A6 inside the Query row. The category wizard screens shift right to Q5/Q6/Q7 while preserving their color → icon → final name flow. | RED/GREEN static DOM/order assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-313 | User correction: `d1: nem tetszenek ezek a logboxok, legyenek ugyanolyan glossy designúak, mint a category carousel container c2-ben` | D1 logboxes use C2 category-carousel glossy surface | Replace the D1-only logbox soft-frost/tinted treatment with the same glossy surface language as the C2 budget category carousel container: white radial highlight, translucent vertical glass gradient, matching inset/drop shadow, blur, and lower fade/mask behavior, scoped only to Mind D1. | RED/GREEN static CSS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-314 | User correction: `nem tetszik, rakd vissza a korábbi ötletedet, csak a zöld gloss nélkül` | D1 neutral soft-frost logboxes | Replace the C2-glossy D1 logbox material with the earlier D1 soft-frost card idea, but remove all green/colored gloss. The D1 logbox remains a neutral readable glass card with white gradients, a bright border, top sheen, and moderate blur, scoped only to Mind D1. | RED/GREEN static CSS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-315 | User request: `c2-ben csinálj egy effectet a category carouselben, a korongoknak legyen 3d hatása, mintha egy benyomott lencse lenne (mint egy vörösvértest)` | C2 biconcave category carousel badges | In Budget C2 stage1 only, category carousel badges get a 3D depressed-lens/biconcave effect using outer rim highlight, central concavity shadow, inner light, and preserved colored avatar identity without changing the global category badge geometry. | RED/GREEN static CSS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-316 | User request: `testportal beállítások... money flow esetében nincs ablakméret... oda nem kell text input... többi mellett ... ablak mérete 10 és 100 között... portal színezési opciókhoz kell egy opacity csúszka, ami az ablak opacityt változtatja, a szövegét nem` | Portal signature controls cleanup | In the standalone test portal, Money flow is slider-only with no adjacent numeric/text input. Traffic, Limit, and Cool keep manual window-size inputs that accept any numeric percentage from 10 to 100 and feed the same signature sampling path. The portal signature/coloring panel adds a separate window-opacity slider that changes only the colored portal window/background/canvas opacity, not the header text or touch-interaction opacity. | RED/GREEN static DOM/CSS/JS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-317 | User bug report: `a színskálák beírásakor... nem lehet teljesen kitörölni a számot... mindig 10 lesz delete esetében... lehessen kitörölni... ha többet írunk be mint 100, akkor 100ban maximalizálódik, de miután entert nyomunk csak, vagy kitappelünk... ha kevesebb mint 10, azt 10-ként értékelje` | Deferred window-size numeric input commit | Color-scale window-size number fields must stay freely editable while typing, including a completely empty field after Delete. They must not clamp or rewrite on the `input` event. The value is normalized only on Enter, blur, or change/tap-out: above 100 commits as 100, below 10 commits as 10, and empty/invalid commit restores the last valid committed value. This applies to the standalone test portal Traffic/Limit/Cool window inputs and the header scale-lab window inputs; Money flow remains slider-only. | RED/GREEN static JS/DOM assertions + JS parse + HTTP inspection + Android manual input screenshot | PARTIAL |
| COLOR-LAB-318 | User correction: `az inverz lencsét visszavonom a c2-ből, inkább kifele domborodó lencse effect legyen` | C2 convex category carousel badges | Supersede the prior C2 inverse/depressed lens visual. Budget C2 stage1 category carousel badges must keep the same scoped placement and colored category identity, but use an outward convex lens effect: bright upper-left specular highlight, soft center glow, lower curvature shadow, raised outer/drop shadow, and no central dark biconcave depression or multiply-shadow pseudo-element. | RED/GREEN static CSS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-319 | User request: `a testheader lesz a portal üzenetközvetítő... legyen rajta egy placeholder balance összeg/felirat, mint b1-ben... header ablak mellé egy gombot is... üzenet trigger testgomb` | Portal relay content and adjacent trigger | Replace the standalone test header's instructional placeholder with persistent, position-matched Balance and portal-message panels. Balance shows B1-style `Balance` plus `-372 047 472 Ft`; message shows the approved portal test copy. Add a separate 44 px trigger beside, not inside, the touch/drag header while keeping the combined row aligned to the existing portal panel width. | RED/GREEN DOM/CSS/accessibility assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-320 | User request: `ha a user a gombot megnyomja... eltűnik az eredeti content, és egy portal testüzenet előjön... infinite loop switch... vissza is lehet morph animációval állítani a balanceot` + approved design | Reversible infinite Balance/message morph toggle | Each trigger press alternates Balance and message indefinitely. Both directions use the selected morph. A press during an active opposite transition reverses from current visual progress without snapping or queuing duplicate animations. Final state, `aria-hidden`, `aria-pressed`, accessible label, and `data-portal-message-state` must agree. | RED/GREEN pure/runtime behavior tests + HTTP interaction smoke + Android rapid-toggle inspection | PARTIAL |
| COLOR-LAB-321 | User request: `valamilyen fade out fade in... blur, vagy etc... több lehetőség legyen, tervezd meg` + approved four-mode design | Four content-only portal message morphs | Add the approved `Diffúz fókusz`, `Portal rekesz`, `Energia-söprés`, and `Spektrális visszhang` modes in that order, with Diffúz fókusz as default. Each mode visibly differs and affects only foreground content; portal colors, canvas/idle animation, touch bloom, trail, ripple, and fades remain unchanged. | RED/GREEN schema/keyframe tests + touch/energy source-contract assertions + four-mode Android inspection | PARTIAL |
| COLOR-LAB-322 | User request: `újabb dropdown menut készítesz, ugyanolyan csúszkákkal, mint jelenleg a fő animáció, hogy állítani lehessen` + approved design | Scrollable detailed message-morph controls | Add a separate compact `Portal üzenetváltás` panel below the main energy panel. It has one dropdown, active-mode-only numbered sliders with synchronized manual number inputs for every approved parameter, per-mode setting persistence, and active-mode-only reset. Its control viewport is vertically scrollable and matches the existing phone-width/height-cap pattern. | RED/GREEN DOM/schema/controller assertions + mobile slider/scroll inspection | PARTIAL |
| COLOR-LAB-323 | Approved portal message morph design invariants | Portal message isolation, containment, and accessibility | Keep the adjacent trigger outside the `touch-action:none` drag surface; keep all existing touch/background behavior unchanged; contain both message states and temporary effects inside the header; use a 160 ms opacity-only reduced-motion fallback and a deterministic no-Web-Animations final-state fallback. | Static source-contract tests + JS parse + HTTP smoke + Android touch/content-containment inspection | PARTIAL |
| COLOR-LAB-324 | User request: `mutass több lehetőséget, ezt is dropdown szerűen rakd be, de legyen olyan is, hogy nincs háttéreffect` + approved design | Six-option message-background response selector | Add a separate `Portal háttérreakció` dropdown after the foreground morph panel with exact order: Nincs háttéreffekt, Energiakompresszió, Refrakciós hullám, Határfény, Mélységi fókusz, Kromatikus riasztás. `Nincs háttéreffekt` is the default and must produce exact pre-feature portal background behavior with no residual overlay animation/style. | RED/GREEN pure/static selector tests + HTTP inspection + six-state Android screenshots | PARTIAL |
| COLOR-LAB-325 | Approved background-response visual alternatives | Five distinct semantic-safe background responses | Implement five visually distinct overlay-only reactions. Energy compression changes density/luminance; refraction wave supplies expanding water-like rings; seam flare follows the semantic A/B split anchor; depth focus adds vignette/central depth; chromatic alert adds a bounded ≤45% lilac/pink pulse. None may rewrite A/B endpoints, Money-flow ratio, canvas settings, foreground content, or touch variables. | RED/GREEN descriptor/boundary/source-contract tests + Android visual comparison | PARTIAL |
| COLOR-LAB-326 | User request for dropdown-style alternatives with the existing detailed control language + approved design | Detailed scrollable per-response controls | Every animated response gets its approved active-mode-only numbered sliders and synchronized manual inputs, including duration, strength, peak, hold, decay, and mode-specific geometry. Preserve settings per mode, hide controls for `none`, and reset only the active background mode. The panel shares the existing phone width, height cap, and vertical scroll router. | RED/GREEN schema/controller/static tests + mobile slider/manual/scroll inspection | PARTIAL |
| COLOR-LAB-327 | User rationale: `hogy a pulse üzenet egyértelműbb legyen` + approved synchronization contract | Background/foreground synchronized reversible portal delivery | Start the selected background response with the Balance/message foreground morph, add it to the same active animation group, and reverse all parts together on a rapid opposite tap. Forward settles to adjustable message hold; returning Balance clears the overlay. Preserve reduced-motion/no-WAAPI fallbacks, trigger accessibility, existing energy canvas, and accepted touch interaction. | RED/GREEN forward/backward/reversal/rest-state tests + protected regressions + Android rapid-toggle inspection | PARTIAL |
| COLOR-LAB-328 | Earlier request: `a háttér szín is változzon... semmi/statikus/dinamikus animációk... skála: fehér-világos rózsaszín-lila`; later corrected by `nem bal fix A jobb fixen B... a jelenlegiek invalidak` | Superseded Portal message-color selector geometry | Preserve the exact Portal palette, but do not complete the earlier left/right mode list. Its message-only mode geometry and exact options are superseded by `COLOR-LAB-333`–`COLOR-LAB-340`; Balance-side modes are not superseded. | Checklist/spec review against latest user correction | NOT DONE |
| COLOR-LAB-329 | Approved dedicated message-color layer architecture | Full temporary portal-color replacement | While Balance is visible, preserve the original financial A/B field. At the message endpoint, crossfade the original CSS/canvas visual opacity to zero and a dedicated normal-blend portal-color canvas to the current window opacity so the financial colors do not bleed through. Returning Balance reverses and clears the message layer without mutating financial A/B, Money-flow, or base energy state. | RED/GREEN endpoint/layer/source-isolation tests + Android message/Balance comparison | PARTIAL |
| COLOR-LAB-330 | Earlier approval to reuse the existing dynamic A/B effects; explicitly revoked by `mivel megváltozik a háttér a/b logika... a jelenlegiek invalidak` | Superseded reuse of four left/right Portal-message fields | Do not reuse Kettős árapály, Mágneses membrán, Lélegző lencse, or Celluláris mező inside the Portal-message endpoint. Replace only their message-side use with the A-base/B-matter modes in `COLOR-LAB-334`; do not remove or change unrelated Balance modes. | Checklist/spec review + source assertion that invalid IDs are absent only from the message-field menu | NOT DONE |
| COLOR-LAB-331 | User request: `legyen egy csúszka is, és ablak méret is` + approved control behavior | Portal palette center/window and detailed controls | Add a shared 0–100 center slider (default 50) and deferred 10–100 window-size number input (default 68) using `A/B = sample(center ± window/2)`. Static shows these controls; dynamic modes additionally show isolated numbered controls; none hides them. Preserve settings, active-mode-only reset, and vertically scrollable phone-width controls. | RED/GREEN sampling/input/controller/scroll tests + Android slider/manual/reset inspection | PARTIAL |
| COLOR-LAB-332 | Earlier simple synchronized crossfade; later corrected by `a portal üzenet háttér és az alapháttér (balance) mindig más... a kettő közti átmenet legyen animálva, valahogy a színeknek át kell változniuk` | Superseded global-opacity-only background transition | Preserve synchronized reversal and settled field-only preview, but replace the global-opacity-only source crossfade with the explicit full-header transformation renderer and selectable morphs in `COLOR-LAB-337`–`COLOR-LAB-339`. | Checklist/spec review + new transition renderer tests | NOT DONE |
| COLOR-LAB-333 | User correction: `nem bal fix A jobb fixen B, hanem az alapszín mindenhol A, és abban vannak B színű vándorló ködök, szigetek` | Portal message A-base/B-matter field semantics | At the Portal message endpoint, fill the whole field with sampled Portal A and introduce sampled Portal B only through a bounded two-dimensional matter mask. B appears as fog/islands with direct A→B color interpolation, never as a permanent side, seam, veil, or rotating linear-gradient endpoint. The exact Portal palette remains `#fffdfd → #ffc4e4 → #8b5cf6`. | RED/GREEN pure sampler/renderer pixel tests + deterministic frame inspection + Android screenshots | PARTIAL |
| COLOR-LAB-334 | User request and approval: `más mozgások kellenek` + approved alternatives `Vándorló köd`, `Élő szigetvilág`, `Keletkező energiafelhők`, static and no-dynamic choices | New Portal endpoint field modes and controls | Replace only the Portal-message dropdown's invalid four left/right modes with exact order: Nincs dinamikus effekt, Statikus köd/szigetek, Vándorló köd, Élő szigetvilág, Keletkező energiafelhők. Dynamic B matter must wander/morph without horizontal bands, repeated flame lanes, fixed sides, or axis rotation. Each mode exposes active-only numbered slider/manual controls, preserved per-mode settings/phase, reset isolation, and scrollable phone-width controls. | RED/GREEN schema/order/per-control renderer tests + source isolation + five-mode Android motion inspection | PARTIAL |
| COLOR-LAB-335 | User clarification: `szöveg morph, szöveg háttér morph, háttér morph, ezek külön rétegek a saját animációjukkal ki be kapcsolással` | Three independently switchable Portal relay layers | Split delivery into foreground text morph, local text-background morph, and full-header background morph. Each has an explicit switch, owns only its DOM/canvas state and animation handles, preserves its mode/settings while off, and can be toggled without resetting or silently driving the other two layers. The accepted touch interaction remains a separate unchanged system. | RED/GREEN state/controller/DOM ownership tests + Android independent-toggle matrix | PARTIAL |
| COLOR-LAB-336 | User bug report: `elvileg van implementálva kikapcsolás, de nincs hatása, ugyanúgy van színeffect` | True off state for text-background morph | Remove the unconditional colored accent from the foreground text controller and make the text-background controller its sole owner. Explicit text-background off must cancel/clear the animation and leave no ghost, hold, opacity, filter, mask, transform, background, pseudo-element, or custom-property residue. Foreground off swaps text without blur/color effects. | Witnessed RED/GREEN none/off residual-style tests + DOM/style inspection + Android off comparison | PARTIAL |
| COLOR-LAB-337 | User correction: `headeren belüli containerben van... padding, ahol ez nem érvényes... nem kell belső container` | Full-header visual layers without inset clipping | Move Portal field, transition compositor, and optional text-centered background reaction outside the padded foreground viewport. They are direct `inset: 0` pointer-free header layers clipped only by the outer header radius. Only text layout retains padding; no colored effect may end at the current 16px/17px content inset or leave static edge strips. | RED/GREEN DOM ancestry/CSS geometry tests + Android edge-coverage screenshots | PARTIAL |
| COLOR-LAB-338 | User correction: `portal üzenet háttér és az alapháttér (balance) mindig más... a kettő közti átmenet legyen animálva, valahogy a színeknek át kell változniuk` | Fixed distinct Balance/Portal endpoints and real color transformation | Keep the current financial Balance field and the new Portal field as two distinct immutable endpoints. A full-header compositor must transform actual source-frame colors/geometries into the target field with a bounded 2D progress mask, then hand off to the live endpoint renderer. Background-morph off skips animation but still switches immediately between the distinct endpoint backgrounds. | RED/GREEN endpoint/source preservation, transition pixel, off/snap, forward/reverse tests + Android comparison | PARTIAL |
| COLOR-LAB-339 | User request: `újabb menut... eredeti A/B színeket... effecttel áttranszformálni az üzenet hátterére` + approved alternatives | Balance→Portal background-transition menu | Add a separate transition dropdown inside the full-background layer with exact order: Pigmentterjedés, Szigetes átalakulás, Folyékony színátírás. Each has isolated active-only slider/manual controls and a distinct non-global-crossfade transformation descriptor; settings persist per mode and the selected mode applies symmetrically in reverse. | RED/GREEN exact-order/schema/per-control/mask/reversal tests + three-mode Android inspection | PARTIAL |
| COLOR-LAB-340 | Approved combined redesign in `docs/superpowers/specs/2026-07-16-portal-message-field-transition-redesign-design.md` | Portal relay lifecycle, fallbacks, performance, and protected behavior | Run enabled text, text-background, and full-background layers in one reversible delivery lifecycle while retaining independent ownership and true disabled behavior. Preserve rapid reverse, endpoint accessibility, field-only settled previews, 160ms reduced motion, deterministic no-canvas/no-WAAPI endpoints, bounded reusable canvases, mobile control scrolling, exact Balance/Money-flow state restoration, and the accepted touch bloom/trail/ripple/release implementation byte-for-byte where protected. | Full pure/static/runtime suites + JS parse + HTTP smoke + diff check + Android rapid-toggle/touch/scroll inspection | PARTIAL |
| COLOR-LAB-341 | User request: `ezek a containerek a header alatt expandable collapseable legyenek egy chevron gombbal, mert sok a content a header card alatt` | Standalone Portal test controls below the header | Every control container below the test Portal header becomes independently expandable/collapsible with a chevron button. The header and trigger remain visible, the compact color/opacity controls stay open by default, advanced controls collapse by default, collapsed bodies take no page height, and internal slider-scroll/touch behavior remains intact. | RED/GREEN static DOM/CSS/JS assertions + HTTP inspection + Android manual collapse/slider-scroll check | PARTIAL |
| COLOR-LAB-342 | User request: `cseréld le a logókat a screenekből, és az ikon szerkesztőből is... új ikon is legyen színezhető... emulated 0 spendee fluvi_vector.svg` | Lower screen live logo previews and path editor | Replace every lower screen live logo preview and the path-level logo editor source with the Fluvi SVG from `/storage/emulated/0/spendee/Fluvi_vector.svg`. The local prototype asset must expose stable path ids, preserve the Fluvi source fills before edits, and keep the existing per-path recolor and live-preview synchronization behavior. | RED/GREEN static SVG/DOM/JS assertions + HTTP asset inspection + browser recolor inspection | PARTIAL |
| COLOR-LAB-343 | User request: `a logo a screeneken legyen 10% kisebb` | Lower screen Fluvi logo sizing | Reduce only the lower screen live logo size by 10%, from the current `79.5px` token to `71.55px`, without changing the logo editor's large editing canvas or the header/content positioning tokens. | RED/GREEN static CSS assertion + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-344 | User request: `a brand nevet mindenhil írd át spendeerő fluvi ra` | Visible lower brand copy and accessible labels | Replace visible lower-section brand copy from `spendee`/`Spendee` to `fluvi`/`Fluvi` in the screen lockups, live logo accessible labels, and visible/editor headings. Internal class names, historical references, and source path names may keep `spendee` when they are not user-visible brand text. | RED/GREEN static DOM/copy assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-345 | User request: `még 10% kisebb` | Lower screen Fluvi logo additional sizing | Reduce only the lower screen live logo size by another 10%, from the current `71.55px` token to `64.395px`, without changing the logo editor's large editing canvas or the header/content positioning tokens. | RED/GREEN static CSS assertion + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-346 | User request: `még 10% kisebb` | Lower screen Fluvi logo third sizing reduction | Reduce only the lower screen live logo size by another 10%, from the current `64.395px` token to `57.9555px`, without changing the logo editor's large editing canvas or the header/content positioning tokens. | RED/GREEN static CSS assertion + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-347 | User request: `milyen színt javasolsz... fluvi-flow-money flow- energiamező flow` then `mutasd, rajzold le ezeket alulra a nagy logo szerkesztő mellé` | Fluvi logo colour recommendation previews | Add three isolated comparison cards beside the large logo editor: original Fluvi palette, app-harmonized palette, and portal/emissive palette. The cards render from the same Fluvi SVG but do not participate in live editor sync, so they remain stable recommendations while the main logo editor stays editable. | RED/GREEN static DOM/JS assertions + HTTP inspection + Android screenshot | PARTIAL |
| COLOR-LAB-348 | User correction from latest screenshot: `3les határ van az objecten belül, mert nem egybefüggőek az f betű komponensei, nem tudtál rendes gradienst csinálni` | Fluvi logo preview continuous object gradients | Recommendation preview cards must render each Fluvi mark as one continuous SVG-level gradient field shared by all paths, not per-path gradient assignments. This removes hard internal seams caused by the split F components while keeping the main path editor separately editable. | RED/GREEN static JS assertions + latest screenshot inspection + HTTP/browser inspection | PARTIAL |
| COLOR-LAB-349 | User request: `c1-c2-ben a logboxok avatarjai is legyenek lencse alakúak, 3d hatásúak` | Budget C1/C2 logbox avatar lens effect | Scope a convex 3D lens material to Budget C1/C2 transaction logbox avatars, including highlight, lower shadow, rim, and protected icon layer, without changing the already scoped C2 category carousel badge lens or unrelated rows. | RED/GREEN static CSS assertions + Android screenshot | PARTIAL |

## Portal rebuild evidence — 2026-07-15

- Automated evidence complete: pure renderer tests, full HTML regression suite, inline-script parse, touch source/CSS hashes, HTTP 200 smoke, and bounded two-phase renderer samples for all four animated modes.
- Algorithm-frame inspection shows broad coherent A/B boundaries with distinct morphology and no rotation seams, repeated flame bands, or white/gray overlay.
- Remaining evidence: Android-browser screenshots for all five modes plus direct mobile touch-trail and vertical-on-slider scroll inspection. Termux could open the local page, but Android denied programmatic system screenshot capture, so `COLOR-LAB-286`–`292` remain honestly `PARTIAL`.
- `COLOR-LAB-293` automated evidence: RED/GREEN static regression, renderer unit test, JS parse, inline-script parse, diff whitespace check, and HTTP smoke confirm the three row-level signature window inputs are present and wired. Remaining evidence: Android-browser screenshot confirming the layout beside the color sliders.
- `COLOR-LAB-294` automated evidence: RED/GREEN static regression, renderer unit test, JS parse, inline-script parse, diff whitespace check, and HTTP smoke confirm the mode/settings panel now shares the slider width, uses a dropdown selector, removes the old horizontal button strip, and caps the active-controls viewport at `min(30vh, 240px)`. Remaining evidence: Android-browser screenshot confirming the phone-height layout leaves room for content below.

## Balance money-flow portal evidence — 2026-07-16

- `COLOR-LAB-295` automated evidence: a witnessed RED→GREEN pure palette test and HTML regression test confirm the fourth `Money flow` signature and the exact A1-derived five-stop sequence. The HTTP-served prototype contains the row, palette, and synchronized manual input.
- `COLOR-LAB-296` automated evidence: exact pure assertions cover semantic `0–100`, `50–50`, and `100–0` states, the 8–92% visual mapping, monotonically ordered stops, manual/range controller wiring, and permanent left-income/right-expense edge ownership.
- `COLOR-LAB-297`–`299` automated evidence: all three appended modes have deterministic bounded samplers, independent detailed control schemas, strength-zero static parity, per-control response coverage, default and all-controls-max ratio-preservation stress tests, and fixed semantic outer edges. The measured worst all-controls-max mean drift was 0.023 percentage point.
- `COLOR-LAB-300` automated evidence: the existing five mode IDs remain the unchanged dropdown prefix, the original four field samplers retain their prior regression coverage, touch source/CSS hash assertions remain green, exactly one test-header canvas is present, inline/external JS parses, `git diff --check` is clean, and HTTP smoke confirms all new controls/modes are served.
- `COLOR-LAB-301` automated evidence: witnessed RED→GREEN static regression confirms Traffic, Limit, and Cool now clear the money-flow semantic attribute and force any active Balance-only render mode back to static so the selected non-money A/B gradient owns the standalone test header again.
- `COLOR-LAB-302` automated evidence: witnessed RED→GREEN static regression confirms the D4 focused monthly grid now overrides the D3 three-column grid after the base rule and forces both the monthly grid and single month-card to `width: 100%`.
- `COLOR-LAB-303` automated evidence: witnessed RED→GREEN static regression confirms D3 `stage2-scroll` monthcards now resolve to the non-cyan `frost` glass variant instead of the `aurora` top-right blue glow variant.
- `COLOR-LAB-304` automated evidence: witnessed RED→GREEN static regression confirms the source B row and the full B/C/D common-header mode area contain zero `.common-header-menu` top-right category buttons.
- `COLOR-LAB-305` automated evidence: witnessed RED→GREEN static regression confirms a standalone Query Menu phone screen is inserted after C/Budget and before D/Mind, uses the latest screenshot as reference, includes preview card, Sum/Year/Month selector, multi-select category/vendor/view controls, threshold/refinement content, section collapse toggles, internal scrolling, and a sticky Apply pill.
- `COLOR-LAB-306` automated evidence: witnessed RED→GREEN static regression confirms Query Menu view choices and filter controls expose tappable `button`/`aria-pressed` state, `data-query-selectable`, filter categories, checkmark synchronization, section counters, and active-filter summary synchronization.
- `COLOR-LAB-307` automated evidence: witnessed RED→GREEN static regression confirms the Query Menu row includes Query Menu, unique A5/Add transaction duplicate, and unique A6/Add recurring duplicate, with original A5/A6 `data-screen` blocks remaining exactly once outside the duplicate row.
- `COLOR-LAB-308` automated evidence: witnessed RED→GREEN static regression confirms the Query row contains the category popup wizard screens after the adjacent A-duplicates. The color step preserves slot-gradient data semantics while replacing the old 3×7 selector with an infinite ticking wheel, and the icon step marks all icons as carousel content while superseding the 21-slot user-selected icon manager concept.
- `COLOR-LAB-309` automated evidence: witnessed RED→GREEN static regression confirms the approved wizard flow remains color → icon → final name/preview. After inserting Q4/A7, the wizard now renders as Q5 color, Q6 icon, Q7 final name. The final name step includes a selected color + selected icon + entered name preview and ends with the `Kész` action.
- `COLOR-LAB-310` automated evidence: witnessed RED→GREEN static regression confirms Mind D1 gets a scoped `header-strong-logbox-soft` demo and the generated D1 header is marked as `strong-glass-energy`; its original soft-frost logbox material was later superseded by `COLOR-LAB-313`.
- `COLOR-LAB-311` automated evidence: witnessed RED→GREEN static regression confirmed the intermediate visible soft-frost logbox override; that intermediate tinted material was later superseded by the C2 category-carousel glossy surface in `COLOR-LAB-313`.
- `COLOR-LAB-312` automated evidence: witnessed RED→GREEN static regression confirms the Query row now has seven screen columns in order: Q1 Query Menu, Q2 A5 duplicate, Q3 A6 duplicate, Q4 A7 push-recurring duplicate, Q5 color wizard, Q6 icon wizard, and Q7 final name wizard. The original A7 `alt-add-recurring-push-sheet` remains exactly once outside the Query duplicate row.
- `COLOR-LAB-313` automated evidence: witnessed RED→GREEN static regression confirmed the intermediate C2 category-carousel glossy material for D1 logboxes; that material was later rejected and superseded by `COLOR-LAB-314`.
- `COLOR-LAB-314` automated evidence: witnessed RED→GREEN static regression confirms the D1-only logbox surface is back to the earlier neutral soft-frost idea with white gradients, bright border, top sheen, 10px blur/saturation, and no green/colored gloss or C2 mask material.
- `COLOR-LAB-315` automated evidence: witnessed RED→GREEN static regression confirmed the earlier Budget C2 biconcave/depressed-lens effect; this visual was later explicitly revoked and superseded by `COLOR-LAB-318`.
- `COLOR-LAB-316` automated evidence: witnessed RED→GREEN static regression confirms the standalone test portal Money flow row is slider-only, Traffic/Limit/Cool window-size inputs use `min=10 max=100` and clamp through the shared 10–100 sampling path, and the signature panel has a separate window-opacity slider that writes `--mind-portal-window-opacity` to the background/canvas only, leaving text and interaction opacity separate.
- `COLOR-LAB-317` automated evidence: witnessed RED→GREEN static regression plus full verification (`color_lab_static_test.js`, `color_lab_portal_energy_test.js`, JS syntax checks, inline-script parse, `git diff --check`, and HTTP exact source checks) confirms the shared deferred window-number helper exists, the standalone test portal Traffic/Limit/Cool inputs and the header scale-lab window inputs no longer clamp on `input`, and commit/clamp is wired only to Enter, blur, or change with the 10–100 bounds.
- `COLOR-LAB-318` automated evidence: witnessed RED→GREEN static regression plus full verification (`color_lab_static_test.js`, `color_lab_portal_energy_test.js`, JS syntax checks, inline-script parse, `git diff --check`, and HTTP C2 convex source checks) confirms the C2 category carousel badge effect no longer contains the revoked central dark biconcave depression or multiply-shadow pseudo-element, and now uses scoped outward convex lens tokens: upper-left specular highlight, soft center glow, lower curvature shadow, raised drop shadow, and stronger raised center-badge highlights.
- Remaining evidence: Android-browser screenshots at `0–100`, `50–50`, and `100–0`, one screenshot for each new animated mode, plus direct mobile inspection of touch/trail preservation, vertical controls scrolling, Money-flow-to-Traffic/Limit/Cool return, D4 focused monthly card width, D3 glow removal, B/C/D category-button removal, Query Menu layout/scroll/collapse behavior, Query Menu tap/select behavior, Query-row A5/A6/A7 side-by-side placement, Q5/Q6/Q7 category wizard popup/wheel visuals, Q5 color → Q6 icon → Q7 final name preview flow, the D1 neutral soft-frost logboxes, C2 outward convex category carousel badges, test-portal signature/window-opacity controls, and manual Android keyboard testing for deferred window-size numeric commit. Therefore `COLOR-LAB-295`–`318` remain honestly `PARTIAL`.

## Portal message morph evidence — 2026-07-16

- `COLOR-LAB-319` automated evidence: witnessed static RED at the obsolete instructional test-header content, then GREEN after adding the position-matched B1-style Balance panel, portal-message panel, phone-width relay row, and 44 px external trigger. DOM assertions prove the trigger is a sibling after `</header>`, not part of the `touch-action:none` drag surface; HTTP source smoke confirms the rendered source.
- `COLOR-LAB-320` automated evidence: the pure module test proves indefinite `balance ↔ message` target alternation, while the runtime source contract requires one active animation array, opposite-target `animation.reverse()` reuse, token-guarded endpoint commit, and synchronized `data-portal-message-state`, `aria-hidden`, `aria-pressed`, and accessible trigger labels. The runtime-controller assertion was witnessed RED before implementation and GREEN afterward.
- `COLOR-LAB-321` automated evidence: `color_lab_portal_message_test.js` verifies the exact four-mode order and labels, frozen control defaults/bounds, distinct deterministic foreground keyframes, mirrored return direction, bounded duration, and absence of portal background/canvas/touch/trail properties. A per-control sensitivity loop proves every one of the 31 exposed sliders measurably changes its active morph descriptor. Existing portal-energy tests and protected touch CSS/function hashes remain green.
- `COLOR-LAB-322` automated evidence: static/runtime assertions confirm a separate `Portal üzenetváltás` dropdown, active-mode-only generated numbered rows, synchronized range/manual inputs, per-mode `settingsByMode`, isolated active-mode reset, and inclusion in the existing vertical control-scroll router.
- `COLOR-LAB-323` automated evidence: the full fresh verification passed `color_lab_static_test.js`, `color_lab_portal_energy_test.js`, `color_lab_portal_message_test.js`, all external/inline JavaScript parse checks, `git diff --check`, and HTTP HTML/module source smoke. The pure module supplies a 160 ms opacity-only reduced-motion descriptor; the controller commits deterministic endpoint state when Web Animations is unavailable.
- Remaining evidence: Android-browser screenshots and direct interaction inspection for all four morphs in both directions, rapid mid-animation reversal, trigger separation, text containment, internal control scrolling, reduced-motion appearance, and unchanged portal background/touch behavior. Therefore `COLOR-LAB-319`–`323` remain honestly `PARTIAL`.

## Portal message background response evidence — 2026-07-16

- `COLOR-LAB-324` automated evidence: witnessed pure-module RED with the expected missing `color_lab_portal_background.js`, plus a separate static RED at the absent overlay/panel. GREEN assertions now prove the exact six-option order/default, module load order, separate post-foreground panel, canvas → pointer-free overlay → content paint order, and an exact empty `none` descriptor/clear path.
- `COLOR-LAB-325` automated evidence: `color_lab_portal_background_test.js` verifies five distinct deterministic response descriptors, exact Money-flow seam mapping at 8/50/92%, monotonic keyframe offsets, forward/backward rest endpoints, bounded schemas, overlay-only property isolation, precomputed mobile-safe CSS geometry, and a hard 45% chromatic opacity ceiling. Existing energy, foreground-morph, and protected touch source-contract tests remain green.
- `COLOR-LAB-326` automated evidence: exact schema/default/bounds tests cover every approved control, and a per-control sensitivity loop proves every exposed value measurably changes its descriptor. Static runtime assertions prove active-mode-only numbered rows, immediate range updates, deferred manual Enter/change/blur commit, per-mode persistence, active-mode-only reset, hidden `none` controls, compact shared panel width, and inclusion in the vertical slider-scroll router.
- `COLOR-LAB-327` automated evidence: witnessed runtime RED→GREEN assertions prove the response animation enters the same `activeAnimations` array and therefore the existing `animation.reverse()` path; Balance/`none` remove all overlay inline style, message commits the selected hold, no-WAAPI commits deterministic endpoints, and reduced motion uses 160 ms opacity-only frames. A temporary pointer-free ghost crossfades visible hold states over 180 ms without replaying foreground content; starting delivery cancels that preview, while changing mode during a running delivery does not cancel the shared animation.
- Full fresh verification passed `color_lab_static_test.js`, `color_lab_portal_energy_test.js`, `color_lab_portal_message_test.js`, `color_lab_portal_background_test.js`, all three external-module syntax checks, inline-script parse (`1`), `git diff --check`, and HTTP 200 smoke for the HTML plus all three portal modules.
- Remaining evidence: Android-browser screenshots/manual inspection for all six choices, both toggle directions, rapid mid-animation reversal, exact `none` parity, hold at 0% and above 0%, visible distinction/readability of all five responses, mode-switch crossfade, slider/manual controls, internal scrolling, reduced motion, and unchanged touch/A-B/Money-flow semantics. Therefore `COLOR-LAB-324`–`327` remain honestly `PARTIAL`.

## Portal message color transform evidence — 2026-07-16

- `COLOR-LAB-328` automated evidence: witnessed pure-model RED with the expected missing `color_lab_portal_color.js`, followed by GREEN assertions for the exact six-mode order/labels/default, exact `#fffdfd → #ffc4e4 → #8b5cf6` stops, piecewise RGB samples, center/window endpoint sampling, clamping, and `none` no-op descriptor. A separate static RED at the missing color canvas/panel became GREEN after adding the post-background panel, exact dropdown, normal-blend layer order, and module load order.
- `COLOR-LAB-329` automated evidence: runtime RED→GREEN source contracts prove the registered base visual-opacity channel, dedicated opaque-pixel message canvas, full financial-source fade to zero at the message endpoint, exact Balance restoration, current window-opacity routing, no-WAAPI direct commit, and no writes from the message-color runtime to financial A/B, Money-flow, base energy mode/state, or accepted touch variables.
- `COLOR-LAB-330` automated evidence: witnessed renderer RED with the expected missing `color_lab_portal_color_renderer.js`, then GREEN deterministic RGBA tests for static plus all four reused dynamic fields. Static output is phase-invariant; every dynamic default changes by phase; `strength: 0` is pixel-identical to static; dimensions/colors are bounded; settings/phases are isolated per mode and persist independently.
- `COLOR-LAB-331` automated evidence: pure and static controller tests prove shared center `50`, window `68`, exact `A/B = sample(center ± window/2)`, immediate center/range updates, a fully deletable window field with Enter/change/blur-only 10–100 commit, sampled A/B swatches, hidden `none` controls, palette-only `static`, complete numbered dynamic schemas, active-mode reset isolation, compact phone width, and inclusion in the vertical slider-scroll router.
- `COLOR-LAB-332` automated evidence: witnessed transition RED→GREEN assertions prove `message-color-base`, `message-color-idle-canvas`, and `message-color-canvas` enter the same `activeAnimations` array as foreground/background response and therefore reuse the existing rapid `animation.reverse()` path. Settled message changes use one cancelable 180 ms canvas-snapshot field crossfade without foreground/background replay; reduced motion uses 160 ms; dynamic geometry freezes; no-canvas falls back to the static portal gradient.
- Full fresh verification passed `color_lab_static_test.js`, `color_lab_portal_energy_test.js`, `color_lab_portal_message_test.js`, `color_lab_portal_background_test.js`, `color_lab_portal_color_test.js`, and `color_lab_portal_color_renderer_test.js`; all five external-module syntax checks; inline-script parse (`1`); `git diff --check`; and HTTP 200/non-empty smoke for the HTML plus all five portal modules.
- Remaining historical evidence: Android-browser screenshots/manual inspection were never completed for the earlier six message-color choices. The later A-base/B-matter redesign explicitly supersedes the old option geometry and global-opacity-only transition, so `COLOR-LAB-328`, `COLOR-LAB-330`, and `COLOR-LAB-332` are now `NOT DONE`; the still-applicable isolated endpoint layer and palette-control work in `COLOR-LAB-329` and `COLOR-LAB-331` remains `PARTIAL` pending reuse and visual verification under `COLOR-LAB-333`–`340`.

## Portal A-base/B-matter relay redesign evidence — 2026-07-16

- `COLOR-LAB-333`–`334` automated evidence: witnessed RED→GREEN pure model and pixel-renderer tests verify the exact five endpoint modes, exact Portal palette/window samples, A-everywhere/B-mask direct color semantics, bounded two-dimensional masks, phase movement, deterministic static output, per-control sensitivity, and the absence of permanent side ownership in the Portal-message menu.
- `COLOR-LAB-335`–`337` automated evidence: witnessed RED→GREEN static/controller contracts verify three independent accessible switches, role-owned cancellation/removal, content-only foreground descriptors, exact zero-output text-background clearing, and the direct full-header paint order `Balance → Portal field → transition compositor → local reaction → padded text viewport`. Protected touch handler and CSS hashes remain byte-identical.
- `COLOR-LAB-338`–`339` automated evidence: transition model, compositor, and fake-clock player suites verify the exact three-mode order, bounded source→Portal-A→Portal-matter conversion, distinct per-control masks, exact endpoints, output-frame reuse, forward/reverse playback, cancellation, and reversal after a completed sub-animation. Reduced motion uses a uniform 160 ms endpoint crossfade rather than a spatial front.
- `COLOR-LAB-340` automated/runtime evidence: the full eleven-suite command passed (`color_lab_portal_energy_test.js`, `color_lab_portal_message_test.js`, `color_lab_portal_background_test.js`, `color_lab_portal_color_test.js`, `color_lab_portal_color_renderer_test.js`, `color_lab_portal_message_field_test.js`, `color_lab_portal_message_field_renderer_test.js`, `color_lab_portal_transition_test.js`, `color_lab_portal_transition_renderer_test.js`, `color_lab_portal_transition_player_test.js`, and `color_lab_static_test.js`). Inline JavaScript parses, `git diff --check` passes, and HTTP non-empty source smoke passes for the HTML plus every Portal dependency.
- A fresh isolated DOM/runtime smoke loaded the real served document and exercised initialization, Balance→message commit, 42% visible-canvas opacity routing, true text-background off cleanup, message→Balance return, 60 ms mid-transition reversal, and all-off immediate distinct endpoint switching. It completed with `Portal DOM/runtime smoke checks passed` and no page runtime errors after browser APIs were supplied by the harness.
- Remaining evidence: Chrome was opened on the phone at the live worktree URL, but Android denied programmatic screenshot capture (`screencap: Capturing failed`). Fresh Android-browser screenshots/manual inspection are still required for all five endpoint modes, all three transitions in both directions, independent layer combinations, zero-residue text-background off, full-header edge coverage, rapid reverse, control scrolling/manual editing, and the unchanged touch trail/release behavior. Therefore `COLOR-LAB-333`–`340` remain honestly `PARTIAL`.

## Portal control collapse and Fluvi logo evidence — 2026-07-16

- `COLOR-LAB-341` automated evidence: witnessed RED at the absent Fluvi/collapse contract, then GREEN after the standalone Portal header controls became six independent collapsible panels with chevron buttons, panel bodies, `aria-expanded` sync, first two compact panels open by default, and the four advanced panels collapsed by default. A DOM smoke exercised init plus click open/close and verified body `hidden` and `aria-expanded` toggling. The existing vertical slider-scroll routing remains unchanged and covered by the static regression.
- `COLOR-LAB-342` automated evidence: witnessed RED at the old Spendee SVG source, then GREEN after all lower live logo lockups and the editor fetch switched to the local `fluvi_vector.svg?v=20260716-fluvi-logo-v1` asset. The local asset preserves the Fluvi source fills and adds stable `fluvi-path-*` ids so the existing path-level recolor and live-preview sync can keep working.
- `COLOR-LAB-343` automated evidence: witnessed RED at the old `--spendee-logo-size: 79.5px` requirement, then GREEN after the lower screen live-logo token changed to `71.55px`. The editor stage remains separate and keeps its large SVG editing canvas.
- `COLOR-LAB-344` automated evidence: witnessed RED at the old visible `spendee`/`Spendee` lockup labels, then GREEN after all 17 lower brand lockups display `fluvi`, all 17 live logo accessible labels say `Fluvi live logo preview`, and visible section/editor/palette headings use Fluvi. Internal CSS class/token names and historical/source paths are intentionally unchanged.
- `COLOR-LAB-345` automated evidence: witnessed RED at the missing `--spendee-logo-size: 64.395px` requirement, then GREEN after the lower screen live-logo token changed from `71.55px` to `64.395px`, another 10% reduction. The editor stage remains separate and keeps its large SVG editing canvas.
- `COLOR-LAB-346` automated evidence: witnessed RED at the missing `--spendee-logo-size: 57.9555px` requirement, then GREEN after the lower screen live-logo token changed from `64.395px` to `57.9555px`, another 10% reduction. The editor stage remains separate and keeps its large SVG editing canvas.
- `COLOR-LAB-347` automated evidence: witnessed RED at the missing `fluviLogoVariantPreview` requirement, then GREEN after the large logo editor gained three isolated recommendation cards: Eredeti Fluvi, App-harmonizált, and Portal emissive. The previews render from `fluvi_vector.svg` through a separate `data-logo-variant-preview` path so editor live-sync does not overwrite the recommendation samples.
- `COLOR-LAB-348` automated evidence: inspected latest Android screenshot `Screenshot_20260716-175012.png`, then witnessed RED against the old per-path recommendation renderer and GREEN after the three Fluvi recommendation previews switched to a shared SVG-level `userSpaceOnUse` continuous gradient field for all paths. The main path editor still uses path-specific editing separately.
- `COLOR-LAB-349` automated evidence: witnessed RED at the missing Budget C1/C2 transaction avatar lens contract, then GREEN after scoped CSS gave `alt-common-header-budget-stage0` and `alt-common-header-budget-stage1` `.logbox-avatar-circle` elements a convex highlight, lower shadow, rim, and protected icon layer. The existing C2 category carousel badge lens remains separately scoped.
- Fresh verification passed the full eleven-suite Portal/Color Lab command, external JavaScript syntax checks, inline-script parse (`1`), `git diff --check`, and HTTP source/asset smoke for `color_lab.html` and `fluvi_vector.svg`.
- Remaining evidence: Android-browser screenshot/manual inspection is still required for the actual collapsed panel ergonomics, chevron tap behavior, slider-scroll behavior inside an expanded panel, rendered Fluvi logo appearance in the screen row, latest additional 10% smaller screen-logo proportion, Fluvi copy in every relevant visible lockup, editor path recoloring, the three rendered Fluvi logo recommendation cards beside the large editor, absence of hard internal Fluvi preview seams, and C1/C2 logbox avatar lens appearance. Therefore `COLOR-LAB-341`–`349` remain honestly `PARTIAL`.

## HTML cleanup requirements — 2026-07-16

| ID | Source instruction / reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| COLOR-LAB-350 | User: `túl nagy a html, tisztítani kell: felső szelektált sor paletta kerüljön az alsó, új design palettába, ezt megtartjuk` | `docs/prototypes/color_lab.html` alternative palette | `#selectedPaletteRow` is no longer in the deleted legacy palette and is inserted inside `#alternativePalette` before the new design app/slot palette rows | Static DOM assertions in `color_lab_static_test.js` + direct source inspection | DONE |
| COLOR-LAB-351 | User: `minden mást, a régi design színslotokat töröld` | HTML palette markup, CSS variables, static tests | Legacy app-source/proposed-neutral/keyboard text rows, previous/original/FAB-blue slot palette rows, their `data-palette-group` markers, and `--previous-slot-gradient-*`/`--original-slot-gradient-*`/`--fab-blue-gradient-*` tokens are absent; the lower new design app palette and current 21-slot palette remain | Targeted `rg` no-match search + `color_lab_static_test.js` forbidden-token assertions | DONE |
| COLOR-LAB-352 | User: `1-2-3-4, s1 töröld (régi konceptet kiszeded ezzel.)` | Legacy screen rows and obsolete stats screen | The old upper `1–4` screen section and S1 stats screen are removed from DOM and related orphan CSS/test count expectations are updated | Targeted `rg` no-match search + `color_lab_static_test.js` screen/count assertions | DONE |
| COLOR-LAB-353 | User: `d-full töröld` | Mind D-full full-year heatmap preview | The standalone D-full screen, full-year heatmap wrapper CSS, render targets, gallery/init functions, and `initMindHeatmapScreens()` call are removed while runtime D3/D4 heatmap screens remain covered | Targeted `rg` no-match search + `color_lab_static_test.js` forbidden-token and focus-init assertions | DONE |
| COLOR-LAB-354 | Cleanup consequence from `1-2-3-4, s1` deletion | Static regression expectations | Counts and assertions that previously included deleted legacy/S1 screens now match the kept lower Fluvi/query/common-header screens only | `node docs/prototypes/color_lab_static_test.js` | DONE |
| COLOR-LAB-355 | Cleanup consequence from deleted legacy palette/screen markup | Orphan CSS and obsolete recolor targets | Orphan `color-swatch[data-palette-role="text"]`, `.magnet-strip/.magnet-track`, legacy `sheet-background`, `magnet-strip`, header-title/label, and `data-fab-base` contracts are removed or explicitly forbidden without deleting live lower Fluvi targets | Targeted source inspection + `rg` no-match search + `color_lab_static_test.js` | DONE |
| COLOR-LAB-356 | User: `a screenekben a fluvi ikon legyen akkora méretű, hogy a magassága a mellette lévő brand + mottó összmagasságával egyezzen` | Screen brand lockup CSS | Lower screen Fluvi live logo size is reduced to match the visible `fluvi` + motto text stack height, with only a few pixels of optical oversize | CSS/static assertion + HTTP source smoke | DONE |
| COLOR-LAB-357 | User: `ezeknek egy komponensnek kell lennie, nem 2 részből állnia... az f betű a két kanyarodó ívből álljon, ne legyenek subkomponensek` | `fluvi_vector.svg` and logo renderer | The Fluvi SVG source contains only two editable logo components: top curve and lower curve; the prior internal wedge/subcomponent paths are removed so a recolor applies to the whole curve component | SVG/static assertions + HTTP SVG smoke | DONE |
| COLOR-LAB-358 | User: `csinálsz egy alá a customizable a/B/c skálát... ablakméret manual text input, és a slider... két végpont customizálható, illetve a közepe is` | Logo editor controls and JS | A logo A/B/C scale panel sits below the icon lab, has a center slider, manual window-size input, tappable A/B/C stop squares, and updates the logo as one continuous field | Static DOM/JS assertions + inline script parse | DONE |
| COLOR-LAB-359 | User: `a középső szín opcionális... új színpalettaelem: no colour` | Logo scale palette/control semantics | A visible `No colour` palette element can be selected and applied to the center stop to disable the C color without deleting A/B endpoints | Static DOM/JS assertions + inline script parse | DONE |
| COLOR-LAB-360 | User: `c1 c2 logbox és carouselnek adtál 3d effectet, de a teteje túl erősen fehér, lágyítsd` | C1/C2 budget lens CSS | The existing convex 3D lens effect remains, but top white highlights on C1/C2 logbox avatars and C2 carousel badges are softened with lower alpha/opacity and less harsh rim glow | CSS/static assertions | DONE |
| COLOR-LAB-361 | User: `a logo az app jobb felső sarkában legyen 33%-al nagyobb` | Screen brand lockup CSS | The lower screen Fluvi live logo token is increased from the prior 42px text-stack match to 56px, preserving the live SVG preview path | Static CSS assertion + HTTP source smoke | DONE |
| COLOR-LAB-362 | User: `c1-2 domború avatarjai fehérjei még lágyabbak legyenek` | C1/C2 budget lens CSS | C1/C2 logbox avatar and C2 carousel badge convex lens whites are softened again: lower radial highlight alpha, lower pseudo-element opacity, and lower center rim glow | Static CSS assertions | DONE |
| COLOR-LAB-363 | User: `test ablak balance színezése, türkiz-fehér-lila túl szűk a középső rész... szélesebb legyen a fade out terület` | `color_lab_portal_energy.js` money-flow scale | Money-flow 50–50 stop positions widen the center fade to `[0, 0.2, 0.5, 0.8, 1]`, preserving the 8/50/92 semantic split mapping while broadening the white/pink transition zone | `color_lab_portal_energy_test.js` + HTTP source smoke | DONE |
| COLOR-LAB-364 | User correction: `a brand név és mottó maradjon a korábbi, csak az ikont kellett volna növelned` | Screen brand lockup CSS | The brand/motto stack remains at the previous 42px sizing and typography (`fluvi` 23px, motto 11px), while only the SVG logo uses a separate 56px icon token | Static CSS assertion + HTTP source smoke | DONE |
| COLOR-LAB-365 | User: `c1 c2 gombjainak a belső árnyékát is lágyítsd` | C1/C2 budget lens CSS | C1/C2 convex controls keep depth but use softer internal shadows: weaker white inset and weaker dark lower inset on logbox avatars and carousel badges | Static CSS assertion | DONE |

## HTML cleanup evidence — 2026-07-16

- `COLOR-LAB-350`–`355` automated evidence: targeted `rg` over `docs/prototypes/color_lab.html` returned no legacy cleanup tokens for the removed upper legacy section/palette, S1, D-full, previous/original/FAB-blue slot palettes, old text palette role, legacy sheet/magnet targets, and deleted D-full JS render/init hooks.
- `COLOR-LAB-350`–`355` regression evidence: `node docs/prototypes/color_lab_static_test.js` passed after updating the source-of-truth assertions to the cleaned prototype shape.
- Whitespace evidence: `git diff --check` passed.

## Fluvi logo lab and softened lens evidence — 2026-07-16

- `COLOR-LAB-356`–`360` TDD evidence: witnessed RED at `node docs/prototypes/color_lab_static_test.js` before implementation, failing on the old `--spendee-logo-size: 57.9555px` state. GREEN after implementation with the updated 42px screen logo token, two-arc SVG, A/B/C logo scale controls, no-colour center stop semantics, and softened C1/C2 lens alpha contracts.
- SVG/source evidence: local and HTTP-served `fluvi_vector.svg` both contain exactly two editable `fluvi-arc-*` paths and no removed internal subpath tokens such as `fluvi-path-4`.
- Runtime safety evidence: inline script parse passed (`1`), `git diff --check` passed, and HTTP smoke confirmed the served HTML contains the A/B/C logo scale and no-colour control.
- `COLOR-LAB-361`–`363` TDD evidence: witnessed RED after changing tests, with `color_lab_static_test.js` failing on missing `--spendee-logo-size: 56px` and `color_lab_portal_energy_test.js` failing because money-flow 50–50 still used `[0, 0.29, 0.5, 0.73, 1]`. GREEN after implementation with the 56px logo token, softer C1/C2 whites, and `[0, 0.2, 0.5, 0.8, 1]` money-flow stops.
- `COLOR-LAB-364`–`365` correction evidence: witnessed RED at `color_lab_static_test.js` after requiring the old 42px brand stack plus separate 56px icon token and softer internal shadows. GREEN after splitting `--spendee-logo-size: 42px` from `--spendee-logo-icon-size: 56px` and reducing C1/C2 inset shadow strength.

## Query recurring common wizard requirements — 2026-07-16

| ID | Source instruction / reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| COLOR-LAB-366 | User: `last screenshot: másik agent q3+q4 helyett egy közös sheetet javasol de az wizard legyen, nézd meg` + `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260716-220027.png` | Query row Q3+ recurring section in `color_lab.html` | The old Q3 A6 duplicate and Q4 A7 duplicate are removed from the query row and replaced by one common recurring wizard sheet concept based on the screenshot | Static DOM assertions + direct screenshot inspection | DONE |
| COLOR-LAB-367 | User: `tervezz annyi screent ahány lépéses a wizard mindkét ága` | Query row recurring wizard screens | The row renders one shared type-selection screen plus four time-based branch screens and four push-based branch screens, all side by side | Static DOM/screen count/order assertions | DONE |
| COLOR-LAB-368 | Screenshot flow: `1. LÉPÉS – TÍPUS VÁLASZTÁS`, `2.A LÉPÉS – IDŐ ALAPÚ WIZARD`, `2.B LÉPÉS – PUSH ALAPÚ WIZARD` | Common recurring wizard card markup | Every recurring wizard screen uses the same bottom-sheet frame, grabber, nav/close header, progress stepper, rounded cards, and primary CTA styling | Static CSS/DOM assertions + HTTP source smoke | DONE |
| COLOR-LAB-369 | User: wizard must cover both branches | Time branch screens | The time branch has four concrete steps: frequency, timepoint, duration, review, with selectable cards/fields and next-run/review summaries | Static DOM assertions + JS parse | DONE |
| COLOR-LAB-370 | User: wizard must cover both branches | Push branch screens | The push branch has four concrete steps: message, recognized elements, selection/training, review, with selectable tokens/cards and parser/review summaries | Static DOM assertions + JS parse | DONE |
| COLOR-LAB-371 | Prior user direction for this query menu: `legyen interaktív, tappelhető kijelölhető elemekkel` | Recurring wizard JS interactions | Wizard option cards/tokens are tappable and toggle `selected`/`aria-pressed` without affecting the existing query menu selection logic | Static JS assertions + inline script parse | DONE |
| COLOR-LAB-372 | User correction: `ezt a menut a wizarddal a kép alapján pontosan tervezd meg, nem így néz ki mint a képen volt` + `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260716-220027.png` | Query recurring chooser and both wizard branches | The recurring chooser and all eight branch steps follow the screenshot hierarchy: one continuous white sheet, top grabber, close-only chooser header, two large cyan/purple type cards, branch back/title/close navigation, connected four-step progress, rounded selectable rows, contextual info/help surface, and bottom primary CTA only on branch steps | Direct screenshot comparison + static DOM/CSS assertions | DONE |
| COLOR-LAB-373 | User: `egy sheet legyen, ugyanolyan széles, és magas, mint a q2` | Q2 transaction sheet and recurring wizard sheet geometry | Q2 and every recurring wizard state share one `430px` sheet-height token; sheets are edge-to-edge (`left: 0; right: 0; bottom: 0`) with the same `26px 26px 0 0` top radius, border and shadow, and contain no inset popup frame | Static CSS assertions + HTTP source smoke | DONE |
| COLOR-LAB-374 | Screenshot correction for the shared type chooser | Recurring type-choice markup | The type chooser has no four-step progress strip, back button, or bottom CTA; tapping either type card remains selectable, while the later eight branch screens retain their progress strip and CTA | Scoped DOM assertions + interaction initializer assertion | DONE |
| COLOR-LAB-375 | User: `q2 design alapján tervezz egy bevételi tranzakciós sheetet is` | New Query-row income transaction sheet after Q2 | A new full phone screen sits directly after Q2 and mirrors Q2 sheet geometry/field hierarchy, but uses positive-income semantics: `Új bevétel`, plus amount, income-specific mint/green hero and add button, income name/category/avatar, and the same date/time row | Static DOM/order/CSS assertions + HTTP source smoke | DONE |
| COLOR-LAB-376 | Consequence of inserting the income sheet | Query-row visible numbering and regression counts | The new income transaction sheet is Q3; recurring screens become Q4–Q12 and category wizard screens become Q13–Q15, with all phone/row/header/logo counts updated without removing original A5/A6/A7 screens | Static screen order/count assertions | DONE |

## Query recurring common wizard evidence — 2026-07-16

- Inspected latest Android screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260716-220027.png`; implemented its shared sheet concept as one type-choice entry plus a 4-step time branch and a 4-step push branch.
- TDD evidence: witnessed RED at `node docs/prototypes/color_lab_static_test.js` after requiring 32 screens and the 1+4+4 recurring wizard shape; GREEN after replacing old query-row A6/A7 duplicates with Q3–Q11 recurring wizard screens and renumbering category wizard to Q12–Q14.
- Runtime/source evidence: inline script parse passed (`1`), and targeted `rg` confirmed the removed Q3/Q4 duplicate screen tokens are absent from `color_lab.html` and only remain as negative assertions in the test file.
- `COLOR-LAB-372`–`376` correction evidence: re-inspected `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260716-220027.png`, then witnessed RED on the absent shared `--query-sheet-h` contract and GREEN after Q2 plus all nine recurring states used the same `430px` edge-to-edge sheet geometry. Static assertions also verify the close-only chooser, exactly eight branch steppers, connected progress line, reference cyan/purple chooser cards, and preserved tap selection initializer.
- Income-sheet evidence: the served Query row contains a new Q3 income screen directly after Q2 with `Új bevétel`, `+320 000 Ft`, `Fizetés`, `Munkabér`, a positive mint/green amount hero and CTA, while visible numbering continues through recurring Q4–Q12 and category Q13–Q15.
- Fresh regression evidence: all eleven `color_lab_*_test.js` suites passed, every external Portal module passed `node --check`, the single inline script parsed, `git diff --check` passed, and an HTTP smoke confirmed all nine `data-recurring-wizard-size="q2-sheet"` states plus the Q3 income sheet in the served HTML.

## Canvas zoom and D-row sum mode requirements — 2026-07-16

| ID | Source instruction / reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| COLOR-LAB-377 | User: `oldd meg, hogy még kijjebb lehessen zoomolni, hogy még több content látszódjon` | `color_lab.html` zoom state/controller | The prototype canvas can zoom out below the previous 55% floor so more of the wide content is visible, while keeping the same pinch/wheel controller and readout behavior | Static JS assertion + HTTP source smoke | DONE |
| COLOR-LAB-378 | User: `a d sorba tervezz meg egy sum modeot is a havi mellé. évkártyákban hónapcellák legyenek, a designhoz az eddigiből vegyél mintát` | Mind mode D row generated stage2 screens | A new D5 Mind stage2 sum screen is generated beside the existing D3 yearly and D4 monthly screens; it uses glossy year cards, and each year card contains 12 tappable/colorable month cells based on the existing D heatmap visual language | Static DOM/CSS/JS assertions + HTTP source smoke | DONE |
| COLOR-LAB-379 | User: `keresd meg a flutterappsban a schedeev2 projectet, és nézd meg, hogy a schedee milyen betűtípussal van írva, az a brand, tesztként a d1 betűit ilyenre változtasd (logboxok brand etc)` | Schedee source inspection + D1 Mind font CSS/JS | The source project is recorded as `schedeev2`; the detected brand font is `Outfit` from `assets/fonts/Outfit-VariableFont_wght.ttf`, and only D1 Mind text/brand/logbox lettering is scoped to this Schedee font test | Direct source inspection + static DOM/CSS/JS assertions + HTTP font smoke | DONE |
| COLOR-LAB-380 | User: `továbbá az éveket d5-ben úgy rendezd, mint a hónapokat d3-ban, azaz 3 oszlopos legyen` | D5 Mind sum screen CSS | D5 sum mode lays out year cards in a 3-column grid like D3 lays out month cards, while each year card still contains its own 12 month cells | Static CSS assertion + HTTP source smoke | DONE |
| COLOR-LAB-381 | User: `túl magasak az évcellák d5-ben, legyenek arányosak` | D5 Mind sum year-card CSS | D5 keeps the 3-column year-card layout, but the year cards become proportionate compact cells by reducing only D5's internal month-cell height/gaps/padding so each card no longer reads as a tall column | Static CSS assertion + HTTP source smoke | DONE |
| COLOR-LAB-382 | User: `utolsó screenshot, a d1-ben korábban betűtípust módosítottál, de a méret nem lett konzisztens... d2-ben sokkal nagyobb a betű, és más a pozíció` + `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260717-004409.png` | D1 Mind Schedee font CSS | D1 keeps the Schedee/Outfit font-family scope, but it no longer overrides common-header title metrics; the D1 title inherits the same font-size, uppercase treatment, and top position as D2/common header titles | Latest screenshot inspection + static CSS assertion + HTTP source smoke | DONE |
| COLOR-LAB-383 | User: `d5-ben az év layout még mindig nem tetszik, legyenek alacsonyabbak` | D5 Mind sum year-card CSS | D5 keeps the 3-column year-card layout, but the year cards are visibly lower than the previous compact pass by using smaller D5-scoped month-cell height, cell gaps, card gaps, and card padding | Static CSS assertion + HTTP source smoke | DONE |

## Canvas zoom and D-row sum mode evidence — 2026-07-16

- `COLOR-LAB-377` TDD evidence: witnessed RED at `node docs/prototypes/color_lab_static_test.js` on the missing `min: 0.3,` token, then GREEN after lowering the canvas zoom floor from 55% to 30% while preserving the existing pinch/wheel/readout controller.
- `COLOR-LAB-378` TDD evidence: the same RED/GREEN cycle now asserts a generated D5 Mind stage2 sum screen beside D4, with `alt-common-header-mind-sum-stage2`, `D5 · ... sum`, a sum heatmap layer, glossy year cards, and 12 month cells per year card using the same local `--heatmap-active-color`/`--heat-alpha` color model as D3/D4.
- Regression evidence: all `docs/prototypes/color_lab_*_test.js` suites passed, every external `color_lab_*.js` module passed `node --check`, the single inline script parsed, `git diff --check` passed, and an HTTP smoke confirmed the served HTML contains the new zoom floor and D5 sum-mode tokens.
- `COLOR-LAB-379` source evidence: inspected `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/schedeev2/pubspec.yaml`, `lib/main.dart`, `lib/main/splash.dart`, and `lib/presentation/widgets/header_card.dart`; Schedee uses `fontFamily: 'Outfit'`, loaded from `assets/fonts/Outfit-VariableFont_wght.ttf`, and its brand text uses `fontSize: 23`, `fontWeight: FontWeight.w400`.
- `COLOR-LAB-379` TDD/runtime evidence: witnessed RED at `node docs/prototypes/color_lab_static_test.js` on the missing copied Schedee Outfit asset; GREEN after copying it to `docs/prototypes/schedee_outfit_variable.ttf`, adding `@font-face SchedeeOutfit`, and scoping `data-d1-schedee-brand-font="outfit"` only to D1 Mind title/value/subvalue/logbox text.
- `COLOR-LAB-380` evidence: D5 sum year cards now use `grid-template-columns: repeat(3, minmax(0, 1fr))`, while each year card keeps its internal 12 month cells. HTTP smoke confirmed both `color_lab.html` and `schedee_outfit_variable.ttf` are served, with the font returning 110572 bytes.
- `COLOR-LAB-381` TDD evidence: witnessed RED at `node docs/prototypes/color_lab_static_test.js` on the missing D5 proportional-cell contract, then GREEN after adding D5-scoped compact year-card overrides: 9px card padding, 6px card gap, 4px month-cell gap, and 20px month-cell height.
- `COLOR-LAB-382` TDD evidence: inspected latest screenshot `Screenshot_20260717-004409.png`, witnessed RED at `node docs/prototypes/color_lab_static_test.js` on the D1-only title metric override, then GREEN after removing that override so D1 keeps Schedee font-family but inherits D2/common title size, uppercase treatment, and top position.
- `COLOR-LAB-383` TDD evidence: inspected latest screenshot directory and found the newest screenshot did not show D5, so the source CSS contract was tightened instead; witnessed RED at `node docs/prototypes/color_lab_static_test.js` on the previous 20px D5 month-cell height, then GREEN after lowering only the D5 summary grid to 7px card padding, 5px card gap, 3px month-cell gap, 14px month-cell height, 6px radius, and 5px month-label font.
