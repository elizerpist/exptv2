# Dashboard rail startup and yearly continuity acceptance checklist

## Architecture card

| Concern | Single owner / write path | Boundary decision |
| --- | --- | --- |
| Physical cyclic viewport, inertia, snapping and rebasing | `CenteredCarouselController` / `CenteredCarousel` | Existing shared motor remains byte-for-byte behaviourally identical to milestone `5b71141`; dashboard code must not recreate or alter it. |
| Canonical parent/child navigation selection | `DashboardTimeNavigationController` | The controller maps only authentic rail intents to preview/settled child state. It must not command an unconfigured physical viewport during construction. |
| Carousel-to-navigation adaptation | `TimeRefinementRail` | UI mounts/renders the carousel and forwards its semantic callbacks; it performs no I/O and owns no query state. |
| Per-tick display data | `DashboardParentDisplayBundleController` → summary/log coordinators | A complete immutable finite deck is the synchronous source for amount, count and LogBox rows. Committed query/watch work occurs only after a real settled intent. |

| ID | Source | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| RSC-01 | User report and `R3 value=-699937` trace, 2026-08-03 | `dashboard_time_navigation_controller.dart`, `time_refinement_rail.dart` | App start/open establishes the selected child as a silent baseline; it produces no automatic preview, settle, navigation revision, or query scope commit before user input. | Red-then-green navigation/widget regressions plus trace inspection. | DONE — the navigation owner no longer commands an unmounted cyclic viewport; the mounted adapter establishes the silent baseline and ignores non-user callbacks. |
| RSC-02 | User: every real fling tick must immediately show its own content | Rail adapter, finite-bundle summary and LogBox coordinators | Five real preview ticks produce five exact immutable amount/count/LogBox states; no `—`, loading state or empty visual frame appears between them. | Existing five-tick finite-deck regression, targeted year-plane regression. | DONE — the YEAR regression checks five distinct child months with exact query key, total, transaction count and concrete LogBox state, all synchronously and without preview reads. |
| RSC-03 | User: yearly child months must be as fluent as monthly child days | Dashboard finite-bundle/query lifecycle | Yearly previews use the ready 12-month deck synchronously; no item-level read is started by a preview. Only a user-settled month may start/retain a committed watch. | Controller regression checks preview read count remains zero; trace has no autonomous `D8*` sequence. | DONE — the finite deck remains the only preview data source; the startup query churn is gated at the adapter boundary. |
| RSC-04 | User: user interaction overrides/continues normally | `TimeRefinementRail` → navigation boundary | A real fling after startup is accepted immediately and resolves to its actual last logical child. | Existing two-fling widget test and focused interaction regression. | DONE — the real two-fling widget regression still reaches the latest selected child, while the focused callback regression verifies every accepted tick is forwarded. |
| RSC-05 | User: do not touch central rail motor / match milestone | `lib/shared/motion/centered_carousel/*` | Both shared motor files remain exactly unchanged relative to `5b71141`. | `git diff --exit-code 5b71141 -- lib/shared/motion/centered_carousel/centered_carousel.dart lib/shared/motion/centered_carousel/centered_carousel_controller.dart`. | DONE — command exited successfully with no diff. |
| RSC-06 | Delivery gate | Dashboard tests, analyzer, CI | Focused regressions, dashboard suite, proot analyzer and online Android build pass; the branch is pushed. | Ubuntu-proot commands, GitHub Actions result and artifact checksum. | PARTIAL — focused test suite (60 tests), dashboard-core controller regression (10 tests) and proot static analysis pass; push and GitHub Actions remain. |
