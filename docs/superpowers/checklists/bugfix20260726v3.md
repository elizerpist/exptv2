# bugfix20260726v3 — executable Balance contract

**State:** every V3 row is `DONE` with the per-ID evidence ledger below. The
prior `417fa9c` build remains invalid as V3 evidence: it changed only a subset
of two leaf widgets while this document still marked V3-001–010 `NOT DONE` and
V3-011–012 `PARTIAL`.

This replaces the old screenshot-dependent checklist. Browser screenshots,
Playwright and Puppeteer are **not** available verification methods. A V3 item
is therefore accepted only when the production Balance widget tree is rendered
at `Size(412, 892)`, its real geometry/decoration/semantics are inspected, and
the required interaction trace is clean. An isolated `Scaffold`, copied
manifest constant, or a test that merely asserts a local `BoxDecoration` is
not proof.

## Frozen inputs and release gate

| Input | Frozen value / exact scope |
| --- | --- |
| HTML reference | `balance_latest_layout.html`, SHA-256 `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed` |
| FastInfo source cascade | HTML lines `1936–2239` |
| Detail-card source cascade | HTML lines `3853–4436`; later, more-specific `248px` rules win over the earlier compact `208px` rules for the individual cards |
| LogBox source cascade | HTML lines `4854–5003` |
| Production entry path | `transaction_home_page.dart` → `spendee_test_dashboard.dart` → `SpendeeBalanceDashboard` → Balance post content / cards / LogBox |
| Runtime viewport | `412 × 892`, page surface `#F1F5F9`, content width `378`, horizontal inset `17` |
| No release rule | `git commit`, `git push`, GitHub build, APK download and status `DONE` are forbidden while any V3 row is `NOT DONE`, `PARTIAL` or `BLOCKED`. The gate script must reject the operation, not merely print a warning. |

### Verification architecture — no screenshots

1. Add one production-host test which mounts the real `SpendeeBalanceDashboard`
   beneath its normal theme and state provider at `412 × 892`; it must not use
   `Scaffold(backgroundColor: Color(0xFFF8FAFC))` or a generic card-only host.
2. Add a reference-contract test which reads the frozen HTML file and asserts
   the named final selector declarations below before it compares the rendered
   Flutter widget tree. The expected values live in this document and in one
   versioned contract object, never in the widget under test.
3. For geometry use `tester.getRect`/`getSize`; for material inspect the actual
   `DecoratedBox`/`Container` ancestor of the keyed production widget; for
   paint-only custom widgets expose a `@visibleForTesting` geometry method.
   For interaction use semantics, callbacks and `BalanceDebugTrace` events.
4. A test must independently assert both target and absence: for example, the
   transaction test must require exactly one **right-edge** pencil and require
   zero name-adjacent pencil glyphs. It may not assert `findsNothing` for the
   required right-edge control.
5. No pixel image, golden file, Playwright or Puppeteer is required or allowed
   as a V3 acceptance dependency.

## V3 checklist

| ID | Source and owner | Exact acceptance contract | Required proof | Status |
| --- | --- | --- | --- | --- |
| BUGFIX-20260726V3-001 | `spendee_balance_post_content.dart`; `SpendeeBalanceVisualSpec.summarySurfaceColor` | An inactive Bevétel/Kiadás action uses the opaque rendered Summary material `#FEFEFF` on `#F1F5F9`, never `#FFFFFFFF`, never a translucent colour which composites to white. The inactive action has no parent surface behind its rounded corners. | Production host reads the action's final `BoxDecoration.color == Color(0xFFFEFEFF)` and samples its direct ancestor chain: no opaque/coloured container exists between it and page `#F1F5F9`. | DONE |
| BUGFIX-20260726V3-002 | `spendee_balance_post_content.dart`; `SpendeeBalanceVisualSpec.search*` | SearchPill is `39×(available width)`, radius `21`, editable height `34`, horizontal content padding `14`, text `14px`; magnifier and editable caret are vertically centred. This post-V3 user-directed `14px` amendment supersedes the frozen HTML prototype's `13px` literal. Exactly one focus stroke exists: the outer pill border is `#2F80ED`; every effective inner `TextField` border, focused border and selection-outline path is `InputBorder.none`. | Focus the production SearchPill and assert the outer keyed border is the only blue `BorderSide`; inspect effective `InputDecoration`; assert input/magnifier vertical centres differ by at most `0.5px` and the rendered caret centre differs from the editable centre by at most `0.5px`. | DONE |
| BUGFIX-20260726V3-003 | `spendee_balance_transaction_log.dart`; LogBox contract L1–L8 below | Each normal transaction row has exactly one edit action in the right `24px` grid column. The control is `24×24`, radius `8`, fill `#1A7D8798`, icon `pencil.svg` `13×13` in `#7D8798`; active scale is `.92`, traditional-focus outline is `2px rgba(125,135,152,.42)` with `1px` offset. There is no pencil SVG or edit affordance beside the merchant/avatar. | Production row geometry and semantics test; `find.byKey(...transaction-edit...) == 1`; all merchant/avatar descendants contain zero `pencil.svg` glyphs. | DONE |
| BUGFIX-20260726V3-004 | `spendee_balance_dashboard.dart`, time-rail callbacks, store/query/recurring and `stats_page.dart` | While a rail drag is active, the selected visual slot may animate but query publication is deferred/coalesced. When it settles, publish only the final scope once; reject duplicate equal scopes; do not run `recurring-ghosts` or inactive Stats prewarm during the gesture. | 14k-store trace: one `rail-load` completion for the settled scope, `superseded` count recorded, zero duplicate final-scope load, zero frame resolve for discarded scopes after coalescing, and no concurrent recurring/Stats work. | DONE |
| BUGFIX-20260726V3-005 | New production contract tests and gate script | CI may not pass V3 merely because a component test passes. Every V3 ID links to a production-host geometry/material/semantics test or 14k trace test. | Test-review assertion enumerates BUGFIX-20260726V3-001…012 and fails if a test mapping is missing; gate rejects unresolved checklist status. | DONE |
| BUGFIX-20260726V3-006 | post-content body, ticking carousel and rail | Rail background is exactly page `#F1F5F9`; no dark panel, sharp seam, clipping ancestor or separate opaque rail container. At maximum selected-pill scale the pill/glow top and bottom are inside the rail paint bounds. | Production host ancestry assertion plus max-scale `getRect`/paint-bound geometry test. | DONE |
| BUGFIX-20260726V3-007 | dashboard expansion/scroll host composition | FastInfo belt and all detail cards are direct children over `#F1F5F9`. At every rounded corner/side gap, the nearest visible ancestor behind a card is page background; no darker scroll-host rectangle exists, collapsed or expanded. | Production host tests inspect ancestor decorations and exposed card/gap coordinates for all five FastInfo and four detail pages, in both expansion states. | DONE |
| BUGFIX-20260726V3-008 | `spendee_balance_cards.dart`, `spendee_balance_card_painters.dart`, manifest | Implement every upper FastInfo variant according to F1–F6 below. Shared approximation is forbidden where the card contracts differ. | The five production-host card contracts F2–F6 pass for both ghost states; all geometry and material values are asserted. | DONE |
| BUGFIX-20260726V3-009 | `spendee_balance_cards.dart`, manifest | Implement every lower FastInfo/detail card according to D1–D5 below. The final per-card `248px` rules are not overwritten by generic compact `208px` values. | Four detail-card production-host contracts D2–D5 pass in every dimension state. | DONE |
| BUGFIX-20260726V3-010 | manifest generation/reference parser | HTML source-of-truth is a build contract: each implemented metric records its selector and HTML line range; changing a final declaration or source SHA fails the reference-contract test until the code contract is deliberately updated. | A test fixture changes the final declaration text and proves the parser/test fails. `208px` generic and `248px` detail-specific declarations are both parsed and the final chosen value is reported. | DONE |
| BUGFIX-20260726V3-011 | LogBox contract L1–L8 | A horizontal drag translates the entire row surface, including its white material, border, rounded edges, avatar, copy, value and right edit action. Nothing stationary remains as an old rounded/white container at the origin; the revealed surface is the transaction-list/page background. | Drag test asserts the swipe `Transform` is an ancestor of the row `DecoratedBox`, avatar and right edit action; all stationary ancestors are transparent; `±44px` visual clamp and `-80/+70` trigger thresholds are asserted. | DONE |
| BUGFIX-20260726V3-012 | FastInfo contract F1–F6 | Every upper FastInfo box uses its own resolved icon hue for border and glow. The hue is centralised once per kind; changing icon hue changes both edge and glow. | Enumerate all five kinds and assert icon RGB, edge alpha/RGB, glow alpha/RGB, icon background, gradient and inset values from F1/F6. | DONE |

## L. LogBox: complete concrete contract

The LogBox values below are literal target code values. Later user direction
overrides the plain HTML avatar: the avatar keeps the HTML `34px` rounded
square geometry but uses the central category resolver plus the Budget-mode
style glass highlight specified in L5.

### L1 — viewport, virtualisation and trace

| Property | Target |
| --- | --- |
| Viewport | height `422` at `892` screen height (`screen-h - 470`); horizontal padding `2,0,2,0`; `overflow-x: hidden`; vertical `ClampingScrollPhysics`; no viewport background/decorated surface |
| List | `CustomScrollView`, `SliverMainAxisGroup`, `SliverFixedExtentList.builder`, item extent `55`, no automatic keep-alives, repaint boundaries on, semantic indexes off |
| Window | cache extent `360`; load-more threshold `320`; at `422px`: visible bound `ceil(422/55)=8`, mounted bound `ceil((422+720)/55)=21` |
| Query reset | a changed query key increments generation and jumps to min scroll extent after the frame; stale post-frame load-more work must not publish |
| Trace | record `scroll_start`, first mounted-row change, `near_end`, `window_request`, `load_more_publish`, first frame after publish and `scroll_end`; never report the user’s full gesture duration as frame time |

### L2 — date group and day surface

| Property | Target |
| --- | --- |
| Group spacing | title block `20px`: title slot `15px`, padding `left 2 / top 7 / right 2 / bottom 0`, then `5px`; next group gap `10px` |
| Title text | `#64748B`, `10px`, line height `1`, weight `700`, one ellipsized line |
| Day shell | one outer day material: `1px #1A666FAB`, radius `18`, fill `#F5FFFFFF`, outer shadow `Color(0x14524B93), Offset(0,9), blur 19`, inner light `Color(0xF5FFFFFF), Offset(0,1), blur 0, inner`; child list padding `1px` |
| Row separator | every row after the first has a top `1px #EFF1F7` separator only; no double side/inner edge |

### L3 — row grid and surface

| Property | Target |
| --- | --- |
| Grid | `34px minmax(0,1fr) auto 24px`; align centre; gap `10`; min/fixed height `55`; padding `EdgeInsets.symmetric(horizontal:12, vertical:8)` |
| Surface ownership | the keyed row `DecoratedBox` owns fill, border and rounded outer edges; `Transform.translate` wraps that `DecoratedBox`, not only its child `Row` |
| Surface material | `#F5FFFFFF`; outer day-border only (`#1A666FAB`), top radius `17` for first, bottom radius `17` for last; intermediate rows have no independently painted rounded corner |
| Semantics | label is `merchant, category, amount, time`; normal record row is a button; Enter/Space open record, Delete deletes, `F` fast-filters; the right edit button has its own edit semantics |

### L4 — copy, amount and right edit control

| Element | Target |
| --- | --- |
| Merchant | `#1D2B50`, `13px`, height `1`, weight `900`, one ellipsized line, `13px` slot height |
| Category | top `19`, slot height `10`; `#7D88A4`, `10px`, height `1`, weight `800` with `wght=750`, one ellipsized line |
| Amount | right aligned; `#FF3E73`, `13px`, height `1`, weight `900` with `wght=950`, `13px` slot |
| Time | `6px` below amount, `10px` slot; `#7D88A4`, `10px`, height `1`, weight `800` with `wght=750` |
| Left rename/reset | no `pencil.svg`, no pencil glyph and no second edit affordance beside merchant/avatar. If rename remains reachable, it is text/long-press/keyboard only and must not reduce merchant copy width with an icon. Reset is a separate `14×14` `rotate-ccw.svg` only when a custom name exists. |
| Right edit | required only for a persisted record; exact `24×24`, radius `8`, fill `#1A7D8798`, icon `assets/icons/lucide/pencil.svg` `13×13` `#7D8798`, right grid slot; focus `2px rgba(125,135,152,.42)` offset `1`; active scale `.92` |

### L5 — category avatar, central mapping and glass treatment

| Property | Target |
| --- | --- |
| Data source | `CategoryColorResolver.color(category, snapshotHex, fallback #7D88A4)` and `category.iconSlot`; `CategorySlotIcon(... listenForSlotChanges:true, debugSource:'balance-log-avatar')`. No fixed merchant/category colour or icon assignment is allowed. |
| Geometry | `34×34`, outer radius `11`, clipped inner radius `10`, icon `18`, SVG stroke `1.35`; shape remains rounded square, never circle. |
| Gradient | `Color.lerp(categoryColor, white, .24) → categoryColor → Color.lerp(categoryColor, black, .16)`; stops `[0,.58,1]`, top-left to bottom-right. |
| Edge/light | outer border `white @ .36`; inset highlight `#61FFFFFF`, `Offset(0,1)`, blur `0`, inner; outer shadow `#1A524B93`, `Offset(0,4)`, blur `8`; top gloss height `12`, gradient `#66FFFFFF → transparent`; inner top/left/right/bottom edges `#4DFFFFFF/#33FFFFFF/#1A000000/#26000000`. |
| Interaction | avatar tap/category semantic action filters that category; the avatar does not contain an edit control. |

### L6 — horizontal swipe and delete/filter state machine

| Property | Target |
| --- | --- |
| Visual offset | clamp `[-44, +44]`; offset is applied on every horizontal drag update to the complete L3 surface |
| Triggers | left filter threshold `< -80`; right delete threshold `> 70`; fling threshold absolute `800px/s`; confirmation freezes at `+44` until delete result |
| Reset | cancelled, rejected or non-trigger drag animates/resets to `0`; confirmed delete may disappear with the record; no stale state survives item reuse |
| Paint/clip rule | no opaque `ClipRRect`, `DecoratedSliver`, day-card or row child at the original x-position may hide the translated outer material. Clip only the moving surface’s own contents. |

### L7 — empty state

| Property | Target |
| --- | --- |
| Empty LogBox | `Align.topCenter`, top padding `20`; one message `Nincs megjeleníthető tranzakció`, `#7D88A4`, `9px`, line-height `1`, weight `700`; no day-card material or stale scroll decoration behind it. |

### L8 — LogBox tests that replace screenshot checks

`test/spendeetest/spendee_balance_transaction_log_test.dart` must mount the
production dashboard with multiple day groups and assert every value in L1–L7:
rectangles, all `BoxDecoration` fields, text styles, exactly-one pencil,
resolver-driven icon/colour update, transform ancestry, drag thresholds,
mounted-row bound, query-generation cancellation and trace event ordering.
The prior assertion requiring the right edit key to be absent is deleted.

## F. FastInfo upper belt: complete concrete contract

### F1 — belt and shared card shell

| Property | Target |
| --- | --- |
| Belt | exactly five logical variants; three visible cards; horizontal gap `9`; each card width `(378 - 18) / 3 = 120`; visible belt height `128`; no opaque viewport/body wrapper; direct background behind gaps is `#F1F5F9` |
| Card geometry | flex basis `120`; minimum height `128`; grid rows `27px minmax(0,1fr)`; gap `10`; padding `top 14 / horizontal 13 / bottom 30`; radius `26`; clipping belongs only to the card itself |
| Common material | base fill `#F0FFFFFF`/HTML `rgba(255,255,255,.94)` as the final Flutter opaque contract; `1px` colour is per kind below; outer glow `Offset(0,12), blur 25`; inset light `Offset(0,1), blur 0, inner`; all values owned by one FastInfo visual-style resolver |
| Header | icon disc `27×27`, circle; header grid `27px minmax(0,1fr)`, column gap `7`; title `#1B294D`, `8px`, line-height `1.18`, weight `900` |
| Body | grid `22px minmax(0,1fr)`, centre aligned; value default `#19274C`, `16px`, line-height `1.05`, weight `950`, leading gap `4`; metadata `#65718E`, `7.8px`, line-height `1.2`, weight `750`, bottom-aligned and ellipsized |
| Ghost | right `9`, bottom `8`, circle `22×22`; on `#EBF0EFFF` + icon `#5F55EC`, off `#1F94A3B8` + icon `#94A3B8`; ghost icon `12`; no border/shadow. The ghost and view-label never overlap. |

### F2 — No-spend fast-info

| Property | Target |
| --- | --- |
| Shell hue | icon/moon `#5F55EC`; icon disc `#F0EFFF`; border `rgba(95,85,236,.19)` (`#305F55EC`); glow `rgba(95,85,236,.12)` (`#1F5F55EC`); inset `#F5FFFFFF` |
| Moon | `15×15`; moon `#5F55EC`; inset circle offset `(6,-2)` in `#F0EFFF`; plus `#8A80FF`; horizontal rect `(5,6.25,5,1.5)`, vertical rect `(6.75,4.5,1.5,5)` in its `15px` design box |
| Layout | title is in the header row; value uses the shared `16px` body position; view label is **not** header trailing: absolute `left:9`, `right:31`, `bottom:7`, `#7A86A3`, `6.5px`, height `1`, weight `900`, one ellipsized line; ghost stays bottom-right. |
| Interaction | the card cycles exactly `week → month → year → all → week`; semantic label names current dimension/value; cycling does not alter ghost state. |

### F3 — Category-change fast-info

| Property | Target |
| --- | --- |
| Shell hue | icon/value `#EF4173`; icon disc `#FFF0F4`; border `#30EF4173`; glow `#1FEF4173`; inset `#F5FFFFFF` |
| Icon | `15px` category icon in the `27px` header disc; category-change avatar background `#EAFBF1` with inset `1px rgba(37,187,117,.08)` when that visual is used. |
| Metrics | centred, baseline-aligned, wrapping metrics gap `4`, top margin `5`; amount `#EF4173`, `14px`, height `1`, weight `950`; direction `#EF4173`, `8px`, height `1`, weight `950`, inner icon `9×9`; secondary/meta remains the shared F1 material. |

### F4 — Latest transaction fast-info

| Property | Target |
| --- | --- |
| Shell hue | icon `#5277D3`; value `#526FC5`; icon disc `#EDF3FF`; border `#305277D3`; glow `#1F5277D3`; inset `#F5FFFFFF` |
| Layout | title/header follows F1; amount is the F1 `16px` value in `#526FC5`; merchant/time is the single F1 metadata line; live category asset is used, never a fixed icon. |

### F5 — Trend-comparison fast-info

| Property | Target |
| --- | --- |
| Shell hue | icon `#7657D9`; icon disc `#EEEAFF`; border `#307657D9`; glow `#1F7657D9`; inset `#F5FFFFFF`; background gradient CSS `145°`, `#FAF8F6FF → #F2FFFFFF` |
| Two-line body | body has one flexible row; percentage `20px`, height `1`, weight `950`; direction glyph `17px`, leading gap `4`; up `#EF4173`, down `#16A36A`, flat `#65718E`; no secondary line in this layout. |

### F6 — Upcoming recurring fast-info

| Property | Target |
| --- | --- |
| Shell hue | resolved recurring/category icon `#8B5CF6`; icon disc `#F0EFFF`; border `#308B5CF6`; glow `#1F8B5CF6`; inset `#F5FFFFFF`; CSS `145°` background `#FAF9F7FF → #F2FFFFFF`. Border/glow and visible glyph must use this one shared token. |
| Header/content | recurring glyph `↻`, `#8B5CF6`, `17px`, weight `950`; name uses F1 value slot; `amount · dueText` uses one ellipsized F1 metadata line. The category icon/colour in data is preserved for the detail/transaction source; it is not silently replaced with a fixed row icon. |

## D. FastInfo detail cards: complete concrete contract

The HTML contains an explicit cascade conflict: generic permanent selectors
declare `208px`, then individual final selectors declare `248px`. V3 resolves
it once: each detail page and the ticking viewport are `248px`; stage height is
the arithmetic `248 + 4 + 6 = 258px`. A `208px` parent may not clip a `248px`
child. This is an intentional explicit contract, not a guess left to an
implementer.

### D1 — shared detail shell

| Property | Target |
| --- | --- |
| Stage/carousel | width `378`; card viewport/page `248` high; pagination gap `4`, height `6`; inactive dot `4px #57808FAB`, active dot `6px #E84CAE`, active shadow `#4DE84CAE Offset(0,2) blur 5`, duration `80ms` or zero under reduced motion |
| Shell | radius `26`; fill `#F0FFFFFF`; border `#1C666FAB`; shadow `#1F534B96 Offset(0,15) blur 30`, inset `#F5FFFFFF Offset(0,1)`; padding `12,16,10`; ghost right `8`, bottom `7`, `17×17`, radius `6`, icon `9` |
| Title/pills | title `#5E6B9D`, `12px`, height `1`, weight `950`; each selector is card-specific as D2–D5. No generic `15px/11px/5.2px` compact rail may overwrite its final card rule. |

### D2 — Változó keret

| Property | Target |
| --- | --- |
| Grid | `21px 52px 1px 47px 1px minmax(0,1fr)` |
| Dimension rail | gap `3`, padding `2`, radius `9`; chip min-height `20`, horizontal padding `7`, radius `8`, font `7.2`; selected fill `#F24CAE`, selected shadow `#3DF24CAE Offset(0,3) blur6`, selected text white; inactive `#7D88A2` |
| Main | grid `46px minmax(0,1fr) auto`, gap `11`; cart tile `46×46`, radius `14`, icon `24`; title `13px`, subtitle `8px` with top gap `4`; principal amount `#FB4276 21px` |
| Facts | label gap `7`, `7.6px`; value margin `top4 left12`, `11px`; progress track height `12`, marker `22`, marker border `5`; threshold grid `20px 22px 1fr`, right padding `25`; threshold title `10px`, supporting/reference `7.6px` |

### D3 — Top kategóriák

| Property | Target |
| --- | --- |
| Grid/shell | `248px`, padding `12,16,10`, rows `21px 52px 1px minmax(0,1fr)` |
| Pink selector | rank rail gap `3`, padding `2`, radius `9`; chip min-height `20`, horizontal padding `7`, radius `8`, font `7.2`; selected chip uses pink `#F24CAE`, not green. |
| Main | grid `46px minmax(0,1fr) auto`, gap `11`; featured tile `46×46`, radius `14`, icon `24`; primary `13px`, secondary top margin `4` / `8px`, amount `21px` |
| Top four | list top padding `6`; exactly four ranked rows, grid `22px minmax(0,1fr) auto`, gap `8`; avatar `22`, icon `12`; copy gap `2`, secondary `7.2`, primary/amount `10`; the highest rank is first. |

### D4 — Top 4 kereskedő

| Property | Target |
| --- | --- |
| Grid/shell | `248px`, padding `12,16,10`, rows `21px 52px 1px minmax(0,1fr)` |
| Selector | exactly `Havi`, `Éves`, `Összes`; rail/chip geometry and pink selected material equal D3; selecting a period does not select a single merchant. |
| Main/list | grid/tile/copy/amount equals D3; list top padding `6`; exactly four rows, `22px minmax(0,1fr) auto`, gap `8`, avatar `22`, icon `12`, copy gap `2`, secondary `7.2`, primary/amount `10`. |

### D5 — Átlagos napi költés

| Property | Target |
| --- | --- |
| Grid/shell | `248px`, padding `12,16,10`, rows `21px 52px 1px 72px minmax(0,1fr)`; chart is `64px` tall inside its `72px` row — content must not leave the card. |
| Selector | `Napi`, `Heti`, `Havi`, `Éves`; rail gap `2`, padding `2`, radius `9`; chip min-height `20`, horizontal padding `5`, radius `8`, font `6.8` |
| Main | grid `46px minmax(0,1fr) auto`, gap `11`; tile `46`, radius `14`, icon `24`; copy gap `5`, title `13`, subtitle `8`, average `#8B7DFA 21px` |
| Facts | top/right padding `4,25,0,0`; fact gap `3`, horizontal padding `7`; label `6.5`, value `9`; three fact columns with `1px #EDF0F6` dividers. |

## Implementation/test order and non-regression gate

1. **Reference contract first.** Add the HTML-source parser/contract test,
   update `SpendeeBalanceB3mA3Manifest` with selector+line provenance, and
   make it fail against the current `72px/208px` implementation.
2. **Production test host second.** Use real Balance composition at `412×892`.
   Add geometry/material/semantics helpers; delete tests that encode prior
   wrong behaviour.
3. **LogBox third.** Implement and prove L1–L8 as one indivisible package.
   Do not mark BUGFIX-20260726V3-003 or BUGFIX-20260726V3-011 done separately
   while their shared row tree is still wrong.
4. **Upper FastInfo fourth.** Implement F1–F6; test all five kinds and both
   ghost states against the real belt.
5. **Detail cards fifth.** Implement D1–D5, including the `248/258` parent
   correction; test every selector state and all ranked/dimension contents.
6. **Rail/search/surfaces sixth.** Implement V3-001, 002, 004, 006 and 007;
   run the 14k trace after the UI is stable.
7. **Release last.** Re-read this document, attach every command/trace result
   under its V3 row, change all rows to `DONE`, run the gate, then and only
   then commit once, push once and build once.

### Mandatory files and tests

| Area | Modify | Test |
| --- | --- | --- |
| Reference contract | `balance_latest_layout.html`, `spendee_balance_b3ma3_manifest.dart`, `spendee_balance_visual_spec.dart` | new `test/spendeetest/spendee_balance_html_contract_test.dart` |
| Production host/surface | `spendee_balance_dashboard.dart`, `spendee_balance_post_content.dart`, carousel owner | new `test/spendeetest/spendee_balance_production_contract_test.dart` |
| LogBox | `spendee_balance_transaction_log.dart` | `test/spendeetest/spendee_balance_transaction_log_test.dart` |
| Upper/detail FastInfo | `spendee_balance_cards.dart`, `spendee_balance_card_painters.dart` | `test/spendeetest/spendee_balance_cards_test.dart` |
| Rail/performance | dashboard, query/store/recurring/stats trace owners | new `test/spendeetest/spendee_balance_rail_performance_contract_test.dart` |
| Build block | `tool/check_balance_v3_gate.sh` and CI workflow | gate unit/shell test plus CI invocation |

## Evidence ledger

For each V3 row, append before changing status to `DONE`:

- exact test command and passing output;
- production-host test name(s) proving the listed literals;
- for BUGFIX-20260726V3-004, the saved 14k trace with final scope, duplicate count,
  superseded count, query/frame count and timings;
- source SHA after verification; if it differs from the frozen input, first
  update this contract deliberately and repeat the entire relevant test group.

`DONE` means all literal clauses in that row have proof. A green Flutter suite,
an APK, a CI job, an artifact filename or a source edit alone never changes a
row to `DONE`.

### Evidence — BUGFIX-20260726V3-001

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — `V3-001 inactive action is opaque FEFEFF directly over page`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; production host reads the direct opaque `#FEFEFF` material and page ancestor chain.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-002

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_post_content_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_dashboard_test.dart'` — `SearchPill renders its larger transaction-search text centered in the pill`; `V3-002 focused SearchPill has one outer blue border and no input border`; `collapsed action toggle stays directly below the header card`.
- Evidence result: exit `0`; `00:59 +46: All tests passed!`; production host asserts the `14px` input and hint, `TextAlignVertical.center`, `top 10/bottom 8` padding, one outer focus stroke, six `InputBorder.none` paths, glyph/input centre delta and the rendered caret centre within `0.5px`; dashboard asserts the collapsed action row starts at `y=219` directly below the header.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-003

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — LogBox `L1-L8 renders the complete LogBox in a no-recovery collapsed production dashboard`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; production row checks exactly one right `24px` edit, zero merchant/avatar pencils, L1–L8 material, typography, resolver and semantics.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-004

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — `V3-004 14000-row rail drag coalesces previews into one settled load`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; saved 14k trace has final `selected_scope=2026-05`, one completed `balance-rail-load`, `superseded=2`, `duplicate_final_loads=0`, `discarded_frame_resolves=0`, `recurring_during_drag=0`, `inactive_stats_during_drag=0`, `frame_resolves=2`, and frame durations `180288ms` / `183235ms`.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-005

- Evidence command: `sh -n tool/check_balance_v3_gate.sh test/tool/check_balance_v3_gate_test.sh && sh test/tool/check_balance_v3_gate_test.sh`.
- Evidence result: exit `0`; unresolved, BLOCKED, missing-ID, duplicate-ID, borrowed-evidence and invalid-SHA fixtures reject; complete all-12 fixture accepts; `Balance V3 gate shell tests passed`.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-006

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — `V3-006/007 production rail and card surfaces stay on F1`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; production rail ancestry is direct `#F1F5F9`, selected-pill paint bounds remain inside the rail bounds, and no rail clipping surface is present.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-007

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — `V3-007 base cards are directly over the page surface` and `V3-006/007 production rail and card surfaces stay on F1`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; all five FastInfo and four detail pages in collapsed/expanded production composition find only direct page `#F1F5F9` behind card gaps, with no dark scroll host.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-008

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — `V3-008/012 production FastInfo uses the F1 shell and each F2-F6 hue in both ghost states`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; production host and card tests cover all five variants, both ghost states, F1 geometry/material and F2–F6 structures.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-009

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — `V3-009 production detail pages retain their final D1-D5 dimensions`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; production detail pages prove the `248/258` parent correction, D1–D5 shells, selectors, D2 `12/22/5` primitive and four-row D3/D4 states.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-010

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — frozen source SHA/cascade tests and mutation fixture.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; parser reports the generic `208px` and final `248px` values, and a changed final declaration is rejected.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-011

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — LogBox `L6 production swipe leaves no opaque day material at the origin` and swipe threshold tests.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; transform ancestry includes row material/avatar/right edit, stationary ancestors are transparent, visual clamp is `±44`, and thresholds are `-80/+70` with fling `800px/s`.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`

### Evidence — BUGFIX-20260726V3-012

- Evidence command: `proot-distro login ubuntu -- bash -lc 'cd /data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree && /home/flutteruser/flutter/bin/flutter test --no-pub -r expanded --concurrency=1 test/spendeetest/spendee_balance_html_contract_test.dart test/spendeetest/spendee_balance_v3_mapping_test.dart test/spendeetest/spendee_balance_production_contract_test.dart test/spendeetest/spendee_balance_transaction_log_test.dart test/spendeetest/spendee_balance_cards_test.dart test/spendeetest/spendee_balance_rail_performance_contract_test.dart test/spendeetest/spendee_balance_rail_projection_regression_test.dart test/spendeetest/spendee_balance_post_content_test.dart'` — `V3-008/012 production FastInfo uses the F1 shell and each F2-F6 hue in both ghost states`.
- Evidence result: exit `0`; `07:21 +106: All tests passed!`; all five kinds enumerate one resolved icon hue coupled to the icon, edge, glow, background, gradient and inset values.
- Evidence SHA: `ff7a00a7aeae8f636b08611443bd3975aec1303828ae5c80bce253ae1d29a2ed`
