# SpendeeTest dashboard implementation checklist

Scope: Flutter implementation and repair of the approved Color Lab C1/C2/C3 budget-mode dashboard slice on branch `spendeetest`.

Revision note (2026-07-13, device feedback): the earlier visual `DONE` statuses were invalid because they relied on widget presence/code inspection without a fresh device screenshot comparison. The app screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260713-192139.png` and the immediately preceding HTML screenshot `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260713-192134.png` are now mandatory verification inputs. The user also reports that the real app does not remain snapped in Stage 1/2 and briefly shows a Flutter error surface.

| ID | Source instruction / reference | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| STD-001 | User: HTML is source of truth, identical design required. Reference: `docs/prototypes/color_lab.html` C1/C2/C3 common header budget mode. | Prototype/spec intake | The final computed HTML state—not only root CSS defaults—is mapped layer-by-layer to Flutter, including JS-derived budget gradient/window and opacity. | Direct computed-state inspection plus screenshot/golden comparison. | PARTIAL |
| STD-002 | User: partial/minimal build only; no data-flow/database change. | `TransactionHomePage`, new experimental widgets | New design consumes the existing `TransactionStore` getters/callbacks; repository/native bridge schema is unchanged. | Code inspection; no repository/native bridge data-flow edits except settings serialization for the feature flag. | DONE |
| STD-003 | User: push/build on new branch `spendeetest`. | Git/GitHub | The completed repair is committed on branch `spendeetest`, pushed, GitHub build is triggered, and its APK artifact is downloaded to `/storage/emulated/0/Download/exptv2`. | Final code SHA `293f573`; successful GitHub Actions run `29279244922`; downloaded `exptv2-debug-293f573.apk`. | DONE |
| SET-001 | User: current design top-right settings button opposite logo. | `TransactionHeaderCard`, `TransactionHomePage`, shell | Current dashboard header shows a settings button at the top-right and it does not break the existing notification/category controls. | Widget test and visual/code inspection. | DONE |
| SET-002 | User: settings button opens same settings menu without bottom card; settings header has back arrow to main menu. | `ExptShell`, `SettingsPage` | Header settings opens a full-screen settings overlay without shell bottom nav/FAB; back arrow closes overlay to home. | Widget test and manual app inspection. | DONE |
| SET-003 | User: settings has option to switch current/new design. | `AppThemeSettings`, `SettingsPage`, shell/home routing | Settings root contains a design-mode selector with current/new options; selected mode persists through theme settings and switches the home dashboard rendering. | Unit/widget test for serialization and settings interaction. | DONE |
| SLO-001 | User: slot-based colors; replace 21 colors with HTML Spendee colour slot gradients. Reference: `--slot-gradient-0..20` in `color_lab.html`. | `CategoryColorManager`, new experimental UI | Central color manager exposes the 21 Spendee gradient slots; fallback solid colors remain available for existing Color-only consumers. | Unit test for slot stops and fallback behavior; code inspection. | DONE |
| CUI-001 | User: implement C1-C2-C3; C1 stage 0 real data. Reference: C1/Budget mode in `color_lab.html`. | New experimental dashboard widgets | Stage 0 header, income/expense pills, summary pill, search pill, transaction count, and logboxes render from real `TransactionStore` data in HTML-mapped layout. | Widget test with fake store data; code inspection. | DONE |
| CUI-002 | User: income/expense live, summary/search pill switch, logboxes filtered. | Experimental dashboard + existing store callbacks | Type toggle calls `store.setActiveType`; search calls `store.setSearchQuery`; category selector calls existing category filter; logboxes reflect `visibleDisplayLogEntries`. | Widget test using fake store/interaction or existing transaction store test extension. | DONE |
| CUI-003 | User: most important is header interaction; header bottom expands downward and pushes all below content down together. | Header stage controller + experimental dashboard layout | A real pointer drag on the visible handle changes header height continuously; after release at the Stage 1 or Stage 2 trigger the header remains at that snap height and the complete content stack moves by exactly the same delta. | Controller test plus 412×892 widget drag test measuring header/content geometry before, during, and after release. | DONE |
| CUI-004 | User: tick/snap/spring mechanics for stage 0→1, stage 1 popout/return or 1→2, stage 2 popout/return. | Header stage controller | Deterministic drag state preserves the armed snap target through rebuild/release, emits one tick per threshold crossing, and implements the specified popout/manual-return behavior without resetting the controller. | Unit tests for every transition plus real GestureDetector widget tests for snap, popout/manual-return and geometry preservation. | DONE |
| CUI-005 | User: stage 1 real avatars and same tick mechanism as existing limit avatar carousel. | Experimental header stage 1 | Stage 1 remains visible after release and displays real category/avatar data without overflow or Flutter exceptions; horizontal carousel interaction remains independent from vertical header dragging. | Widget test with more than five real-shaped categories, physical horizontal carousel gesture and exception capture. | DONE |
| CUI-006 | User: stage 2 real filtered diagram matching HTML design. Reference: `buildCommonBudgetCategoryPieContent()` in HTML. | Experimental header stage 2 chart | Stage 2 is reachable by a second real handle drag, remains snapped after release, and renders the real-data glossy pie/list panel without overflow. | 412×892 widget drag test with multi-category totals plus screenshot comparison. | PARTIAL |
| VIS-001 | User: HTML design is the most important; gradients must be more expressive. Reference: `color_lab.html` `.common-header-card::before`, `buildSpendeeHeaderGlow`, `buildReactiveGlassAccent`. | Dedicated glass renderer + `SpendeeTestDashboard` integration | Header reproduces the final computed HTML paint graph: window-sampled gradient, layer-only opacity, left radial, reactive right radial, 164° diagonal gloss, masked outer glow, backdrop blur, and exact paint order. | Pure style-model tests, painter/widget tests, inspected 412×892 Stage 0/1/2 goldens; fresh Android overlay remains required. | PARTIAL |
| VIS-002 | User: avatar details from HTML, including top 1px curved highlight. Reference: `color_lab.html` `.common-context-badge`. | Experimental category avatar selector | Avatar circles render gradient fill, white border, top inset/highlight arc, selected outer white ring, and category-colored glow matching HTML proportions without clipping. | Stage 1 device screenshot and focused golden crop. | PARTIAL |
| VIS-003 | User: pie chart/gloss must match HTML more closely. Reference: `color_lab.html` `.common-budget-pie-*`, `buildCommonBudgetCategoryPieContent()`. | Experimental budget pie panel/painter | Pie panel reproduces the HTML glass surface, donut ring, selected slice, focus text and rows, and is visually verified in reachable Stage 2. | Inspected Stage 2 full-dashboard golden and real-data widget test; fresh Android screenshot remains required. | PARTIAL |
| NAV-001 | User: new version must not show bottom nav. | `ExptShell` navigation gating | When dashboard design mode is `spendeeTest`, shell bottom nav and FAB are not rendered on home. | Widget test. | DONE |
| BTN-001 | User: settings button must be in app top-right corner, not inside header. | `ExptShell`, `SpendeeTestDashboard` | A settings button is positioned at app/screen top-right in Spendee mode and opens the existing settings overlay. | Widget test and visual/code inspection. | DONE |
| BTN-002 | User: where settings was in the header, show a glass category button like HTML, every detail. Reference: `color_lab.html` `.common-header-menu`, `.menu-bars`. | Dedicated menu surface/painter | Header top-right menu matches the HTML 33.6×33.6 surface, 13.6 radius, translucent fill, foreground border, true inset highlights and exact 16×3 bars; it must not read as a flat grey button. | Inspected 34×34 golden crop plus exact geometry and pixel sampling tests for border/surface/highlights. | DONE |
| INT-001 | User: stage 1/2 popout release should collapse to previous stage unless dragged back manually. | `SpendeeHeaderStageController` | From stage1, small downward popout then release targets stage0; dragging back up before release restores stage1. From stage2, small downward popout then release targets stage1; dragging back up restores stage2. | Unit plus real-pointer widget drag tests for both popout and manual-return branches. | DONE |
| INT-002 | User: stage 2 must be pullable a few px downward to pop out; vertical drag should accept down and up. | `SpendeeHeaderStageController`, dashboard handle gesture | Controller and recognizer allow overshoot beyond Stage 2 and negative deltas for manual return on a physical pointer sequence. | Unit plus real-pointer widget drag tests with positive and negative deltas. | DONE |
| INT-003 | User: backheader avatar swipe momentum/tick experience must be ported perfectly; user flick spins further. Reference: `CategoryBudgetStage` center carousel logic. | Experimental category avatar carousel | Experimental carousel uses slot-distance residual, velocity-based inertial travel/step planning, repeated haptic ticks, visible residual belt offset, and active category updates during belt movement. | Pure controller tests for slow/fast/live-tick releases plus Stage 1 physical gesture/exception test. | DONE |
| VIS-004 | User report: the HTML has a trivial visible 1 px white header border; the app does not. Screenshots `192134` HTML and `192139` app. | Header foreground painter | A 1 logical-pixel pure-white border is painted after the clipped glass background and remains visible around the complete card in all three heights. | DPR-1 pixel sample is pure white and Stage 0/1/2 goldens show the foreground edge; fresh Android screenshot remains required. | PARTIAL |
| VIS-005 | HTML source: budget scale `center=50`, `window=28`, mode opacity `center=50`. | Shared immutable header visual spec | Default budget stops resolve to `#61E1FB`, `#14C5E1`, `#0390CA`; graphic-layer opacity resolves to `0.57`; reactive accent is derived from the sampled right stop mixed 42% toward white. | Pure unit tests with exact colors/opacity. | DONE |
| VIS-006 | HTML `.spendee-dashboard-screen::after` and `.common-header-card::before`. | Custom glow/header painters | Outer glow includes the HTML top fade and radial falloff masks; background blur remains visible through the translucent graphic layer; masks expand with header height. | Inspected Stage 0/1/2 full-dashboard goldens and exact growth/mask spec tests; fresh Android comparison remains required. | PARTIAL |
| VIS-007 | HTML C1 screenshot and prior user requirement: logo/wordmark above header; exact type/summary/search/logbox geometry. | Dashboard brand and home-content widgets | Spendee brand lockup is present; `BUDGET`/value content matches C1; no unapproved Stage 0 subvalue; type row uses 28 px sides/21 radius and HTML shadows; summary/search use 28 px sides/20 radius; logboxes use 18 radius. | Inspected 412×892 full-dashboard golden, byte-identical packaged SVG and direct geometry/shadow assertions. | DONE |
| ERR-001 | User report: a Flutter error box flashes while trying to expand and Stage 1/2 do not appear. | Header, Stage 1/2 content, store-driven rendering | Repeated Stage 0→1→2→1→0 gestures with a real-shaped, multi-category/high-count store produce no `FlutterError`, layout overflow, assertion or uncaught async exception. | Widget test capturing `tester.takeException()` after every pointer phase and device smoke test. | PARTIAL |
| VER-003 | User requires 100% HTML parity; screenshots are mandatory evidence. | Golden/screenshot verification | Structural key tests are not accepted as visual evidence. Same-size reference and Flutter renders are compared by overlay/diff; all intentional data-content differences are masked/documented. | Inspected Stage 0/1/2/menu goldens pass; fresh installed-APK Android screenshots and reference overlay remain required. | PARTIAL |
| VER-001 | Global rules: TDD and verification before completion. | Tests/analyze | Relevant tests are written and watched fail before production code; after implementation targeted tests and analyze run in Ubuntu proot. | Captured RED/GREEN command outputs. | DONE |
| VER-002 | Global rules: do not claim complete with checklist gaps. | Final report | Any remaining partial/blocked/not-done items are explicitly reported even if APK builds. | Checklist review before final response. | DONE |

Notes:

- Mandatory current comparison screenshots: HTML `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260713-192134.png`; app `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260713-192139.png`.
- Earlier visual rows were verified only by direct HTML/CSS-to-code inspection and widget smoke keys; that method is explicitly rejected for this repair because it did not detect hidden borders, wrong computed gradients, missing masks, wrong inset shadows, or unreachable runtime stages.
- `STD-003` evidence: final code SHA `293f57303eead58a47caf4b212cb62b3c7b5abe7`; successful run `https://github.com/elizerpist/exptv2/actions/runs/29279244922`; APK `/storage/emulated/0/Download/exptv2/exptv2-debug-293f573.apk`; SHA-256 `03ab34e5a8e083507fe1de939c39e139f1de280133ec7d23b2de4a3a79927c23`.
- Fresh installed-APK Android screenshots remain intentionally `PARTIAL`: Android requires foreground Package Installer approval, which cannot be granted by the Termux application UID.
