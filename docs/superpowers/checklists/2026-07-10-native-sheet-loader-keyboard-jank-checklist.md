# Native add transaction sheet loader and keyboard jank checklist

| ID | Source instruction / evidence | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| NSJ-1 | User: "last screenshot: crashel a sheet"; latest screenshot shows black native sheet with only spinner | `NativeImeSheetApp`, native host loading path | Native add transaction sheet does not expose a black transparent loading surface during startup | `flutter test test/transactions/native_ime_sheet_app_test.dart`; native host code inspection | DONE |
| NSJ-2 | User logs: `native open dispatched elapsed=1215ms/1275ms`; code shows `_store.start()` in native sheet bootstrap | `NativeImeSheetApp`, `TransactionStore`, `TransactionRepository` | Native add transaction content does not call full `expenseLoadBootstrap` to render the form | Failing-then-passing widget test with method-channel call counters | DONE |
| NSJ-3 | User: keyboard and content lag together; logs show main shell `Keyboard metrics` work during native sheet IME animation | `ExptShell`, `NativeImeSheetBridge`, Android `NativeImeSheetHost` | Main Flutter shell suppresses per-frame keyboard metric work while native add transaction sheet is open | Bridge callback test plus direct code inspection | DONE |
| NSJ-4 | Add transaction form must still save correctly from native hosted mode | `AddTransactionSheet`, `TransactionStore`, native sheet bridge | Native hosted save sends transaction payload, notifies main shell, closes native host, and main shell refreshes after commit | `flutter test test/transactions/native_ime_sheet_app_test.dart`; `flutter test test/widget_test.dart`; `flutter test test/core/debug_floating_button_test.dart` | DONE |
| NSJ-5 | Android native-owned motion architecture remains required | `NativeImeSheetHost.kt` | Sheet container remains moved by Android `WindowInsetsAnimationCompat.onProgress`; no return to Dart-driven per-frame sheet motion | Direct code inspection: `onProgress` still sets native `sheet.translationY` | DONE |
| NSJ-6 | Project requirement: verify with tests/analyze before completion | Flutter tests/analyze | Targeted tests and analyzer pass in Ubuntu proot; any environment-only limitation is reported | Fresh `flutter test` / `flutter analyze` / `git diff --check` output | DONE |
