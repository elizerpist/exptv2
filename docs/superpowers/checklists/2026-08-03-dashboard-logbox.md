# Dashboard LogBox acceptance checklist

| ID | Source/reference | Code area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| LGB-01 | User §§1–5; Spendee source audit | mapping doc, tokens, group/row widgets | Audited Balance geometry is ported; Fluvi token/resolver owns color/icon/radius | source inspection + 10 golden states | DONE |
| LGB-02 | User §§6–10 | committed snapshot, coordinator | Exact committed query key/revision/facets bind summary metrics and LogBox; preview cannot rebind it | snapshot/coordinator/controller tests | DONE |
| LGB-03 | User §§11–14 | core read service, bridge/domain models | Local-date day group page is keyset paged, sorted and contains complete days with joined projections | Room test is compiled locally; clean execution awaits GitHub Actions Ubuntu | PARTIAL |
| LGB-04 | User §§15–17 | LRU cache/prefetch coordinator | Bounded data-only cache; final-target/tap prefetch is invisible and shares only its matching in-flight request, so cache warming cannot invalidate the final target | prefetch/cache/row-budget tests plus concurrent warm/final-target regression | DONE |
| LGB-05 | User §§18–20, 26–29 | log area state/coordinator | Explicit loading/data/empty/error states, stale rejection, guarded append, no retained old list; preview never pages the independently committed query | coordinator/widget tests, including preview pagination guards | DONE |
| LGB-06 | User §§21–25 | main scroll/LogBox UI/projector | Sliver lazy rendering, stable group/row identity, preformatted models and central category badge | dense-day lazy widget test + goldens | DONE |
| LGB-07 | User §§30–33 | boundaries/debug | UI has no repository/SQL; coordinator owns cache/paging; structured deduplicated logs added | boundary script + diagnostic widget tests | DONE |
| LGB-08 | User §§35, 43 | presentation tests | Ten required visual states plus semantics are covered | 10 passing golden tests + widget semantics | DONE |
| LGB-09 | User §§36–39 | e2e/Room tests | Month/Day/Income consistency, open-close cache hit, stable paging and constant query count | Dart tests pass; in-memory Room execution awaits GitHub Actions Ubuntu | PARTIAL |
| LGB-10 | User §§40–42 | rail/perf tests | 100 preview ticks cause 0 LogBox query/rebind; physics unchanged; targeted rebuilds only | full 234-test Flutter suite + rail regression tests | DONE |
| LGB-11 | User §§41, 45 | profile notes/CI | 100/500/1000 row evidence, cold/cache timing and all required acceptance statuses | deterministic renderer evidence recorded; device profile and clean CI still pending | PARTIAL |
| LGB-12 | Screenshot `Screenshot_20260803-062705.png`; user | `CoreDashboard`, `DashboardLogArea` | Entry count is a fixed sibling overlay below the handler; day groups keep their initial clearance then scroll behind its opaque page surface | passing scroll-geometry widget test + inspected screenshot | DONE |
| LGB-13 | Screenshot `Screenshot_20260803-062705.png`; user | shared geometry + LogBox tokens/day groups | LogBox day headers and joined boxes have exactly the SummaryPill’s outer left/right bounds; row-internal inset remains unchanged | passing geometry/widget test + inspected screenshot | DONE |
| LGB-14 | User performance report + supplied FLOW trace | `CurrentQueryController`, `DashboardLogPageCoordinator` | A bounded, data-only child-scope warm cache is populated outside the rail-preview hot path; finite rails warm every child, while unbounded SUM rails warm a bounded target window | finite-month, bounded-SUM and concurrent warmer/final-target regressions; no preview watch assertions | DONE |
| LGB-15 | User performance report + supplied FLOW trace | LogBox coordinator/state/viewport | Every distinct preview child immediately selects the matching cached LogBox page and transaction count; a cache miss shows only that child’s scoped loading state, never the prior child’s list | cached 20 → 21 → 22 preview sequence, scoped-miss and settle-stability coordinator regressions | DONE |
| LGB-16 | User performance report | `CoreDashboard`, `DashboardMotionHost` | Header collapse/expand and rail preview do not rebuild the expensive LogBox subtree unless its own list state changes | LogBox viewport widget identity is retained through collapse frames; CoreDashboard code inspection | DONE |
| LGB-17 | User performance report + rail invariants | rail integration / coordinator | Preview starts zero committed watches, subscriptions or list reads; settle retains one committed query or a cache selection; rail physics values remain unchanged | 100-preview-tick summary regression, preview/watch/cache-settle coordinator regressions and centered-carousel physics suite | DONE |
| LGB-18 | User debug-log requirement | LogBox coordinator | Deduplicated `LOG_PREVIEW_BOUND`, `LOG_PREVIEW_CACHE_MISS` and warm-cache diagnostics identify the preview scope without logging animation frames | coordinator diagnostic-deduplication regression plus code inspection | DONE |

## Verification note

On 2026-08-03 the user explicitly made golden/screenshot proof non-blocking: visual acceptance will be given from the downloaded APK. The local functional gate is therefore the full 262-test Flutter suite plus `flutter analyze`; physical-device timing and clean x64 Room execution remain the separately recorded LGB-03/LGB-09/LGB-11 follow-up items for GitHub Actions and user feedback.
