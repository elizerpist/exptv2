# Query menu visual prototype — acceptance checklist

This checklist scopes the interactive HTML design prototype only. It does not
change Flutter production behaviour.

| ID | Source | Intended area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| QM-P01 | User, 2026-08-10 | `querymenu.html` | Query is a full-height slide-up task sheet. It covers the dashboard, BNB-03 navigation and FAB while open. | Browser inspection | DONE |
| QM-P02 | `bnb03_bottom_navigation.dart` | Navigation geometry/tokens | The dashboard behind the sheet uses the BNB-03 428×75 bar, 24px upper overflow, 96px centre FAB, and Fluvi highlight ramp. | Code/source inspection | DONE |
| QM-P03 | User correction, 2026-08-10 | Time section | The quick periods are `Összes idő`, `Aktuális hónap`, `Utolsó 3 hónap`, `Ez év idáig`, and `Egyedi`. Multiple non-contiguous selected months remain possible; the selected time set constrains SummaryPill navigation without creating a second time-state owner. | Interactive browser check | DONE |
| QM-P04 | User refinement, 2026-08-10 | Category/partner semantics | A selected category expands to its time-valid child partners. The effective query is the union of explicit partner selections and category-derived child partners; time prefilters available category/partner choices. | Interactive browser check | DONE |
| QM-P05 | `CurrentLedgerQueryScope` / `DashboardQueryArguments.kt` | Refinements | Shows only supported production facets: category, Partner, amount min/max, note text; no invented recurring/day/outlier filter | Source inspection | DONE |
| QM-P06 | User, 2026-08-10 | Saved filters | Bookmark opens named, localStorage-backed saved query configurations. They are loadable, explicitly updateable when dirty, saveable as new, renameable and deletable; they never store result counts. | `querymenu.test.js` state probe and browser inspection | PARTIAL |
| QM-P07 | User request | Interaction | Filter chips, time months, category/partner options, amount, note, reset, apply, snapshot, sheet close/open, and nav/FAB affordances react without a page reload | Browser inspection | DONE |
| QM-P08 | User request | Visual source of truth | Sheet component design follows `/storage/emulated/0/spendee/query.png`; Fluvi code remains the source for shell geometry, semantic colors and supported scope fields. | Screenshot and file inspection | DONE |
| QM-P09 | User, 2026-08-10 | Search resolver | A live search resolves time-scoped partner, category and note candidates without introducing a hidden all-field OR query. Selecting a result adds the explicit matching condition; live candidate/result counts update while typing. | Interactive browser check | DONE |
| QM-P10 | User, 2026-08-10 | Custom time section | The `Egész év` control selects or clears all 12 canonical month IDs for the shown year in one tap and presents none/partial/full state without introducing a second year-level time state or a redundant selected-period list. | Interactive browser check | DONE |
| QM-P11 | User, 2026-08-10 | Custom time section | A non-open year chip visibly indicates that it contains selected months, without being confused with the currently open year. | Interactive browser check | DONE |
| QM-P12 | User, 2026-08-10 | Category/partner facets | Time is the sole hard availability filter. Category-derived child partners are shown as inherited and promoted; all other time-valid partners and all effective selected partners remain available. The current effective-partner query count is live. | Interactive browser check | DONE |
| QM-P13 | User correction, 2026-08-10 | Category-child narrowing | A category starts in `minden partner` mode. Selecting a child partner narrows only that category's child set; adding a standalone partner extends the effective partner set. Example: `Étel + MOL` → `{Tesco, Lidl, MOL}`; `Étel > Tesco + MOL` → `{Tesco, MOL}`. | Interactive browser check | DONE |
| QM-P14 | User, 2026-08-10; `CategoryColorCatalog` / `CategoryVisualResolver` | Category and partner visuals | The redundant category-state chip is absent. A single category visual token (prototype mirror of central `colorId` resolver data) supplies the selected category tile and every associated partner chip's icon/dot, tint, border, and text colour. | Source inspection and interactive browser check | DONE |
| QM-P15 | User repro, 2026-08-10 | Category-child state machine | Removing the final explicit child exits the category's override mode and restores its inherited `minden partner` set; the selected partner tick uses its category token colour rather than an empty white circle. | `querymenu.test.js` RED/GREEN regression check and interactive browser check | DONE |
| QM-P16 | User refinement, 2026-08-10 | Category picker entry | The horizontally scrollable category rail shows every explicitly selected category, followed by `Kategória hozzáadása`. It never uses `+N`, `Összes` or an ellipsis as a picker opener. | `querymenu.test.js` render probe and browser inspection | PARTIAL |
| QM-P17 | User refinement, 2026-08-10 | Partner picker entry | The horizontally scrollable partner rail shows every explicitly selected partner, followed by `Partner hozzáadása`. Category-inherited partners never duplicate there; they remain selected only in the picker list. | `querymenu.test.js` render probe and browser inspection | PARTIAL |
| QM-P18 | User, 2026-08-10 | Picker state ownership | Category and partner morph pickers render and update the same `draft` state as their compact previews; Back/Kész returns to the intact Query draft. | Source inspection and browser interaction | PARTIAL |
| QM-P19 | User, 2026-08-10 | Inline amount threshold | `További szűrők` expands an inline two-thumb amount range: the left thumb starts at `0 Ft`, the right thumb at the greatest absolute transaction amount in the current time/category/partner/note candidate set. Both may move inward; the domain deliberately excludes its own amount constraint, the live count updates immediately, and an empty candidate set disables the controls. | `querymenu.test.js` RED/GREEN check; interactive browser check | DONE |
| QM-P20 | User, 2026-08-10 | Compact active-query header | Directly under the title row, the prototype renders a derived, stable-height query sentence plus secondary result count. The time/category/partner formatters compress active state without horizontal scrolling or enumerating arbitrary numbers of months/chips; the current `draft` remains the only source state. | `querymenu.test.js` RED/GREEN cases passed; 360–430px browser inspection pending | PARTIAL |
| QM-P21 | User, 2026-08-10 | Section heading hierarchy | The `Időszak`, `Kategóriák`, and `Partnerek` section headings receive the same small blue leading-icon treatment as `További szűrők`, without changing their controls, compact header height, or selection semantics. | `querymenu.test.js` RED/GREEN check passed; browser inspection pending | PARTIAL |
| QM-P22 | User, 2026-08-10 | Whole Query composer | The complete sketch is one premium Fluvi visual query composer: light sheet shell, compact query sentence, human section language, temporal rail, category objects, partner chips, the sole advanced-filter disclosure and sticky result CTA. Existing filtering remains interactive. | Sketch behavior tests passed; 360/390/412/430px full-page visual inspection pending | PARTIAL |
| QM-P23 | User, 2026-08-10 | Amount range interaction | Expanded advanced filtering has a visual two-thumb min–max range with formatted endpoint clusters, active-track priority, drag bubble, non-default quiet reset and correct domain/active-filter semantics. | Range behavior tests passed; viewport inspection pending | PARTIAL |
| QM-P24 | User, 2026-08-10 | Saved-filter lifecycle | Bookmark toggles an in-sheet saved-filter manager. Named query configurations persist in prototype localStorage, load completely, derive dirty state, support explicit update/save-as-new, inline naming, rename, confirmed delete and current-query-only reset. Result counts are never persisted. | State-transition test passed; saved-panel visual inspection pending | PARTIAL |
| QM-P25 | User, 2026-08-10 | Responsive visual coherence | All composer/saved-filter states have no full-page horizontal overflow and retain coherent rhythm, type, touch and icon hierarchy at 360/390/412/430px. | Full-page screenshots and direct browser inspection | NOT DONE |
| QM-P26 | User refinement, 2026-08-10 | Shared visual grammar | Time detail, saved filters and advanced filtering use one soft Fluvi surface; selected filters use blue→violet tokens; only the sticky CTA uses violet→pink action tokens. | `querymenu.test.js` token/markup check and browser inspection | PARTIAL |
| QM-P27 | User refinement, 2026-08-10 | Header and density | `Kinél?` has no redundant result counter; bookmark active state is a small indicator rather than a second primary CTA; spacing and icon family remain shared across sections. | Source inspection and browser inspection | PARTIAL |
| QM-P28 | User surface/depth correction, 2026-08-10 | Query Menu visual system | The sheet is pure white. Detailed time, saved filters and expanded advanced filtering use the same `#FAFBFE` neutral floating surface; available controls use `#F3F6FB`; selected objects use blue→violet; only the sticky CTA uses violet→pink. | `querymenu.test.js` token/markup checks passed; screenshot review pending | PARTIAL |
| QM-P29 | User refinement, 2026-08-10 | Horizontal time/facet rails | Fast time presets show selection through fill only, without a checkmark. Time, year, month and facet rails stay horizontally scrollable while reserving an internal gutter so edge shadows are not clipped. | `querymenu.test.js` markup/CSS check passed; browser inspection pending | PARTIAL |
| QM-P30 | User surface refinement, 2026-08-10 | Sheet/footer depth | The sticky footer uses exactly the pure-white sheet surface. Query Menu components use neutral fill and hairlines only; no component or sheet shadows remain. | `querymenu.test.js` CSS check passed; browser inspection pending | PARTIAL |
| QM-P31 | User refinement, 2026-08-10 | Category and advanced-filter surfaces | A selected category uses its centralized category-specific gradient token. The white amount card remains visibly nested inside the neutral advanced-filter mother surface. | `querymenu.test.js` CSS check passed; browser inspection pending | PARTIAL |
| QM-P32 | User refinement, 2026-08-10 | Compact facet selection affordances | Inactive category avatars use the neutral control surface; selected partner chips use their tint and category-colour dot only, with no check icon. | `querymenu.test.js` markup/CSS check passed; browser inspection pending | PARTIAL |
| QM-P33 | User refinement, 2026-08-10 | Selection accents | `Kategória hozzáadása` uses the partner-add plus accent. Every selected temporal control—fast preset, year, month and `Egész év`—uses one restrained blue-violet temporal fill. | `querymenu.test.js` CSS check passed; browser inspection pending | PARTIAL |
| QM-P34 | User refinement, 2026-08-10 | Detailed time controls | A selected `Egész év` control has no checkmark; its temporal fill communicates state. The whole-year row and monthly rail retain small intentional padding/gaps. | `querymenu.test.js` markup/CSS check passed; browser inspection pending | PARTIAL |
| QM-P35 | User correction, 2026-08-10 | Applied dashboard query | After applying a Query Menu selection with a category and/or explicit partner, the dashboard places `X tranzakció listázva`, then a compact horizontally scrollable facet expansion, then the LogBoxes. Every category/explicit-partner chip has its own `×` removal action; the separate `×` clears the complete applied query and synchronizes its draft. The two-thumb amount range belongs only to Query Menu; a separate one-thumb Mind heatmap threshold remains future scope. | `querymenu.test.js` state/ordering/removal regression passed; live browser inspection pending | PARTIAL |
| QM-P36 | User refinements, 2026-08-10 | Category and partner full-list transition | Only `Kategória hozzáadása` / `Partner hozzáadása` open the full category/partner list; an existing horizontal chip toggles only itself. Both lists preserve the shared draft, provide full-row multi-select and return to the compact rail through the established shared opacity/translate transition. Entrance stagger is limited to initial open and never restarts on a toggle. | `querymenu.test.js` shared-state/no-reflash/entry-point/opacity-transition regression passed; mobile visual inspection pending | PARTIAL |
