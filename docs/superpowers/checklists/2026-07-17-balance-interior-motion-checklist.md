# Balance Interior Motion Acceptance Checklist

**Design:** `docs/superpowers/specs/2026-07-17-balance-interior-motion-design.md`

**Mandatory references inspected directly:**

- `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260717-013843.png` (1084×2412 Android Chrome screenshot; 420 dpi system density implies an approximately 413 CSS px viewport).
- `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260717-130441.png` (latest Android Chrome screenshot showing the incorrect light/white center band left by the previous no-draw corridor behavior).
- `/storage/emulated/0/Pictures/Screenshots/Screenshot_20260717-132149.png` (latest Android Chrome screenshot showing the incorrect replacement-layer behavior where the original white center gradient disappeared).

**Current revision:** The user rejected the previous four-primitive interior catalog and then rejected the side-mask replacement layer on 2026-07-17. Current acceptance requires the original animated Balance gradient as the lower layer, plus a dedicated common translucent interior overlay whose effects and controls match Portal background morph 1:1.

| ID | Source instruction / approved reference | Intended code area | Acceptance condition | Verification method | Current evidence / status note | Status |
| --- | --- | --- | --- | --- | --- | --- |
| COLOR-LAB-384 | User request: add visible internal moraj; `Screenshot_20260717-132149.png` correction that the original Balance gradient must remain underneath | Balance test-header renderer in `docs/prototypes/color_lab.html` and dedicated interior renderer modules | The original animated turquoise-white-magenta-purple Balance gradient remains visible as the lower layer, while a common upper morph overlay creates internal movement. | Multi-timestamp mobile screenshots plus renderer tests proving overlay alpha and base-first draw order. | Renderer tests prove translucent `rgba(...)` overlay pixels; static tests prove the interior renderer call runs after `ctx.putImageData(image, 0, 0)`. Fresh Android visual confirmation is still pending. | PARTIAL |
| COLOR-LAB-385 | User request: make it an extra animation that can be independently added and toggled | Dedicated Color Lab interior controls and interior-motion state | A dedicated control defaults to off, can be enabled independently, and disabling it removes only the interior layer. | State/unit test and control integration test; manual coexistence check with existing effects enabled. | `DEFAULT_INTERIOR_MOTION_STATE.enabled` is false; static tests assert a dedicated row/toggle and no private scheduler; disabled/non-Balance renderer calls do zero pixel work. | DONE |
| COLOR-LAB-386 | User correction: same effects as Portal background morph, 1:1 | Interior mode selector and state reducer | The dedicated interior selector exposes exactly `solid-a`, `static-matter`, `wandering-mist`, `living-archipelago`, and `forming-clouds`, in the same order and labels as Portal background morph; unknown values normalize the same way. | Unit tests comparing against `PortalMessageField.modeOrder`, `modeLabels`, and `normalizeMode`; DOM/static option assertions. | Model tests assert exact `PortalMessageField` mode/label parity; static tests assert the five exact option values and absence of old `driftingMist`/`innerCurrent` control API. | DONE |
| COLOR-LAB-387 | User correction: top morph is one shared layer and does not need A/B side masking | Dedicated interior renderer | The interior renderer draws one full-canvas overlay pass with no left/right clip masks and no boundary-respecting geometry. | Renderer unit test asserts zero `clip()` calls and a single overlay pixel count; static test asserts no `boundary` object is passed to the renderer call. | Renderer now exports only `renderPortalInteriorMotion`, returns `overlayPixelCount`, draws no clip/path calls, and the Color Lab integration passes no boundary object. | DONE |
| COLOR-LAB-388 | User correction: overlay colors follow A/B area colors only | Interior palette derivation utilities and overlay tint mapping | Overlay tint uses the known darker green endpoint in A/turquoise areas and the known darker purple/lilac endpoint in B/magenta-lilac areas, derived from the current Balance palette rather than fixed Portal background colors. | Renderer unit tests inspect `rgba(...)` fill colors at left and right sample pixels; static test asserts current Balance palette is passed into renderer. | Renderer tests assert left tint `rgba(73, 207, 197, …)` and right tint `rgba(216, 180, 254, …)` from the current Balance palette. | DONE |
| COLOR-LAB-389 | User request: internal material should moraj; latest correction that the white center gradient must remain visible | Dedicated morph field renderer and compositing | Sampled interior matter draws as translucent `rgba(...)` overlay pixels, never opaque replacement pixels, so the lower gradient remains visible through the motion. | Renderer unit tests assert alpha below 1 and no base-clearing operations; mobile screenshot review checks the white center remains. | Renderer tests assert every fill style is `rgba(...)`, alpha is below 1 and capped at 0.42, and no `clearRect` occurs. Fresh Android visual confirmation is still pending. | PARTIAL |
| COLOR-LAB-390 | User correction: one common upper layer | Dedicated morph field renderer | One shared selected effect/settings/phase drives the whole overlay field; the renderer must not create independent left/right fields. | Renderer unit test checks a common overlay result and absence of side-specific clipped render passes. | Renderer samples `PortalMessageField.sampleMatter` once per pixel across the full canvas, with no side-specific render options or split polygons. | DONE |
| COLOR-LAB-391 | User correction: same control sliders, same everything 1:1 | Dedicated interior dynamic controls | For every selected interior mode, sliders/numbers match `PortalMessageField.controlsForMode(mode)` exactly: key, label, min, max, step, default, unit, clamping, and reset. | Schema parity tests, static DOM integration tests, and interaction tests for range/number/reset behavior. | Model tests compare every control schema/default against `PortalMessageField`; static tests assert generated range/number controls use `PortalInteriorMotion.controlsForMode(activeMode)` and `PortalInteriorMotion.normalizeValue`. | DONE |
| COLOR-LAB-392 | Approved architecture: Balance-only, independent shared render-loop layer | Interior renderer lifecycle and integration | Interior work runs only when enabled in Balance mode, uses the existing Balance frame clock, and preserves all other portal state when stopped. | Lifecycle unit tests, animation-loop instrumentation, and regression checks for existing effects. | Static tests assert exactly one renderer call inside `drawMindPortalEnergyFrame`, no private scheduler in lifecycle helpers, and independent eligibility from the existing energy motion. | DONE |
| COLOR-LAB-393 | User correction: dedicated feature, not the same Portal background morph | Interior state, DOM bindings, and renderer call sites | The Balance interior feature has separate enabled flag, mode, settings, phases, palettes, controls, and render call; it may share pure sampler math but not Portal background-morph state or canvas. | VM/static tests proving separate state maps, separate DOM attributes, and no coupling to Portal background-morph panel state. | Implemented as `PortalInteriorMotion` state with `settingsByMode`/`phaseByMode`, dedicated `data-portal-interior-motion-*` DOM, and renderer call. It shares `PortalMessageField` pure semantics but not background-morph panel state/canvas. | DONE |
| COLOR-LAB-394 | User correction: effects themselves are 1:1 with Portal background morph | Interior effect implementations / shared morph kernel | `static-matter`, `wandering-mist`, `living-archipelago`, and `forming-clouds` produce the same sampled matter signatures and phase behavior as Portal background morph before side-specific seed/phase offsets are applied. | Signature parity tests against `PortalMessageField.sampleMatter`, `advancePhase`, and `renderProfile`. | Model tests assert mode/control/default parity and `advanceInteriorPhase` equals `PortalMessageField.advancePhase`; renderer directly calls `field.sampleMatter` for the interior pixels. | DONE |
| COLOR-LAB-395 | Global UI verification rules and mandatory screenshot references plus user request for rotated upper-layer wandering | Full Color Lab prototype at mobile viewport | The implementation matches the compact control language, has no overlap or text clipping, preserves header content, restores the lower white center gradient from `Screenshot_20260717-132149.png`, supports upper-layer rotation KI/BE and speed, and uses off-window overscan for vertical-plane wandering. | Actual mobile screenshot inspection at multiple timestamps and `git diff --check`; model/renderer/static tests for rotation controls and overscan. | Model tests cover `rotationEnabled`, `rotationSpeed`, and clamping; renderer tests cover 90-degree projection, speed-0 no-rotation, width-based overscan, and changed sampled matter; static tests cover the rotation toggle/speed UI wiring. Fresh Android visual review is still pending. | PARTIAL |

## Historical Task 4 Capture Ledger

These captures document the rejected four-primitive implementation. They remain useful for viewport and masking context, but they do not satisfy the current dedicated 1:1 Portal-background-morph acceptance requirements.

Method for every entry: genuine Firefox 143 ARM64 headless rendering driven through Geckodriver 0.36.0/WebDriver BiDi at `innerWidth=413`, `innerHeight=785`, `devicePixelRatio=2.625`, with production Color Lab zoom `0.8`. Times are UTC. These are explicitly **not Android screenshots**.

| State | Timestamp | Exact screenshot path |
| --- | --- | --- |
| Disabled baseline t0 | 2026-07-17T05:51:36.848Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/baseline-off-t0.png` |
| Disabled baseline t1 | 2026-07-17T05:52:14.708Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/baseline-off-t1.png` |
| Vándorló köd t0 · Strength 100 · Speed 100 | 2026-07-17T05:52:49.574Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-driftingMist-t0.png` |
| Vándorló köd t1 · Strength 100 · Speed 100 | 2026-07-17T05:52:59.051Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-driftingMist-t1.png` |
| Belső áramlás t0 · Strength 100 · Speed 100 | 2026-07-17T05:53:44.051Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-innerCurrent-t0.png` |
| Belső áramlás t1 · Strength 100 · Speed 100 | 2026-07-17T05:54:08.850Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-innerCurrent-t1.png` |
| Lágy hullám t0 · Strength 100 · Speed 100 | 2026-07-17T05:55:13.598Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-softTide-t0.png` |
| Lágy hullám t1 · Strength 100 · Speed 100 | 2026-07-17T05:55:29.824Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-softTide-t1.png` |
| Lassú örvény t0 · Strength 100 · Speed 100 | 2026-07-17T05:56:28.963Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-slowVortex-t0.png` |
| Lassú örvény t1 · Strength 100 · Speed 100 | 2026-07-17T05:56:51.611Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/effect-slowVortex-t1.png` |
| Strength 0 · Belső áramlás · Speed 0 | 2026-07-17T05:58:04.399Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/strength-0.png` |
| Strength 100 · Belső áramlás · Speed 0 | 2026-07-17T05:58:18.531Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/strength-100.png` |
| Speed 0 t0 · Belső áramlás · Strength 100 | 2026-07-17T05:58:58.725Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/speed-0-t0.png` |
| Speed 0 t1 · Belső áramlás · Strength 100 | 2026-07-17T05:59:08.587Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/speed-0-t1.png` |
| Speed 100 t0 · Belső áramlás · Strength 100 | 2026-07-17T05:59:39.154Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/speed-100-t0.png` |
| Speed 100 t1 · Belső áramlás · Strength 100 | 2026-07-17T05:59:47.821Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/speed-100-t1.png` |
| Post-disable returned baseline t0 | 2026-07-17T06:01:08.394Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/baseline-off-return-t0.png` |
| Post-disable returned baseline t1 | 2026-07-17T06:01:28.622Z | `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/spendeetest-worktree/.superpowers/sdd/task-4-evidence/baseline-off-return-t1.png` |

## Completion status

Feature acceptance is **PARTIAL** for the latest layered-overlay correction. Automated renderer/static verification now covers the common translucent overlay contract, but fresh Android/mobile screenshots are still required before the feature can be called complete.
