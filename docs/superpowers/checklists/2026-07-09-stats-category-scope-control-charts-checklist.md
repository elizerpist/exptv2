# Stats Category Scope Control Charts Checklist

Approved reference:
- `.superpowers/brainstorm/26096-1783533355/content/stats-category-scope-current-and-ideas-v1.html`
- Required sections: `I. Vegleges jelolt: G2 primer + azonos x tengelyu severity line`, `J. X tengely teszt: 12 honap teljes kiirassal`, `K. Kiadas vs bevetel: ugyanaz a chart, forditott jelentes`, `L. Bevetel ritka mintaval: havi 1-2 bevetel, 12 honap`, `M. Konkret beveteli pelda: Jan 300k, Feb 4x75k, Mar 400k`.

| ID | Source | Intended code area | Acceptance condition | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| CSC-001 | Approved HTML sections I-M | `lib/features/stats/data/stats_category_scope_series.dart`, `lib/features/stats/widgets/stats_fast_info_graph.dart` | Category scope FastInfo uses the approved two-chart structure, not the old three-chart occurrence/value/MACD/monthly stack layout. | `flutter test test/stats/stats_category_scope_series_test.dart test/stats/stats_page_test.dart` in Ubuntu proot: 25/25 passed. | DONE |
| CSC-002 | User: "2 kulon fuggveny kell, mint a kiadasnal" | `StatsCategoryScopeSeries`, `StatsFastInfoGraph` | Expense side renders two separate functions: primary dynamic-EMA control histogram and secondary orange magnitude line. | Unit test `expense control bars use dynamic EMA around a neutral 50 baseline`; widget metadata test. | DONE |
| CSC-003 | User income correction: Jan 300k, Feb 4x75k, Mar 400k | `StatsCategoryScopeSeries` | Income side renders two separate functions with monthly/event buckets: primary income-strength histogram and orange Ft/active-income-day line. Jan/Feb/Mar orange values are 300k, 75k, 400k. | Unit test `income scope uses monthly buckets and keeps orange Ft per active day`. | DONE |
| CSC-004 | Approved HTML sections K-M | `StatsCategoryScopeSeries`, painter colors | 50 is the measured-period average baseline. Expense above 50 is red and below 50 is green. Income above 50 is green and below 50 is red. | Unit tests verify control bar values and color hex. | DONE |
| CSC-005 | Approved dynamic EMA discussion | `StatsCategoryScopeSeries` | Sparse expense samples are smoothed with `emaPeriod = clamp(round(22 - 0.45 * activeScopeDays), 7, 18)` and `alpha = 2 / (emaPeriod + 1)` to avoid chunky charts. | Unit test verifies `dynamicEmaPeriod == 8` for 31 active days and multiple smoothed values. | DONE |
| CSC-006 | User accepted month axis | `StatsCategoryScopeSeries`, `StatsFastInfoGraph` | X axis uses active graph months. If 12 months are active, all 12 labels are available; fewer active months produce fewer labels. | Unit test `month labels expose every active month on a full-year graph`; widget metadata uses `honapok`. | DONE |
| CSC-007 | User threshold questions | `StatsCategoryScopeSeries` | Threshold `0` or no threshold switches the secondary denominator from Ft/kiugras to Ft/aktiv nap, so every active scoped day can contribute without fake spike semantics. | Unit test `threshold zero labels the orange line as Ft per active day`. | DONE |
| CSC-008 | Existing stats menu behavior | `StatsPage`, `StatsYearCalendar`, scope sheet | Summary pill, income/expense switch, render mode selector, category multi-scope, all-scope normalization, 12 month cards, and month tap focus navigation are not removed. | Existing targeted stats widget tests passed, including `stats month card tap opens focused month view and back returns`. | DONE |
| CSC-009 | Build workflow | GitHub Actions, `/storage/emulated/0/Download/exptv2` | Changes are committed and pushed; APK is built online and downloaded without deleting existing files in the download folder. | GitHub Actions run `28980400020` succeeded; downloaded `/storage/emulated/0/Download/exptv2/exptv2-debug-49ff7f9.apk` at 154874299 bytes without deleting existing APKs. | DONE |
