name: fluvi_codegraph
description: Deterministic SCIP extraction and query tooling for Fluvi source review.
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  crypto: ^3.0.7
  scip_dart: 1.6.2

dev_dependencies:
  lints: ^5.1.1
  # scip_dart 1.6.2 currently pins analyzer ^5.13.0.
  test: ^1.25.8
